# 公開しました／施錠は「カード登録の画面」で中止しました（2026-08-09 第11便・システム開発T・model=Opus 5 (1M context)）

## ★このファイルにURLを書きません（今回からの決まり）

このリポジトリ（`atspect-handoff`）は**Public**です。
いまサイトは**公開されていますが、鍵がかかっていません。**
ここにURLを書くと、**このリポジトリを読んだ人が誰でもダッシュボードに入れてしまいます。**

**→ 施錠が済むまで、URLはVault（ローカル）と代表のお手元の紙にのみ書きます。**
（正本＝`C:\Vault\ARTS-RESPECT\_本番_ダッシュボード施錠設定_20260808_ATSPECT.md` §7）

---

## 1. ゲートは通りました（着手時の実測）

| 確かめ方 | 結果 |
|---|---|
| `api/v4/user`（`cache:'no-store'`＋キャッシュ破り） | 200・**`email_verified: true`** |
| `modified_on` | **`2026-08-09T01:40:19.231394Z`（＝10:40 JST）に更新** ＝前便で再送した確認メールを押していただけた |
| Workers & Pages の画面 | **「アカウントを確認」モーダルが消え、操作できる**（実測） |

## 2. 公開しました（★実データ0件）

- **希望どおりの名前が取れました**（近い名前への変更はしていません）。
- アップロード前に**ハッシュを再照合**＝
  `8cb40144cc99d732ec2d56257f45ce1e550a7835715f3510dedc00109247ae02`（19ファイル）。
- **12画面すべて HTTP 200**（`.html` は Pages の仕様で 308 リダイレクト後に 200）。
- **見本データへの切り替えが実画面で働くことを確認**（「いまは見本のデータを表示しています
  （実在しないお名前です）」と表示・画像あり）。**実データは1件も接続していません。**
- スマートフォン幅（390px）の画像6枚を同梱＝`live_index_390.png` ほか。
  ★**これは「公開URLの画面」ですが「ログインを通した画面」ではありません**（鍵がまだ無いため）。
  第8便の `zip_*_390.png`（手元でZIPを展開して開いたもの）とは別物です。

### ★導線の注意（次に触る人へ）

**「アプリケーションを作成する」→「Upload your static files」は Pages ではなく Worker を作ります**
（ドメインが `workers.dev` になる）。**Pages を作るなら `/pages/new/upload` を直接開くこと。**

## 3. ★施錠は中止しました（カード登録を求められたため）

Zero Trust を開くと、**無料プラン（$0/シート/月）でも決済画面**に進みました。画面の内容＝

- Apple Pay ／ Google Pay ／ PayPal ／ **保存済みのクレジットカード**
- **請求先住所**の入力欄（氏名・国・住所・市区町村・ZIP・組織名・VAT/GST）
- チェック**「利用規約とプライバシーポリシーに同意します」**
- チェック**「このカードに対し、無料枠を超える使用量を毎月課金することを Cloudflare に許可します」**
- ボタン**「Zero Trust Free を有効化」**／注文概要は**本日支払額 $0/月**

**★何も入力せず、どのチェックも入れず、どのボタンも押していません。**
代表のご指示（カード登録を求める画面が出たら中止）どおりに止めました。画像あり
（★画像は撮影前に、画面上のメールアドレスとカード下4桁を伏せてあります）。

**＝施錠の設定値（One-time PIN・許可リスト4件・Session 2 hours）は、まだ1つも入力していません。**

## 4. ★中身を「準備中」の1枚に差し替えました（いちばん大事なところ）

施錠できないまま公開状態を放置し、**しかもこのPublicリポジトリに存在を書く**ことになるため、
**中身を一時的に「準備中」の1枚へ差し替えました。**

- **★削除していません。差し替えただけです。**
  ハッシュ照合済みの一式（`8cb40144…`）で**いつでも元に戻せます**。
- 差し替えた中身＝`index.html` と `404.html` の2枚。**「準備中」の3文字のみ**
  （社名も内部情報も入れていません）。画像＝`live_いまは準備中の1枚のみ.jpg`

**実測（差し替え後）**

| パス | 結果 |
|---|---|
| `/` ・ `/index.html` | 200・**「準備中」のみ**・「あつぺくと」の語 **0件** |
| `daicho.html` `karte.html` `eigyo.html` `mail.html` `tobira.html` | **404**・語 **0件** |
| `assets/app.js` ・ `data/ledger.sample.json` ・ `/admin` | **404**・語 **0件** |

★**副産物**＝`404.html` を置いたことで、**5.の「存在しないパスに index.html を200で返す」問題も消えました**
（＝是正案が正しかったことの実証。**本番に戻すときは、配信物に `404.html` を足すべきです**）。

### ★Codexの独立レビュー（この判断について）

判断を独立レビューにかけ、**同じ結論**を得ました（採用）。

> 「公開・未施錠」のまま存在をPublicリポジトリで知らせる組み合わせは不妥当。URL非記載も、
> 名称を推測しやすいなら緩和効果は限定的。**まず可逆的に内容を無情報の「準備中」1枚へ差し替え、
> その後、URLを伏せて障害を報告するのが適切。プロジェクト削除は不可逆削除に当たり、明示確認なしでは不可。**

Codexはあわせて「**noindexはアクセス制御ではない**」「**過去のデプロイURLが残る可能性**」も指摘しました。
後者は**実際に残っていました**（下記）。

## 4b. ★残っている露出（代表のご判断をお待ちしています）

**Cloudflare Pages は、デプロイごとに固有のURLを永続的に持ちます。**
本番URLを差し替えても、**最初のデプロイは固有URLで生きており、ダッシュボードの中身をいまも返します**
（実測＝200・6,120バイト・タイトルは「あつぺくと ホーム」）。

- 固有URLは**8桁の16進数**（約43億通り）で、**総当たりでの発見は現実的ではありません**が、**ゼロではありません。**
- **消すには「デプロイの削除」が必要＝不可逆削除**にあたるため、**承認をお待ちしています。**
- ★**この固有URLはここに書きません**（Vaultにのみ記録しています）。

**代表のご判断をお待ちしているもの（3つ）**
1. **カードを登録して Zero Trust Free を有効化してよいか**（→ 施錠まで進めます）
2. **最初のデプロイを削除してよいか**（→ 上の残存露出が消えます・不可逆）
3. **本番に戻すとき、配信物に `404.html` を足してよいか**（→ ハッシュ `8cb40144…` が変わります）

## 5. ★新たに分かった仕様（今後の前提になります）

**Cloudflare Pages は、存在しないパスに `index.html` を HTTP 200・`text/html` で返します**
（手元のローカル検証では 404 でした。**環境が違えば挙動も違う**という実例です）。

| 影響 | 実測した結果 |
|---|---|
| `assets/config.local.js`（意図的に入れていない秘密設定ファイル）への script タグ | **MIMEエラー**になり、**全12画面でコンソールエラーが毎回1件**出る |
| `data/ledger.json` 等の実データファイル | 200+HTML が返るが、アプリが `res.json()` の例外を拾って見本へ切り替えるため、**見本表示は正しく働いた**（実画面で確認） |
| 任意のパス（例 `/admin`） | **ダッシュボードのホームが 200 で返る** |

**★是正案＝配信物に `404.html` を1枚足す**（Pagesは `404.html` があればそれを404で返す）。
**ただし今回は足していません。** 理由＝**代表と確定した配信物のハッシュ `8cb40144…` が変わるため、
独断で変えない**と判断しました。**ご承認をいただければ足します。**

## 6. ★実施していないこと・判定していないこと

- **施錠していません。** 許可リスト4件・One-time PIN・Session 2時間、いずれも未入力。
- **施錠の実測（許可リスト外で入れないこと／許可アドレスで入れること）＝していません。**
- **★届く数字の桁数＝確かめていません。**（実物を見ていないため、6桁とは書きません。）
- **実データの接続（台帳・メール・お金・接続先）＝していません。**
- **公開URLをログインなしで叩いて実データに到達できないことの実測＝
  「実データを置いていないので到達しようがない」ことは確認しましたが、
  「鍵で守られていること」は確認していません**（鍵が無いため。ここは別物として区別します）。
- **通しの実測（ログイン→…）＝していません。** ログインの仕組み自体がまだありません。
- **Cloudflareの無料枠の実際の上限＝確認していません。**
- 見本の画面が「4人にとって分かりやすいか」は判定していません。

## 7. 紙の状態

`C:\Users\ataga\Desktop\ダッシュボードの開き方.txt` に**実URLを記入**しました。
そのうえで冒頭に**「★★この紙はまだ配らないでください（鍵がかかっていません）」**を明記し、
「1〜3の入り方は鍵がかかったあとのもの。いまはURLをひらくとそのまま画面が出る」と書きました。
**「入ってから2時間で入り直し（作業中でも切れます）」の訂正済み文言は、そのまま残しています。**
