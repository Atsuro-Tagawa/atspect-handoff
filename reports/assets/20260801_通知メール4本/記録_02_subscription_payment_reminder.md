# 記録：subscription_payment_reminder の是正（本文＋件名）2026-08-01・システム開発T

対象＝Shopify管理画面「お客様への通知 → 支払いリマインダー」
このメールが飛ぶ相手＝**次回のシステム利用料を請求する前の作家さま**（毎月の事前案内）。

## 1. 件名（★忘れずに直した）

| | 内容 |
|---|---|
| 変更前 | `次のサブスクリプション注文は間もなくです` |
| 変更後 | **`まもなく次回のシステム利用料を承ります`** |

## 2. 本文

★このテンプレートには `{% capture email_title %}` / `{% capture email_body %}` が**無く**、
見出しと本文がHTMLに直書きされていた（4行目の `<title>{{ email_title }}</title>` は空になる）。

| 行 | 変更前 | 変更後 |
|---|---|---|
| 61（見出し） | `サブスクリプション注文の請求がまもなく行われます` | `まもなく次回のシステム利用料を承ります` |
| 63（本文） | `{{ product_names }}の次回のサブスクリプション注文に対して、{{ total_price }}の請求がまもなく行われます。この注文は{{ date }}より前に変更できます。` | `次回のシステム利用料 {{ total_price \| money }} を、{{ billing_attempt_expected_date }}に承ります。それより前であれば、下のボタンから内容を変更いただけます。` |
| 71（ボタン） | `サブスクリプションを管理する` | `毎月のお手続きを管理する` |
| 95 | `<h3>注文サマリー</h3>` | `<h3>お手続きの内容</h3>` |
| 404 | `<span>注文のディスカウント</span>` | `<span>ディスカウント</span>` |
| 427〜453 | 「受取／配送」の小計行が**無条件で描画**されていた | 外側を `{% if ...shipping_price > 0 %}` で囲んだ |
| 520 | `{% if ...shipping_address %}` | `{% if ...shipping_address and ...delivery_method %}` |

★本文から `{{ product_names }}` を外した理由＝商品名が「あつぺくとシステム利用料（月額）」であるため、
「あつぺくとシステム利用料（月額）の次回のシステム利用料」と重複して読みづらくなるため。
**サブスク商品が2種類以上に増えた場合は、どれの請求かが分からなくなるので書き戻しが必要**（申し送り）。

## 3. 反映の確認（実測）

- 保存後に**ページを再読込**して永続化を確認。
- 全文の長さ＝21,022文字（変更前20,941文字）
- 全文SHA-256（8桁区切り・実値は連結）
  - 変更前＝`3e825088 3a6ea48d 52f076d0 0fbfac7d d2e91c43 e4319594 6fc04b02 29597da9`
  - 変更後＝`3eba7919 366d2e8d d8a614e5 d8a5947c 5feb7462 b8add00f 7129ed27 755cca1d`
- 全文の機械走査＝**「注文」0件／「発送」0件／「サブスクリプション」0件**。
  「配送」3件はいずれも条件付き（発送のない当店のサブスクでは描画されない）。
- **レンダリング画像で目視**＝`02_subscription_payment_reminder_プレビュー.jpg`
  （実描画＝「次回のシステム利用料 $79.11 を、11月1日に承ります。それより前であれば、下のボタンから内容を変更いただけます。」）

## 4. ★実施していないこと・判定していないこと

- **テストメールを送っていない**。実際の受信箱での見え方は未確認。
- プレビューのダミーデータは発送のある商品のため、画像には「配送 $9.99」等が出ている。
  実データで消えることは条件式では確認したが、**実請求での描画は未確認**。
- Codexレビューは本ファイルには未実施（1ファイル1観点の原則により order_confirmation を優先）。
