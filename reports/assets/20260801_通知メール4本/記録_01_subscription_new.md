# 記録：subscription_new の是正（本文＋件名）2026-08-01・システム開発T

対象＝Shopify管理画面「お客様への通知 → 新規サブスクリプション」
このメールが飛ぶ相手＝**月額システム利用料のお手続きを始めた作家さま**（初回のサブスク成立時）。

## 1. 件名（★忘れずに直した）

| | 内容 |
|---|---|
| 変更前 | `サブスクリプション注文 ({{ subscription_contract_billing_cycle.origin_order.name }}) が確認されました` |
| 変更後 | **`システム利用料のお手続きを承りました　番号：{{ subscription_contract_billing_cycle.origin_order.name }}`** |

プレビューでの実描画＝`件名：システム利用料のお手続きを承りました　番号：#9999`

## 2. 本文

### 見出し（`{% capture email_title %}`）
- 変更前：`サブスクリプション購入していただきありがとうございます！`
- 変更後：**`システム利用料のお手続きを承りました`**

★最初は「『あつぺくと』のシステム利用料のお手続きを承りました」としたが、
**レンダリング画像を見たところ2行に折れて「承り／ました」と泣き別れしていた**ため短縮した。
（機械判定＝禁止語0件はどちらでも通っていた。**目視で見つけた問題**。）

### 本文（`{% capture email_body %}`）
- 変更前：`{% if delivery_method != blank %}` で2分岐し、どちらにも「注文」、片方に「発送」があった。
- 変更後（分岐を廃止し1本化）：

```
『あつぺくと』は、作家とともに築く日本の美術のプラットフォームです。<br><br>変更のお申し出がない限り、{{ subscription_contract_billing_cycle.billing_frequency }}ごとに{{ subscription_contract_billing_cycle.total_price | money }}を承ります。内容の変更・停止をご希望のときは、下のボタンからお手続きいただけます。<br><br>ご不明な点は media@atspect.com までお問い合わせください。
```

★分岐を廃止した理由＝当店のサブスク商品は「あつぺくとシステム利用料（月額）」のみで
`requiresShipping = false`（実測済み）。発送のあるサブスクは存在しないため、
発送ありの分岐は見出しも合わなくなる。**発送ありのサブスクを将来作る場合は、この分岐の作り直しが必要**（申し送り）。

### 構造ラベル（Shopify既定のまま残っていた「注文」語）

| 行 | 変更前 | 変更後 |
|---|---|---|
| 56 | `注文 {{ order_name }}` | `番号 {{ order_name }}` |
| 95 | `注文を表示する` | `お手続きの内容を表示する` |
| 100 | `サブスクリプションを管理する` | `毎月のお手続きを管理する` |
| 124 | `<h3>注文サマリー</h3>` | `<h3>お手続きの内容</h3>` |
| 433 | `<span>注文のディスカウント</span>` | `<span>ディスカウント</span>` |
| 456〜482 | 「受取／配送」の小計行が**無条件で描画**されていた | 外側を `{% if ...shipping_price > 0 %}` で囲んだ |
| 549 | `{% if ...shipping_address %}` | `{% if ...shipping_address and ...delivery_method %}` |

## 3. 反映の確認（実測）

- 保存後に**ページを再読込**して永続化を確認。
- 全文の長さ＝22,089文字（変更前22,127文字）
- 全文SHA-256（8桁区切り・実値は連結）
  - 変更前＝`df99f104 55164b31 12515b58 cd17201f 13baa105 3d305b1e 54db60d3 21a379b9`
  - 変更後＝`5a2fd4cd 9e24573e 17c84d38 c4872d6f a75ce9a5 901ce33b 81b8867f ce29332d`
- 全文の機械走査＝**「注文」0件／「発送」0件**。「配送」3件はいずれも条件付き
  （小計行＝`shipping_price > 0`／配送先住所＝`shipping_address and delivery_method`／配送方法＝`delivery_method`）で、
  発送のない当店のサブスクでは描画されない。
- **レンダリング画像で目視**＝`01_subscription_new_プレビュー.jpg`

## 4. ★実施していないこと・判定していないこと

- **テストメールを送っていない**（送信は外向きの操作のため）。したがって
  **実際の受信箱での見え方・迷惑メール判定は未確認**。
- プレビューのダミーデータは**発送のある商品**のため、画像には「配送 $9.99」「配送先住所」「配送方法」が出ている。
  当店の実データ（発送なし）でこれらが消えることは**条件式の実測で確認したが、実注文での描画は未確認**。
- **販売プラン名は当テンプレートでは直せない**。実データを確認したところ
  `1か月ごとに配達` となっており、商品行の下に**そのまま描画される**。§記録_05を参照。
- Codexレビューは order_confirmation の変更に対してのみ実施（1ファイル1観点の原則による）。本ファイルは未実施。
