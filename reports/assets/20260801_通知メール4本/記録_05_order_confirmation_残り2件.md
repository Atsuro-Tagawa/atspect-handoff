# 記録：order_confirmation の未完了2件を完了（レンダリング目視＋Codexレビュー）2026-08-01・システム開発T

前回（同日）本文と件名は是正済み。残っていた2件を片づけた。
**その過程で、機械判定では見つからない不具合を1件見つけて直した。**

---

## 1. ★目視で見つけた不具合（機械判定は全PASSだった）

発送なしの注文（情報登録料33,000円・月額3,300円）を実際に描画させたところ、
**本文は直っているのに、まわりの構造ラベルに「注文」が残ったまま**だった：

- ヘッダー＝**「注文 #1001」**
- ボタン＝**「注文を表示する」**
- Shopアプリのボタン＝**「shopで注文を追跡します」**
- 見出し＝**「注文サマリー」**
- 割引行＝**「注文のディスカウント」**

前回の是正は `{% capture email_title %}` と `{% capture atspect_pending_notice %}` の2ブロックだけを
分岐させていたが、**これらのラベルは分岐の外**にあり、発送あり・発送なしのどちらでも同じ語が出ていた。

### 直し方

先頭（`{% capture email_body %}` の直前）に、発送の有無でラベル語を切り替える変数を置いた：

```liquid
{% if requires_shipping %}
  {% assign atspect_no_label = '注文' %}
  {% assign atspect_view_label = '注文を表示する' %}
  {% assign atspect_summary_label = '注文サマリー' %}
  {% assign atspect_discount_label = '注文のディスカウント' %}
  {% assign atspect_track_label = '注文を追跡します' %}
{% else %}
  {% assign atspect_no_label = '番号' %}
  {% assign atspect_view_label = 'お手続きの内容を表示する' %}
  {% assign atspect_summary_label = 'お手続きの内容' %}
  {% assign atspect_discount_label = 'ディスカウント' %}
  {% assign atspect_track_label = 'お手続きを確認' %}
{% endif %}
```

そのうえで、該当8箇所（ヘッダー1・ボタン3・Shopアプリ1・見出し1・割引2）を変数に差し替えた。
**発送ありのときの文言は1文字も変わらない**（同じ語を代入しているため）。

### あわせて直した1文

発送なしの本文が「…お支払いを承りました。**作家とともに築く日本の美術のプラットフォームです。**」となっており、
主語が抜けて文が成り立っていなかった。**「『あつぺくと』は、作家とともに築く日本の美術のプラットフォームです。」**に直した
（確定タグラインの部分は一字一句そのまま）。

---

## 2. レンダリング画像での目視（両分岐・ご指示のとおり両方）

| 分岐 | 画像 | 実際に描画された内容 |
|---|---|---|
| **発送なし**（情報登録料・月額） | `04_order_confirmation_発送なし分岐.jpg` | 「番号 #9999」／「このたびは、『あつぺくと』へのお手続きをいただき、ありがとうございます。」／「『あつぺくと』は、作家とともに築く日本の美術のプラットフォームです。」／「お手続きの内容を表示する」「shopでお手続きを確認」／「お手続きの内容」。**「注文」「発送」「お買い物」は1つも出ていない** |
| **発送あり**（作品16万〜30万円） | `05_order_confirmation_発送あり分岐.jpg` | 従来どおり。「注文 #9999」「注文を表示する」「shopで注文を追跡します」「注文サマリー」、および配送料・輸送保険料が別途になる旨の告知が**そのまま保全されている** |

★発送なしの描画方法＝プレビューのダミーデータは発送ありに固定されているため、
**一時的に先頭へ `{% assign requires_shipping = false %}` を1行入れて保存→撮影→直後に削除して保存**した。
削除後の全文SHA-256が一時変更の**直前と完全一致**することを確認済み
（`0369c6a6 715d88e3 b96d95d8 f6a16ff9 f2695c27 6f552d3b 8d541e0e 80b09fdd`）。
一時変更が入っていた時間は約1分。**この間に実際の注文は発生していない**（注文は現時点で発生していないため）。

---

## 3. Codexによる独立レビュー（1ファイル1観点・テキストのみを渡した）

観点＝「Liquidの評価順序・スコープとして実際に壊れる箇所があるか」。結果：

- (a) 変数の定義が使用箇所より後になっている箇所は**無い**（変数は47行目まで、使用は156行目以降）
- (b) `{% assign %}` が `{% capture %}` や `for` の内側に入っている箇所は**無い**
- (c) `requires_shipping` が nil / 未定義なら **else 側（発送なし）** に落ちる。
  ただし**空文字 `""` の場合は if 側（発送あり）**に落ちる（Liquidでは空文字は真）
- (d) 提示していない約3,900行については仕様だけでは断定できない。特に `{% render %}` があると
  変数スコープが切れるので注意が必要

### (d) を自分で潰した（実測）

- `{% render %}` / `{% include %}` は**このテンプレートに0件**（スコープ切れの心配は無い）
- 全文を走査し、残る「注文」「発送」は次のいずれかであることを1件ずつ確認した：
  - 22・29行目＝**発送あり分岐の中**（正しい）
  - 34〜40行目＝上記の**発送あり用の変数定義**（正しい）
  - 53〜77・1038行目＝`{% capture email_body %}`（48〜110行目）の中と、
    `delivery_method_type == 'pick-up'` の中。**どちらも当店では描画されない**
    （`{{ email_body }}` は193行目で `{% comment %}` に囲まれている／店舗受取は使っていない）
  - 164行目＝`{% if po_number %}` の中（注文書番号。当店の注文には付かない）

---

## 4. 反映の確認（実測）

- 全文の長さ＝164,028文字（この作業の前は163,292文字）
- 全文SHA-256（8桁区切り・実値は連結）
  - 作業前＝`4f9f0568 26bd1925 1a496b1e 5ee55eac 23cf7b4d 7cef1e34 87374a2c 3f64a8fb`（前回セッションの記録と一致することを確認）
  - 作業後＝`fcc01e67 9bd3d02a 62358632 9b9ac5ca c0557da3 61694794 af49d924 9e010ed3`
- 件名は前回のまま＝`お手続きの確認　番号：{{name}}`（変更していない）
- 保存後に**ページを再読込**して永続化を確認。

---

## 5. ★実施していないこと・判定していないこと／新たに見つかった宿題

- **テストメールを送っていない**。実際の受信箱での見え方・迷惑メール判定は未確認。
- **実際の注文で描画を確かめていない**（注文がまだ無いため）。確認はプレビューによる。
- **★販売プラン名に「配達」が入っている（テンプレートでは直せない）**
  実データを確認したところ、月額商品の販売プラン名が **`1か月ごとに配達`** になっている
  （管理用コードは「あつぺくと 月額3,300円 自動継続 システム利用料」で、こちらは対外に出ない）。
  このプラン名は `{{ line.selling_plan_allocation.selling_plan.name }}` として
  **サブスク系メールの商品行の下にそのまま描画される**。発送の無い商品に「配達」は実態と合わない。
  → **直すにはサブスクリプション（決済）の本番設定に触れる必要があるため、代表のご判断待ちとした。**
  推奨する名称＝**「1か月ごとに更新」**（配達を伴わないことが分かる語）。
  確認スクリプト＝`atspect-system/scripts/_readonly-selling-plan-name-20260801.mjs`（読み取りのみ）
- 「ショップにアクセスする」というリンク文言は残している（禁止語を含まないため）。
