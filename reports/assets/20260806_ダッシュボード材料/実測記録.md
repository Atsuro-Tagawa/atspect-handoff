# ダッシュボード設計の材料（実測）— 2026-08-06 システム開発T

1人目の実データが入ったので、推測抜きで実測した。**何も書き換えていない。**
※公開リポジトリのため、**金額・氏名・連絡先・非公開URLは書いていない**。

---

## ★先に1件：自動確定が効いていた（前回からの持ち越しが解決）

前回の引き継ぎの「最初の一手＝決済の確定方法を自動に変えたが、効くかは未検証」の答えが出た。

| 注文 | 決済状況 | 残高 | 「支払いを確定する」ボタン |
|---|---|---|---|
| 情報登録料（実申込・1人目） | **支払い済み** | なし | **無い** |
| 月額（実申込・1人目） | **支払い済み** | なし | **無い** |
| 8/3のテスト注文2本 | 承認済みのまま | 残ったまま | 残っている |

→ **自動確定は効いている。** 一覧のバッジだけでなく注文を開いて3点を見て判定した。
入金予定＝**2026年8月14日の支払いに加算される**旨がタイムラインに出ている（初回の実入金日が初めて確定した）。
なお**注文日は2026年8月6日**（13:45と15:15）。「昨日（8/5）」ではなく**本日**だった。

---

## 1. ★同意記録はどこに残ったか＝**Shopifyのどこにも残っていない**

実データで全数確認した（管理画面を開いて実測）。

| 見た場所 | 結果 |
|---|---|
| 注文の「追加詳細」（customAttributes） | **項目そのものが無い** |
| 注文のメモ | 「お客様からのメモはありません」 |
| 顧客のメモ | **なし** |
| 顧客のタグ | `Login with Shop` / `Shop` の2つだけ（**「テスト」も業務用タグも無い**） |
| 顧客のメタフィールド | **欄が存在しない**（定義なし） |
| metaobject の定義一覧 | `artist` のみ。**同意記録用の定義は無い** |
| Webhook購読 | **0件**（Shopifyの外へ何も飛んでいない） |

**＝申込フォームの同意記録（規約版・同意日時・本文ハッシュ・スクロール到達）は、
通知メール1通の中にしか存在しない。** 検索も集計も後追いもできない状態。

★これはダッシュボード設計で最初に埋めるべき穴だと考える（**メールが唯一の保管場所**）。
なお `read_orders` / `read_customers` のAPIスコープが未承認のため、
**プログラムからは注文も顧客も一切読めない**（`ACCESS_DENIED` を実測）。管理画面を人が開くしかない。

### ハッシュが規約本文と照合できることの確認

ページ側の実装（`#aterms-doc` の textContent を空白正規化→SHA-256）を**同じ手順で再計算**した。

- 再計算値＝`sha256:3540eabf374b9cddf4b52e684b573dc8ba66a7035fd11eefa4aec33aee5e32d2`（本文2,854文字）
- **申込フォームのページと規約ページの両方で同一値**。
- この値は**2026-07-31の実装ログに記録されている値と一致**した（`sha256:3540eabf…`）。
- 同ページの隠しフィールド「規約版」＝`AR-K1（制定日2026年8月1日・紙の契約規約書と同一内容）`。

→ **照合の基準値は確定した。** メールの同意記録にこの値が入っていれば、
現行のAR-K1本文に同意されたことが機械的に確認できる。
**ただしメール実物は見られていないため、実際に一致するかは未確認**（下記2）。

---

## 2. メールの実物 — **開けなかった**

- `atspect.com` / `atspect.co.jp` とも MX は **Xserver**（実測）。
- Xserverのwebmailを開いたが、**ログイン画面のみでセッションが無い**。
- **パスワード入力はしない規律のため、ここで止めた。** 受信箱は1通も見ていない。
- したがって**差出人・件名の形・本文の中身は一切確定できていない**。振り分けルールの材料は未取得。

**分かったのは「設定上どこから出るか」だけ**（管理画面で実測）：
- Shopifyの**差出人メールアドレス＝`media@atspect.com`**、ドメイン認証は**認証済み**。
- 認証/DMARCが無い場合のバックアップ送信元として `store+…@shopifyemail.com` を使う旨の表示あり。
- 注文確認メールが顧客あてに送信された記録は、注文のタイムラインに残っている（「メールを表示」ボタンあり）。
  **★このボタンから本文を開けるので、メールボックスに入れなくても注文メールだけは中身を見られる可能性がある**（今回は開いていない）。

→ 代表にお願い：webmailにログインした状態にしていただければ、こちらで分類できる。

---

## 3. ECの注文の入り方（**テスト注文は作らずに、実注文で実測した**）

★テストモードへの切り替えは**していない**。理由＝本日まさに実運用が走っており、
テストモードにすると**実際のお客様の決済が通らなくなる恐れ**があるため。
実注文2件が既にあり、テスト注文より確かな材料になるのでそちらを使った。

実測できた事実：
- チャネル＝`Online Store`。「配送不要」と表示される（配送先住所は取得されない）。
- タイムラインに残るもの＝①注文確認メールの送信記録②入金予定日と加算額③カードブランドと末尾4桁と決済処理④確認番号の生成⑤チェックアウト番号。
- **フルフィルメントは「未発送」のまま**（発送するものが無いのに未発送が残る＝既知の持ち越し）。
- 顧客側に**定期購入（サブスクリプション契約）が作られている**＝「1か月ごとに配達／次回の注文まであと31日」。
- **顧客タグに業務用のタグが付いていない**（自動付与の仕組みが無い）。

---

## 4. いま業務で使っているものの棚卸し（**判定できたものだけ**）

| もの | 4人で共有できるか | 判定の根拠 |
|---|---|---|
| Shopify管理画面 | **できる**（スタッフを増やせば） | 管理画面にログインして実在を確認 |
| GitHub `atspect-handoff` | **できる**（現に共有中） | 本リポジトリ |
| Vault（`C:\Vault\…`） | **代表のPCにしか無い** | ローカルフォルダであることを実測 |
| Googleスプレッドシートの受付台帳 | **できる想定だが、作られたかは未確認** | 手順書は存在する（3名で同じ1枚を見る前提）。実物のシートは見ていない |
| Box | **未判定** | 開いていない |
| LINE公式 | **未判定** | 開いていない |

★**Box・LINE公式・スプレッドシート実物は開いていない＝判定していない。** 推測で書かない。

---

## 5. リサーチTからの申し送り2件 — どちらも実測した（直していない）

### 5-1. `name_en` の順 → **仕様書が誤りで確定**

- ライブの作家索引（テーマの `assets/artists.json`）を全数機械判定した。
- **判定できた606件すべてが「姓が先」（Family-first）。「名が先」は0件。** 判定不能4件。
- 仕様書＝`実装仕様書_現存作家ページ_v1.md` §2-1 の「鈴木の値＝`Chikako Suzuki`」は**名が先**＝ライブと逆。
- 背景（過去の決定）＝`_台帳_姓名順統一と読み違い点検_20260725` で
  **文化庁2019方針の姓先で統一**が方針として整理済み。ライブはその方針どおりになっている。
- → **直すべきはライブではなく仕様書**。**本便では直していない**（リサーチT／司令塔の管掌）。

★件数の食い違いを1つ発見（**判定していない・経緯不明**）：
metaobject定義の件数＝**611**、`artists.json`＝**610**、sitemapの metaobject ページ＝**625**。
申し送りの「625件」はsitemapの数と一致する。**どれが正かは決めていない。**

### 5-2. 販売作品の素材・寸法が独語ページで英語のまま → **確認。ただし申し送りより限定的**

独語ページと日本語ページを実ブラウザで取得して比較した。

| 出どころ | 独語ページでの表示 | 判定 |
|---|---|---|
| 代表作（作家metaobject側） | `Japanische Hinoki-Zypresse, Farbfassung, Metallblatt; …` | **独語になっている** |
| 販売作品3点（product側） | `Japanese cypress (hinoki), polychromy, gold leaf …` | **英語のまま** |

★**寸法は英語のままではない**＝独語ページでは記号が `H … × B … × T …`（日本語は `H … × W … × D …`）と
**言語連動している**。**英語のまま残っているのは「素材」の文字列だけ**（販売作品3点）。
申し送りの「素材・寸法が英語のまま」は、**素材のみが正しい**。**本便では直していない。**

---

## 実施していないこと（正直な申告）

- **メールの実物を1通も見ていない**（webmailにセッションが無く、パスワードは入力しない）。
  →**差出人・件名・本文の分類＝未実施**。振り分けルールの材料は取れていない。
- **テストモードの注文を作っていない**（本日実運用中のため。理由は上記3）。
- **Box・LINE公式・Googleスプレッドシート実物を開いていない**＝共有可否は未判定。
- **注文のタイムラインの「メールを表示」を開いていない**（開けば注文メール本文は見られる可能性がある）。
- **§5の2件はどちらも是正していない**（実測と起票のみ）。
- 件数の食い違い（611 / 610 / 625）は**どれが正か決めていない**（経緯不明として残す）。
- Codexレビューは実施していない（本便は読み取り・実測のみ）。
- 作家データ・テーマ・規約類には一切触れていない。
