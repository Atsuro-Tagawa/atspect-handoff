# 記録：推薦の仕組みを作りました（2026-08-01・システム開発T）

**★ご指示の「別々に持たない」を最優先で設計しました。** 7/31のパンフの事故と同じ型を作らないためです。

---

## 1. 推薦用フォーム＝**一般用と同じファイル1つ**です（別管理にしていません）

- 実体＝`sections/atspect-artist-registration.liquid`（**一般用が使っているのと同じファイル**）
- 推薦用ページは、同じセクションを **`form_variant = referral` という設定1つだけ変えて**呼び出しています
- **したがって、一般用フォームを直せば、推薦用にも必ず同じ変更が反映されます。**
  片方を直し忘れるという事故が、構造的に起きません（直す場所が1つしかないため）

### 実測（両方を実際に開いて比べました）

| 見たもの | 一般用 `/pages/artist-registration-form` | 推薦用（URLのみでお渡し） |
|---|---|---|
| 見出し | 登録のお申し込み | **登録審査申し込み（ご推薦者あり）** |
| 入力項目の数 | 22 | 23（＝22＋見えない推薦者コード欄1つ） |
| 種別（通知メールの振り分け） | 作家登録審査申込 | **作家登録審査申込**（同じ） |
| 推薦者コード欄 | なし | あり（作家さまには見えないhidden） |

**★一般用は見た目も項目も一切変わっていません**（増えたのは推薦用だけに出る見えない欄1つ）。

## 2. 推薦者ごとの識別＝**URLの末尾だけ**。フォームは増えません

```
（推薦用フォームのURL）?r=<推薦者コード>
```

- **フォームに推薦者名の入力欄は作っていません**（作家さまの手間を増やさないため・ご指示のとおり）
- **推薦者が何人増えても、フォームは1つのままです。** 増えるのはコードだけ
- コードは英数字・ハイフン・アンダースコア・32文字以内に制限しています
  （通知メールに任意の文字列を差し込まれないようにするため）
- **実測**＝`?r=TANAKA-01` → そのまま記録／`?r=` なし → 「（未取得＝URLに ?r= が付いていない）」／
  `?r=<script>x` のような不正な文字 → 「（形式が正しくないコード）」として記録し、そのまま通しません

**★URLは秘匿のため、この公開リポジトリには書いていません**（Vaultの確定記録にのみ記載）。
sitemapに載らない設定（`seo.hidden=1`）も入れています。

## 3. 管理表（見るべきものを全部入れました）

`node scripts/referral-report.mjs`

出るもの＝**①推薦者ごとの集計**（成約件数・金額・入金待ち件数）／**②月ごとの内訳**／**③明細**
（どなたの推薦か・作家さまのお名前・申込日・入金確認日・5,000円の対象か・**その判定の理由**）。

**★数え方はご指示のとおりです。**

- 1件＝**情報登録料のご入金が確認できたとき**。**申し込みだけでは1件と数えません**（明細に「入金の確認がまだ」と理由が出ます）
- **集計は入金確認日を基準**にしています（申込日ではありません）
- 受付期間（2026年8月1日〜11月30日）の外の入金は、対象外として理由つきで表示します
- 報酬＝1件 5,000円／清算＝12月一括、と表の冒頭に必ず印字されます

**★台帳の実データは Vault に置いています**（作家さまのお名前が入るため、この公開リポジトリには置きません）。
この公開リポジトリにあるのは**動作を見せるためのサンプル（見本 太郎などの架空名）だけ**です。

## 4. 毎月の集計

上記②が月ごとの表です（入金確認日の月で数えます）。**文面はリサーチTにお願いする前提で、器だけを作りました。**

---

## 5. ★販売プラン名「1か月ごとに配達」の変更案（実行していません）

**画面を自分で開いて確認しました。**
アプリ →「Subscriptions」→「プラン」→「あつぺくと 月額3,300円 自動継続 システム利用料」

- **画面に「変更は新規登録者にのみ適用。既存の登録者には影響しません。」と明記されています**
  ＝プラン名を変えても既存のご契約は壊れません
- 画面で確認できた欄＝タイトル／内部説明／商品／ディスカウントタイプ／**配達頻度（1・か月）**

### ★正直に：画面からは変えられない可能性が高いです

**「1か月ごとに配達」を直接入力する欄は、確認できた範囲には見当たりませんでした。**
「配達頻度」の数値と単位から自動で組み立てられているとみられます。
**画面の下部は、今回も最後まで確認できていません**（このアプリは別ドメインのiframeの中で動いており、
ブラウザの窓が小さいままだとスクロールが効かず、窓の拡大もこの環境ではできませんでした）。

### ★ただし、APIでは変えられることが分かりました（実測）

Admin APIの型を調べたところ、`SellingPlanInput` に **`name` フィールドが存在します**
（`id, name, description, billingPolicy, deliveryPolicy, inventoryPolicy, metafields, pricingPolicies, options, position, category`）。
つまり **`sellingPlanGroupUpdate` で顧客向けのプラン名を直接指定できます。**

### 案（実行はしていません）

| # | 変えるもの | 変更前 | 変更後（案） |
|---|---|---|---|
| 1 | 販売プラン名（顧客向け・メールに出る） | `1か月ごとに配達` | **`1か月ごとに更新`** |
| 2 | （変えないもの）タイトル | あつぺくとシステム利用料（月額） | そのまま |
| 3 | （変えないもの）内部説明 | あつぺくと 月額3,300円 自動継続 システム利用料 | そのまま（対外に出ません） |

**手順＝Admin APIの `sellingPlanGroupUpdate` で、当該プランの `name` を「1か月ごとに更新」にする。**
実行前後に同じAPIで読み直して一致を確認し、通知メールのプレビューでも描画を目視する、という手順にします。

**★決済の本番設定にあたるため、ご確認をいただいてから実施します。**

---

## 6. ★実施していないこと・判定していないこと（正直に）

- **推薦用フォームの実送信テストをしていません**（hCaptchaのため人にしか解けません）。したがって
  **通知メールに「ご推薦者コード」の行が実際に出るかは未確認**です。**1回テスト送信していただく必要があります。**
- **一般用フォームの実送信テストもしていません**（画面上は無変更を確認済みですが、送信までは通していません）。
- **管理表は実データで動かしていません**（台帳はまだ空です）。サンプルデータでの動作は確認済みです。
- **推薦者コードの実際の割り当てはしていません**（どなたに何のコードをお渡しするかは未定のため）。
- **販売プラン名は変更していません**（上記5のとおり案のみ）。**画面の下部は未確認**のままです。
- **Codexレビューは未実施**です。
- 作家向けページのliquidのうち、**terms-artist／entry／onboarding には一切触れていません**。
  今回触ったのは `atspect-artist-registration.liquid`（登録審査申し込み）です。
  **これはご指示の「別々に持たない」を満たすために避けられませんでした**（見出しの切替と推薦者コードの欄は、
  フォーム本体に入れるほかにないため）。差分は上記1・2のとおりで、**一般用の表示は変わっていません**。
  編集前の版は `_backups\_auto\theme161-atspect-artist-registration.liquid\` に自動保存されています。
