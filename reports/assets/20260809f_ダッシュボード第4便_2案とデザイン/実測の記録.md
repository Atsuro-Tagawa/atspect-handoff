# ダッシュボード第4便＝ARTS RESPECT化・デザイン2案・機能追加（2026-08-09・システム開発T・model=Opus 5 (1M context)）

★このリポジトリはPublicのため、**URL・固有デプロイURL・アカウントID・チーム名は書きません**。
正本＝`C:\Vault\ARTS-RESPECT\_本番_ダッシュボード施錠設定_20260808_ATSPECT.md`

★**Codexの独立レビューは、本便では実施できませんでした**（利用上限に到達。詳細は§7）。

---

## 1. ★デザイン2案（代表にお選びいただくもの）

**同じ見本データ・同じ「ホーム」を、色と質感だけ変えて2案作りました。**

| 画像 | 中身 |
|---|---|
| `2案を並べた画面.png` | **左右に並べた比較画面**（この1枚でお選びいただけます） |
| `A案_PC.png` / `A案_スマホ.png` | A案＝白基調＋状態色 |
| `B案_PC.png` / `B案_スマホ.png` | B案＝華やか |

- **A案**＝白と細い線で静かに。**色は「状態」にだけ**使う（止まった=赤／今日=黄／入金=緑／完了=灰）。
  数字を大きく。担当は**左の細い線の色**で分ける。
- **B案**＝和紙のような温かい地色に淡い光。見出しに色の帯、**担当は面で色分け**、
  数字の枠も意味の色で塗る。数字の大きさと状態色はA案と同じ。

**切り替えは色の指定だけ**で行っています（`?theme=a` / `?theme=b`）。
**どちらを選んでも作り直しはありません。**

## 2. ARTS RESPECT化

- ヘッダーに**既存のARTS RESPECTロゴ**（コーポレートサイトと同じもの）＋社名＋「あつぺくと」。
- **「〜部」を全廃**＝あつぺくと／SNS／経理／SHIMON。見出しの「部署」も「担当」に。
  ★**データ側（基準の表）の値にも「あつぺくと部」があり**、コードだけ直すと
  **突き合わせが外れて基準が引けなくなるところでした。** 両方直して一致を確認しています。

## 3. 機能の追加

| 追加 | 中身 |
|---|---|
| **目標バー** | 最上部に「◯月の登録 ◯件／37件」＋進捗バー＋今月の入金。**出すのは件数と入金だけ** |
| **3秒で掴む数字** | 止まっています／メール（仕事）／迷惑・その他／今月の登録 を大きな数字で横並び |
| **有効期限の表示** | ヘッダー右上に**常時**「◯時まで有効（あと◯時間◯分）」。残り30分で黄、10分で赤＋一言 |
| **下書きの自動退避** | 入力中の内容をこのブラウザへ自動退避。入り直したあと**「戻す／捨てる」**を選べる |
| **メールの2箱** | 「仕事」「迷惑・その他」の2箱＋件数。**まちがいはワンタップで教えられる**（画像あり） |

### ★有効期限の表示で気をつけたこと（嘘を書かないため）

時刻の出どころが**2つ**あります。

1. Cloudflare Access の身元情報が読めたとき＝**本当のログイン時刻**。このときだけ「**ログイン ◯時**」と出す。
2. 読めないとき（鍵の外・手元で開いたとき）＝**この画面を開いた時刻**から数える。
   このときは「**開いてから ◯時**」と出す。★**「ログイン」とは書きません。**

★**1の経路は、まだ実際に確かめられていません**（誰もログインできていないため）。
　いま画面に出ているのは**すべて2の経路**です。

### ★メールの2箱で気をつけたこと

**サーバー側の迷惑メールフォルダは触っていません。** 画面にもそう明記しました。

> 「★この2つの箱は**この画面での見え方だけ**です。メールの箱そのもの（サーバー側の迷惑メールフォルダ）は
> 触っていません。分け方がちがっていたら…教えてください。教えていただいた内容は
> **このパソコンにだけ**残ります（4人では共有されません）。」

## 4. 設定ファイルを配信物から外しました（裁定1）

`config.local.js` の読み込みを**全13ページから削除**しました（残り0件）。
非公開の行き先は、配信物に一切埋め込まない形になりました。

## 5. セッションの変更（裁定済み）

| 対象 | 前 | 後 |
|---|---|---|
| 本番ホスト名のアプリ | 30分 | **6時間**（APIで実測確認） |
| 自動作成のアプリ（デプロイ固有URL用） | 24時間 | **24時間のまま（変更できず）** |

★**自動作成側は、画面の選択肢が開かず変更できませんでした。**
`セッション期間` の欄は見えるものの、押しても一覧が出ず、要素の位置も画面外の値を返し続けました
（2便にわたって同じ症状）。指示どおり**現状のまま**にし、理由を記録します。
**機能上の穴ではありません**（このアプリで入れるのは代表1名のみ・デプロイ固有URLは普段使いません）。

## 6. ★全経路の再実測（未認証・キャッシュ破り）

デプロイ後に、**6経路すべて**を素のcurlで測り直しました。

| 対象 | 結果 |
|---|---|
| 本番のホスト名 | **302 → Accessのログイン画面**（本文は返らない） |
| 今回の新デプロイの固有URL | **302** |
| 直前のデプロイの固有URL | **302** |
| さらに前のデプロイの固有URL | **302** |
| 前々便で削除したデプロイの固有URL | **302** |
| 空だったデプロイの固有URL | **302** |

**6経路すべてで「あつぺくと」の語＝0件。** 迂回路は塞がったままです。

## 7. ★Codexの独立レビューができませんでした（正直に）

依頼を投げたところ、次のエラーで止まりました。

> `You've hit your usage limit. … try again at Aug 16th, 2026 10:00 AM.`

これは**ハングではなく利用上限**です（「usage limit」「Upgrade」「try again at 日時」の語で判別）。
`--fresh` や再試行では復旧しません。**次に使えるのは 2026-08-16 10:00 以降**の見込みです。

**代わりに行った自己検証**（すべて機械で実施）
- **全13ページ × 2案 × 2画面幅＝52通り**を描画し、**JSエラー0件・「〜部」の残り0件**。
- 出た404は**意図した4つだけ**（実データ3件＋Access配下でのみ存在する身元情報の照会1件）。
- `app.js` の文法検査、`kijun.json` のJSON検査とコード側の突き合わせ一致。
- 配信物の検査（入ってはいけないもの0件）とZIPの往復ハッシュ一致。
- **両案・両幅を目視**（画像を同梱）。1回目のKPIがリンクの下線・青色に負けていたのを発見して是正しました。

★**「Codexが見ていない」ことを、見たことにはしません。** 復旧後に遡って独立レビューにかけます。

## 8. ★実施していないこと・判定していないこと

- **実データは接続していません**（裁定1どおり。見本データのままです）。
- **★誰かが実際にログインできたことは、まだ一度も確認できていません。**
  したがって**有効期限の表示の「本物のログイン時刻」経路も未検証**です。
- **★届く数字の桁数は、依然として未確認です**（6桁とは書きません）。代表の初ログイン待ち。
- **通しの撮影（ログインを通した画面）はできていません。**
- **自動作成アプリのセッション24時間を6時間に揃えられていません**（§5）。
- 許可リストの4件目は**触っていません**（裁定3どおり）。
- 見本の画面が「4人にとって分かりやすいか」は判定していません（お使いいただいて分かることです）。
- 実機（スマートフォン実物）では見ていません。**画面幅を390pxにした表示**までです。

## 9. 成果物

- 4人に配る紙＝`C:\Users\ataga\Desktop\ダッシュボードの開き方.txt`（**6時間**の説明に更新済み）
- 手元ですぐ見る用＝`C:\Users\ataga\Desktop\ダッシュボードを今すぐ見る\`（★中身は第4便のものに未更新）
- 配信物＝`C:\Users\ataga\atspect-system\scratch\deploy`（23ファイル）
