# 2026-08-03 第3便：作品寸法の件・受付台帳・A-1直後の段取り（システム開発T・Opus 5）

**個人情報（作家さまのお名前・ご連絡先）は置きません。台帳は見本の名前だけのひな型です。**

---

## 1. 作品の寸法の件＝**当方が直す前に、別のターミナルが先に直していました**

### 起きたこと（時刻つき）

| 時刻 | 出来事 |
|---|---|
| 11:40 | ライブを読み、食い違いを確認。`masterpiece_1_caption`＝**H23×W30×D20㎝**／`artist_i18n.works[0].medium`＝**8言語とも高さ（H23）だけ** |
| 11:43 | 8言語ぶんの是正の計画を作成（**書き込みはしていない**） |
| 11:47 | Codexの独立レビューで8言語の訳を確認＝「修正案は適切。直す必要なし」 |
| 11:50 | **適用の前にライブの画面を見に行ったところ、日本語も英語も既に H23×W30×D20 になっていた** |
| 11:52 | ライブのデータを取り直したところ、**8言語すべてが H23×W30×D20 に直っていた**。さらに素材が「箔／gold leaf」から**「金属箔／metal leaf」へ変わっていた** |

### そこで、当方の計画は**適用しませんでした**

**適用していたら退行になるところでした。** 当方の計画は「gold leaf」のままの文面だったため、
今から当てると**別のターミナルが直した「metal leaf」を巻き戻して**しまいます。

計画ファイルには `_STALE_適用しないこと_` を頭に付け、理由を書いた説明を同じ場所に残しました。

**機械での確認**＝`node scripts/artist-i18n-update.mjs verify`
→ **作品の寸法の食い違い＝0件**（625名全員。当方が最初に測ったときは1件でした）。
**画面でも確認**＝日本語・英語とも `H23 × W30 × D20 cm`（画像01・02）。

### ★教訓（同じ型を繰り返さないために）

**計画を作ってから当てるまでの間に、他のターミナルが同じ場所を直していることがある。**
**当てる直前に必ずライブを取り直す。** 今回は「当てる前に画面を見る」という手順があったから気づけました。

## 2. それでも道具は直しました（次に同じことが起きたとき使えるように）

`scripts/artist-i18n-update.mjs` に2つ足しました（**テスト11件・全197件green**）。

1. **works / publications に対応**（これまでは「未知のキー」として弾かれ、この経路では直せなかった）。
   **配列まるごとの差し替えはさせません。**「何番目の・どの小項目の・どの言語」を指定して**1マスだけ**直す形です。
   理由＝まるごと渡させると、指示書に書き忘れた作品や言語が**消えます**。作家さまの掲載内容なので消してはいけません。
   ライブに無い番号を指定したときも、配列でない値が入っていたときも、**エラーで止まります**。
2. **寸法の食い違いの自動検出**＝`masterpiece_N_caption` と `artist_i18n.works[N-1].medium.ja` の
   **寸法の数字**を突き合わせます。`㎝`と`cm`、全角と半角、空白の有無は吸収し、**数字が違うときだけ**鳴らします
   （題名や年号の数字は拾いません）。**625名で実走し、リサーチTが見つけた1件だけを検出**しました
   （今は直っているので0件）。

## 3. 受付台帳（8月に50件をお受けする前提）

**手順書＝`docs/手順_受付台帳をGoogleスプレッドシートで始める.md`（このリポジトリの docs 配下）**
**ひな型＝`docs/reception-ledger-samples/受付台帳.template.csv`（見本の名前のみ）**

- **自社では作りません。Googleスプレッドシートで今月から始めます。** 足りないと分かった部分だけ後で作ります。
- **★表は1枚だけ。** 8月1日に作った推薦の台帳に**4列を足したもの**を受付台帳にします。別の表は作りません。
- 列＝`段階 / 作家さまのお名前 / 申込日 / 入金確認日 / 推薦者コード / 推薦者名 / 次にやること / 担当 / 備考`
  （**前の4つは8月1日の列名のまま**＝二重管理にしない）
- 段階の6つ＝`申込 / 入金確認 / 素材お預かり中 / 原稿作成中 / 公開のご確認待ち / 公開済み`（プルダウンにする）
- 推薦者名は**VLOOKUPで自動表示**（手で打たないのでコードと名前が食い違いません）
- **★実際に確認しました**＝この9列のCSVを推薦の集計 `referral-report.mjs` に食わせて、
  **これまでどおり動く**こと（列を足しても壊れない）を実行して確かめました。

## 4. A-1が通った直後にやること

**`手順_A-1が通った直後に測ること.md`（このフォルダ内）**

いまは決済実績が0件のため、管理画面に**入金予定日を出す欄そのものが出ていません**（第2便で実測）。
**1件通った瞬間に初めて表示されます。** そこで測らないと初回入金日が分からないままになります。
**API経由では測れません**（`shopifyPaymentsAccount` も `orders` も当店のアプリでは ACCESS_DENIED＝**実測済み**）。
**＝画面を開いて目で見て記録するしかありません。**

## 5. 実施していないこと・判定していないこと

- **作家データへの書き込みは1件も行っていません。**（そもそも不要になりました。）
- **`SHOPIFY_ALLOW_WRITE=1` は当方では立てていません**（代表専管の一線）。計画は作りましたが適用していません。
- **受付台帳の実物（スプレッドシート）は作っていません**＝作成と共有は代表の操作（Googleアカウントが要るため）。
  当方が用意したのは**手順書とひな型**までです。
- **決済は1件も通していません。** 初回入金予定日・着金日数は引き続き**未実測**。
- 「metal leaf」への変更が正しいかどうかは**当方では判定していません**（作家さま情報の正否はリサーチTの領分）。
  当方が確かめたのは「8言語が互いに同じ内容になっていること」と「寸法の数字が caption と一致すること」だけです。
