# コントラストとタップ範囲の是正（2026-08-01・SNS運用T 第5便）

対象＝当ターミナル専任の2ファイル（`atspect-terms-artist.liquid`＝terms-artist／entry共用、`atspect-artist-onboarding.liquid`）。
測り方は**制作T第4便の方法に合わせた**（実効文字色は半透明なら合成、**実効背景は祖先を遡って解く**、不足は**二分探索で必要値を解いてから**是正）。

## 1. 繰-制作-06（制作Tからの起票）＝是正しました

`.atspect-aterms__form-eyebrow`（「Registration」の金茶）

| 項目 | 値 |
|---|---|
| 実効背景（このページで解いた値） | `rgb(255,255,255)` |
| 是正前 `#a08763` | **3.42:1**（制作Tの実測値と一致） |
| **是正後 `#786549`** | **5.59:1** |

★制作Tの報告値は4.88でしたが、それは**登録フォーム側のクリーム地**で解いた値です。
本ページの実効背景は白のため**5.59**になります（＝どちらもAA 4.5:1を満たす）。

**二分探索の結果**：色相を保ったまま4.5に届く**最も明るい**色は `#8a7354`（4.51）でした。
それでも **`#786549` を採用**しています。理由＝**作家さまは登録フォーム→本ページと続けて通るため、ぎりぎりの値より「同じ金茶で揃える」ことを優先**したためです（制作Tが登録フォームで採用済みの値と同一）。

## 2. onboarding のコントラスト全数測定＝**不足0件**（是正の必要なし）

| ページ | 測定件数 | 不足 |
|---|---|---|
| onboarding | 22 | **0** |
| terms-artist | 34 | 0（上記1の是正後） |
| entry | 31 | 0（同上） |

★**測り方の穴を1つ自分で塞ぎました**＝当初「子要素を持つ要素」を対象外にしていましたが、
`<p>地の文<strong>強調</strong>地の文</p>` のように**子要素を持ちつつ自分の色で地の文を描く要素**を取りこぼします。
**直下にテキストノードを持つ要素はすべて対象**に変更した結果、測定件数が onboarding 16→22／terms 29→34 に増えました（結論は変わらず）。
`::placeholder`／`::before`／`::after` も対象に含めています（制作Tが第4便で「擬似要素を拾えず見落とした」と申告されていた死角）。

## 3. タップ範囲の全数確認＝★モバイルだけでは見落としていました

**第2便では320〜430pxしか測っておらず、44px以上に見えていました。今回6幅で測り直したところ、
文言が1行に収まる広い幅（430px以上・PC）で24〜37pxまで縮んでいました。**

| 対象 | 320〜390px（文言が2行以上） | 430px以上（1行に収まる） |
|---|---|---|
| `.atspect-aterms__check-row` | 44px以上 | **24px** ★ |
| `.atspect-aonb__check-row` | 44px以上 | **37px** ★ |

**是正＝両ファイルの同意チェック行に `min-height: 44px` を追加**（1行のときも44pxを確保。2行以上のときは従来どおり）。

**是正後の実測＝3ページ × 6幅（320／390／430／768／1440／1920）＝18通りすべてで 44px未満 0件。**

※`170x21` の入力欄が検出されましたが、`contact[hp_website]`・`tabindex="-1"`・`aria-hidden="true"`・`left:-9999px` の**ハニーポット**でした（実タップ対象ではないため除外）。
※**文中リンク**（`display:inline` の `<a>`）は行の高さに従うため別枠にしています。ブロック化すると文章の流れが崩れるため、**当方では是正対象としていません**。

## 4. 実施していないこと・判定していないこと

- **色を濃くしたことの意匠の良し悪しは判定していません**（色相は保持・明度のみ変更）。ブランド色の明度変更として許容できるかは代表のご判断です。
- **AAA（7:1）には合わせていません**（AAの4.5:1まで）。
- **ホバー時・フォーカス時の色は測っていません**（通常状態のみ）。
- **色覚特性（色弱）への配慮は検証していません**（コントラスト比のみ）。
- **8言語それぞれでの測定はしていません**（文字色は言語で変わりませんが、フォントの太さの差は未検証）。
- **文中リンクのタップ範囲は是正していません**（上記のとおり）。
- **実機での目視・実タップは未実施**（実機確認は代表対応事項）。
- 専任外のファイルには一切触れていません。

---

## 5. hCaptchaに到達するかの確認＝★到達しました（どこまで自動／どこから人か）

自分で挙げた未確認（「送信は1件も行っていない」）のうち、**hCaptchaに到達するかまで**を確かめました。
**フォームの送信POSTだけを遮断**したうえで送信ボタンを押しています（**実際の送信は発生していません**）。

### 結果

| 段階 | 状態 |
|---|---|
| ページを開いた時点 | hCaptchaのscript・器・challenge要素とも**0件**＝**まだ存在しない** |
| 全項目を入力・選択・チェック | HTML5検証「通る」／送信ボタン「押せる」 |
| **送信ボタンを押す** | **hCaptchaのパズルが実際に出る**（`hCaptchaチャレンジ` iframe **320x400・見えている**／文言「パターンと異なる動物アイコンを選択してください」＋画像グリッド） |
| フォームのPOST | **発生していない**（hCaptchaが先に立つため、解くまでPOSTに到達しない） |

→ 画像＝`hcaptcha_reached.png`

### ★切り分け

- **自動で通せる範囲**＝ページを開く → 全項目の入力・選択・チェック → **送信ボタンを押して hCaptcha のパズルが出るところまで**。
  **ここまでは毎回自動で再現できます**（本日、幅390pxで実施）。
- **人の操作が必要な範囲**＝**hCaptchaのパズルを解く（画像を選ぶ）以降のすべて**。
  解かない限りフォームのPOSTは発生しないため、**送信後の画面・受信内容・自動返信メールは、人が解かない限り一度も確認できません**。
  これは「人間にしか解けないもの」＝代表にお回しする6区分の①に該当します。

### ★自分の測り方の誤りを1つ訂正

最初の計測では「`atspect.com` を含むPOSTを遮断」としたため、**hCaptcha自身の設定要求
（`https://api.hcaptcha.com/checksiteconfig?...&host=atspect.com&...`＝クエリにドメイン名が入る）まで巻き込んで遮断**しており、
パズルが初期化できず「見えていない」と出ていました。**URLのoriginで厳密に判定**するよう直したところ、上表のとおりパズルが実際に表示されました。
（＝「出ない」と報告する前に自分のスクリプトを疑う、の実例）

### この件で判定していないこと
- **hCaptchaを解いていません**（＝送信していません）。**実際に送信が成立するか、受信内容が正しいかは未判定**です。
- **パズルの難易度・出題頻度は測っていません**（1回出たことのみ確認）。
- **他のフォーム（第1段階・対面用・解約）でも同じ挙動かは未確認**（onboardingで確認したのみ）。

---

# 第6便（19:00）＝繰-制作-07/08の是正・8言語測定・hover/focus・文中リンクの解決

## 6. 繰-制作-07／08＝是正しました（同じ測り方で再現してから）

**まず制作Tの測定値を自分で再現しました**（幅390px）：

| 画面 | 必須 | ラベル | ヒント | 判定 |
|---|---|---|---|---|
| 第1段階（登録フォーム） | 12 | 14 | 13 | 正常 |
| 第2段階（onboarding） | **10** | 14 | 13 | 正常 |
| terms-artist | **10** | 14 | **16** | ★ヒント>ラベルの逆転 |
| entry | **10** | 14 | **16** | ★同上 |

＝制作Tの報告と完全に一致。そのうえで是正しました。

- **繰-制作-07（必須バッジ）**＝`10px → 12px`（terms／entry／onboarding）。**代表指示「小さいほうに揃えない」に従い、大きい側へ揃え**ました。
- **繰-制作-08（ヒントの逆転）**＝terms／entry の `16px → 13px`。
  ★16→13は縮小ですが、①**逆転そのものが不具合** ②**4画面中3画面が既に13px** ③ラベルを上げると**ラベルの画面間の揃い（全画面14px）が崩れる**、の3点から13pxを選びました。
  WCAGの「大きい文字」は24px以上（太字18.66px以上）のため、**16→13でも判定基準は4.5:1のまま変わりません**（コントラストの再測定でも不足0件）。

**是正後（幅390px実測）＝4画面すべて 必須12px・ラベル14px・ヒント13px で揃いました。**

## 7. 8言語での測定＝**144通り全合格**

**3ページ × 6幅（320/390/430/768/1440/1920）× 8言語（ja/en/de/fr/es/zh-cn/zh-tw/ko）＝144通り。**
**44px未満0件・泣き別れ0件・横溢れ0件・ページ横スクロールなし。**

### ★あわせて分かったこと（範囲の限界）
言語を切り替えたときの本文の文字数を実測しました：

| ページ | ja | en | de | ko | 言語で変わるか |
|---|---|---|---|---|---|
| terms-artist | 4585 | 4688 | **4718** | 4601 | **変わる**（deが最長＝ご指摘のとおり） |
| entry | 4402 | 4440 | **4457** | 4397 | **変わる** |
| **onboarding** | 1729 | 1729 | 1729 | 1729 | **変わらない＝日本語のみのページ** |

- **onboardingには多言語スパンが0本**＝審査通過者向けの日本語のみの導線のため、言語を切り替えても文言が変わりません。**したがって「en/deで長くなる」影響はこのページには生じません**（実測で確認）。
- terms-artist／entry で言語により変わるのは**「本規約の正文は日本語です」の注記と、本日追加した読み上げラベル**の2か所です。**フォーム本体は日本語のみ**。
- **★将来これらのページを多言語化した場合は、この144通りの測定をやり直す必要があります。**

**コントラストは言語で変わりません**（文字色・背景色は言語に依存しないため）。念のためja/en/de/koで再測定し、不足0件を確認しています。

## 8. hover時・focus時＝**279測定・不足0件**（制作T第4便の実ホバー方式）

**静的なCSS読み取りではなく、実際に `hover()` させ・実際に `focus()` させて、計算後の値を読みました。**

| ページ | 対象 | 測定数 | 結果 |
|---|---|---|---|
| terms-artist | 50件 | 150（通常/hover/focus） | **不足0件** |
| onboarding | 43件 | 129（同上） | **不足0件** |

## 9. ★文中リンクのタップ範囲＝**解決しました**（第5便の「崩れるので是正しない」を撤回）

第5便では「ブロック化すると文章の流れが崩れる」として見送りましたが、**代わりの手がありました。**

実測で3案を比較（幅390px）：

| 案 | リンクの高さ | 行位置の移動 | ページ高さの変化 | 判定 |
|---|---|---|---|---|
| 現状 | 16〜18.4px | — | — | 44px未満 |
| **案A：inlineのまま上下padding** | **44〜46.4px**（14px時） | **0段落** | **0px** | **★崩れない** |
| 案B：`display:inline-block` | 42.4〜45.4px | — | **+102px** | 崩れる（第5便の想定どおり） |

**＝`display` を変えずに `padding-top/bottom` だけを足せば、行の位置もページの高さも1pxも動かないまま、当たり判定だけが2倍以上に広がります。**
paddingを11→13→14→16pxと振って、**14pxで両ページとも44px以上に到達**することを確かめて採用しました（**リンク同士の当たり判定の重なりも0件**）。

### あわせて「リンク名の泣き別れ」も解消
幅320pxで「こちら」「お問い合わせ」「プライバシーポリシー」が**行をまたいで割れていました**（3〜10文字の短いリンク名が途中で切れる）。
`white-space: nowrap` を追加。**最長リンク「パンフレット（料金のご案内を含む）」でも201pxで、行の幅268pxに収まる**ため横溢れは起きません（幅320pxで実測・横スクロールなし）。

**→ 第5便で「実施していないこと」に挙げた文中リンクの件は、これで閉じます。**

## 10. 第6便で実施していないこと・判定していないこと

- **8言語のうち、フォーム本体は日本語のみです**（onboardingは完全に日本語のみ、terms/entryも注記2か所以外は日本語）。**「多言語の文章が長くなって崩れる」ことの検証は、現状では原理的にできません**（訳文が存在しないため）。多言語化されたら再測定が必要です。
- **CJKのフォントフォールバックによる線の太さの差は測っていません**（制作Tも同じく未測定）。
- **色覚特性（色弱）への配慮は検証していません**（コントラスト比のみ）。
- **AAA（7:1）には合わせていません**（AAの4.5:1まで）。
- **実機での目視・実タップは未実施**（実機確認は代表対応事項）。
- **ヒント16→13pxが読みやすさとして妥当かは判定していません**（「4画面で揃っているか」だけを基準にしました）。
- 専任外のファイルには一切触れていません。決済・通知メール・API・作家データ・紙にも触れていません。

---

# 第7便（20:00）＝読みやすさの判定と是正・色覚特性・極小文字・実機の切り分け

## 11. ★「ヒント13pxが読みやすいか」の判定＝**小さすぎると判定し、引き上げました**

第6便で「揃っているかだけを基準にした」と書いた点を、**読みやすさそのものとして判定し直しました。**

### 判定の根拠＝このサイト自身の本文は16px

| ページ | 本文`<p>`の最頻サイズ |
|---|---|
| terms-artist の**規約本文** | **16px**（27段落） |
| privacy（法的文書） | **16px**（22段落） |
| about | **16px**（7段落） |
| faq | **16px**（9段落） |
| artist-guide | 14px（4段落） |

**＝サイトの本文標準は16px。フォームだけが13pxでした。**

★**とくに問題だったのは terms-artist です**：作家さまは**規約本文を16pxで読んだ直後に、同意文13pxに同意する**形になっていました。**読む文字より、同意する文字の方が小さい。** 年配の方が多い前提では、ここは逆です。

### 是正した内容（専任2ファイル）

| 項目 | 前 | 後 | 根拠 |
|---|---|---|---|
| 本文（lead・form-lead・step-t） | 13px | **16px** | サイトの本文標準 |
| **同意文（check-text）** | 13px | **16px** | 同意する文字を、読む文字と同じ大きさに |
| ラベル | 14px | **16px** | ヒントとの逆転を作らないため |
| ヒント | 13px | **15px** | ラベルより一段小さく・可読性は確保 |
| ラジオの選択肢 | 13.5px | **16px** | 本文級 |
| 注記（consent-note） | 11.5px | **14px** | — |
| **読了ゲートの案内（gate-note）** | 11.5px | **15px** | ★**なぜチェックが押せないかを説明する文**。読み落とすと2026-07-31に代表が遭遇した「進めない」状態の再発につながる |
| 料金の案内（pdf-note） | 11.5px | **15px** | 料金をどこで見るかの案内 |
| アイブロウ（form-eyebrow） | 10.5px | **12px** | onboardingの12pxに合わせた |

### 影響の実測（代表のご指示どおり）

| ページ | ページ高さ（幅390px） |
|---|---|
| terms-artist | 7,157px |
| onboarding | 5,918px |

**縦に長くはなりますが、崩れは1件も出ていません。**
**再検証＝3ページ×6幅×8言語＝144通りすべてで、44px未満0・泣き別れ0・横溢れ0・12px未満0・横スクロールなし。コントラストも不足0件。**

画像＝`before13_onb.png`（13px時）／`after16_onb.png`・`after16_terms_consent.png`（16px化後）

### ★第1段階（登録フォーム）は制作Tの担当のため触っていません
**当方の3画面だけが16pxになり、第1段階（本文13px・ラベル14px）とは一時的に揃わなくなります。**
代表のご指示「4画面すべてを大きい側で揃え直してよい」に沿うには**制作Tでの是正が必要**です → 判断待ちマスターへ起票しました（具体値つき）。

---

## 12. 色覚特性への配慮＝**色だけで意味を伝えている箇所はありませんでした**

| 確認項目 | 結果 |
|---|---|
| 必須の示し方 | **文字「必須」**＋赤 → **色だけではない** |
| 任意の示し方 | 文字「任意」＋灰（該当ページのみ） |
| エラーの示し方 | **文字「メールアドレスが一致していません。ご確認ください。」**＋赤 → 色だけではない |
| ラジオ・チェックの選択状態 | **ネイティブの●／✓（形）**で分かる＋枠色の変化 |
| 読了ゲートの状態 | **文言そのものが変わる**（「〜チェックできるようになります」→「お読みいただきました」） |
| リンク | onboarding＝**全6本に下線**／terms-artist＝5本中4本に下線 |
| 藍と朱の使い分け（AR-P1/AR-P2） | **サイト側にはプランを色で区別している箇所はありません**（本文の色は藍系リンク・赤系必須のみ） |

**実際に色覚特性のシミュレーション画像を作って目視しました**（1型＝P型・2型＝D型）。
**赤が灰色に見える状態でも「必須」の文字はそのまま読め、リンクの下線もラジオの形も残ります＝情報が失われません。**
画像＝`colorblind_1gata.png`／`colorblind_2gata.png`

### 参考（不具合とは判定していない）
terms-artist の「← 作家の方へ（ご案内）に戻る」だけ下線がありません。ただし**色でリンクを示してもおらず**（本文と同じ灰）、**「←」の記号と独立した位置**で示されているため、**色覚特性の問題ではない**と判定しました。制作Tの登録フォームも同じ様式のため、当方だけ変えると不揃いになります。

---

## 13. 小さすぎて読めない図・ラベルの全数確認＝**図はありません／極小文字は0件になりました**

制作TがAboutの図で5.4pxのラベルを発見された件を受けて、専任3ページを全数確認しました。

| ページ | SVG内の文字 | 画像 | canvas | 12px未満の文字 |
|---|---|---|---|---|
| terms-artist | **0** | **0** | **0** | 4種→**0**（上記11で是正） |
| entry | **0** | **0** | **0** | 4種→**0**（同上） |
| onboarding | **0** | **0** | **0** | **0**（元から） |

**＝専任3ページには図・画像・canvasが1つもないため、「図の中の文字が小さくて読めない」事象は原理的に起こりません。**
残っていた11.5px・10.5pxの文字はすべて上記11で12px以上に引き上げ、**144通りの検証で12px未満0件**を確認しました。

---

## 14. 実機での目視・実タップ＝★**当方では実施できません**（代表にお願いします）

**当ターミナルには実機（スマートフォン）がありません。実際に指で押した感触は、一度も確かめられていません。**
これは「実機の目視」＝代表にお回しする6区分の②に該当します。**できないことを、できたようには書きません。**

### 代わりに実施したこと（実機ではありません）

**指は点ではなく面で当たる**ため、要素の中心だけでなく**端寄りを押しても正しく反応するか**を、`elementFromPoint` で5点判定しました。

| ページ | 同意チェック | 5点判定（中心／左端+8／右端-8／上端+6／下端-6） | 送信ボタン |
|---|---|---|---|
| terms-artist | 4行（58〜86px） | **全行・全5点で○** | 338x54・3点とも○ |
| onboarding | 5行（73〜134px） | **全行・全5点で○** | 221x58・3点とも○ |

★この測定でも**自分の誤りを1件見つけて直しました**＝最初は行を画面内にスクロールせずに判定したため、`elementFromPoint` が全点で `null`（＝「外れた」）を返していました。**ビューポート外の座標は判定できない**ためで、サイト側の問題ではありません。

### 代表へのお願い
`daihyo-hcaptcha-check.md` に**実機確認の項目を追記**しました（**hCaptchaの確認と同じ1回のスマホ操作で済む形**・追加所要3分）。
とくに①**同意チェックを普通の速さで指で押せるか（隣を誤爆しないか）** ②**送信ボタンを片手の親指で押せるか** の2点をお願いしています。

---

## 15. 第7便で実施していないこと・判定していないこと

- **実機での目視・実タップは実施していません**（実機を持たないため）。上記14はブラウザ上の座標判定であり、**指の感触の代わりにはなりません**。
- **キーボード表示時に入力欄が隠れるかは未確認**（実機固有）。**屋外の明るさでの見え方も未確認。**
- **第1段階（登録フォーム）は制作Tの担当のため触っていません**＝当方の3画面と一時的に不揃いです。
- **16px／15pxが「年配の方にとって十分か」は、実機での見え方で最終判断が必要です**（当方はブラウザ上の見え方とサイトの本文標準との一致で判定しました）。
- **色覚特性は1型・2型のシミュレーションで確認しました。3型（T型）は画像を生成しましたが目視していません**（赤の識別が論点のため1型・2型を優先しました）。
- **色覚特性の当事者による確認は行っていません**（シミュレーションです）。
- 決済・通知メール・API・作家データ・紙には一切触れていません。
