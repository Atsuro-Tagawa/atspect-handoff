# 新文言は1ページに入らず保留／14・15の実測／メモ帳での目視 — 2026-08-06 制作T

**出荷物（03・A版・B版・デスクトップ19ファイル）は変更前の版のまま・全MD5一致で確認済み。**
本便でも本番へ出したものはない（テーマ161への反映なし／印刷なし／送付なし）。

## 1｜★確定した新文言は1ページに収まらなかった（実測3.12mm超過）＝保留

| 何 | 値 |
|---|---|
| 同意枠の一文 | 2行 → **3行** |
| 内容の下端 | **283.63mm** |
| A4 1ページの限界（297.011 − 下余白16.5） | **280.51mm** |
| **★超過** | **3.12mm** |

**指示どおりその場で止めた。** 詰めていない・文字を小さくしていない。

### ★1ページに収める方法を1つ、実測で見つけた（未適用）

**上下の余白 16.5mm → 14.0mm** ＝ 1ページ・上の白 15.41mm・下の白 15.75mm・余裕 1.75mm。
ほかは一切触っていない（文字サイズ／記入欄の高さ・幅／項目間8.8mm／弊社記入欄の上の間隔）。

★**当方判断で狭めなかった理由**＝上下の余白は代表が2026-08-01に「上も広げる」と決め、
15mm/15mm案を明示的に見送った箇所。**同じ箇所を当方の判断で狭め直すべきでない。**

**待機版は用意済み**（この資料の `01_保留版_申込書（未出荷）.pdf` と画像2点）。
**ご指示があれば1コマンドで適用できます。**

### 試してダメだった案（いずれも2ページのまま）

余白14.8mm単独／項目間を各0.5mm詰める／弊社記入欄の上を9.75→6.4mmに戻す。
★`break-inside: avoid` があるため、超過が3.12mmでも**弊社記入欄＋フッターの塊（約30mm）が
まるごと2ページ目へ落ちる**。「3.12mm削れば足りる」とは限らない。

## 2｜★PDFのバイト列は作り直すたびに変わる（MD5だけでは中身を判定できない）

実測＝同じHTMLから作り直すとMD5は変わったが、**抽出テキストは完全一致・300dpiの画素差は0**。
→ 両方のビルドスクリプトに「**中身が同じなら書き換えない**」判定を入れた
（ページ数・抽出テキスト・画素の3つが一致したときだけ据え置く）。
★**MD5が違う＝中身が違う、ではない。中身は実体で確かめる。**

## 3｜14・15（前便で「要起票」としていたもの）

- **廃止済みの語21件＝14も15ももともと残存0件**（実測）。**是正は不要だった。**
- ただし**14を作り直したら最終行が「実演」の2文字だけになった**。原因は本文の変更ではなく、
  **作った時期とレンダラの違いで折返し位置が変わった**こと（本文は一字一句同一・実測確認）。
  1語として保護して解消。★**古いPDFを作り直すと、本文を触っていなくても文字組が変わる。**
- **14・15も検査対象に追加**（いま0件でも、次に誰かが直したときに静かに戻るため）。
- 14に載っている**3つのURLを実在確認＝すべてHTTP 200**（代表が当日開く画面のため）。

## 4｜★メモ帳で実際に開いて目視した（前便の宿題）

**先頭にBOMが3個並んでいた**（1個が正）。メモ帳で開くと**本文の前に文字化けが見えていた**。
★当方が作った defect ではない（以前からの持ち越し）が、**当方の文字化け検査が
制御文字とU+0080〜U+009Fしか見ておらず素通りしていた**＝検査の死角。
**U+FEFFを検査に追加**し、対照実験で検出できることを確認。
是正後＝先頭BOM1個・本文中0個・CRLF158行・LF単独0・1行目が正しく表示（`16_メモ帳_BOM是正後.png`）。
★この画像は**メモ帳のウィンドウだけに切り出してあります**。最初に撮った全画面には
他ターミナルの画面とチャットの文面（個人名を含む）が写り込んでいたため、**公開前に切り出しました**
（-handoff系リポジトリはPublicのため・置く前に毎回確認する恒久ルール）。

## 5｜検証（すべて実測・全PASS）

layout_check 38項目 ／ check_doc_text ／ verify_before_print ／
build_day_sheet 5ファイルとも1ページ・廃止語0件・すべて据え置き ／
gen_nyuko_list 全文包含OK・文字化け0 ／ **デスクトップ19ファイルすべてMD5一致**。

## 6｜★実施していないこと

新文言の適用（保留）／印刷／CMYK変換／実機プリンターでの出力確認／
**Codexの B-1・B-3 の解消**（新文言で解消する見込みだが、未適用のためまだ解消していない）／
**サイト側の同じ一文の是正＝SNS運用Tの担当**（申し送りを起票・当方はサイト側を検索していない）／
09・10・11の作り直し（★要起票）／14のURL列の折返し／00_入稿一覧.txtの改行コード統一。

## ★決めどころは1つだけです

**上下の余白を 16.5mm → 14.0mm にしてよいか。**
よければ1コマンドで適用し、実測と画像をお出しします。
よくなければ①文言を1行ぶん短くする②申込書を2ページにする、のどちらかになります。

★この資料に個人名・作品点数は含まれない（確認済み）。
