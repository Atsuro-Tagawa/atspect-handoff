# 作家個人ページ610名 全数点検（読み取りのみ）

作成：SNS運用ターミナル（Sonnet 5）。実測日時＝2026-07-29。読み取りのみ・是正はしていません。

## この点検で見つけられないもの（限界・先に明記）

- **HTTPステータス200は「ページが開く」ことの確認であり、「作家さまに見せられる品質である」ことは保証しません。** 誤字・事実誤認・翻訳の質・レイアウト崩れは対象外です。
- 「功績（viewpoint）」「代表作（masterworks）」「画像」はJavaScriptで描画され、curl取得の生HTMLには出力されません（実測で判明・下記「検査方法」参照）。本点検はShopify側の元データ（`artist_i18n`フィールド等）の有無で判定しており、**実際にブラウザで見た目のレンダリングを確認したわけではありません**（JS側の描画バグ自体は検出できません）。
- Shopify公開状態（ACTIVE/DRAFT）は**実測時点のスナップショット**です。過去に一度も起きなかったことを保証するものではなく、今後の新規追加時に同型の事故が起きないことも保証しません。
- 8言語すべての内容一致・翻訳品質は、問題が見つかった1件のみ簡易確認しました。前便の`i18n-audit.mjs`のような全数の多言語欠落検査は、610名全件には今回実行していません（別便の見積もりどおり時間がかかるため対象外・依頼どおり）。
- 画像がないページが609件ありますが、これは`image_permission`フィールドが609件とも「未取得」（画像使用許諾を得ていない）という、既知・意図的な状態と判明したものです。新規の不具合としては報告していません（詳細は後述）。

## 実測件数

**610名**（`artists.json`の`count`フィールド・実データ件数・Shopify metaobject件数の3つとも610で一致。重複handle 0件を確認）。田川さんの依頼文にある「597名」との差13名は、7/28前後に追加された建築家バッチ等の正当な増加分です（Vault判断待ちマスターS-1に記録済みの経緯と整合）。

## 検査方法・判定基準（冒頭に明記）

1. **HTTPステータス**：610ページすべてを実際にHTTP GETし、日本語版のステータスコードを記録。
2. **Shopify公開状態**：システム開発Tの既存ツール`artist-csv-sync.mjs pull`（読み取り専用モード）でShopify Admin APIから610件の`publishable.status`を取得。
3. **本文・画像の完全性**：当初はcurlで取得した生HTMLから本文文字数・画像点数を機械抽出する設計でしたが、**実測の結果、「略歴（bio）」はサーバー側で生成された静的HTMLに直接出力される一方、「功績（viewpoint）」「代表作（masterworks）」「画像（portrait/masterpiece）」はJavaScriptによりクライアント側で描画され、curl取得の生HTMLには一切出力されないと判明しました**（サンプル実測で確認：`<div class="aad-body" data-aad-vp></div>`のように中身が空のまま）。このため、これら3項目はcurlでの生HTML走査ではなく、**Shopify側の元データ（`artist_i18n`JSON内の`viewpoint`・`masterworks`、および`portrait`/`hero_image`/`masterpiece_1_image`/`masterpiece_2_image`の各フィールド）の有無**で判定する方式に切り替えました（元データが空であれば、JS描画の結果も必ず空になるため、判定の正確性はむしろ高くなります）。
   - 略歴：`bio_ja`の文字数が0なら空と判定。
   - 功績：`artist_i18n.viewpoint.ja`が空文字なら空と判定。
   - 代表作：`artist_i18n.masterworks`が空配列、または空文字列なら空と判定（スキーマが「作品ごとの配列」と「言語別の1文字列」の2種類存在することを実測で確認し、両方に対応）。
   - 画像：`portrait`・`hero_image`・`masterpiece_1_image`・`masterpiece_2_image`のいずれか1つでも値があれば「画像あり」と判定。
   - **唯一の存命作家（鈴木千賀子・`death_year`が空欄の1名で確認）は、功績・代表作が無いのが仕様（作家本人の三人称的な「功績」記述は用意しない設計・代わりに「作家のことば」欄を使う）と判断し、集計対象から除外しています。**

## 結果①：HTTPステータス

**610/610が200**。404・5xx・301等は0件でした。

★実測中、7便目・最終便で計11件が一時的に429（レート制限）を返しましたが、いずれも間隔を空けて個別に再実測した結果200であることを確認・訂正しました（自分の並行アクセスが原因の一時的事象で、サイト側の不具合ではありません。詳細＝`audit-full.csv`のコミット履歴に記録）。

## 結果②：Shopify公開状態

**610/610がACTIVE。DRAFTは0件**でした。背景にあった「新規5名が下書き状態のまま404だった事故」は、本点検の実測時点（制作Tの是正後）では再現しませんでした。

## 結果③：本文・画像の完全性

| 項目 | 空の件数（存命作家1名を除く609名中） |
|---|---|
| 略歴（bio_ja） | **0件** |
| 功績（viewpoint、全8言語で空） | **1件** |
| 代表作（masterworks、空配列） | **1件** |
| 画像（portrait/hero/masterpiece のいずれもなし） | **609件**（★下記の理由により新規の不具合としては扱っていません） |

### 機械判定で1件検出→Vault照合の結果「新規の不具合ではない」と判明＝深山龍洞（fukayama-ryudo・書家／かな・1903-1980）

略歴（bio_ja、153文字）は8言語すべてに存在しますが、**「功績」欄と「代表作」欄が8言語すべてで空**でした（該当のみ8言語を確認・下記）。

| 言語 | 略歴(bio) | 功績(viewpoint) |
|---|---|---|
| ja | 153文字 | 空 |
| en | 486文字 | 空 |
| zh_cn | 129文字 | 空 |
| zh_tw | 129文字 | 空 |
| ko | 182文字 | 空 |
| fr | 509文字 | 空 |
| es | 508文字 | 空 |
| de | 485文字 | 空 |

代表作（masterworks）は言語別ではなく単一の配列のため、8言語共通で空（`[]`）です。

**★測定結果を鵜呑みにせず、Vaultで経緯を確認した結果、これは新規の不具合ではなく、2026-07-08に既に完了・代表確認済みの意図的な状態と判明しました**（詳細＝[[_実装ログ_深山龍洞_情報修正_20260708_ATSPECT]]）。当時、metaobjectの「功績」「代表作」「所属していた美術団体」欄には、深山龍洞を別人物（読み・出身地・書風が異なる人物）と取り違えた**捏造疑いのプレースホルダ情報**（「一東書道会を率い」「日展で活躍」「龍洞書作」等）が入っており、司令塔がWeb裏取りした確定事実（かな書の書家・淡路/神戸出身、作品集『深山龍洞書芸集成』）と全面的に矛盾していました。「捏造ゼロ」の原則に基づき、確定事実の範囲を超える記述は追記せず、矛盾する記述を**意図的に空欄化**した結果が現在の状態です。田川さんご自身も同日、実機で「功績／代表作／所属していた美術団体欄が非表示（空のため）」であることを目視確認済みです。

このため、**本件は「④判断できなかったもの」「新規」のいずれにも該当せず、③意図的な状態として扱い、新規の不具合一覧には含めません**。原稿の追記（確定事実の追加裏取りができた場合）は、当時のログにも「田川さん判断事項」として残っており、本便で新たに追加で判断を仰ぐものではありません。

**この発見の教訓＝「機械判定＝0件でない」を即座に「不具合」と報告せず、Vaultで経緯を確認したことで、誤報告を防げました**（グローバルCLAUDE.md「測定結果は判断ではない」の実例）。

## 結論：新規の不具合＝0件

上記の経緯確認により、**610名全数点検の結果、HTTPステータス・Shopify公開状態・本文（略歴・功績・代表作）の完全性のいずれにおいても、新規の不具合は0件でした。**

### 画像なし609件について（新規の不具合として扱わなかった理由）

Shopifyの元データを確認したところ、**画像なし609件すべてで`image_permission`フィールドが「未取得」**（画像使用許諾が未取得）でした。唯一画像がある1名（鈴木千賀子）はこのフィールドが空欄（許諾不要・本人の意向で使用可）です。「取得済み」等の別の値を持つ作家は1件も存在せず、609件は一貫して「許諾が下りていないため画像を掲載していない」という既知・意図的な運用状態と判断しました。新規の不具合一覧には含めていません（無理に0件を作らず、事実として記載）。

## 実施していないこと

- 是正は一切していません（一覧化のみ、依頼どおり）。
- 610名全件の8言語欠落検査（前便`i18n-audit.mjs`相当のフル実行）はしていません。問題が見つかった深山龍洞1件のみ8言語を確認しました。
- 功績・代表作・画像がJSでどう実際に描画されるかは、ブラウザでの目視確認をしていません（データの有無のみで判定）。
- 誤字・事実誤認・翻訳の質・レイアウトの点検はしていません。

## Codex独立レビュー（audit-artists-full.mjs・観点＝空判定ロジックの見落とし）

3点の理論上の懸念が指摘されました。①`viewpointJa()`がJSON解析失敗を「空」と同一視し判定不能を隠す可能性②`masterworksEmpty()`が「配列に要素はあるが中身が空」のケースを「あり」と誤判定する可能性③`deathYear`欠落レコードが存命作家と誤って同一視され、本来集計すべき物故者が集計から漏れる可能性。

**いずれも実データ610件を再確認した結果、実際の判定結果には影響していませんでした**（①JSON解析失敗＝0件②配列だが中身が空のケース＝0件③death_year空/欠落＝suzuki-chikako1件のみ、他に該当なし）。コード自体の頑健性（将来データが増えた際に同種の問題が起きうる余地）としては妥当な指摘のため、指摘内容は記録として残しますが、**今回の610件の結果自体を訂正する必要はない**と判断しました。

## 証拠の置き場所

`reports/assets/20260729_artistaudit/`
- `audit-artists-full.mjs`（検査スクリプト本体）
- `audit-full.csv`（610件全件の生データ：handle・name・url・httpStatus・shopifyStatus・bioLen・viewpointEmpty・masterworksEmpty・imagePresent・imagePermission・deathYear）
- `artists.json`（母集団の元データ・atspect.comから取得）
- 本ファイル（`audit-full-report.md`）
