# 寄贈の生成経路の退役／見出しの語中割れ是正（2026-08-11 第3便・あつぺくと制作T）

モデル＝★セッション起動時コンテキストは Fable 5（司令塔指示は Opus 5。`/model` は「新規セッションの既定として保存」の表示で、本セッション内で切り替わったかは当方から確認できません＝前便と同じ状態）。

前便の申し送り2件の裁定（①退役 ②見出しの是正）を受けた便。テーマ161のみ。全push読み戻しSHA-256一致・CAS衝突0件。

---

## A：build-gifts.mjs の退役（完了）

**退役先＝`C:\Users\ataga\Desktop\SNS運用\_scratch_0810\_退役_20260811_再実行禁止\`**（削除ではなく移動・README同梱）。

### 「移動しただけでは止まらない」ので二重に止めた

移動だけでは相対 import が保たれ、退役フォルダの中でなら**まだ動きます**。そこで実行停止を挿入しました。

**実測**＝退役フォルダから実行→**終了コード1・出力0バイト**／元の場所から実行→ファイル不在で実行不能／
`gifts-content.mjs` を `import()` →**停止**。

### 可逆であることをハッシュで証明

| ファイル | 移動前MD5 | 停止部を除いた本体MD5 | 判定 |
|---|---|---|---|
| build-gifts.mjs | `cc708b0c36c178fc6f96ce6ed7e5b568` | 同じ | **一致** |
| gifts-content.mjs | `55235f98486d018998340e67feda84de` | 同じ | **一致** |

＝**追加したのは停止部分だけ。処理本体も文言データも1バイトも変えていません。**

★**自分が入れた誤字を、自分の検索で見つけました**＝停止文に「巻き戻る」を「巧き戻る」と書いていた（Unicodeエスケープの取り違え）。
全文検索で2箇所を検出して是正し、**是正後にMD5の可逆性を再証明**しました。

### 併せて直したこと
`sections/atspect-gifts.liquid` の冒頭に「文言はデータから生成している」という**退役済み経路の案内**が残っていたため、
直接編集が正であることと、生成をやめて失われる検算（8言語スパン数の確認）を手で行う旨に書き換えました。

---

## B：見出しの語中割れ是正

### 走査（全27ページ・両エンジン・390px）
検出器を見出しへ広げて走査＝**WebKitで11件**。
★**前便で「1件」と報告したのは*両エンジンの切れ目の食い違い*を見ていたため**で、*語中割れそのもの*を基準にすると11件です＝**精度不足として訂正**します。

### 指示のCSS方式は、当てて測った結果ひとつ撤回しました

| 当てたもの | 実測結果 | 採否 |
|---|---|---|
| `text-wrap: balance` | Chromiumで改善（外すと「作／品」の1文字孤立になる） | **採用** |
| `word-break: keep-all` | ★**WebKitで悪化**（寄贈h1が「ってい／る作品」→「いる作／品」で**1文字孤立が新発生**） | **撤回** |

理由＝句読点を持たない見出しは keep-all で1つの塊になり、入り切らない分が任意の位置で折られるため、位置を選べなくなります。

★**さらに分かったこと＝`word-break: auto-phrase` は明示しても文節で折れていません**
（Chromiumで `!important` を当てても切れ目が変わらず）。**2026-08-10の是正はほとんど効いていなかった可能性があります。**
ただしこれはヘッドレスのChromiumでの結果で、**実機Chromeでも同じかは確かめていません**。

**結論＝CSSだけでは日本語の見出しの折り返し位置は決められない。**

### 採った方式＝文節を `.nb`（nowrap）で包む

塊の中では折れず、**塊と塊の間でだけ**折れます。どのエンジンでも同じ結果になります。
★包む前に、**各塊が390pxの本文幅に収まることを実ページの実フォントで実測**してから当てました。

**6箇所に適用 → 5箇所を是正、1箇所は撤回。**

| 見出し | 結果 |
|---|---|
| 寄贈のご相談を承っている作品（3ページに出る） | 是正 |
| 世界はまだ、…／世界とつながる、…／まずは「登録審査」へ… | 是正3件 |
| 登録審査申し込み（ご推薦者あり） | 是正 |
| 「寄贈をご検討の作品」枠の見え方（見本） | 是正 |
| 作家さまの活動を、世界へ。 | ★**撤回**（下記） |

### ★撤回した1件（自分が入れた退行を自分で見つけて戻しました）

スクショの定点実測で**WebKit 390pxに横溢れ6px**。原因は**当方の `.nb`**で、WebKitではこの見出しだけ
塊の境目で折り返されず、2塊が1行のまま372pxになり見出し幅346pxを超えていました（Chromiumは正しく2行）。
**横溢れは語中割れより重い不具合**なので、この見出しだけ戻しました。**戻した後は両エンジンとも横溢れ0**。

★**反省＝収まりの実測をChromiumだけで行っていました。** WebKitでは同じ文字列が**約1.8倍の幅**で描かれます。

### 検証
- ページごとに**ブラウザを立て直して**再測定（同一ブラウザだと配信キャッシュで古いHTMLを掴む実例があったため）。
  **是正した5箇所は両エンジンとも語中割れ0件**。
- スクショ＝390/768/1280（Chromium）＋390（WebKit）の**20枚すべてで横溢れ0**（`checks.json`）。
- `.nb` を入れた見出しで**8言語とも可視スパン1個ずつ・横溢れ0**を実測。

### Codexレビュー（NO-GO 2件）
- **退役**＝(b)データ側が野放し・(c)可逆性を検証できない＝**いずれも妥当として対応済み**（上記）。(a)(d)はPASS。
- **`.nb`**＝(b)(c)(d)はPASS。**(a)は「`.atsl` の出し分けCSSがリポジトリ内に見つからず検証できない」というNO-GO**で、
  当方の8言語実測で**出し分けは正常**と確認。見つからなかった理由＝出し分けは `layout/theme.liquid` にあり、本日SNS運用Tが全ページ共通化した箇所。

### 残っている語中割れ＝5件（すべて理由あり）
| ページ | 見出し | 扱い |
|---|---|---|
| artist-registration-form／登録審査申し込み（ご推薦者あり）のページ〈識別子〉 | 作家さまの活動を、世界へ。 | 撤回（横溢れ回避）＝**要判断** |
| 作家情報のご提出のページ〈識別子〉 | 作家情報のご登録・規約同意に関するお手続き | ★**規約本文を含む法的文書のファイル**のため不可触 |
| privacy ×2 | 外国にある第三者への提供（越境移転）／開示・訂正・利用停止等のご請求 | ★**法的文書**のため不可触 |

★**当方の検出器の偽陽性も1件見つけました**＝privacyで「孤立」11件が出ましたが、実体は**画面外のDawn要素**（top=−21px）を
拾っていたことと行判定の副画素ずれによるものでした。**「孤立」の件数は実数として扱いません**（語中割れ5件のみを実数とします）。

---

## 実施していないこと
- **iPhone実機での確認**（WebKitエンジンでの代替実測のみ）。
- 法的文書2ファイル（規約書・プライバシー）は**1文字も触っていません**。
- `atspect-terms-artist.liquid` の見出し是正は**行っていません**（同ファイルに規約本文があるため）。
- 実機Chromeでの `auto-phrase` の挙動確認。所要時間は測っていません。

## 証拠（このフォルダ）
- スクショ20枚＝gifts／artist-guide／artist-registration-form／suisen／dwp の 390・768・1280 と WebKit390。
- `checks.json`＝各幅の横溢れと `.nb` 個数の実測。
- `README_退役_の写し.md`＝退役フォルダに置いたREADMEの写し。

## 要判断
1. 「作家さまの活動を、世界へ。」＝WebKitで割れが残ります。**文言を変えずに直す手がありません**（塊にすると溢れる）。
   ①このままにする ②文言を短くする（確定コピーのため要裁定）③この見出しだけ字を少し小さくする。
2. 法的文書2ファイルの見出しの割れ＝触ってよいか。
3. 2026-08-10の `auto-phrase` 是正が実は効いていなかった可能性＝実機Chromeでの確認。

最終更新：2026-08-11
