# 追記：version-3 は有効。ただし**ストア側の権限は増えていません**（2026-08-10）

あつぺくとシステム開発ターミナル／model=Opus 5
★ハンドル・URLは1文字も書いていません。件数と実測のみ。

---

## 0. 結論（1行）

**未閉鎖・原因＝ストア側が新しい権限をまだ許可していない（版は有効・許可は未反映）・持ち越し理由つき。**

## 1. 実測（version-3 は確かに有効です）

| 確かめたこと | 結果 |
|---|---|
| 合鍵を**取り直した**（2回） | 成功（HTTP 200） |
| 取り直した合鍵の権限 | **`read_orders` の1つだけ**（書き込み0） |
| `read_translations` / `read_locales` | **どちらも付いていません** |
| 翻訳データを実際に読めるか | **読めません**（同じ拒否） |
| **版の一覧（Dev Dashboard）** | **version-3 が「有効」**（2分前に作成）／version-2・version-1 は過去版 |

**→ リリースは通っています。しかし「ストアが許可する権限」は増えていません。**

★これは本日2回目の**同じ型**です。
2026-08-03 と本日の実測で確認したとおり、**「版が宣言しているスコープ」と「ストアが実際に許可しているスコープ」は別物**で、
**ストア側で承認し直すまで効きません。** 代表が「承認画面は表示されなかった」と仰ったのは、
**まさにその工程が走っていない**ことを示しています。

★当方は**管理画面の「インストール済み」の一覧も開いて確認**しましたが、
**権限の更新をうながす表示は見当たりませんでした**（＝当方からは、承認をやり直す入口を特定できていません）。

## 2. 現状の露出の実害評価（実測）

| 見たところ | 結果 | 意味 |
|---|---|---|
| 旧URL3件の `robots` | **`noindex,nofollow`**（3件とも） | **検索には載りません** |
| `sitemap.xml`（親） | 旧・新ハンドルの出現 **0件** | サイトマップから辿れません |
| 子サイトマップ6本 | 旧・新ハンドルの出現 **0件** | 同上 |
| サイト内で利用者が踏むリンク | **0件**（前便で全数走査済み） | サイトを見て回っても辿り着けません |
| 転送3件 | 作成済み・**出番待ち** | 旧の綴りが落ちた瞬間に自動で効きます |

**→ いま辿り着けるのは「旧URLの綴りを既に知っている人」だけ**です。
新たに漏れが広がる経路（検索・サイトマップ・サイト内リンク）は、**実測でいずれも0件**でした。

★**とはいえ「漏れたURLが開ける」状態は続いています。** 安全になったとは言えません。

## 3. 明日に持ち越す理由

1. **次の一手が代表の操作に依存します**（ストア側での権限の承認）。当方だけでは進めません。
2. 当方から承認をやり直す入口を、**管理画面上で特定できていません**（★未確認）。
   **推測で操作しない**という規律に従い、手探りで押すことはしません。
3. 露出の広がりは**実測で止まっています**（検索・サイトマップ・サイト内リンクいずれも0件）。
   **夜通し追う必要のある状態ではない**と判断しました。

## 4. 明日の最初の一手（順に）

1. **代表**＝権限の承認をやり直す。うまくいかない場合は、
   **アプリを一度アンインストールして入れ直す**と、承認の画面が出るはずです
   （★当方は未確認のため、実行前に画面名を教えてください）。
   ※アンインストールすると**注文の読み取りも一時的に止まります**（実データはまだ使っていないので実害はありません）。
2. **当方**＝権限が届いたら、翻訳データを全言語で読み、**旧スラッグの残存を判定**（1回の読み取りで決着します）。
3. 残っていれば**旧の綴りだけを削除**（新スラッグ・翻訳本文・他ページには触れません）。
4. 削除後に旧URL3件を再測定し、**転送が効く（または404）ことを確認**して閉じます。

## 5. 本便でしたこと・していないこと

**したこと**＝合鍵の再取得2回／権限の機械確認／版の一覧の実測／翻訳の読み取り可否の実測／
露出の実害評価（robots・サイトマップ親子・サイト内リンク）。

**していないこと**＝翻訳データの読み取り（権限が無いため）／削除（対象が読めないため）／
承認のやり直し（代表の操作・当方は入口を未確認）／ページ・テーマ・翻訳本文への書き込み（一切なし）。
