# 追記：翻訳の読み取り権限は、まだ届いていません（2026-08-10）

あつぺくとシステム開発ターミナル／model=Opus 5
★ハンドル・URLは1文字も書いていません。件数と実測のみ。

---

## 0. 結論（1行）

**旧URL3件＝閉鎖できていません。** 翻訳データを読む権限が**まだ有効になっていない**ためです
（人の言葉ではなく、**実測で確認**しました）。

## 1. 実測（4つの角度から確かめました）

| 確かめたこと | 結果 |
|---|---|
| 合鍵を**取り直した**（IDとシークレットから再発行） | 成功（HTTP 200） |
| 取り直した合鍵の権限 | **`read_orders` の1つだけ**（増えていません） |
| いつも使っている合鍵で翻訳を読めるか | **読めません**（同じ拒否のまま） |
| 「注文読み取り（読むだけ）」のバージョン一覧 | **version-1 と version-2 だけ。新しいバージョンはありません**（有効＝version-2） |
| アプリの一覧 | **4つのまま**（新しいアプリは作られていません） |

**→ どのアプリにも、新しいバージョンが作られていません。**
**→ したがって、権限はまだ1つも増えていません。**

★**代表のお手を煩わせないよう、当方で追える範囲はすべて追いました**（下記2）。

## 2. 権限が無くても追えた分（翻訳データ説は「弱まった」）

権限を待たずに、**外から見える情報だけ**で翻訳データ説を検証しました。

| 検査 | 結果 | 読み取り |
|---|---|---|
| 旧URLの `html lang` | **ja**（3件とも） | 別の言語のスラッグなら、その言語で描かれるはず |
| 新URLの `html lang` | **ja**（3件とも） | 同上 |
| `hreflang`（言語別URLの宣言） | **0件**（旧・新とも） | この店は**1つのURLで多言語を出す作り**（言語別URLが無い） |
| 新URLの中に旧の綴りが出てくるか | **いいえ**（3件とも） | 新しいページが旧の綴りを持ち回っているわけではない |

**→ 翻訳データ説は弱まりました。** ただし**排除はできていません**
（この店は言語別URLを持たないため、翻訳のスラッグがあっても同じ道すじで日本語で描かれる可能性が残ります）。
**権限が届けば、この点は1回の読み取りで決着します。**

## 3. いまの状態（変わっていません）

| 項目 | 状態 |
|---|---|
| 新URL 3件 | **200・noindex維持・フォーム表示**（正常） |
| 旧URL 3件 | **200・フォーム表示**＝**閉鎖できていません** |
| 転送 3件 | 作成済み・**効いていない**（旧の綴りが生きているため出番が来ない） |
| 利用者が踏むリンクの残存 | **0件** |

## 4. 代表にお願いすること（残っている手順はここだけ）

手順書（`01_代表の操作手順書_翻訳の読み取り権限.md`）の **③④⑤** が未実施です。

1. **③ 新しいバージョンを作る**＝「注文読み取り（読むだけ）」→**バージョン**→右上**「バージョンを作成」**
   スコープに `read_orders,read_translations,read_locales`（★`write_` は1つも入れない）
2. **④ リリースする**（作っただけでは効きません）
3. **⑤ ストアで承認し直す**（★**ここが最も忘れやすい所**です。リリースだけでは権限は増えません）

★**新しい文字列を貼る作業はありません。** 終わったら「リリースして承認しました」とだけお知らせください。
当方が合鍵を取り直して、機械で確認します。

## 5. 権限を待たずに試せる、もう1つの手（★代表の判断待ち）

**ページを「同じ内容で保存し直す」**という手があります。

- ねらい＝ページを保存し直すと、配信側の道すじの索引が作り直され、**古い綴りの別名が落ちる**可能性があります。
- **中身は1文字も変えません**（いまの新しい綴りを、同じ値でもう一度書き込むだけ）。
- **本便では実施していません**（本便の禁止事項に「ページの変更」があるため）。
- 効けば、**その瞬間に旧URLが404になり、作ってある転送が働きます**（＝閉鎖完了）。

**当方の推し＝これを先に試すことです**（代表の操作が要らず、当方だけで完結し、すぐ元に戻せるため）。
権限の追加は、これで解決しなかった場合の原因究明として、引き続き有効です。

## 6. 次の一手（1行）

**「ページを同じ値で保存し直す」を許可いただければ、当方だけで試して結果を報告します。**
それでも閉じなければ、翻訳の権限（③④⑤）で原因を特定します。
