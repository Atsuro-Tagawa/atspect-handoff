# 旧URLの無効化（転送）＋原因調査の手順書（2026-08-10）

あつぺくとシステム開発ターミナル／model=Opus 5

★**ハンドル・URLは1文字も書いていません**（公開リポジトリのため）。件数と実測のみ。
　詳細の正本＝Vault（`_作業_ハンドル差し替え_20260810/`・`_実装ログ_ハンドル差し替えとB34_20260810_ATSPECT.md`）。

---

## 0. 結論（先に）

- **転送は3件とも作りました**（転送先は**サイトのトップ「/」**。新URLには向けていません）。
- ★**しかし、まだ効いていません。** 旧URLは相変わらず200でフォームが出ます。
- 理由＝**Shopifyの転送は「その道すじに中身が無いとき」にだけ働く**ためです。
  いまは**旧の綴りでページが生きている**ので、転送の出番が来ません。
- **→「即時の無効化」は達成できていません。** 転送は**将来の保険**として残してあります
  （旧の綴りが消えた瞬間から効きます）。**いつでも消せます。**

---

## 1. 作業1＝転送の作成（実測）

| 手順 | 結果 |
|---|---|
| 作る前のライブ確認 | 旧URL 3件とも200・フォーム4／**旧URL宛の転送は0件** |
| 作成 | **3件作成**（失敗0・重複0） |
| **転送先が「/」であること** | **3件とも「/」**（機械で確認） |
| **★新しいハンドルへ向いていないこと** | **0件＝安全**（作る前・作った後・確認のたびに毎回機械で検査） |
| 転送の総数 | 14件 → **17件** |

★**最重要の規律**＝「新URLには絶対に向けない」は、**道具の中に検査を組み込みました**。
　転送先が「/」以外だったり、新しいハンドルの文字が混じっていたら、**その場で中止します**。

### 転送は消せます（不可逆ではありません）

作成した3件の**転送IDを対応表（Vault）に控えました**。消すときは次の1行です。

```
node で GraphQL の urlRedirectDelete に、控えた転送ID を渡すだけ（1件ずつ・すぐ戻せます）
```

★**管理画面のどのメニューから消せるかは、当方は開いて確かめていません＝未確認**です。
　推測で画面名を書きません。当方の道具（API）からは確実に消せます。

## 2. ★転送が効いていないこと（実測）

| 経過 | 旧URL 3件 |
|---|---|
| 作成直後 | 200／200／200 |
| 1分後 | 200／200／200 |
| 約2分後 | 200／200／200 |
| 約12分後 | **200／200／200**（HEAD・GETとも／フォーム数4のまま） |

**転送そのものは管理側に存在します**（3件・転送先はすべて「/」）。
**それでも旧URLが200を返す**＝Shopifyの転送は**404になる道すじにだけ働く**という優先順位のためです。

★**これは前便の「B案（301を作る）」が、この状況では効かないことを意味します。**
　選択肢Bの前提が、実測で崩れました。

## 3. 新URLは無事です（巻き込み事故なし）

**3件とも 200・`noindex,nofollow` 維持・フォーム表示あり**（送信は0回）。
転送を作ったことで新URLが壊れていないことを、**毎回いっしょに確認しています**。

## 4. 8/3の前例には触っていません

指示どおり、2026-08-03に差し替えた旧URL（現在404）には**一切触れていません**。

## 5. 作業2＝原因調査（翻訳データ説）の手順書＝**できました。代表の操作待ちです。**

手順書＝`01_代表の操作手順書_翻訳の読み取り権限.md`

### 実測でわかったこと

**いまのどの合鍵でも、翻訳データを読めません。** お店からの返事（そのまま）：

```
Access denied for translatableResource field. Required access: `read_translations` access scope.
Access denied for shopLocales field. Required access: `read_locales` access scope or `read_markets_home` access scope.
```

- 足すのは **`read_translations`**（＋言語一覧を見るなら `read_locales`）。**どちらも読むだけ**です。
- 足す先の推し＝**本日作られた「注文読み取り（読むだけ）」**（合鍵が増えず、読むだけの性質も変わらないため）。
  ★**いつも使っている合鍵には足さないでください**（あれは書き込みの権限を多数持っています）。
- **代表が新しい文字列を貼る作業はありません。** `.env`のIDとシークレットはそのまま使えます。
  当方が合鍵を取り直します。

### 手順書に「★未確認」と明記した2点

1. 「バージョンを作成」を押したあとの**用紙の項目名・並び**
2. ストアで**承認し直すときの画面**

（当方はこのアプリを作っていないため、作成の用紙を開いていません。**推測では書きません。**）

### 権限が来たら当方がすること

1. 合鍵を取り直し、**`read_orders`／`read_translations`／`read_locales` の3つだけ**（書き込み0）を機械で確認
2. 差し替えた3ページの翻訳データを読み、**古いスラッグが残っていないか判定**
3. 残っていれば、**消してよいか・どう消すか**を報告
   ★**判定するだけで、この段階では消しません**（書き込みはしません）

## 6. いまの全体像（正直に）

| 項目 | 状態 |
|---|---|
| 新URL 3件 | **使えます**（200・noindex・フォーム表示） |
| 旧URL 3件 | **まだ開けます**（200・フォーム表示）＝**無効化は未達** |
| 転送 3件 | **作成済み・効いていない**（将来の保険として残置） |
| 利用者が踏むリンクの残存 | **0件** |
| 原因の特定 | **未了**（翻訳データ説の検証待ち＝代表の操作1回） |

★**「旧URLは塞がった」とは、まだ書けません。**

## 7. 次にやること

1. **代表**＝翻訳の読み取り権限を足す（手順書のとおり・操作1回）
2. **当方**＝権限が来たら翻訳データを読み、原因を判定
3. **当方**＝旧URLの再測定を続ける（転送が効き始めたら、それも記録）
4. 営業用QR・リンク集・ブックマークの作り直し（3本ぶん・別便）
