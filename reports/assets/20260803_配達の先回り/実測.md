# 「配達」の先回りの一行（2026-08-03・SNS運用T）

## 置いた文言（1〜2行・言い訳をしない・煽らない）

> お支払いの画面に「1か月ごとに配達」と表示されますが、お届けするものはございません。
> 毎月のシステム利用料のお支払いです。

- **正面から書いた**＝作家さまが実際に目にする語（「1か月ごとに配達」）をそのまま引用した。
- **言い訳をしていない**＝「Shopifyの仕様で」「システム上」といった、道具のせいにする言い方を使っていない。
- **謝っていない**＝「申し訳ございません」を入れていない。事実を先にお伝えするだけにした。
- **煽っていない**＝赤字・警告の枠にしていない。左に細い罫を引いただけ。

## 置いた場所

`sections/atspect-publish-consent.liquid` の**決済ボタンの直前**（`.atspect-aterms__pay-btn` の1行上）。

★**セクション設定にせず、liquidに直書きにした。** 設定にすると、設定を触ったときに黙って消え、
作家さまが説明のないまま「配達」を目にすることになるため。理由はliquid内のコメントにも残した。

## ★自分で実測してから書いた（人の言葉だけを根拠にしていない）

1. **「配達」が出ることの確認**＝システム開発Tが残した決済画面のスクリーンショットを開いて目視。
   **商品名の下に「1か月ごとに配達」／下部に「定期小計 1か月ごとに ￥3,300」**を確認した。
2. **どの決済に出るのかの切り分け**＝**当ターミナル専任ファイル（terms-artist / entry）の決済リンク**
   （情報登録料 33,000円・一括／`/cart/48240441622786:1`）を**実際に開いて数えた**。
   → **「配達」0件／「定期」0件／「ごとに」0件。**
   **＝この一行が要るのは月額（publish-consent）だけ。terms-artist / entry 側には置いていない。**
   ※決済は行っていない。画面を開いて文字を数えただけ。

## 6幅での実測（ライブのCSS・ライブの段組の中で測定）

決済ブロックは `form.posted_successfully?` の中にあり、かつ受付オフのため**画面には出ない**。
そこで**ライブのページを開いたうえで、決済ブロックのマークアップをそのDOMへ差し込んで**測った
（＝本物のCSS・本物のフォント・本物の段幅で測っている）。
**CSSがライブに載っていること（`atspect-aterms__pay-headsup` がページ内の style に存在すること）を
毎回先に確かめてから測定している。**

| 幅 | 文字 | 段落の大きさ | 行数 | ボタンとの間 | ボタンの高さ | 横はみ出し | 泣き別れ |
|---|---|---|---|---|---|---|---|
| 320px | 15px | 234x131 | 5行 | 14px | 51px | **0px** | **0件** |
| 390px | 15px | 304x105 | 4行 | 14px | 51px | **0px** | **0件** |
| 430px | 15px | 344x79 | 3行 | 14px | 51px | **0px** | **0件** |
| 768px | 15px | 634x53 | 2行 | 14px | 51px | **0px** | **0件** |
| 1440px | 15px | 634x53 | 2行 | 14px | 51px | **0px** | **0件** |
| 1920px | 15px | 634x53 | 2行 | 14px | 51px | **0px** | **0件** |

- 泣き別れの検出＝改行位置を1文字ずつ求め、前後がカタカナ同士・始め括弧が行末・終わり括弧が行頭に
  なっていないかを判定する方式。**全幅0件。**
- 文字サイズ15px＝当ターミナルが2026-08-01第7便で決めた「説明文は15px」に合わせた
  （同ブロックの他の説明文は12pxだが、**これは読み落とすと決済画面で戸惑う文**のため小さくしていない）。
- **画像でも目視した**（`320.png`〜`1920.png`）。

## ★担当外の所見（直していない・報告のみ）

**幅390pxで、金額の行「システム利用料（月額）　3,300円（税込）」が「（税」／「込）」で分かれる。**
- 320px・430px以上では起きず、**390px（iPhone 12〜15の標準幅）だけ**で起きる。
- **直していない。** この文字列はセクション設定（`pay_amount_label`）のプレーンテキストで、
  **HTMLを入れられないため `nowrap` を挟めない**。日本語は漢字のあいだで改行できるので、CSSだけでは防げない。
  → **文字列そのものを変える（例：全角スペースの位置を変える／「（税込）」の前で改行させない書き方にする）
  必要があり、それはシステム開発Tの判断。**
- なお現在この設定は**空**で、liquidの `| default:` の値が出ている（当方の測定もその値で行った）。

## 実施していないこと・判定していないこと

- **決済は1件も行っていない。** 決済画面を開いて文字を数えただけ。**テスト決済もしていない。**
- **実際の作家さまの画面では、この一行はまだ誰にも見えていない**（受付オフ＝`accept_submissions: false`）。
  **送信成功時の実際の描画は確認できていない**（hCaptchaと受付オフの二重で到達できないため）。
  確認したのは「ライブのCSSが載っていること」と「そのCSSで組んだときの見え方」まで。
- **「定期小計 1か月ごとに ￥3,300」への言及は入れていない**（1〜2行に収める指示のため、
  いちばん誤解を招く「配達」1点に絞った）。
- **`accept_submissions` と `pay_url` の設定は触っていない**（受付オフのまま）。
- **実機での目視は未実施。**
