# 紙紋UI 作業サマリ（2026-08-08）

**ターミナル：紙紋UI ／ 使用モデル：Opus 5（claude-opus-5[1m]）**

★このファイルには**是正が済んだことの結果だけ**を書く。
**まだ直していない項目の内容・手口・該当コードはPublicリポジトリに書かない**（紙紋の恒久ルール）。
未是正項目の一覧はVault（ローカル）に保存済み。

★画像について：**実在の社員名は写していない**（操作者欄は「（試験用）担当A」に置き換えて撮影）。
画面に出る作家名・作品名は、システムに組み込まれた**デモ用の架空データ**（実在しない）と試験用の入力。

---

## 1【指示1】画面から呼ばれていない機能の入口を閉じた

司令塔判断（「露出をやめる」）に従って実施。**機能のコード本体は消していない**（入口だけを閉じた・戻せる）。

| 実測項目 | 是正前 | 是正後 |
|---|---|---|
| 画面（JS）から呼べる機能の数 | **39** | **32** |
| 対象の機能が画面から呼べるか | **呼べた** | **呼べない** |
| 対象の機能がプログラム内部から使えるか | 使える | **使える（機能は残っている）** |

**裏取り（陽性対照）**：同じ試験を**是正前のコードにも当て**、「呼べる」と出ることを確認した
（出なければ試験が役に立っていないため）。

### ★8/7の報告を訂正します

8/7に「画面から呼ばれていない機能は**2件**」と報告しましたが、**これは誤りでした。**
数え方（名前の全文検索）が原因で、**実際には7件**でした。
うち2件は書き方が特殊なだけで**使われていた**ため、**本当に未使用は5件**。
**この5件も同じ判断基準で閉じました**（合計7件を閉じ、39→32）。
※Codexの独立レビューの指摘がきっかけで気づきました。

---

## 2【指示2】残っていた項目の是正

- **直した＝3件**（失敗しているのに成功したように見えていた表示を、画面に出るようにしたもの）。
- **直さず判断をお願いする＝6件**（**内容はVault**。理由は下記）。

判断をお願いする理由（手口には触れずに書きます）：

1. **1件は「直し方そのものが逆効果」**でした。一見よさそうな直し方だと、
   **記録の食い違いを新たに生む**ことが分かったため、副作用のない形（失敗を必ず表に出す）に留めました。
2. **2件は運用の決めごと**（証明書の発行と記録を1回の照合で両方できるようにするか／
   部品が入っていないPCでアプリを起動できなくしてよいか）。
3. **3件は設計変更**（紙紋1との合流と代表判断が要る規模）。

---

## 3【指示3】画面の確認

### 実アプリは起動した

保存先を試験用に差し替えて起動し、**窓が開くこと**（タイトル・大きさ）と
**起動エラーが出ていないこと・保存領域が作られること**を確認しました。

★**ただし実アプリの窓の「中身」は撮影できませんでした。**
撮影方法を2通り試しましたが、片方は中身が写らず、もう片方は
**他のウィンドウが写り込む**ため中止しました（写り込んだ1枚はその場で削除し、公開していません）。

### 画面の見え方は、同じ描画エンジンで確認しました

同じHTML/CSS/JSを Chromium（実アプリのWebView2と同じ描画エンジン）に読ませ、
**画面側のプログラムはそのまま動かして**確認しました。
＝「実アプリの窓の写真」ではなく「**同じ画面部品を同じ描画エンジンで描いたもの**」です。

| 画像 | 内容 |
|---|---|
| 01 | **前便で直した証明書の発行ゲートの文言**。実際に発行ボタンを押し、拒否されて**赤字が出て発行ボタンが消える**ところまで確認 |
| 02 | 今回の是正2件（確認できなかったことを画面に出す） |
| 03 | 今回の是正1件（ホーム画面。確認できなかったことを画面に出す） |
| 04 | 今回の是正1件の**文言の見え方**（★下記の注記あり） |

★**04だけは「文言の見え方」しか確認できていません。**
ブラウザ経由では撮影画像の取り込みが成立せず、手前のゲートで止まってしまうため、
**その場面を画面上で再現できませんでした。**
**そこが実際に働くことは、コードの確認のみで判断しています（画面では未確認）。**

---

## 4 回帰試験

| 対象 | 結果 |
|---|---|
| UI回帰 11本（入口を閉じた後に再実行） | **11/11 正常終了・失敗0** |
| エンジン collision | **PASS 27 / FAIL 0**（基準値と一致） |
| エンジン identify | **PASS 12 / FAIL 0**（基準値と一致・下記注記） |
| エンジン dup_semantics | **本日も完走を確認できず**（下記） |
| 画面ファイルの構文チェック | **エラー0** |
| 禁止語の検査 | **0件** |

★**identify は1回目に失敗しました。** そのとき別の重いテストを同時に走らせていました。
**単独で走らせ直すと 12/0 で通りました。** 原因は同時実行と考えられますが**断定はしません**。
事実は「同時実行では落ち、単独では通った」です。
→ **テストの合否は単独実行で判定する**ことにしました。

★**dup_semantics（23件）は今回も完走を確認できませんでした**（50分以上・途中経過が出ない作り）。
本日の変更はこのテストが読み込まないファイルのみのため**構造的に影響を受けません**が、
**「エンジンの全スイートを確認した」とは言えません。**

---

## 5 触っていないことの確認

- **本番の保存領域は本日も0件**（試験はすべて隔離した一時フォルダで実施）。
- **提出書類は本日1件も更新していません**（更新日時で実測）。
- 変更前のバックアップを日付つきで保存済み。

<!-- created: 2026-08-08 by model=Opus 5(claude-opus-5[1m]) terminal=紙紋UI -->
