# ジャンル動線の統合検索接続＋作品タイトル検索の降格＋モバイル文字組み残存是正（2026-08-11・あつぺくと制作T・モデル Fable 5）

代表のiPhone実機指摘を受けた3本立て。テーマ161のみ・4ファイル（atspect-genre / atspect-artist-archive / atspect-collection / atspect.css）・全push読み戻しSHA-256一致。

## A. ジャンルアイコン→統合検索（行き止まり解消）

- トップ「あらゆる表現」8アイコンの遷移先＝ `/collections/all?genre=` → **`/pages/artists?genre=`（作家と作品を探す）** へ差し替え（8本とも・実DOM取得で確認）。
- 着地側に `?genre=<日本語>` 読み取りを新設＝**押したジャンルが選択済みで開く**。
- **行き止まりの原因**＝旧画面は「表示中ページに作品が無いジャンルのボタンを disabled にする」設計。統合検索側はボタンを disabled にしない＝**乗り換え自由**。
- 実測＝彫刻着地→彫刻選択済み・作家60名表示→絵画ボタン（disabled=false）クリック→絵画60名へ即切替。8ジャンル全て着地選択OK（作家13〜60名・0件ジャンルなし）。8言語すべてで選択状態・ボタンラベル連動・placeholder切替OK。

## B. 作品タイトル検索の降格（可逆）

- 代表確定＝作品のタイトルで検索する人はいない。検索の軸は作家名・ジャンル（2026-08-09のタイトル照合追加を更新）。
- 統合検索（作品タブ）と作品一覧の**タイトル照合をコメントアウト残置で無効化**（data-title属性はDOM残置＝戻すのはコメント差し替えだけ）。
- 文言＝placeholder「作家名で検索」・注記「※作家名は、よみがな（ひらがな）やローマ字でも検索できます」等へ8言語で整合（関所通過済みの既存訳を再利用・新訳を増やさない）。
- 実測＝作品名「カエル仏」→両画面0件／作家名「鈴木 千賀子」→両画面3件。
- **残し**＝寄贈一覧（atspect-gifts）の検索窓にタイトル照合が残る＝SNS運用Tの持ち場・作業中のため不触（申し送り）。`/search`（Shopifyネイティブの作品検索ページ）＝**可視の導線が3幅すべてで0件を実測**＝休眠ページ・扱いは要判断。

## C. モバイル文字組み残存（退行か対象漏れかの特定→是正）

**特定結果＝退行でも一覧漏れでもなく、構造的な素通り2つ**：
1. **エンジン対象漏れ**＝8/10是正の `word-break: auto-phrase` は **WebKit（iPhone Safari）26.5で未対応**（`CSS.supports('word-break','auto-phrase')=false` を実測。Chromiumはtrue）。
2. **当て先対象漏れ**＝8/10の当て先は h1〜h5・label のみで、**中央寄せの短い案内文（p）が対象外**。

**是正（atspect.css・両エンジン対応）**＝中央寄せ案内文16クラスに `text-wrap: balance`＋ja限定 `word-break: keep-all` / `line-break: strict`。文中リンク「プライバシーポリシー」（contact）の語中割れも既存ja限定リストへ追加で解消。

**是正箇所数＝可視7箇所**（統合検索hint/prompt・作品一覧hint/motif-hint/lead・寄贈preparing/visit・contactリンク※leadは予防適用含む）。
是正後実測＝WebKit・Chromiumとも折返しは句読点境界のみ（例＝「作家名を入力するか、／ジャンルのボタンをお選びください。／作品もご覧いただけます。」）・横溢れ0（3幅）・可視の検出残0件。

**触っていないもの**＝privacy法文の左揃え2字残り3件（別型・auto-phrase全文適用の副作用を8/10に実測済みで安全な横断手段なし＝記録のみ）。

## 証拠（このフォルダ）

- `<ページ>_<幅>.png`＝390/768/1280の3幅フルページ（chromium）＋`_390_webkit.png`＝iPhoneエンジン相当。
- 対象＝top／artists-initial／artists-genre-sculpture（彫刻着地）／collection／gifts／contact。
- `checks.json`＝各幅の横溢れ・価格DOM・hidden規則の定点実測。
- ※gifts_768/1280はvisit囲みの追加適用前の撮影（当該文は広幅では1行のため見え方は同一）。

## Codex二刀流（6分割・ハング0）

genre遷移設計GO／archive実装GO／collection 8言語GO／archive 8言語＝es注記の言い回し指摘1件（2026-08-09関所通過済みの既存訳のため据え置き・翻訳品質Tへ）／collection降格NO-GO 2件＝1件は妥当（SSR旧文言の対応関係コメントを追記で是正）・1件は却下（意図した注記コメント）／CSS副作用NO-GO 2件＝1件は妥当（hero-desc/leadを`[data-col-i18n="browse_lead"]`限定に是正）・1件は反証却下（giftsクラスはローカルに無いライブ専用ファイル＝ライブ実測で実在・寸法確認済み）。

## 実施していないこと

- iPhone実機での確認（WebKitエンジンでの代替実測のみ）。
- gifts（寄贈一覧）本体ファイルの変更（SNS運用T持ち場）・/searchページの文言変更（要判断）。
- 8言語の全ページスクショ（言語検証はDOM実測＋ja/en等の代表確認）。
- 検証中にキャッシュバスター連打で当方IPが一時レート制限を受けた（数十秒・一般訪問者への影響なし・以後は間隔を空けて回避）。

最終更新：2026-08-11
