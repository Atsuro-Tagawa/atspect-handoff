# 迂回路を塞ぎました／実データは接続していません（2026-08-09 第14便・システム開発T・model=Opus 5 (1M context)）

★このリポジトリはPublicのため、**URL・固有デプロイURL・アカウントID・チーム名は書きません**。
正本＝`C:\Vault\ARTS-RESPECT\_本番_ダッシュボード施錠設定_20260808_ATSPECT.md`

---

## 1. ★迂回路を塞ぎました（前便の問題A・解決）

### 方式＝Pagesの設定「プレビューを制限」

前便で試みた**ワイルドカードのAccessアプリの手作り**は、今回も**作成画面が入力を受け付けず**失敗しました
（「カスタム入力に切り替える」を押してもフォームが切り替わらない。
ドメインの選択肢も空。指示の(a)既存アプリへの追加＝「宛先」に選択肢なしで不可）。

**そこで指示の(c)＝Pagesの設定を全数確認したところ、専用の機能が見つかりました。**

> **プレビュー アクセス**
> 「プレビュー デプロイはデフォルトで公開されています。Cloudflare Access でプレビューを制限すると、
> 訪問者は閲覧前にサインインが必要になります。」
> 「**これはプレビュー デプロイの URL のみを保護します。Production の pages.dev と
> カスタムドメインは Zero Trust で別々に管理されます。**」

これを有効化したところ、**`*.<プロジェクト名>.pages.dev` を対象とするAccessアプリが自動で作られました。**

### ★封鎖後の再検証（未認証curl・1経路ずつ）

| # | 対象 | 封鎖**前** | 封鎖**後** |
|---|---|---|---|
| 1 | 本番のホスト名 | 302→ログイン（元から守られていた） | **302→ログイン** |
| 2 | 現行デプロイの固有URL | **200・中身を返す** | **302→ログイン**（中身は返らない） |
| 3 | 一つ前のデプロイの固有URL | **200・中身を返す** | **302→ログイン** |
| 4 | **前便で削除した**デプロイの固有URL | **200・中身を返す** | **302→ログイン** |
| 5 | 空だったデプロイの固有URL | 404 | **302→ログイン** |

**＝5経路すべてで、未認証では中身が返らなくなりました。**
★#2は最初1回だけ200が返りましたが、**キャッシュ破りのヘッダを付けて4回測り直すと4回とも302**でした
（`Www-Authenticate: Cloudflare-Access` ヘッダも確認）。**エッジのキャッシュでした。**

### ★自動作成されたアプリの中身（必ず確認しました）

| 対象 | 許可 | セッション |
|---|---|---|
| 本番のホスト名（手作りしたアプリ） | **社内4名** | **30分** |
| `*`（自動作成されたアプリ） | **代表1名のみ** | **24時間** |

- ワイルドカードは**本番ホスト名を含みません**。**両方あって初めて全経路が守られます**（両方とも設定済み）。
- 自動作成側は**代表1名のみ＝より厳しい**ため、そのままにしました（ほかの3人は固有URLを使いません）。
- ★**セッション24時間を30分に揃えようとしましたが、画面が反応せず変更できませんでした（未完了）。**
  機能上の穴ではありませんが、**設定が揃っていないことは事実として残します。**

### Codexの独立レビュー（封鎖について）

> 限定的に妥当。5経路すべてで未認証時に302となり内容を返さないため、
> **「未認証公開」は対処・再検証済みと言える。**
> ただし**認証後に正しい利用者だけが閲覧できるかは未検証**なので、Access全体の検証完了とは言えない。

---

## 2. ★実データは接続していません（理由を4つ）

指示は「封鎖の実測が通ったら実データを接続」で、**封鎖の実測は通っています。**
それでも接続を見送り、ご判断をお返しします。

1. **中身が重い**＝実際に届いたメールの差出人・件名・本文（約193KB）、作家さまの台帳、
   非公開のスプレッドシート/BoxのURL等を含む設定ファイル。
2. **★承認済みの安全装置を、自分で外すことになります。**
   配信物を組み立てるスクリプトには「入れるものだけを並べる許可リスト」＋
   「メールアドレス・非公開URLが混ざっていたら出発を止める検査」があります（Codexレビュー済み）。
   **上記のファイルは、この検査に必ず引っかかります。**
3. **★一度載せたら、確実に消す手段がありません。**
   **デプロイの削除が効かないことを、こちらで実測しています**（前便で削除したデプロイが数時間、中身を返し続けた）。
   いまはAccessの内側に入りましたが、**スナップショット自体は残っています。**
4. **誰かが実際にログインできたことを、まだ一度も確認できていません**（下記）。

### Codexの独立レビュー（この判断について）

> **妥当。** 条件上は接続段階に進めるが、**安全装置の解除・削除不能な機微データ・ログイン未確認**という
> 新たな重大事情がある。**独断で接続せず、事情を明示して依頼者へ再判断を返すのは、
> 指示違反ではなく適切なエスカレーション。**
> 決定的なのは**理由2と3**。
> ★追加の指摘＝**`config.local.js` はブラウザへ配信した時点で「非公開設定」ではなくなるため、
> 配信物に含める設計自体を再検討すべき。**

---

## 3. ★許可リストの4件目は差し替えていません

ご指示では「Vaultの許可リスト文書に本日追記されている値を使う」とのことでしたが、
**Vaultを全文検索した結果、新しいアドレスはどこにも追記されていませんでした**
（4件目は従来どおり共有アドレスのまま。本日更新されたVaultファイルで該当するものは
この施錠設定の文書1つだけで、その中にも新しい値はありません）。

**推測でアドレスを入れることはしていません。** 値をお知らせいただければ、1件の差し替えで済みます。

---

## 4. ★実施していないこと・判定していないこと

- **実データを接続していません**（上記2の理由）。したがって
  **「接続後に未認証で実データへ到達できないことの実測」も行っていません**（載っていないため）。
- **★「中から入れること」を実測していません。** 誰かが実際に入れたことは、まだ一度も確認できていません。
- **★届く数字の桁数を確かめていません。**（6桁とは書きません。）
- **通しの撮影ができていません**（入れていないため）。**代表の初ログイン待ちです。**
- **自動作成されたアプリのセッション24時間を30分に揃えられていません**（画面が反応せず）。
- 許可リスト4件目の差し替え（値が存在しないため）。
- Cloudflareの請求画面は、この便では確認していません。

## 5. ご判断をお待ちしているもの

1. **実データを接続してよいか**（上記2の4点、とくに**安全装置を外すこと**と
   **一度載せたら消せないこと**をご承知のうえで）。
   ★代案＝**`config.local.js` は配信物に入れない設計へ変える**（Codexの指摘）／
   **メールの実データは載せず、画面から共有受信箱を開く導線だけにする**、なども可能です。
2. **桁数の確認方法**＝(a)代表が1回ログインして桁数を教えてくださる／
   (b)共有受信箱のCloudflareメール1通だけを読む許可をいただく。
3. **許可リスト4件目のアドレス**（決まっていれば）。

## 6. 成果物

- 4人に配る紙＝`C:\Users\ataga\Desktop\ダッシュボードの開き方.txt`
  （URL・**ボタン名「Send login code」**・**セッション30分**・
  ★**初回ログインで届いた数字の桁数を一言お知らせください**の依頼行を追加）
- 配信物＝`C:\Users\ataga\atspect-system\scratch\deploy`（20ファイル・`b1f84c03…`・**実データ0件**）
