---
name: atspect-artist-data-repair
description: あつぺくと掲載作家（610名）のページデータを、一次資料で検証して8言語ライブ＋CSV正本へ是正し切るための運用型。確認する13の型・序列語のA/B/C判断・確定できない情報の扱い・artist-i18n-update.mjs による8言語同時更新・Codexが使えないときの代替・1名あたりの11手順・同じ型をまとめて直す一括是正・止まってよい4条件・報告の型を一本化する。「作家データの是正を続けて」「フェーズ2を進めて」「続けて」で使う。捏造ゼロ検証は atspect-no-fabrication-verify、文体は atspect-artist-profile-writing、8言語品質は atspect-8lang-quality、表記は atspect-copy-rules、不変条件は common-invariants に委譲＝本スキルは是正作業の運用のみ。
---

# 作家データの検証と是正（610名・8言語ライブ反映）

> **単一参照点**：不変条件は `common-invariants`／捏造ゼロの検証手順は `atspect-no-fabrication-verify`／文体と序列をつけない書き方は `atspect-artist-profile-writing`／8言語の品質は `atspect-8lang-quality`／禁止語のgrep点検は `atspect-copy-rules`／長丁場の規律は `long-run-discipline`／段と再開は `checkpoint-workflow`／記録は `vault-logging`。**本スキルはそれらを前提に、この是正作業に固有の運用だけを書く。**

## 0. まず読む3つ（毎回・省略しない）

| ファイル | 何が書いてあるか |
|---|---|
| `atspect-handoff/reports/assets/artist-audit/progress.md` | **どこまで終わったか・次に着手する作家**。このファイルを読めば中断から再開できる |
| `atspect-handoff/reports/assets/artist-audit/priority-list.md` | 610名の**着手順**（重み付き順を使う。単純合計順は選別できないと実測済み） |
| `atspect-handoff/reports/assets/20260731_作家8言語更新経路/手順_作家ページ8言語の更新.md` | 8言語更新ツールの**実装仕様**（できること・できないこと） |

補助＝`wording-under-research.md`（「調査中」の8言語表記）／`rank-words.md`（序列語の全数抽出）／`pattern-list.md`（同型の誤りの機械抽出）。

---

## 1. 確認する13の型（★1名ごとに全部あてる）

実際に誤りが見つかった型。**チェックリストとして機械的に当てる。**

1. **代表作の実在** — その作家の作品か。★別作家・国宝との混同（松田権六＝尾形光琳の国宝と混同／太田喜二郎＝前田寛治作／新海竹太郎＝東博の館史は別人の作と記載）
2. **viewpointが架空の鑑賞記述になっていないか** — 代表作が誤れば鑑賞記述ごと架空になる（松田権六の型）。★**viewpointは `artist_i18n` にしかない**
3. **受賞は賞の名称だけでなく対象作品まで**（大高正人の型）
4. **栄典の記載漏れ・誤記載** — ない栄典を書く／ある栄典を落とす／別団体と混同の3方向すべてが出る（土門拳＝文化功労者が一次資料に皆無／西川寧＝文化勲章の記載漏れ）
5. **読み** — 姓名とも誤る（雨田＝あめだ→あまだ）／注記が二重で矛盾する（深山＝ふかやま/みやま）
6. **収蔵先の名称と実在** — 施設名が実在するか（会津八一「會津八一記念館」）／同名の館が複数ないか（森鷗外記念館＝文京区立と津和野町立）／別所在の2施設を1つにしていないか（本郷新）／代表作の所有者が収蔵欄に無くないか（菱田春草＝永青文庫）
7. **収蔵欄への説明文の混入** — 収蔵先でなく文化財指定の説明が入っている（建築10名）。★**ライブに表示される**
8. **文化財の指定区分** — 国指定のDBだけで判断しない。**都道府県・市区町村指定も見る**
9. **「選定」と「指定」の混同** — 景観条例等の「選定」と法的文化財の「指定」は別
10. **没年月日** — 時差による誤認（圓堂政嘉の型）
11. **同じ肩書き・賞・収蔵先が複数の作家に出現していないか**（秋山庄太郎・芳賀日出男の「日本写真家協会会長」の型）★1件見つけたら**同系統をまとめて確認する**
12. **作品名の正式表記** — 略題・汎称でないか（三岸節子「さいたさいた」→「さいたさいたさくらがさいた」／朝倉文夫「たま(猫)」→「たま（好日）」）
13. **共同制作・共作の可能性**（土浦亀城邸の型）

★★**過去に「確定」としたものも再検証で覆り得る。前回の結論を根拠にしない。**（西川寧・鈴木信太郎は「誤り」と指摘した箇所がライブでは既に直っていた）

---

## 2. 序列を感じさせる語（★司令塔判断 2026-07-31）

### 判断の基準＝**誰がそう言ったかを辿れるなら残す。辿れないなら外す。**

| 区分 | 中身 | 扱い |
|---|---|---|
| **A｜通称** | 当時からそう呼ばれていた呼称。例＝「東洋のロダン」「漆聖」「植田調」 | **残す。ただし「〜と呼ばれた」の形にする**（「である」は当社の評価になる）。★一次資料で呼称の使用記録を確認できなければ**Bへ落として外す** |
| **B｜当社が付けた順位** | `第一人者` `代表する` `代表的` `中心的人物` `中心人物` `中心となった` `重鎮` `先駆的` `先駆者` `先駆` `確立者` `主導し` `を主導` `牽引` `草分け` `巨匠` `大家` `随一` `屈指` `最高峰` `一流` `権威` `双璧` `正統` `本流` `頂点` `名家` `重要人物` `重要作家` | **外す**。実績が伝わらなくなる場合は**同じ文中に既にある具体的な事実**に置き換える |
| **C｜美術史の用語** | `の祖` `三筆` `明治三筆` `能書` `創始` | **出典が確認できるものだけ残す**（美術館・辞典・学術資料での使用を確認）。確認できなければ外す |

★**『あつぺくと』は有名・無名を序列づけない場である。第一人者がいれば二番手がいることになる。**
★**迷ったら外す。書かなければ後で足せる。誤って評価を貼り続ける方が重い。**

### 書き換えのしかた（この順で考える）
1. その語を含む句を落として文が成立するなら、**落とすだけ**にする
2. 落とすと実績が伝わらない場合は、**同じbio内に既にある事実**へ言い換える
3. ★**bio内に無い事実は絶対に足さない。** 足すくらいなら句ごと落とす
4. 「〜と呼ばれた／と称された／と評された」の形は**そのまま残す**
5. 語を外したあと、日本語として自然につながるよう**最小限だけ**整える

### 各言語で外す語（対応表）
- **en**: `leading` `foremost` `pioneer(ing)` `led` `spearheaded` `central figure` `major/important figure` `representative of` `master`（巨匠の意）
- **zh_cn/zh_tw**: `代表` `主导/主導` `先驱/先驅` `引领/引領` `中心人物` `重要人物` `巨匠` `泰斗` `重镇/重鎮`
- **ko**: `대표하는` `주도` `선구` `견인` `중심인물` `중요 인물` `거장`
- **fr**: `figure de proue` `pionnier` `chef de file` `mena` `dirigea` `figure centrale/importante` `représentatif de` `maître`
- **es**: `pionero` `figura central/destacada` `lideró` `dirigió` `representativo de` `maestro`
- **de**: `führend` `Wegbereiter` `Pionier` `führte an` `zentrale Figur` `bedeutende Persönlichkeit` `Meister`

---

## 3. 確定できない情報の扱い

| 場面 | 扱い |
|---|---|
| **独立した項目**（受賞歴・収蔵先・代表作・所属団体） | **空欄にする。「調査中」と書かない**（作家さまが自分のページを見たときに準備不足の印象になるため） |
| **文章の中に紛れている場合** | **その箇所だけに「（調査中）」を添える。文ごと削除しない**（例＝「紫綬褒章を受章（受章年は調査中）」） |
| **裏が取れる範囲まで後退できる場合** | **確認できる範囲へ後退させる**（武石弘三郎＝「新潟県長岡(中之島)」→「新潟県」。市町村名の裏付けが取れなかったため） |

★8言語の表記は **`wording-under-research.md` を必ず参照**（言語ごとに違う語を使わない）。

### 「無いことの証明」
- 訃報記事など**受賞歴を網羅する性質の資料に記載が無い**ことをもって削除してよい（土門拳の文化功労者がこの型）。
- ★ただし `progress.md` に「**無いことの証明はできていない**」と明記し、**どの資料を見て記載が無かったか**を列挙する。
- **削除した文言そのものを `progress.md` に残す**（後日、裏が取れれば戻せるように）。

---

## 4. 8言語の更新（`artist-i18n-update.mjs`）

```bash
cd C:\Users\ataga\atspect-system
node scripts/artist-i18n-update.mjs show   --name "作家名"    # 現状を2箇所並べて見る（★46文字で切れる）
node scripts/artist-i18n-update.mjs verify                    # 全625名の不一致件数
node scripts/artist-i18n-update.mjs plan   --file <指示書> --label <ラベル>
$env:SHOPIFY_ALLOW_WRITE="1"; node scripts/artist-i18n-update.mjs apply --plan <計画> --yes
$env:SHOPIFY_ALLOW_WRITE="1"; node scripts/artist-i18n-update.mjs restore --backup <backup.json> --yes [--handle a,b]
```

### ★必ず守る
- **個別フィールドと `artist_i18n` の両方を更新する。** `i18n` に書けば自動で両方に入る（`flat` でミラー対象を単独指定することはツールが弾く）。
- **`plan` の「2箇所の不一致：反映前N件→反映後M件」を毎回見る。** ★**増やさない**（全体の基準値は 2026-08-01 時点で201件）。
- **長文（bio・viewpoint）を直すときは `show` でなく backup.json を読む。** `plan` を1回流すと `C:\Vault\ARTS-RESPECT\_backups\_auto\artist-i18n\<日時>_<ラベル>\backup.json` に**全40フィールドが落ちる**。
- **PowerShellでは `set VAR=1 && node …`（cmd構文）は動かない。** `$env:SHOPIFY_ALLOW_WRITE="1"; node …` を使う。
- `masterworks` は自動ミラーされない＝**`i18n.masterworks`（配列）と `flat.masterworks_text` を両方書く。**

### CSV正本の同期
```bash
node <scripts/csv-set.mjs> <出力先> <ジョブJSON>   # 作家名＋列名で1列ずつ差し替え。BOM/CRLF保持
node C:\Users\ataga\atspect-system\scripts\vault-file-update.mjs <対象CSV> <新内容> <ラベル>  # バックアップ＋読み戻しSHA検証
```
正本＝`C:\Users\ataga\Desktop\作家リサーチ\artists_master_cleaned.csv`（610件）。
★**Vault `_DATA_TO_PRODUCTION\` 側のコピーは古い。正本はデスクトップ側。**

---

## 5. Codexが使用制限に当たった場合

★**止まらない。Codexを飛ばして先へ進む。**
1. その作家は、**下請けエージェント（Sonnet）を2つ立てて独立に調査させ**、2つの結果を突き合わせる
2. 本体（Opus）が両方を見て判断する
3. ★`progress.md` に「**Codexレビュー未実施**」と明記する
4. 枠が戻ったら、未実施の作家をまとめて再レビューする

★便の開始時にCodexへ**1件だけ軽い依頼**を投げ、応答を確認する。
★★**`codex exec` はエラーでも終了コード0を返す。** 出力に `usage limit` `Upgrade to Pro` `purchase more credits` `try again at` `Not inside a trusted directory` が無いか**毎回中身を見る**。
★エラーが返り始めたら**再試行を繰り返さず切り替える**（ハングと利用上限は別障害＝`codex-hang-vs-usage-limit`）。
★**Codexへは1回1作家1観点。** まとめて渡すとハングする。**画像・バイナリを渡さない。テキストと数値だけ。**
★兆候の多い作家にCodexを優先して使う。兆候の少ない作家はCodex無しでよい。

**Codexは実際に誤りを検出している**（例＝田口善国の英訳 `pampas grass` は別種 *Cortaderia selloana* を指す→訂正）。形式的に回すのではなく主力に据える。

---

## 6. 1名あたりの手順（★これを繰り返す）

1. **apply前の `page-audit` スナップショット**（是正前の画面）
   `node scripts/page-audit.mjs --url "https://atspect.com/pages/artist/<handle>" --lang ja,en,de --width 1280 --out scratch\audit\phase2\<名>_before`
2. 下請けエージェント（**Sonnet**）に一次資料の調査をさせる。★**調べる範囲を絞る**（広いと応答停止する。実例あり）
3. **Codexに1名1観点**でレビューさせる（代表作の実在／受賞の対象作品／収蔵先／読み／没年月日／師事関係）
4. 本体（**Opus**）が両者を突き合わせて判断する
5. `plan` → **差分確認** → `apply` → `verify`（8言語すべて）
6. **CSV正本を同期**する
7. **是正後の `page-audit`** を取り、`diff` で比較する
   `node scripts/page-audit.mjs diff <before> <after>` → ★**意図した行だけが変わっていること**を確認（`shopify-y` メタタグは毎回変わる＝無視してよい）
8. **残留点検**：外した語を**全言語**で検索し0件を確認
9. `progress.md` を1名1ブロックで更新
10. **push**（1名ごと）
11. ★**確認を求めず次の1名へ進む**

---

## 7. 同じ型をまとめて直す（一括是正）

**判断が既に確定している型は、1名ずつの検証を待たずにまとめて外す。**

### 手順
1. **機械抽出**して対象を確定する（例＝`rank-words.md` の序列語226件／`pattern-list.md` の同型の誤り）
2. **15名前後を1バッチ**にして、`plan` で全フィールドのバックアップを取る（＝1名ぶんの probe を兼ねる）
3. **下請けエージェント（Sonnet）に backup.json を読ませ、8言語の書き換え指示書JSONを作らせる**
   ★指示は「日本語の指定箇所を直し、同じ変更を7言語にも施す。**指定箇所以外は1文字も変えない**」
4. 本体が **`plan` の差分で全件を目で確認**する
5. `apply` する。★**ツールは1作家＝1回の `metaobjectUpdate` を順次実行**し、失敗したらその作家で停止する＝**実質1名ずつ**。戻すときは `restore --handle <handle>` で**対象を絞れる**
6. CSV正本を同期し、`progress.md` に**バッチ表**で記録して push

### ★同じ肩書き・賞・収蔵先の横展開を潰す
1件見つけたら、**その文字列で610名を全文検索**して同系統をまとめて確認する。
`pattern-list.md` に (a)同一備考 (b)同一栄典 (c)同一収蔵先 の抽出結果がある。

---

## 8. 止まってよい場合（★これ以外では止まらない）

1. **使用量の制限に当たったとき**
2. **データが壊れたとき、または壊れる可能性を検知したとき** → `restore` で戻し、`progress.md` に記録してから止まる
3. **承認していない操作が必要になったとき**
4. **同じ誤りが100名以上に及ぶ構造的な問題を発見したとき** → 1名ずつ直すより、まとめて直す方が確実。記録して報告する

---

## 9. 承認済みの事項（★改めて確認を求めない）

| | |
|---|---|
| **承認済み** | `SHOPIFY_ALLOW_WRITE=1` の使用（作家ページの是正のため）／**8言語ライブへの反映**／**CSV正本の同期** |
| **★承認していない（実行しない）** | 作家データの**不可逆な削除**／metaobjectの**フィールド定義の変更**／**テーマファイル・紙の書類・決済設定**への変更 |

★触るのは **metaobject(artist) と正本CSVのみ**。テーマ・紙・決済・サイトのページには触らない。

---

## 10. 報告の型

| どこに | 何を | どれくらい |
|---|---|---|
| `research.md` | **20名ごと**に進捗の要約 | **1〜3行**。長く書かない |
| `progress.md` | **1名1ブロック**で詳細（完了日時／誤りの有無／是正した箇所／未確認として残した項目と理由） | 制限なし。★**1名ごとに更新してpush** |
| 画面への報告 | 何をしたか・何が分かったか | 短く。**★「実施していないこと」を必ず書く** |

★push後は `git log -1 --format=%H` でフルSHAを取り、**SHA固定blob URLを1行出す**。

---

## 11. この作業で分かっている数字（2026-08-01 時点）

- 掲載作家＝**610名**（CSV正本）／Shopify metaobject＝625件
- 2箇所の不一致＝**201件**（着手時206件・**1件も増やしていない**）
- 是正済み＝**27名**・延べ320箇所超
- 序列語＝**B 226件／A 19件／C 31件**・該当**137名**（うちライブに出る欄に該当96名）
- viewpointに《作品名》＝**172名（28%）**＝代表作が誤れば鑑賞記述ごと架空になる構造リスク群
- 検証済み46名の誤り率＝**61〜65%**。分野で偏る（絵画6/8・彫刻7/9・工芸7/9・写真4/5）
