# 引き継ぎ：あつぺくとシステム開発ターミナル（2026-08-10・セッション終了）

model=Opus 5／slug＝`system`／作業ディレクトリ＝`C:\Users\ataga\atspect-system`（実測で役割を確認）

★このファイルは公開リポジトリにあります。**ハンドル・URL・鍵・実データは1文字も書いていません。**
詳細の正本＝**Vault**（ローカルのみ）。

---

## 1. 本日の到達点（完了した便・1行ずつ）

1. **ダッシュボード再設計 第5便・段1**＝設計図6枚（メール／トップ／あつぺくと中ページ×390・1440）＋対照表28行＋比較表2本を作成。
2. **同 段2＝実装**＝設計図どおり実装。全14ページ×2幅＝28通りで横あふれ0・タブ1段・44px以上・JSエラー0。操作の通し15項目PASS。
3. **カレンダーの方式を(a)自前保存で確定**＝(b)埋め込みは「ログインしていない機械ではログイン画面が出る」を実測して不採用。
4. **段階の語を作家台帳の9語へ統一**＝旧15ステータスの対応表を作成（捨てた語0／寄せ先未決は「物故」1つのみ）。
5. **メール取り込み（候補②）の仕組みを完成**＝定時実行の道具と手順書。**切り替えは未実施（関門2つ待ち）**。
6. **注文読み取りの接続**＝代表の操作手順書を作成 → 合鍵を受領 → **`read_orders` のみ・書き込み0を実測** → 注文を1件読めることを確認（保存なし）。
7. **「承認どまり／入金済み」を見分ける画面**を あつぺくと中ページの「お金」に実装＝12項目PASS（緑と赤で確実に分かれる）。
8. **非公開ページのハンドル差し替え3件**を本番適用（規約書1件は法的文書のため対象外と判断）。
9. **B-34（費用の一文の削除）は未実施**＝該当の一文が2段階目フォームに無く、テーマ側にあったため制作Tへ申し送り。
10. **旧URLの無効化**＝転送3件を作成（トップ宛・新URLへは向けない検査を道具に内蔵）。**ただし効かず**。
11. **原因の切り分け**＝描画キャッシュ説を自分で反証 → **同一ページIDが返ることから「Shopifyの別名」と特定**。
12. **翻訳データ説は否定**＝権限を得て実測。言語は `ja` の1つだけ・翻訳の登録0件・旧スラッグ0件。
13. **目視点検④「動きのある状態」**＝10状態×6通り＝60通りを目視。**異常0件・迷い8件**。
14. **言語トーストの重なりの原因特定**＝`layout/theme.liquid` 683〜691行の貼り付く帯。**差分案3行（未適用）**。

コミット＝本日 **12件**（`atspect-system` はremote無しのためcommitのみ）。
報告＝`atspect-handoff` に **push済み**（未pushなし）。

---

## 2. 作業中のカーソル・次にやると決まっていること

### ★最優先＝旧URLの再測定（1分で終わります）

- 差し替えた**旧URL3件が、まだ開けます**（本日の最終測定でも200）。
- **転送3件は作成済みで、旧の綴りが落ちた瞬間に効きます**（それまでは出番が来ません）。
- **朝いちばんに測り直してください。** 404または転送が効いていれば、**そこで閉鎖完了**です。
- 測る道具＝`node scripts/handle-swap-20260810.mjs verify <対応表>`
  （対応表の場所はVaultの `_作業_ハンドル差し替え_20260810/`）。

### 実データ切り替えの関門＝残り2つ

| # | 関門 | 状態 |
|---|---|---|
| 1 | **代表の初ログイン実測**（まだ誰も鍵の内側に入れていない） | **未達** |
| 2 | **許可リストの4件目を個人のアドレスにする** | **未達** |

この2つが済めば、メール・台帳・注文の実データへ切り替えられます（手順書は用意済み）。

### そのほか、決まっている次の一手

- **営業用QR・リンク集・ブックマークの作り直し**（3本ぶんが旧URLのまま）。Vaultの正本の冒頭に注意書き済み。
- **B-34の削除**＝制作Tでの実施。**どの一文を消すかは代表のご判断**（2段階目フォームには無く、案内ページにある）。
- **料金PDFリンク2本が404**（既存の不具合・当便では触っていない）。作家さまが費用を見られない状態。
- **言語トーストの3行の差分**＝SNS運用Tの持ち場。★**トーストが今のライブで出ないこと自体**も要確認。

---

## 3. 持ち越しの注意（自分の持ち場に関わるもの）

1. **★B-38の旧URLは未閉鎖。転送は保険で待機中。** 「差し替えたから安全」とは書かないこと。
   露出は実測で広がっていません（robotsはnoindex／sitemapに0件／サイト内リンク0件）が、
   **旧の綴りを知っている人は開けます**。
2. **原因はShopifyの「前のハンドルの別名」**。当方の持つどのAPIからも見えず、消せません。
   消えるまでの時間は**「9時間半より長く、7日以内」**（今回と8/3の前例の2点から）。**それ以上は書かない。**
3. **規約書のURLは変えていません**（同意記録から参照されるため）。今後も安易に変えないこと。
4. **合鍵は `.env` にのみ**。控えは**リポジトリの外**（`C:\Users\ataga\_env-backups\`）。
   **チャット・LINE・GitHubには絶対に貼らない。**
5. **紙の書類は最後に一括**（URLが確定してから刷る。いま刷ると作り直しになります）。
6. **共有リポジトリの他ターミナルの未コミットを巻き込まない**。`scripts/` には他ターミナルの
   作業ファイルが多数未コミットで残っています（本日時点で8/10付きだけで43件）。**触らないこと。**
7. **`reports/assets/` 配下は1ファイルも削除しない**（恒久ルール）。

---

## 4. ★本日の新しい恒久ルール2件（全ターミナル共通）

### ルール1：文字・組版・CSSを変える便は、3幅のスクリーンショットを目で見てから完了と書く

**文字・組版・CSSを変えた便は、390／768／1440 の3幅でスクリーンショットを撮り、
1枚ずつ目で見てから「完了」と書く。**

- 機械の判定（禁止語0件・差分ゼロ・ハッシュ一致）が全部通っても、**それは完了の根拠にならない**。
- 本日、**静止画では出ない崩れ**（スクロールした状態でだけ起きる重なり、
  時間で薄くなる文字）が実際に見つかっており、**目で見る以外に気づく方法がなかった**。
- 撮っていないなら「**まだ実物を見ていないので見た目は判断できない**」と正直に書く。

### ルール2：翻訳品質ターミナルは常時運転（待機を出さない）

**翻訳品質Tには「待機」「終了」の便を出さない。** 常に走り続ける前提で運用する。
本セッション終了便も、翻訳品質Tには出していない。

---

## 5. 道具の状態（Codex）

**Codexは使用可能です**（2026-08-09の夜にリセット済み・本日の実測にもとづく）。
★**古い「2026-08-16まで上限」という記述を、次の便に書き写さないこと。**
使いどころは従来どおり、**本番反映・不可逆・対外・作家原稿**の4つに絞ります。

---

## 6. 本日つくった道具（次の人がすぐ使えるもの）

| 道具 | 何をするか |
|---|---|
| `scripts/handle-swap-20260810.mjs` | ハンドルの計画・適用・検証（適用直前にライブを取り直して照合） |
| `scripts/old-url-redirect-20260810.mjs` | 旧URLへの転送の作成・検証（**新URLへ向けない検査を内蔵**） |
| `scripts/page-resave-20260810.mjs` | ページの同値保存（控え→保存→読み戻しで1バイト一致まで確認） |
| `scripts/translation-slug-20260810.mjs` | 翻訳スラッグの読み取り・削除（削除は旧の綴りだけ） |
| `scripts/_check-orders-scope-20260810.mjs` | 合鍵の権限を確かめる（**値は画面にもログにも出さない**） |
| `scripts/_orders-token-exchange-20260810.mjs` | ID・シークレット → 合鍵の引き換え |
| `scripts/_shot-dan2-20260810.mjs` ／ `_sweep-dan2-20260810.mjs` | ダッシュボードの撮影と機械検査（横あふれ・タブ段数・44px・JSエラー） |
| `scripts/_readonly-mokushi4-20260810.mjs` | 動きのある状態の撮影（10状態×6通り） |

手順書＝`docs/手順_段階の語の対応表_20260810.md`／`docs/手順_メールの取り込み_20260810.md`／
`docs/手順_実データへの切り替え_20260810.md`／`docs/手順_代表_注文読み取りの合鍵を発行する_20260810.md`／
`docs/手順_代表_翻訳の読み取り権限を足す_20260810.md`

---

## 7. 本日の証拠の置き場所（すべてpush済み）

- `reports/assets/20260810_ダッシュボード再設計/`（設計図6枚＋比較表3本）
- `reports/assets/20260810_ダッシュボード段2/`（画面9枚＋実装記録）
- `reports/assets/20260810_注文読み取り/`（画面2枚＋代表の操作手順書）
- `reports/assets/20260810_ハンドル差し替え/`（記録＋旧URL無効化の再測定）
- `reports/assets/20260810_旧URL無効化/`（記録＋手順書＋追記3本）
- `reports/assets/20260810_目視点検_システム開発/`（シート13枚＋個別3枚＋一覧）
- `reports/assets/20260810_言語トースト原因/`（記録＋証拠4枚）
