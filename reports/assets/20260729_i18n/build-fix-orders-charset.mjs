import { readFileSync, writeFileSync } from "node:fs";

const SCRATCH = "C:/Users/ataga/AppData/Local/Temp/claude/C--Users-ataga-Desktop-SNS--/807f89a1-5dfd-41cf-b368-8a0c60fd3e3c/scratchpad";
const merged = JSON.parse(readFileSync(SCRATCH + "/merged-sorted.json", "utf-8"));

const byFile = new Map();
for (const r of merged) {
  if (!byFile.has(r.file)) byFile.set(r.file, []);
  byFile.get(r.file).push(r);
}

const md = [];
const P = (s) => md.push(s);
const CODE = (s) => { md.push("```"); md.push(s); md.push("```"); };

P("# 文字種検査 是正指示票（テーマ側・.com）");
P("");
P("作成：SNS運用ターミナル（Sonnet 5）。読み取り・実測のみ。サイトは一切変更していません。");
P("元データ＝コーポサイト制作Tが実装した文字種検査（`i18n-audit.mjs`拡張・`i18n-audit-report3.md`）＝テーマ側103箇所136件（未仕分け）。");
P("");
P("## 仕分けの基準（冒頭に明記）");
P("");
P("- **列挙の区切り＝是正対象**：「材料・技法」「预约・咨询」のように、2つ以上の項目・語句を並べる用途で日本語の中黒（U+30FB）が使われているもの。対象言語では中黒を使わないため、その言語の正しい記号に置き換える。");
P("- **語の連結・固有名詞・意図的な表記＝除外**：人名・専門用語の原語併記など、置き換えると意味を損なう、または誤りではないもの。");
P("- **置換の目標文字（既に本日是正されたcorp側privacy.htmlの74件と同一の基準）**：簡体字(zh-cn)・繁体字(zh-tw)は中国語の頓号「、」（U+3001）へ。韓国語(ko)は中点「·」（U+00B7、日本語の中黒U+30FBとは別のUnicode文字）へ。");
P("- **検索文字列は完全一致・行番号に依存しない**（apply-fixes.mjs方式に準拠）。1件ずつ、ライブのテーマファイルに対して実際に完全一致1件のみであることを機械的に検証済み（後述）。");
P("");
P("## ★重要な発見＝既存検査の「切り詰め」による過小報告を発見・完全な文で是正");
P("");
P("元の検査（`i18n-audit-report3.md`）は、1要素あたり最大400文字までしか読み取らない仕組み（`snippetAfter`）のため、長い段落では実際より少ない件数しか報告されていませんでした。本票の作成にあたり、同じ抽出ロジックをテーマファイルに対して再実行して**切り詰めていない完全な文字列**を取得し、特に長い5箇所（`atspect-privacy.liquid`153行目、`atspect-tokushoho.liquid`98・99・138・152行目）で、元の報告より多くの中黒を発見しました。とくに`atspect-tokushoho.liquid`98行目（配送費用の説明・簡体字）と99行目（同・繁体字）は、それぞれ**13箇所ずつ**の中黒を含む長大な1ブロックでした（元の報告ではこの一部しか件数に出ていませんでした）。本票はこの完全な件数を対象にしています。");
P("");
P("## サマリ");
P("");
P("| 区分 | 件数（票の行数） |");
P("|---|---|");
P("| 是正対象 | " + merged.length + "件（うち中黒type 98件・ひらがな/カタカナtype 1件） |");
P("| 除外（固有名詞等） | 2件 |");
P("| **母集団合計** | **103箇所（元検査の集計どおり）** |");
P("");
P("---");
P("");
P("## 除外（2件・固有名詞・意図的な表記のため対象外）");
P("");
P("### 除外1｜atspect-privacy.liquid 65行目・韓国語・漢字混入(雅號)");
P("現在の文字列：`성명·읽는 법·아호(雅號)`");
P("");
P("理由：「아호」（雅号＝ペンネームの意）に「(雅號)」という漢字を併記しているのは、韓国語の公式文書で人名・専門用語の意味を明確にするための一般的な慣習（グロス表記）であり、翻訳ミスではないと判断しました。同じ行内の他の中黒（「성명·읽는 법·아호」の区切り）は既に正しく「·」（U+00B7）を使用済みで、修正不要です。この判断はコーポ制作T自身の検査レポート（`i18n-audit-report3.md`）内でも「翻訳ミスとは言えない」と同様の判断が記録されており、本票でもこれを踏襲しました。");
P("");
P("### 除外2｜atspect-tokushoho.liquid 43行目・韓国語・漢字混入(代表者名)");
P("現在の文字列：`Atsuro Tagawa（田川 篤郎）`");
P("");
P("理由：特定商取引法に基づく表示という法的文書の性質上、代表者のローマ字表記に日本語の原表記（漢字の氏名）を括弧書きで併記することは、除外1と同種の固有名詞グロス表記であり、翻訳ミスではないと判断しました。");
P("");
P("---");
P("");
P("## 是正対象（99件・ファイル別）");
P("");
P("★検索文字列はすべて、対象ファイルに対して完全一致で1件のみ存在することを機械的に検証済みです（複数箇所で同一の短い語句が重複していた14箇所は、前後の文脈を含めて一意になるまで検索文字列を拡張しています。うち4件はzh-cn/zh-tw2言語を1件に統合しています）。");
P("");

for (const [file, rows] of byFile) {
  P("### " + file + "（" + rows.length + "件）");
  P("");
  for (const r of rows) {
    P("**" + r.line + "行目・" + r.lang + "**（" + r.type + "）");
    P("");
    P("現在：");
    CODE(r.finalSearch);
    P("置換後：");
    CODE(r.finalReplace);
    P("");
  }
}

P("---");
P("");
P("## 対外文言の禁止事項の確認");
P("");
P("置換は句読点（中黒→頓号／中点）の変更のみで、文の意味・数字・固有名詞は一切変更していません。掲載作家数・「物故／現存」の分け方・価格・確定タグライン等への抵触はありません。");
P("");
P("## 実施していないこと");
P("");
P("- 是正の実行はしていません（票の作成まで。実行は制作T）。");
P("- corp側（index.html・404.html・privacy.html）はコーポ制作Tが別途対応中のため、本票の対象外です（テーマ側のみ）。");
P("- `apply-fixes.mjs`への実際の収録・実行はしていません。`fixes-charset.json`（同梱）はapply-fixes.mjsが読み込める形式で用意しましたが、投入・実行の判断は制作T側に委ねます。");
P("");
P("証拠の置き場所＝`reports/assets/20260729_i18n/fix-orders-charset.md`（本ファイル）・`fixes-charset.json`（機械可読版）・`extract-charset-fixes.mjs`（抽出スクリプト）。");
P("");

writeFileSync("C:/Users/ataga/atspect-handoff/reports/assets/20260729_i18n/fix-orders-charset.md", md.join("\n"), "utf-8");
console.log("書き出し完了。行数:", md.length);
