# 代表作欄のテーマ側・言語分岐の器（①のみ・データは触っていない）

- 日付＝2026-08-10 ／ 担当＝SNS運用ターミナル ／ モデル＝Opus 5
- 触った範囲＝**`sections/atspect-artist-detail.liquid` の1か所（JavaScript 5行→24行）だけ。**
- **作家データ・作品データ・`artist_i18n`・`masterworks_text` は1バイトも書いていない。**
- **代表作の欄が出ない19名の表示条件（L242 / L246）は1文字も変えていない。**
- 前提＝リサーチT `reports/assets/20260810_代表作欄の翻訳残り/`（全文を先に読んだ）

---

## 1. 直したもの

**代表作だけ言語分岐が無く、`lang==='ja'` 以外は必ず `en → romaji → ja` の順で出ていた。**
そのため、データに6言語の訳を足しても**画面には1文字も出ない**状態だった（リサーチT実測＝5,000セル中3,488セルがNG）。

**是正前**（`sections/atspect-artist-detail.liquid`・JS）

```js
if(key==='masterworks'&&Array.isArray(v)){
  var sep=(lang==='ja')?'、':', ';
  var items=v.map(function(w){return (lang==='ja')?(w.ja||w.en):(w.en||w.romaji||w.ja);}).filter(Boolean);
  if(items.length)el.textContent=items.join(sep);
}
```

**是正後**（要点だけ抜粋）

```js
var u=String(lang).replace('-','_');      // 画面は 'zh-cn'／データは 'zh_cn'。両方見る
var own=0;
var items=v.map(function(w){
  if(!w) return '';
  var cur=w[lang]||w[u];
  if(cur){ own++; return cur; }           // ★そのページの言語のキーがあればそれを出す
  return (lang==='ja')?(w.ja||w.en):(w.en||w.romaji||w.ja);   // ★無ければ従来の式そのもの
}).filter(Boolean);
```

★**落ち方（フォールバック）は従来の式を書き写している**ので、キーが無い間の出力は**式として同一**になる。

---

## 2. ★合格条件＝いまの見た目から1文字も変わらないこと

**3つの経路で確かめた。3つとも差分0件。**

| # | 何を | 範囲 | 結果 |
|---|---|---|---|
| ① | **全数の機械比較**（ライブのデータを取得し、旧式と新式の**両方を写した関数**で表示値を計算して突き合わせ） | **625名 × 8言語 ＝ 5,000セル** | **差分 0件** |
| ② | **実画面の前後比較**（是正前にページを開いて読み、当ててから同じページを読み直す） | 7名 × 8言語 ＝ 56ページ | **差分 0件** |
| ③ | **②をもう一度**（★配信物に新しいコードが載っていることを確かめた**後**に再取得） | 7名 × 8言語 ＝ 56ページ | **差分 0件** |

★**③を分けた理由**＝「表示が変わらなかった」と「新しいコードがまだ配信されていないだけ」は**別物**。
先に配信物を確かめないと区別できない。**確認した印**＝ページのHTMLに新しいコードの3つの印があり、
**旧いコードの1行版が残っていない**こと（`検証/配信物の確認.txt`）。

①の内訳（ライブ実測・リサーチTの数と一致）＝**代表作の欄そのものが出ない19名**／**オブジェクト形式で別経路の25名**。

②③に使った7名（型を1つずつ拾った）＝
`kano-hogai`（型A・3点）／`yokoyama-taikan`（型A・4点）／`saigo-kogetsu`（型A・1点）／
`azuma-kenjiro`（型A・作品名がラテン文字）／`tange-kenzo`（型B・オブジェクト形式）／
`fukayama-ryudo`（型D・配列が空＝日本語が残る）／`kageyama-koyo`（型C・欄そのものが出ない）。

---

## 3. 訳が入ったときにどう出るか（★本番データには足していない）

**確かめ方**＝ブラウザが受け取るHTMLだけを手元で書き換えて開いた。
作家ページに埋め込まれた `artist_i18n` のJSONのうち、**`masterworks` の各要素にだけ見本の6言語を足して**読み込ませた。
**サーバへの書き込みはゼロ。ライブのデータは1バイトも変えていない。**（全リクエストの記録＝`検証/見本データでの動作.json`）

| 言語 | 見出し | 代表作の欄に出た値 |
|---|---|---|
| ja | 代表作 | 悲母観音、不動明王図、仁王捉鬼図（**見本を無視して従来どおり**） |
| en | Selected Works | Merciful Mother Kannon, …（**同上**） |
| zh-cn | 代表作 | 【见本1】…**、**【见本2】…**、**【见本3】… |
| zh-tw | 代表作 | 【見本1】…**、**【見本2】…**、**【見本3】… |
| ko | 대표작 | 【견본1】…**, **【견본2】…**, **【견본3】… |
| fr | Œuvres choisies | [Échantillon 1] …**, **… |
| es | Obras destacadas | [Muestra 1] …**, **… |
| de | Ausgewählte Werke | [Muster 1] …**, **… |

★見本には「见本／見本／견본／Échantillon／Muestra／Muster」の語を必ず入れて、**実在の訳と取り違えられない形**にした。

---

## 4. ★1つだけ足した判断（区切り記号）

**中国語の並列は「、」にした。ただし、その言語の値が全作品に揃ったときだけ。**

- 理由＝2026-08-09に、中国語の並列記号を「、」へ揃える是正を**42件**当てている。
  器を作る時点で古い作法（`, `）を焼き付けると、訳が入った日に同じ是正をもう一度やることになる。
- **揃っていない間は従来どおり `, `** なので、**いまの見た目は変わらない**（①②③の差分0件がその証拠）。
- 対象は zh-cn / zh-tw のみ。ja は従来どおり「、」、ko・fr・es・de・en は従来どおり `, `。
- ★**この1行はご指示に無い追加**です。不要なら `if(lang!=='ja'&&(lang==='zh-cn'||lang==='zh-tw')&&…) sep='、';`
  の**1行を消すだけ**で元に戻ります。

---

## 5. 出力経路が本当に1か所かを自分で数えた

リサーチTの申し送り「L242/L246の1か所のみ・別経路なし」を、**人の言葉ではなく現物で**確かめた。
テーマ**525ファイル**を全文走査し、`masterworks` / `masterworks_text` / `data-aad-prof` / 8言語の見出し語を数えた。

**当たったファイル＝1（`sections/atspect-artist-detail.liquid`）だけ。申し送りのとおりだった。**
（同ファイル内の L204・L213・L216 の「代表作」は**画像**のラベルで、文字列の一覧とは別物。）
結果＝`検証/出力経路の走査.txt`

---

## 6. 触っていないもの（明示）

- **`artist_i18n` / `masterworks_text` / 作家データ・作品データ＝1バイトも書いていない。**
- **L242 / L246（代表作の欄を出すかどうかの条件）＝1文字も変えていない。**
  19名の欄が出ない状態は**そのまま**（②③の実測でも `kageyama-koyo` は前後とも8言語すべて「欄なし」）。
  群2の取り残しの扱いはリサーチTの裏取り待ちのため、**表示を勝手に復活させていない。**
- オブジェクト形式（型B・25名）の経路（`aadPick`）＝**触っていない。**
- 法的文書・規約・決済・ヘッダー・他セクション＝**触っていない。**

## 7. していないこと（正直に）

- **6言語の訳そのものは1件も作っていない**（データ側は翻訳品質Tの持ち場）。
  したがって**現時点では画面は何も変わらない。器ができただけ。**
- **全625名のページを1枚ずつ開いてはいない。** 開いたのは7名×8言語×2回＝112ページ。
  残りは①のライブデータ＋旧式・新式の両方を写した関数で計算した（5,000セル）。
- **実機（実物のスマートフォン）での確認＝していない。**
- **Codexによる独立レビュー＝受けていない**（本便はJavaScript 1か所の分岐であり、
  ①②③の全数比較で差分0件が取れているため。必要なら次便で回せる）。
- **所要時間は測っていないので書かない。**

## 証拠

- `検証/全625名8言語の前後比較.txt` / `.json`（5,000セル・差分0件）
- `検証/実画面の前後比較_是正前.json` / `_是正後1.json` / `_是正後2.json` / `差分.txt`
- `検証/配信物の確認.txt`（新しいコードが載っていること／旧いコードが残っていないこと）
- `検証/出力経路の走査.txt`（テーマ525ファイル・当たり1ファイル）
- `検証/見本データでの動作.json`（差し替えの全リクエスト記録）
- `画像/是正後_*_ko.png`（7名・韓国語面＝**是正前と同じ英語のまま**であること）
- `画像/見本データ_*.png`（8言語・**訳が入ればその言語で出る**こと）

---

## 8. ★残留点検＝同じ型が他に残っていないか（恒久ルールの①②③）

**直した理由**＝「そのページの言語のキーを見ずに、必ず `en → romaji → ja` へ落としていた」。
この**理由**を検索語にして、テーマ**529ファイル（うち liquid/json/js 290）**を全文走査した。結果＝`検証/同じ型の残留点検.txt`

**当たり＝13行。読んで3つに分けた（当たり＝欠陥ではない）。**

| 区分 | 件数 | 中身 |
|---|---|---|
| 本便で直した箇所・および本便で書いたコメント | 3 | `atspect-artist-detail.liquid` L681（コメント）／L689（**意図して残した落ち方**） |
| ★**同じ型が残っている＝作品名（作品タイトル）** | **7** | 下表 |
| 判定＝意図した設計（本便では変えない） | 3 | 同上の一部。理由は下記 |

### ★残っている同じ型＝作品名のラテン表記（7か所）

| ファイル | 行 | 書き方 |
|---|---|---|
| `sections/atspect-artist-detail.liquid` | L302 | `wp_pi.title.en \| default: wp_pi.title.romaji \| default: wp.title` |
| `sections/atspect-artist-detail.liquid` | L708 | `w.title.en \|\| w.title.romaji`（代表作画像のキャプション） |
| `sections/atspect-artist-archive.liquid` | L92 | 同型 |
| `sections/atspect-artwork-detail.liquid` | L95 | 同型 |
| `sections/atspect-collection.liquid` | L317 | 同型 |
| `sections/atspect-collector-cta.liquid` | L55 | 同型 |
| `sections/atspect-search.liquid` | L91 | 同型 |
| `snippets/atspect-wishlist-button.liquid` | L12 | 同型 |

**データ側も同じ形だった（実測）**＝商品7件のうち `product_i18n` を持つ3件を読むと、
`title` の鍵は **`ja` / `romaji` / `en` の3つだけ**。**代表作とまったく同じ「器が3言語ぶんしかない」型。**

★**ただし、いま直すべきとは判断していない。** 理由は3つ：
1. **規模が違う**＝代表作は3,488セルだったが、作品名は現時点で**3作品×6言語＝18セル**。
2. **`atspect-artist-detail.liquid` L707 に「B案」という設計コメントが残っている**
   （日本語＝原題／非日本語＝en→romaji の**単一表示**にする、と明記）。**過去の決定で意図的にそうなっている可能性が高い。**
   経緯を確かめずに「同じ型だから直す」とすると、決定を巻き戻すことになる。
3. **作品名の訳を誰が作るかが決まっていない**（作家さま提供の作品名でもあり、翻訳の持ち場の切り分けが要る）。

→ **判断待ちマスターへ起票した。** 本便では**1文字も変えていない。**
