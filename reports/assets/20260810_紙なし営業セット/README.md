# 紙なし営業セット素材一式（2026-08-10・あつぺくと制作T・モデル Opus 5）

代表決定＝**出力紙は使わない**。営業3名はスマートフォン・タブレットで宣伝し、連絡先を交換し、
申込フォームを送る。社内は3名のLINEグループ（**正本は台帳とShopify・LINEは伝達のみ**）。
公式LINEは登録後の作家さまとの原稿受け渡し窓口（**会場では出さない**）。

**★これに伴い、申込書40部の刷り直し・アクセア入稿・AR-P1/P2の封筒分けは取りやめ**（捨てる判断として記録）。

---

## 1. 中身

| ファイル | 何か |
|---|---|
| `01_LINEノート_営業ツール.md` | LINEノートに貼る文面①（3つのURLとQR画像の対応・お見せ方の注意） |
| `02_LINEノート_報告の定型文.md` | LINEノートに貼る文面②（【報告】お名前／会場／状態／一言メモ） |
| `03_LINEノート_運用の決め.md` | LINEノートに貼る文面③（正本・流さないもの・説明の4段・公式LINE） |
| `04_ホーム画面に追加する手順.md` | iPhone／Android両方・文字だけ・画面の写真なし |
| `05_営業の朝の1枚_ノート版.md` | 既存の「営業の朝の1枚」（2026-08-09・A4）を紙なし前提へ改訂 |
| `QR_あつぺくとトップ.png` | 画面提示用QR（1080×1920px） |
| `QR_申込フォーム.png` | 画面提示用QR（1080×1920px） |
| `_検証_QR仕様.json` / `_検証_QR2ルート照合.json` / `_検証_文書チェック.json` | 下記の実測ログ |

**3つのLINEノート文面には、いずれも末尾に `最終更新：2026-08-10` を入れてある**（機械で全件確認済み）。

---

## 2. URLは推測していない（ライブ実測・2026-08-10）

実際にHTTPアクセスして確かめた（HTTPステータス・`<title>`・`<h1>`・`<link rel=canonical>`の自己一致）。

| 呼称 | URL | 実測 |
|---|---|---|
| サイトのトップ | `https://atspect.com/` | **200**／title「あつぺくと｜日本の美術と世界をつなぐ」／canonical自己一致 |
| 登録審査のお申し込み | `https://atspect.com/pages/artist-registration-form` | **200**／title「作家登録フォーム｜あつぺくと」／h1「作家さまの活動を、世界へ。」／canonical自己一致 |

**★このリポジトリはPublicのため、公開ページのURLだけを載せている。**
非公開ハンドル（`noindex,nofollow`）のフォームURLは**1件も含めていない**（機械で残存0件を確認）。
- 上の2本は**いずれも `noindex` 指定なし**、`robots.txt` に `Disallow: /pages` なし、
  `/pages/artist-guide` から公開リンクあり＝**検索に出る公開ページ**であることを実測した。
- 非公開ハンドルのURLの正本＝Vault `_営業用QRとリンク集_20260809_ATSPECT\_確定_営業で使う公開フォームの実URL_20260809_ATSPECT.md`（**ローカルのみ・GitHubへ転記しない**）。

---

## 3. QRの検証（見た目で判定していない・2ルート＋空振り）

| 検査 | 結果 |
|---|---|
| ルートA＝`cv2.QRCodeDetector` で実デコードし期待URLと文字列一致 | **2/2 一致** |
| ルートB＝描画画像からモジュール格子を読み、期待行列と1ビットずつ照合 | **2/2 不一致ビット 0** |
| 空振り①＝トップの画像を「申込フォームのURL」という誤った期待値で検査 | **正しくFAIL**（格子の大きさも33 vs 41で不一致） |
| 空振り②＝1モジュールだけ反転した改ざん版 | **ルートAはPASSのまま（誤り訂正が吸収）・ルートBが1ビット不一致でFAIL** |

**★空振り②は、2ルートを併用する意味がそのまま出た例**（片方だけでは1モジュールの改ざんを見逃す）。

### 大きさ（実測値・数値なしの安全宣言はしない）

| 項目 | トップ | 申込フォーム |
|---|---|---|
| 画像 | 1080×1920px | 1080×1920px |
| QRの描画幅 | **760px**（実測 x=160, y=650.34） | **760px**（同左） |
| 版・モジュール数 | 版2／25＋余白8＝**33** | 版4／33＋余白8＝**41** |
| 1モジュールあたり | **23.03px** | **18.537px** |

**QRの描画幅760pxは、既存の画面提示用QR6枚（2026-08-09）と同一**（既存の実測座標も 760×760・x=160）。
既存の実測では、**この設計で当社スマートフォンに全画面表示すると50.3mm角・タブレットで126.0mm角**。
★**当方は実機での読み取りを試していない**（下記§6）。

### 既存のストレステストから引いた「お見せ方の注意」（発明ではない）
- 反射の白飛びが**3つの角の四角（ファインダー）**のどれかに掛かると、面積の大小によらず読めなくなる（0/6）。
- **遠くから小さく＋手ぶれ**の組合せで読めなくなる（0/6）。
- → 文面①の「お見せ方」3行は、この実測をそのまま平易にしたもの。

---

## 4. 文書の機械検査（全PASS・`_検証_文書チェック.json`）

| 検査 | 結果 |
|---|---|
| 非公開ハンドルの残存（md・json・**PNGバイナリ**も走査） | **0件** |
| 禁止語（合格／不合格／静かな美術館／鑑定／真贋／有名・無名を問わず／誰でも／掲載できる／物故／現存） | **0件** |
| 金額表記（¥・￥・○円・月額○） | **0件** |
| 全mdに `最終更新：2026-08-10` | **5/5** |
| **空振り検査**＝違反を混ぜた文字列に4検査すべてが反応するか | **4/4 反応（＝検査は空振りしていない）** |

★**検査対象は配布物の5ファイル（01〜05）**。**本README自身は禁止語・スタンプの検査から外している**
（検査した禁止語を一覧としてこの文書に書いてあるため、対象に入れると必ず当たる）。
**ただし「非公開ハンドルの残存」だけはREADMEも対象に含めている**（Publicリポジトリのため）。

---

## 5. ★判断待ち・当方で決めなかったこと

1. **作家ページの見本のURL＝代表指定待ち。**
   文面①には**枠だけ**を置き「代表指定待ち」と明記した。**QR画像も作っていない。**
   （ご参考＝既存の営業リンク集2026-08-09版には「作家ページの見本（鈴木 千賀子さま）」
   `https://atspect.com/pages/artist/suzuki-chikako` が入っており、本日 **200** を実測した。
   **これを見本にするかどうかは代表のご判断**であり、当方では決めていない。）

2. **会場でお渡しするフォームをどれにするか＝未決**（繰り越し **B-18**「どれを送っても項目が欠ける」）。
   本セットは**公開ページである「登録審査のお申し込み」**で作った。**決まり次第、文面①とQR画像を差し替える。**
   文面①にもその旨を1行入れてある。

3. **「審査」の禁止語の扱い＝代表判断待ち（繰り越し B-34・期限8/12）。**
   QR画像の見出しは、既に配布済みの `02_画面提示用QR\1_登録審査のお申し込み.png` と**同じ文言**に揃えた。
   B-34の判断が出たら**この画像も作り直しが要る。**

4. **「営業の朝の1枚」③の一言は、紙のパンフレット前提の文**（冒頭が「こちらの紙の流れの前に」）。
   紙を使わない運用でそのまま使うかは**代表のご判断待ち**として、**一字一句そのまま引用したうえで注記**した。
   **当方では確定コピーを書き換えていない。**

5. **「説明の4段」の中身の根拠**＝確定資料にある**第1段階・第2段階・第4段階**という呼称と、
   営業リンク集2026-08-09版の①〜⑥の並びから組んだ。各段の説明文は**同リンク集の文言をそのまま**使っている。
   ★**「第3段階」に当たる呼称は確定資料に無い**ため、3段目は呼称を付けず内容だけ書いた。
   ★**代表の意図する「4段」がこれと違う場合は差し替える。**

6. **文面③に §5「お相手に出さないこと」を足した**（件数・人数を言わない／急かさない／「審査通過・お見送り」だけを使う）。
   **ご指示の3項目の外側**だが、いずれも既存の確定事項の再掲であり新規の約束ではない。**不要なら削除してよい。**

---

## 6. 実施していないこと／判定していないこと

- **実機で1度も試していない。** QRの読み取り（スマートフォンのカメラ・LINEのリーダー）も、
  ホーム画面への追加も、当方は行っていない。検証はすべてデジタル画像上の機械デコードのみ。
- **`04_ホーム画面に追加する手順` のメニュー名は実機で確認していない**（機種・OSの版で違うことがある旨を
  手順書自身にも明記し、うまくいかないときの代替経路と連絡先を添えた）。
- **LINEへの投稿・LINEノートの作成はしていない**（文面を用意しただけ）。3名の端末にも触れていない。
- **Box・デスクトップ・Vaultへの配布はしていない**（本フォルダに置いただけ）。
- **Codexの独立レビューは受けていない**（利用上限。2026-08-09時点の表示＝「try again at Aug 16th, 2026 10:00 AM」）。
  ＝**二刀流は成立していない。** 復旧後に遡ってレビューする対象。
- **所要時間は書いていない**（測っていないため）。
- **本番（テーマ161・作家データ・作品データ・決済・規約）は1文字も変更していない。**
