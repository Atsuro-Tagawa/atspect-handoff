# 足りない欄が「画面の外」に着地する件＝3ファイルを是正（2026-08-03・SNS運用T）

## 直したもの

| ファイル | 是正前（入力枠の上端） | 是正後 | 判定 |
|---|---|---|---|
| `atspect-artist-registration`（第1段階・一般／推薦の共用） | **−48px** | **+377px** | ○ |
| `atspect-artwork-entry`（第3段階B・作品情報） | **−64px** | **+359px** | ○ |
| `atspect-artist-materials`（第3段階A・作家情報） | **−79px** | **+344px** | ○ |
| `atspect-artist-onboarding`（第2段階・当T専任・元から効いている） | ＋364px | 変更なし | ○ |

※値は幅1440px・固定ヘッダーの下端116px・画面高900pxでの実測。
※マイナス＝**画面の上端より外**＝作家さまには「押したのに、どこが足りないのか見えない」状態。

## 3幅×4フォーム＝12通りすべて○

| フォーム | 390px | 768px | 1440px |
|---|---|---|---|
| 第1段階 登録フォーム | ○ ラベル315／枠352〜402 | ○ ラベル343／枠380〜430 | ○ ラベル340／枠377〜427 |
| 作品情報 artwork-entry | ○ ラベル309／枠344〜397 | ○ ラベル322／枠357〜410 | ○ ラベル324／枠359〜412 |
| 第2段階 onboarding | ○ ラベル269／枠306〜355 | ○ ラベル326／枠363〜412 | ○ ラベル327／枠364〜413 |
| 作家情報 artist-materials | ○ ラベル285／枠320〜373 | ○ ラベル314／枠350〜403 | ○ ラベル309／枠344〜397 |

判定条件＝**入力枠の上端も、ラベルの上端も、固定ヘッダーの下端より下**にあり、かつ画面内に収まること
（固定ヘッダーの高さは幅で変わるため、幅ごとに実測して判定している）。
**画像でも目視した**（`shots_landing/`）。

---

## 原因は2種類あり、直し方も分けた

### ① 第1段階の登録フォーム＝**書かれているのに、届かない場所にあった**

中央に寄せる処理 `jumpInto()` は**存在した**が、`submit` イベントの中にあった。
このフォームには **`novalidate` が無い**ため、**ブラウザのネイティブ検証が先に走って `submit` が発火しない**。
＝**`jumpInto()` は一度も呼ばれていなかった。**

→ **`submit` ではなく `invalid`（捕捉フェーズ）から呼ぶように変えた。** 関数そのものは既存のものを再利用している。
　もともとあった `scroll-behavior` の一時退避は、`jumpInto()` 自身が持っているうえ共通側も700msで後始末するため、
　**二重に持たない形にした**（重複を1つ減らした）。

### ② 作品情報／作家情報＝**中央に寄せる処理をそもそも持っていなかった**

どちらも `invalid` を拾ってはいたが、やっていたのは **`scroll-behavior` を `auto` に落とすことだけ**で、
これは2026-08-02に共通側 `assets/atspect.js` へ入った処理と**完全に同じ役割**（＝重複）。
**共通側が覆っていないのは「どこへ寄せるか」のほう。**

→ **足りない分（`scrollIntoView({block:'center'})`）だけを持つ形に置き換えた。**
　`behavior:'instant'` を指定しているため、共通側が `scroll-behavior` を落とすかどうかに依存しない。

★**当ターミナルの `atspect-artist-onboarding` / `atspect-terms-artist` で2026-08-01（第8便）に実証済みの型**を、
そのまま横展開したもの。新しいやり方は作っていない。

---

## ★自分の測定の誤りを2回見つけて訂正した

1. **反映直後に測ると、古いHTMLを掴んで「直っていない」と出る。**
   `artwork-entry` を反映直後に測って **−60px（×）** と出したが、**配信物のHTMLに自分のコードが載っているかを
   先に確かめてから測り直すと +359px（○）**だった。`artist-materials` でも同じことが起きた。
   → **恒久：反映後の判定は、まず「配信物に自分の変更が載っているか」を確かめてから行う。**
   　載っていない状態で挙動を測っても、測っているのは古い版。
2. **`artwork-entry` が効かないと見えたとき、まず自分のコードを疑って切り分けた。**
   `invalid` の発火数（20件）・フォーム側の捕捉が効くこと・IDが実在すること・配信物に載っていることを
   順に確かめ、**スクロールの軌跡を追ったところ、自分のハンドラ直後に欄の上端が423pxへ動いていた**
   ＝コードは最初から効いていた。**壊れていたのは測り方だった。**

---

## 実施していないこと・判定していないこと

- **実送信は1件も行っていない**（hCaptchaに到達しないため）。**確かめたのは、必須欄が空のまま送信を押したときの
  「検証が失敗する経路」だけ。** 送信が成立したあとの挙動は確認していない。
- **`artist-materials` は今回の起票（繰-SNS-0803A／B）に入っていなかった。**
  指示は「上端 −64px／−48px の2件」だったが、**同じ手順で測ったところ3つ目にも同じ欠陥があった**ため、
  同じ型で直した。**指示に無いものを1件直している。**
- **独自の日本語検証文言は入れていない。** 今回直したのは着地位置だけで、
  **3ファイルとも吹き出しの文言はブラウザ標準のまま**（当Tの2ファイルだけが独自文言を持つ状態は変わっていない）。
  ※測定時に吹き出しが英語で出たのは**測定に使ったブラウザの表示言語**の可能性が高く、
  　サイトの不具合として報告していない。
- **モバイル実機での目視・タップは未実施**（実機を持たないため）。
- **320px／430px／1920px では測っていない**（3幅＝390／768／1440のみ。固定ヘッダーの高さが変わる境界を
  またぐ3点を選んだ）。

---

## 追記｜残留点検（ライブ518ファイルの再走査）で2件を追加で発見

全事業共通の恒久ルール「直した理由を検索語にして資料全体を全文検索する」に従って再走査した。

### ①【是正した】解約ページ（`atspect-artist-cancellation`）にも同じ欠陥があった

必須欄9・invalidガード0・中央に寄せる処理なし。**実測して確かめてから直した。**

| 幅 | 是正前（枠上端／ラベル上端） | 是正後（枠上端／ラベル上端） |
|---|---|---|
| 390px | **−46px ／ −81px** | **+382px ／ +347px** ○ |
| 768px | 未測定 | **+395px ／ +360px** ○ |
| 1440px | **−28px ／ −63px** | **+395px ／ +360px** ○ |

**これも指示（繰-SNS-0803A／B の2件）には入っていない。** 同じ欠陥・同じ直し方で、
**登録を止めたい作家さまが「押しても、どこが足りないか見えない」状態**だったため直した。
★**是正の前に、配信物に自分の変更が載っていることを確かめてから測定している**（本日2回踏んだ誤りの再発防止）。

### ②【直していない・申し送り】料金プランの商品ページに販売プラン名が出る条件がある

`sections/atspect-artist-plan.liquid`（`templates/product.artist-plan.json` から描画）は、
**販売プランが2つ以上あるときだけ**、選択欄に `{{ alloc.selling_plan.name }}` をそのまま表示する。

```liquid
{%- if ap_allocs.size == 1 -%}   <input type="hidden" name="selling_plan" ...>   ← 名前は出ない
{%- elsif ap_allocs.size > 1 -%} <option>{{ alloc.selling_plan.name }} — ...</option>  ← 名前が出る
```

**いまは販売プランが1つのため、画面に「1か月ごとに配達」は出ていない**（＝当面の対応は不要）。
→ ★**年額プラン等をもう1つ増やした瞬間に、商品ページの選択欄に販売プラン名がそのまま出る。**
　決済画面だけの話ではなくなるため、**プランを増やすときは必ずここを見ること。**
　（プラン名の変更はアプリ側＝決済の本番設定にあたるため、当ターミナルでは触っていない。）

### 残留点検の結果まとめ

- **理由A（足りない欄が画面の外に着地）** で、必須欄を持つ作家さま向けセクションを全数点検した。

| セクション | 必須欄 | 中央に寄せる処理 | 状態 |
|---|---|---|---|
| `atspect-artist-registration` | 12 | あり | **本日是正** ○ |
| `atspect-artist-onboarding`（当T専任） | 16 | あり | 元から○ |
| `atspect-terms-artist`（当T専任） | 17 | あり | 元から○ |
| `atspect-artwork-entry` | 20 | あり | **本日是正** ○ |
| `atspect-artist-materials` | 16 | あり | **本日是正** ○ |
| `atspect-artist-cancellation` | 9 | あり | **本日是正** ○ |
| `atspect-publish-consent`（第4段階） | 5 | **なし** | **未判定**（受付オフで送信ボタンが描画されず、測定できない） |
| `atspect-artwork-detail` | 4 | あり | 作家さま向けではない（作品ページ）＝対象外 |

→ **作家さまが通るフォームで、着地位置の問題が残っているのは `atspect-publish-consent` だけ。**
　ただし**受付オフのため測定できず、直すべきかどうかも判定していない**（システム開発Tの担当）。

- **理由B（決済画面に「配達」と出る）** で `selling_plan` を持つ全ファイルを点検。
  作家さまが押す決済導線は2つだけ＝**publish-consent（月額・本日一行を追加）**と
  **terms-artist / entry（情報登録料・一括・「配達」は出ないことを実測済み）**。
  他は Shopify 標準のカート表示（`cart-drawer` 等）で、作家さま向けの導線ではない。
