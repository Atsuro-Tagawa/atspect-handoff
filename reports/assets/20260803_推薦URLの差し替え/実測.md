# 推薦用フォームのURLを差し替えた（2026-08-03・SNS運用T）

## ★このファイルに新しいURLは書きません

**新しいスラッグはVaultにのみ記録しています**（`_確定_推薦フォームのURLと台帳_20260801_ATSPECT.md` の末尾）。
このリポジトリは公開のため、**ここに書けば同じことの繰り返しになる**からです。
代表・司令塔へは、Vaultの上記ファイルからお渡しください。

## 何をしたか

| 項目 | 内容 |
|---|---|
| 対象 | 推薦用フォームのページ（題名＝`登録審査申し込み（ご推薦者あり）`） |
| 変更したもの | **ページのスラッグ（handle）だけ** |
| 変更していないもの | 題名／テンプレート（`artist-registration-referral`）／公開状態／セクションの中身／`?r=` の仕組み |
| 旧URL | **HTTP 404**（下記のとおり実測） |
| リダイレクト | **0件**（自動生成されていないことを確認。もし作られていたら消す処理も入れてある） |

## ★適用の直前にライブを取り直してから当てた

2026-08-03の代表指示「本番データを書き換える直前に、必ずライブを取り直す」に従い、
**変更の直前にライブのページを取得し、次の4つが計画時と一致することを確かめてから**当てました。
1つでも違えば当てずに中止する作りにしています。

- handle が旧スラッグのままであること
- 題名が `登録審査申し込み（ご推薦者あり）` であること
- テンプレートが `artist-registration-referral` であること
- 公開状態であること

## 実測（画像は同フォルダ）

### ① 旧URL＝404（`01_旧URL_404.png`）

- **HTTP 404**／題名＝`404 Not Found｜あつぺくと`
- **着いたパスは旧パスのまま**＝**リダイレクトされていない**（＝両方が生きる状態になっていない）
- 画面には「お探しのページが見つかりませんでした。URLが変更されたか、ページが削除された可能性があります。」
  と出て、トップ・作家・作品への導線が出る。**画像で目視済み。**
- ※このページ上にも `action="/contact"` のフォームが1つ数えられますが、
  **フッター共通のメール登録フォーム**であり、申込フォームではありません。

### ② 新URL＝`?r=` 付きで動く（`02_新URL_見出し.png` / `03_新URL_フォーム見出し.png`）

| 実測項目 | 結果 | 差し替え前（2026-08-03 第1便の実測）と同じか |
|---|---|---|
| HTTP | **200** | ― |
| 題名 | `登録審査申し込み（ご推薦者あり）｜あつぺくと` | 同じ |
| H1 | 作家さまの活動を、世界へ。 | 同じ |
| H2 | **登録審査申し込み（ご推薦者あり）** | 同じ |
| 入力要素 | **31**／必須 **12** | **同じ** |
| ページ高さ | **3,823px** | **同じ（1pxも違わない）** |
| `contact[90_ご推薦者コード]` | **テストコードがそのまま入った** | 同じ |
| robots メタ | `noindex,nofollow` | 同じ |

**＝スラッグ以外は何も変わっていない**ことを、数値で確認しました。**画像でも目視済み。**

### ③ サイトマップに載っていないこと（今回あらためて実測）

`sitemap.xml` → `sitemap_pages_1.xml` を実際に取得。**載っているのは公開ページ11件のみ**で、
**推薦用フォームを含む秘匿ページ6種は1件も載っていません**（`reg-r` / `artist-onboarding` /
`entry-…` / `publish-…` / `artist-materials` / `artwork-entry` すべて0件）。

★なお、サイトマップの索引に出る `to=135968653570` という数字は
**ストア内のページIDの最大値**であって、そのページが載っていることを意味しません
（実際に取得して中身を数え、載っていないことを確かめました）。

## 他ターミナルの記述は消していません

旧URLは、このリポジトリの5ファイル（`20260801_errorpaths` / `20260801_journey` /
`20260803_form-count` 2件 / `deferred-master.md`）に残っています。**消していません。**
理由＝**消してもgitの履歴に残るため意味がなく**、他ターミナルの記録を書き換えることにもなるためです。
**旧URLは404になったので、残っていても実害はありません。**

## テーマ側の参照はありませんでした

ライブのテーマ161（518ファイル）を全文検索したところ、`reg-r` を含むファイルが1件出ましたが、
**`data-areg-referral` という属性名の部分一致**（a-**reg-r**eferral）で、**ページのスラッグへの参照ではありません**。
→ **スラッグを変えても、テーマ側で直す箇所はありませんでした。**

## 実施していないこと・判定していないこと

- **実送信はしていません**（hCaptchaに到達しないため）。確かめたのは
  **`?r=` の値が隠し欄に入って画面に保持されるところまで**です。
- **新しいURLは、まだ誰にもお渡ししていません。**
- **旧URLが外部（検索エンジンのキャッシュ・他者のブックマーク等）にどれだけ残っているかは調べていません。**
  当方から消せるものではないため。**404になっている以上、開いても中身は見られません。**
- **`atspect-handoff` の既存5ファイルの旧URL記述は、削除も書き換えもしていません。**
- **今回変えたのは推薦用の1ページだけ**です。他の秘匿URL（第2段階・対面用・第4段階）は**触っていません**。
  ★ただし下記の申し送りがあります。

## ★申し送り（当方では判断していない）

**`/apply` という短いパスから、対面用エントリー（`/pages/entry-…`）への恒久リダイレクトが設定されています。**
URLリダイレクトの一覧を実測して見つけました。
**これは秘匿URLに対する公開の入口**にあたります。意図的な設定である可能性が高い（パンフレット等に
短いURLを載せるため）と読めますが、**当方では経緯を確認できていない**ため、変更していません。
**推薦用URLを秘匿する判断と同じ物差しで見るなら、一度どなたかに確認いただきたい1件です。**
