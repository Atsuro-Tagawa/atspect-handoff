# OGP是正の総仕上げ（表示側） — 2026-08-08 SNS運用T

実行モデル＝**Opus 5 (1M context)**。Codexによる独立レビュー1本（Liquidの正しさ）を実施し、指摘1件を反映。
触ったファイル＝**`snippets/meta-tags.liquid` の1つだけ**。作家データ・訳文には触っていません。
このフォルダには金額・個人名・件数の内部基準・メールアドレス・非公開URL・トークンを書きません。

---

## 洗い出し（全件・是正前のライブ実測）＝`実測_OGP_是正前.txt`

主要19ページ（固定ページ15＋トップ・作品一覧＋作家ページ4）を実測しました。

| 見つかったこと | 件数 |
|---|---|
| **説明文が全ページ同じだった** | **15件**（作家ページ以外のすべて） |
| 画像が会社ロゴの既定のままだった | 18件（うち作家ページ3＝下記②） |
| 説明文が個別だった | 4件（作家ページのみ＝システム開発Tの是正が効いている） |

**15ページすべてが同じ1文**でした（`shop.description`）。SNSにどのページを貼っても同じカードになり、
**何のページか画像からも文からも分かりません。**

★**当方の測り方の誤りを1件、自分で見つけて直しました。** 最初、定型文の判定に
liquid の最終フォールバック文字列を使っていたため「定型文0件」と出ました。
実際に効いていたのは `shop.description` で、**同じ説明文で束ね直して15件と分かりました。**

### ★あわせて見つけた不具合（今回いちばん危ないもの）

既定のOGP画像URLが **`https://atspect.com/cdn/shop/t/3/assets/ogp-atspect.png`** と
**テーマ番号を直書き**していました。**実測＝ライブのテーマ番号は `t/4`。**
`t/3` のURLは今も生きていますが、Shopifyが古い番号の配信をやめた時点で
**全ページのOGP画像が一斉に壊れます。**

---

## 是正した内容（4点・すべて `snippets/meta-tags.liquid`）

### ① 固定ページの説明文を、ページの内容に合ったものにした（15ページ）

前後の全件＝**`前後の対照表.md`**。すべて**別々の文**になっています（同じ文の重複は0件）。

- ページのハンドルごとに説明文を持つ形にしました。
- ★**ページ側にSEOの説明文が設定されているときは、そちらを優先**します（この表は設定が無いときだけ効く）。
- ★**非公開の導線ページ（個別にURLをお送りしているもの）は入れていません**（SNSに貼る前提が無いため）。

### ② 既定のOGP画像URLを `asset_url` で組み立てるようにした

`t/3` の直書きをやめました。**実測＝是正後は `t/4` を指し、HTTP 200で取得できます。**

### ③ 近影をお持ちでない作家さまの既定画像を、サイトの顔の画像にした

| | 是正前 | 是正後 |
|---|---|---|
| 近影あり | ご本人の近影（変更なし） | ご本人の近影（変更なし） |
| **近影なし** | **会社ロゴ**（`82_…png`） | **`hero-enso.jpg`**（`83_…jpg`） |

**対象＝624名**（実測＝作家625名のうち、近影または主画像をお持ちなのは1名のみ）。

★**なぜこの画像を選んだか**＝`hero-enso.jpg` は**トップページのヒーローに実際に使われている画像**です
（`sections/atspect-hero.liquid` と `templates/index.json` が参照。機械で確認）。
**サイトの顔として来訪者が目にしている画像なので、その作家さまの作品と取り違えられにくい**と判断しました。

★**採らなかった候補と理由**
- `hero-brush-bright.jpg`（絵筆の写真・1920x1080）＝**テーマのどこからも使われていない**素材で、
  西洋画材の写真のため日本の美術の作家さまのカードとしては合わないと判断。
- `hero-thread.png` ＝**ほぼ透明の重ね用レイヤー**でカードには使えない。
- `hero-brush.jpg` ＝正方形（1:1）でカードの比に合わない。
- `logo-horizontal-washi.png` ＝横に細長い（3.52:1）。
- `hero8a.png` ＝テーマのどこからも使われていない。

★**残る懸念（判断をお願いします）**：`hero-enso.jpg` は墨で描いた円相を含みます。
**書や日本画の作家さまのカードでは、ご本人の作品と読まれる可能性がゼロではありません。**
当方は「サイトのトップで使っている画像だから作品ではないと分かる」と判断しましたが、
**気になる場合は、作品ではないと一目で分かる専用の1200x630の画像を作るのが確実**です（繰り越しへ）。

### ④ `og:image:width` / `og:image:height` を実際の画像に合わせた

決め打ち（1200x630）だったため、③で別寸法の画像を使うと食い違います。変数にしました。

**実測＝4経路すべてで、宣言した寸法と実物の寸法が一致します。**

| ページ | og:image | 宣言 | 実物 | |
|---|---|---|---|---|
| トップ | ogp-atspect.png | 1200x630 | 1200x630 | ○ |
| 寄贈 | ogp-atspect.png | 1200x630 | 1200x630 | ○ |
| 作家（近影あり） | 近影 | 1200x630 | 1200x630 | ○ |
| 作家（近影なし） | hero-enso.jpg | 2200x1229 | 2200x1229 | ○ |

---

## ③寄贈ページのOGP＝**正しく出ています**

| | |
|---|---|
| og:title | **作品の寄贈について** |
| og:description | **作品の寄贈について、『あつぺくと』でできることをまとめました。作家さまと、美術館・文化施設などのご担当者さまへ。** |
| og:image | ogp-atspect.png（HTTP 200・1200x630） |
| og:type / twitter:card | website / summary_large_image |

説明文はページ本文の導入と同じ言い方に揃えました。

---

## ④カードのスクリーンショット＝`81_SNSカードの見え方_是正後.png`

19ページぶんのカードを1枚にまとめました。
★これは**実測した og:title / og:description / og:image から当方が組んだ再現**です。
**SNS各社の実際の描画（文字の切れ方・余白）とは細部が異なります。**
画像は実物を読み込んでいます（**読み込めなかった画像＝0件**）。

---

## 走査（実測）＝`走査_説明文の禁止語.txt`

```
★対外禁止語 29語すべて0件（19ページ・説明文 計830字で実測）
★確定タグライン（トップ）: 一字一句あり
★確定タグライン（About）: 一字一句あり
★『』無しの「あつぺくと」: 0件
★説明文の長さ 最短22字 / 最長75字
```

掲載作家数・作品数などの数字、「審査」「通過」「正式」「本登録」「物故」「現存」は**1件も使っていません**。

---

## Codexによる独立レビュー（Liquidの正しさ・反映）

**指摘1件・反映しました。**

> 作家さまの一行（statement・ジャンル・拠点）が**すべて空**の場合、作家用のブロックが説明文を
> 上書きしないため、**固定ページ用の文が残る恐れ**がある。

→ **作家ページはこの表を一切通さない**ガード（`ogp_is_artist`）を先に置き、経路そのものを無くしました。
★**実測＝該当する作家さまは 625名中 0名**（今は起きていない）ですが、構造で塞いであります。

そのほか（`case`/`when`/`endcase` の構文、`if`/`endif` の対応、`page.handle` の読み取り、
`asset_url` のフィルタ順序、全ページのOGP画像が空になる経路）は**指摘なし**でした。

★Codexが付記した「`page_image` がある経路で寸法が1200x630のまま実寸と合わない可能性」は、
**今回の変更で入った問題ではなく元からのもの**で、**当方は触っていません**（下記）。

---

## ★是正しなかったものと、その理由

1. **`page_image` のある経路（商品・記事など）の og:image と寸法**
   元のコードが `'https:' | append: page_image | image_url: width: 1200` という書き方で、
   **今回の測定範囲（19ページ）ではこの経路を通るページが1つも無く、動作を確かめられませんでした。**
   確かめられないものを直すと壊す恐れがあるため**触っていません**。
2. **ページの題名（og:title）**
   実測では**すべてページごとに個別**で、内容と合っていました。
   ただし `/pages/artists` の題名が「**作家**」の一語で、カードとしては用が伝わりにくいと感じます。
   **題名はページのデータ側**（Shopifyのページ設定）であり、本便の触る範囲（liquid・OGP関連）の外なので
   **変更していません**。表示側だけ別名にすることもできますが、画面の見出しと食い違うため見送りました。
3. **非公開の導線ページの説明文**（個別にURLをお送りしているもの）
   SNSに貼る前提が無いため、表に入れていません。
4. **作家ページの説明文・近影そのもの**
   システム開発Tの是正が効いており、**当方は触っていません**。
5. **`twitter:site`**（`@…`）が残っています。過去に「削除で確定」との記録がありますが、
   **本便の指示の範囲外**のため触っていません（別途ご確認ください）。

---

## ★実施していないこと・判定していないこと

- **作家データ・訳文には1バイトも触っていません**（翻訳品質T・リサーチTの担当）。
- **SNSの実物では確認していません。** カードの画像は当方が実測値から組んだ再現で、
  **X・Facebook・LINE等に実際に貼って見てはいません**（外部への投稿は承認事項のため）。
  各社のキャッシュ（一度読まれた古いカードが残る）も確認していません。
- **8言語での確認はしていません。** OGPの説明文は**日本語のみ**です
  （og:locale は ja_JP、alternate に7言語を並べていますが、説明文の翻訳は持っていません）。
- **`hero-enso.jpg` の中央切り抜き**＝カードの比（1.91:1）に対し画像は 1.79:1 のため、
  上下がわずかに切られます。再現画像では円相が収まっていますが、**各社の実際の切り抜きは確認していません**。
- **商品ページ・記事ページのOGPは測っていません**（19ページの範囲外）。
- **実機での確認はしていません。**

<!-- created: 2026-08-08 by model=Opus 5 (1M context) terminal=SNS運用 -->
