# 紙紋UI セキュリティ点検サマリ（2026-08-07）

**ターミナル：紙紋UI ／ 使用モデル：Opus 5（claude-opus-5[1m]） ／ 作業：P0是正状況の実測確定と、残存項目の全件棚卸し**

★このファイルには**是正が完了した項目の結果だけ**を書く。
**未是正の項目の内容・攻撃の手口・該当コードは、Public リポジトリには書かない**（紙紋の恒久ルール）。
未是正項目の全件一覧は Vault（ローカル）に保存済み。

---

## 1. パストラバーサル（最優先の項目）＝是正済みであることを実測で確定した

過去の「封鎖済み」という報告を**信用せず、新しく攻撃試験を書いて自分で撃った**。
本番の保存領域には触れず、隔離した一時フォルダで実施（試験前後とも本番は0件のまま）。

| 実測項目 | 結果 |
|---|---|
| 撃った攻撃 | **22 件** |
| 通ってしまったもの | **0 件** |
| 保存領域の外に置いた「被害役」ファイル2点 | **2点ともSHA-256が変化なし** |
| 保存領域の外に作られた余計なファイル | **0 件** |
| 正常系（普通の保存・読み出し） | **8件中 失敗 0 件** |

### この試験が本当に穴を見つけられることの裏取り（陽性対照）

「全部拒否された」という結果は、試験が壊れていても同じに見える。
そこで**同じ試験を修正前のコードにも当てた**：

- **修正前のコードでは 22件中 5件が通った。**
- うち1件は、**保存領域の外にあるファイルを実際に上書きした**（SHA-256が変化したことで確認）。
- ＝**試験は穴を検出できる。その試験で現行コードは0件。**

---

## 2. 証明書の発行ゲート＝穴が実在した。本日 是正した

前回の棚卸しで「未着手」として残っていた項目。**実際に撃って穴を再現し、塞いだ。**

内容は「登録データが変わっていないことを確かめられない状態でも、証明書が発行できてしまった」というもの。

| 試験 | 期待 | 是正**前** | 是正**後** |
|---|---|---|---|
| 正常な登録から発行 | 発行 | 発行 | **発行** |
| 改ざんを検知した登録から発行 | 停止 | 停止 | **停止** |
| 確認手段が無い登録から発行（照合証明書） | 停止 | **★発行された** | **停止** |
| 確認手段の情報が欠けた登録から発行（照合証明書） | 停止 | **★発行された** | **停止** |
| 確認手段が無い登録から発行（登録証明書） | 停止 | **★発行された** | **停止** |
| 正常な登録から発行（登録証明書） | 発行 | 発行 | **発行** |
| **期待と食い違った件数** | | **3 件** | **0 件** |

是正前は証明書のHTMLが実際にディスクへ書き出されていた（＝紙が出せる状態だった）。
是正後は**発行が止まり、HTMLも書かれない**。正常な発行は従来どおり動く。

---

## 3. 回帰試験（是正が他を壊していないこと）

| 対象 | 結果 |
|---|---|
| UI回帰 11本 | **ALL PASS（11本すべて正常終了）** |
| エンジン redteam_fixes_test | **PASS 13 / FAIL 0**（2026-07-13監査の基準値と一致） |
| エンジン a3_c123_test | **PASS 17 / FAIL 0**（同上） |
| パストラバーサル試験（修正後に再実行） | **22件中0件通過・正常系8/8** |

---

## 4. 認証情報の点検

| 確認項目 | 結果 |
|---|---|
| ソース・設定・文書に平文の認証情報があるか | **無い** |
| `.env`・鍵ファイルの類 | **1件も存在しない** |
| 紙紋の作業フォルダはGitリポジトリか | **リポジトリではない**（GitHubへ上がる経路が無い） |

---

## 5. 提出書類（フリーズ済み）

**本日更新されたファイルは0件**（更新日時で実測）。フリーズ記録のSHA-256と照合し、
正の書類はすべて記録どおり。**一切触れていない。**

---

## 6. 本日 判定していないこと（正直に）

- **アプリの画面を起動しての通し確認はしていない**（持ち越し）。今回の変更は画面に出る文言を
  含むが、**実際の見え方は確認していない**。
- エンジンの残り3スイート（dup_semantics・collision・identify）は**走らせていない**
  （エンジンを変更していないため。走らせたのは安全に直結する2本）。
- 実紙・実機での精度確認は**していない**（従来からの持ち越し）。
- 未是正として残っている項目がある。**件数と内容はVaultに記載**（本ファイルには書かない）。

<!-- created: 2026-08-07 by model=Opus 5(claude-opus-5[1m]) terminal=紙紋UI -->
