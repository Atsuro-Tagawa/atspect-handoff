# 作家さま向け6ページ モバイル幅の通し検査（2026-08-01・SNS運用T）

対象＝terms-artist ／ 対面用エントリー ／ artist-registration-form ／ artist-guide ／ artist-cancellation ／ artist-onboarding
幅＝**320 / 360 / 390 / 430px** の4つ。**DOM実測とスクリーンショットの両方**で判断し、片方だけでは通していない。

---

## 0. まず「不具合ではないもの」を除いた（測定結果は判断ではない）

機械検出には出るが、経緯・意図を確認して**不具合ではないと判定した**もの：

| 検出 | 実体 | 判定 |
|---|---|---|
| `div.*__hp` が `right=-9998` で画面外 | `aria-hidden="true"` ／ `tabindex="-1"` ／ `autocomplete="off"` ／ `name="contact[hp_website]"` ／ ラベル「このフィールドは空のままにしてください」 | **ハニーポット（スパム対策）＝意図的。不具合ではない** |
| `a.skip-to-content-link` が 7px・他要素と重なる | キーボード操作時のみ表示されるスキップリンク | **アクセシビリティ機能＝意図的** |
| `a.atspect-globe__option`（各言語）が「重なり」 | 閉じた状態の言語ドロップダウン内の項目 | **閉じているため＝誤検出** |
| `input.atspect-aterms__checkbox` / `button.atspect-aterms__submit` が `disabled` | 規約を最後まで読むまで押せない**読了ゲート** | **意図的な仕様。不具合ではない** |
| 浮いている「TOP」ボタン（52x52・`position:fixed`・`z-index:100`）が本文に被る | どの幅でも被るのは**1要素・横52px分**のみ | **浮遊ボタンの通常動作。不具合とは判定していない**（参考として記録） |
| ラジオ・チェックの `input` が 13〜16px | いずれも `<label>` に包まれており、実タップ範囲はラベル全体 | **入力欄の実寸だけでは判断できない＝ラベル込みで再測定した** |

**ページ全体の横スクロールは、6ページ×4幅の24通りすべてで「なし」。**

---

## 1. ★是正した（当ターミナルの専任ファイル）

### artist-onboarding（`sections/atspect-artist-onboarding.liquid`）＝文字が小さすぎ・タップ範囲が狭すぎ

**根拠＝サイト内の同種フォームとの比較。** 同じ作家さまが記入する terms-artist と並べて実測したところ、
ルートのfont-size は両方とも10pxで同じなのに、**onboarding だけが一貫して小さかった**。

| 項目 | terms-artist | onboarding（是正前） | 比 | 是正後 |
|---|---|---|---|---|
| ラベル | 14.0px | **10.0px** | 71% | **14.0px** |
| 必須バッジ | 10.0px | **8.8px** | 88% | **10.0px** |
| ヒント文 | 16.0px | **9.2px** | **58%** | **13.0px** |
| 同意文 | 13.0px | **9.8px** | 75% | **13.0px** |
| 注記 | 11.5px | **9.0px** | 78% | **11.5px** |
| ラジオ | 13.5px | **10.0px** | 74% | **13.5px** |
| 冒頭の説明文・フォーム前書き（契約規約書へのリンクを含む） | — | **10.5px** | — | **13.0px** |

**タップ範囲（ラジオ・チェックの実タップ高）**

| ページ | 是正前 | 是正後 |
|---|---|---|
| terms-artist | 47〜75px | （変更なし・元から良好） |
| artist-cancellation | 47〜76px | （変更なし・元から良好） |
| artist-registration-form | 44〜49px | （変更なし・元から良好） |
| **artist-onboarding** | **18〜37px** | **51〜111px（44px未満＝0件）** |

**onboarding だけが site 内で唯一 44px を下回っていた。** 独自の基準を作らず、**terms-artist のラジオと同じ方式（枠線＋`padding:13px 16px`）に揃えた。**

あわせて `.atspect-aonb__req` / `__opt` に `white-space: nowrap` を追加（幅360pxで「必須」が「必」「須」に泣き別れていたため。terms-artist で本日先に直したのと同じ型）。

**是正後の実測（320/360/390/430px の4幅すべて）**
- 44px未満のタップ範囲＝**0件**
- 「必須」バッジの泣き別れ＝**0件**
- ページ横スクロール＝**なし**（`document.scrollWidth` 386 ≤ 390）
- 12px未満で残るのは `__req`（10px）と `__consent-note` 内のリンク（11.5px）のみ＝**いずれも terms-artist と同値**

変更は**CSSの数値のみ**。文言・HTML構造・送信項目には一切触れていない。
バックアップ＝`_backups\_auto\theme161-atspect-artist-onboarding.liquid\20260801-151259.bak`

画像＝`onb_390_before.png` `onb_390_before2.png`（是正前）／`onb_390_after.png` `onb_390_after2.png`（是正後）

---

## 2. 判定していないこと

- **スマートフォン実機での目視は未実施**（実機目視は代表対応事項）。上記はすべて実ブラウザのモバイル幅エミュレーションでの確認。
- **文字サイズの「読みやすさ」そのものは主観であり、当方は「サイト内の同種フォームと揃っているか」だけを判定基準にした。** 絶対的な最適値は判定していない。
- **浮いている「TOP」ボタンの被りは、不具合とは判定していない**（参考記録）。

---

## 3. 実際に操作した（表示だけで通していない・幅390px）

**5つのフォームすべてを、入力→選択→チェック→送信の直前まで進めた。★送信は1件も押していない。**
念のため **POSTリクエストを遮断した状態**で操作したため、仮にJSが働かなくてもサーバーへは送られない。

| ページ | 結果 |
|---|---|
| **terms-artist** | **読了ゲートが指の操作で解除できることを確認**。解除前＝チェック4つ中3つが押せない・送信も押せない。規約領域（表示488px／全長6371px）を実際のホイール操作で最後まで送ると、**チェック0個が無効・送信が押せる状態**に変わり、案内文も「規約全文をお読みいただきました。チェックのうえ、送信にお進みください。」へ変化。氏名〜電話の8欄入力・ラジオ3組・同意チェック4つすべて操作でき、送信ボタン338x54・必須の未入力0件・HTML5検証も通る状態まで到達。 |
| **entry（対面用）** | terms-artist と完全に同じ結果（同一セクションを共用しているため）。 |
| **artist-onboarding** | 入力・ラジオ4組・チェック5つすべて操作でき、送信ボタン221x58・必須の未入力0件・検証も通る。**是正後のラジオはラベル全体（51〜111px高）で押せることを実クリックで確認。** |
| **artist-registration-form** | 文字12欄・生年月日のセレクト3つ・ラジオ・チェック7つすべて操作でき、送信346x54・必須の未入力0件・検証も通る。 |
| **artist-cancellation** | 7欄・ラジオ2組・チェック1つすべて操作でき、送信338x54・必須の未入力0件・検証も通る。 |
| artist-guide | フォームなし（ご案内ページ）。 |

### ★入力の誤りを知らせる仕組みも実際に試した（onboarding）
メールアドレスと確認欄を**わざと違う値**にして送信を押したところ：
- **送信は止まり**、「メールアドレスが一致していません。ご確認ください。」が**13pxの赤字で表示**され、確認欄にフォーカスが移った。
- ★ただし**エラーの表示位置まで画面が動くのに約2.5秒かかる**（押した直後1秒の時点ではエラーは画面外2094px上にあり、2.5秒後に画面内へ）。**最終的には見えるので不具合とは判定していない**が、押した直後は「何も起きていない」ように見える瞬間がある。
- 画像＝`onb_mismatch_error.png`

**※この検証で一度、blur（欄から外れた時点）でエラーが出ないことを「不具合か」と考えたが、ソースを読むと一致チェックは `submit` イベントに実装されており、設計どおりだった。**（terms-artist 側は別方式で、欄から外れた時点でも出る）

画像＝`terms-artist_operated.png` `entry_operated.png` `reg_operated_final.png` `cancel_operated.png`

---

## 4. 専任外＝制作Tへ起票した4件（当Tでは修正していない）

1. **サイト上部ナビの「作家の方へ」がスマホ全機種で切れて押せない**（`overflow-x:hidden` かつ `scrollWidth 432 > clientWidth 346`・幅430pxでも切れる・約476px必要／幅768px以上では切れない）。
   **★前便の記述を訂正します**：フッターには「作家の方へ」→`/pages/artist-guide` が全ページ・全幅で存在します（90x29px）。したがって正確には**「ヘッダーからは到達できない／フッターからは到達できる」**であり、前便で書いた「入口に辿り着けない」は言い過ぎでした。深刻度は前便の記述より低くなります。
2. **registration-form の「← ご案内のページに戻る」が 9.5px・タップ範囲 111x14px**・色 rgb(111,106,99) の淡い灰（`backlink390_view.png`）。
3. **registration-form の「任意・複数選択可」が泣き別れ**（360px＝「任意」+「・複数選択可」／390px＝「任意・複数」+「選択可」／430px＝「任意・複数選択」+「可」）。**430pxでは「お問い合わせ」も2行に割れる。**
4. **cancellation の「プライバシーポリシー」が幅320pxで2行に割れる**（37px+86px・`cancellink320_view.png`）。

参考（判定は保留）：幅320pxでナビのリンク9.4px、「TOP」ラベル8.5px、ヘッダーのお気に入りアイコン38x38px。

---

## 5. ★実施していないこと・判定していないこと

- **スマートフォン実機での目視・実機でのタップ操作は未実施**（実機目視は代表対応事項）。すべて実ブラウザのモバイル幅エミュレーションでの確認。
- **送信は1件も行っていない。** したがって**送信後の画面・受信内容・自動返信メールは一切確認していない**。
- **hCaptcha を通過していない**ため、実際に送信できるかどうかは判定していない。
- **専任外の3ファイル（`atspect-artist-registration.liquid`／`atspect-artist-guide.liquid`／`atspect-artist-cancellation.liquid`）は一切変更していない。**
- **日本語の文中でリンク文字列が改行されること自体は、不具合と判定していない**（日本語の通常の折り返し）。上記に挙げたのは、2〜8文字の短い単位が割れているものだけ。
- **色のコントラスト比は測定していない**（「淡い灰」は色値の記載であって、WCAG等の基準での判定ではない）。
- 決済・通知メール・API・作家データ・紙には**一切触れていない**。
