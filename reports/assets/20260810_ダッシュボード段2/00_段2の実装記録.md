# ダッシュボード再設計 第5便・段2＝実装（2026-08-10）

あつぺくとシステム開発ターミナル／model=Opus 5

**設計図6枚のとおりに実装しました。** 実データは接続していません（見本のみ）。
このフォルダの画像は、すべて**見本データで撮ったもの**です（実在するお名前・件名は写っていません）。

---

## 1. 決定2＝カレンダーの方式は **(a) 自前保存** にしました（判定の根拠）

指示どおり、まず **(b) 埋め込み** を判定基準の3点で確かめました。

| 基準 | 結果 | 根拠 |
|---|---|---|
| ① ログインしていない機械で見えるか | **不合格** | まっさらなブラウザ（Googleにログインしていない状態）でカレンダーの埋め込みを開くと、**予定ではなく「Sign in to continue to Google Calendar」というログイン画面が出た**（実測。画像＝`根拠_埋め込みカレンダーはログイン画面が出る.png`） |
| ② 390pxで指で操作できるか | **判定に進まず** | ①で不合格のため |
| ③ 施錠の内側で拒否されないか | 枠そのものは出た（HTTP 200） | ただし**中身がログイン画面**なので、意味がない |

**あわせて確かめた事実**：当ダッシュボードの設定には**カレンダーの行き先が1つも入っていません**
（`config.js` も `config.local.js` も空）。**つまり(b)は「設定1行」で試せる状態ですらありませんでした。**

★①を通すには**カレンダーを誰でも見られる状態（公開）にする**必要があります。
　営業日程には**訪問先と会場名**が入るため、公開は選べません。**よって(b)は採れません。**

**→ 指示の「1つでも駄目なら即(a)」に従い、(a)自前保存で実装しました。**

### (a)で作ったもの
- 予定は **足すだけ**。消しません。やめるときは「**取りやめ**」を1件足す形（打ち消し線で残ります）。
- 誰の予定かは **名前の文字**で出します（**色分けしません**＝憲法2）。
- 書き込み口は**メールの印と同じ型**を1本増やしただけ（一覧を読む／1件足す）。
  置き場所が未設定のあいだは各パソコンの中に残り、**画面にその旨を必ず出します**（黙って隠しません）。

---

## 2. 決定3＝カレンダーの初期データ（確定している営業日程だけ）

| 予定 | 会期 |
|---|---|
| 読売書法展（国立新美術館） | 2026-08-21 〜 08-30 |
| 読売書法展（東京都美術館） | 2026-08-23 〜 08-29 |
| 工芸集中（東京都美術館） | 2026-09-28 〜 10-04 |
| 工芸集中（国立新美術館） | 2026-11-25 〜 12-07 |

- **これ以外の予定は入れていません。**
- ★**「都美」＝東京都美術館、「新美」＝国立新美術館 と読みました。** 違っていたら
  `dashboard/data/calendar.json` の「内容」の文字を直すだけで直ります（作り直し不要）。
- 会期は**月の図では毎日**に出し、図の下の「近い予定」では**1行にまとめて「8/21〜8/30」**と出します
  （同じ名前が何日も並んで読みにくかったため、実際に描画して直しました）。

---

## 3. 決定5＝メールに「対応不要」を足しました

- 押せるのは **私が対応します ／ 済にする ／ 対応不要 ／ 未対応に戻す** の4つ。
- **どれも足すだけ**。押した人と時刻が残ります。取り消しは「未対応に戻す」を**1件足す**形。
- **済のタブの中で「対応した」と「対応不要」が見分けられます**
  （画像＝`操作_済タブに対応不要.png`。緑で「◯◯が◯月◯日◯時に**対応不要**にしました」と出ます）。
- **迷惑・その他に分けたものは、未対応に載せません**（従来どおり）。機械でも確認済み。

---

## 4. 決定4＝メールの取り込み（候補②）

- **仕組みは完成。まだ動かしていません**（定時実行を登録していません＝合図待ち）。
- 画面のいちばん上に「**いまの一覧は◯月◯日◯時◯分に取り込んだものです**」を**常時**出します。
- **取り込みから2時間を超えると、この行が赤くなります**
  （画像のメール画面が赤いのは、見本データの取り込みが2026-08-08 18:00＝42時間前のため。**仕掛けが働いている証拠**です）。
- 取り込むのは**差出人の表示名・件名・日時・箱・型**だけ。**本文は取り込みません**
  （取り込みのたびに機械で確かめ、混ざっていたらその場で止めます）。
- 手順＝`docs/手順_メールの取り込み_20260810.md`

---

## 5. 決定6＝段階の語を作家台帳の9語に合わせました

**種まき → フォーム送付済 → 申込受付 → 入金確認 → 原稿待ち → 制作中 → 公開**（＋**見送り**・**未確認**）

- **旧15ステータスからの寄せ方を1件ずつ表にしました**＝`docs/手順_段階の語の対応表_20260810.md`。
  **捨てた語はありません。**
- ★**寄せ先が決まらなかったのは「物故」の1つだけ**です。台帳側で
  「お亡くなりの扱いは未決・**見送りで代用しない**」と決まっているため、**未確認に置いて理由を書きました。**
- ★**寄せるときに新しい日数を作っていません**（もともとの14日・7日だけを移しました）。

---

## 6. 決定7＝入金額はトップに出さない／A・B切替は廃止

- 入金は**あつぺくと中ページの「お金」**にだけ出しています（トップの細帯は登録件数だけ）。
- 見た目の案A/Bの切り替えは**廃止**。`assets/themes.css` を消し、全ページから読み込みを外しました（残存0件）。
  `hikaku.html`（2案を並べる画面）は**消さずに**「使わなくなりました」の案内に差し替えました。
- エピグラフは**仮置きのまま**（`data/board.json` の1行を書き替えるだけで差し替えられます）。

---

## 7. 機械で測った結果（実測）

### 6画面（設計図と同じ4項目）

| 画面 | 幅 | 中身の高さ | 横あふれ | タブ段数 | 44px未満 | JSエラー | 想定外の404 |
|---|---|---|---|---|---|---|---|
| メール | 390 | 1666px | 0 | **1段** | 0 | 0 | 0 |
| メール | 1440 | 1474px | 0 | （PCはタブ無し） | 0 | 0 | 0 |
| トップ | 390 | 1559px | 0 | — | 0 | 0 | 0 |
| トップ | 1440 | **864px（900px以内）** | 0 | — | 0 | 0 | 0 |
| あつぺくと中ページ | 390 | 718px | 0 | **1段** | 0 | 0 | 0 |
| あつぺくと中ページ | 1440 | 1863px | 0 | （PCはタブ無し） | 0 | 0 | 0 |

### 全14ページ × 2画面幅＝28通り

**28通りすべて、横あふれ0・タブは1段・44px未満の押す所0・JSエラー0・想定外の404が0。**

### 操作の通し（見本データで実際に押した・15項目すべてPASS）

- 「私が対応します」→ 記録が1件増え、**押した人の名前**が残る。未対応−1／対応中+1
- 「対応不要」→ 記録が**さらに1件増える**（前の記録は消えていない）。済+1
- 済のタブで「**対応不要**」だと**見分けられる**
- 「未対応に戻す」→ **記録は3件のまま**（`working, noreply, reopen`）＝**1件も消えていない**
- 迷惑・その他に分けたものは、未対応に**出ない**
- カレンダーに書き込み → **読み込み直しても残る**／「近い予定」にも出る
- 伝言も**誰が・いつ**が入って1件足せる
- JS例外 0件

### 配信物（鍵の内側に置く一式）

- **実データは1つも入っていません**（`data/` に入っているのは見本3つ＋`kijun.json`・`calendar.json`・`board.json`）。
- 実データを入れる指定（`--with-live-data`）を付けても、**切り替えの印が無ければ止まります**（終了コード1で実測）。

---

## 8. この便で**やっていないこと**（正直に）

- **実データへの切り替えはしていません**（関門2つが未達＝代表の初ログイン実測／許可リスト4件目の個人化）。
  切り替え手順＝`docs/手順_実データへの切り替え_20260810.md`
- **定時実行の登録をしていません**（`-WhatIf` で「何が登録されるか」だけ確かめました）。
- **`mail-auto-refresh` を実行していません**（Outlookを触るため。構文の検査だけ行い、0件でした）。
- **実機（スマートフォンの実物）では見ていません。** 幅390pxで描画した表示までです。
- **Codexの独立レビューは受けていません。**
- **受付台帳の実データ（`ledger.json`）の段階の語は、当ターミナルでは書き替えていません**
  （正本は受付台帳のスプレッドシートで、書き替えは受付の担当者の仕事のため）。旧の語のままでも画面は動きます。
- **共有の置き場所（印・予定・伝言）は未設定のままです。** いまはそれぞれのパソコンの中に残ります
  （画面の下に必ずそう出ます）。4人で共有するには置き場所の設定が要ります。

---

## 9. 画像の一覧

| ファイル | 中身 |
|---|---|
| `メール画面_390.png` / `_1440.png` | 最優先の1枚。赤い帯は「取り込みが42時間前」の警告が働いている状態 |
| `トップ_390.png` / `トップ_1440_1画面.png` | 全社の玄関。PCは**1画面（864px）に収まっている** |
| `あつぺくと中ページ_390.png` / `_1440.png` | 工程管理。9語の段階の帯が出ています |
| `操作_済タブに対応不要.png` | 「対応不要」を押したあと。済の中で見分けられることの証拠 |
| `操作_カレンダー書き込み後.png` | 予定を書き込み、読み込み直しても残っていることの証拠 |
| `根拠_埋め込みカレンダーはログイン画面が出る.png` | カレンダー方式(b)を採らなかった根拠 |
