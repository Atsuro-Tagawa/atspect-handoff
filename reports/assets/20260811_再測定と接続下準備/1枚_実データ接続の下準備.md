# 1枚：ダッシュボードに実データをつなぐための下準備（2026-08-11・★接続はしていません）

いまの画面は**すべて見本のデータ**です。この紙は「つなぐには何が要るか」を1枚にしたもの。
**つなぐのは代表の合図があってからの別便です。**

---

## 0. 8/10に置いた2つの入り口の条件＝**どちらも本日クリアしました**

| 条件（2026-08-10に設定） | 本日の状態 |
|---|---|
| 代表の初ログインが実測できていること | **達成**（本日、鍵の内側の画面を実測） |
| 許可リストの4件目が個人の宛先になっていること | **達成**（本日、差し替えて4件ちょうどを実測） |

→ **「つないではいけない理由」は、この2つについては無くなりました。** 残りは下の準備です。

## 1. 3つの接続の準備状況

| 接続 | いまできること | 足りないもの | 誰がやるか |
|---|---|---|---|
| **① メール**（差出人・件名・日時だけ） | **道具あり・読むだけで動く**（本文は取り込まない／消さない・既読にしない） | **このパソコンでメールソフトが開いていること**／定時実行の登録（道具あり・未登録） | 当方（登録まで）★ただし**このパソコンが動いている間だけ**更新される |
| **② 受付台帳**（作家さまの段階・日付・担当） | 台帳そのものは**Googleの表で運用中**（手順書あり） | **表からダッシュボード用のデータを作る道具が無い**（未着手）／**表を読む鍵**（公開せずに読む方法の決めごと） | 当方（道具）＋**代表（表を読めるようにする設定）** |
| **③ Shopifyの受注** | **本日、実際に1件読めることを確認**（合鍵は有効・許可は読み取りのみ3つ） | **受注をダッシュボード用のデータに書き出す道具が無い**（未着手） | 当方 |

★①②③とも、**画面に出す項目は「見本のデータと同じ形」**にします（作り直しは不要）。

## 2. ★つまずきが1つあります（つなぐ前に決めていただく必要があります）

**「いつもの画面を開く」ボタンと、4人での共有（印・予定・伝言）が、いまの配信物では動きません。**

- 理由＝**非公開のURLを配信物に入れない**と決めた（2026-08-09の裁定）ため、
  **それらのURLを書いた設定ファイルが、上げる一式に入っていません**（読み込みも外してあります・実測）。
- そのため画面では「まだ設定されていません」と出ます。
  **印・予定・伝言も、いまは各パソコンにだけ残ります**（4人で共有されません）。
- ★**この裁定を決めたときと、いまで状況が変わっています**＝当時と違い、
  **いまは鍵がかかっていて、入れるのは4人だけ**です。

**選べる道（当方は決めません）**

| 案 | 中身 | 良い点 | 気になる点 |
|---|---|---|---|
| **A** | 非公開のURLを**鍵の内側の配信物に入れる** | ボタンも共有もそのまま動く | 鍵の内側とはいえ、**配信物に非公開のURLが載る**（裁定の変更が要る） |
| **B** | 入れない（いまのまま） | 何も増やさない | **ボタンは使えないまま／共有もされないまま** |
| **C** | 画面を開いたときに**別の場所から設定を取りに行く** | 配信物には載らない | **結局その場所を鍵の内側の画面が読める＝Aとほぼ同じ**（作りが増えるだけ） |

★**当方の見立て**＝実質AかB。**Cは手間が増えるだけで、守りは強くなりません**（正直に書きます）。

## 3. つなぐときの順番（合図が出たら、この順で）

1. **切り替えの印**を置く（この1ファイルが無いと、実データは絶対に入らない作り）
2. **①メール**を1回書き出す → 画面で見本の名前が1つも出ていないことを確認
3. **③受注**の書き出しの道具を作る → 1回書き出す
4. **②台帳**の書き出しの道具を作る → 1回書き出す（**表を読む設定が要る**）
5. 実データ入りの一式を作る（★印が無いと止まる） → **一式のハッシュを控える**
6. 上げたあと、**ライブから取り直してハッシュ照合**／**外からログインなしで見えないことを再実測**
7. 定時実行を登録（自動更新にする場合）

## 4. つないだあとも変えないこと

- **公開リポジトリには実データを1バイトも上げない**（除外の設定を外さない）。
- **証拠の画面は見本モードで撮る**（実データのファイルを一時的にどけて撮る道具あり）。
- **メールの本文は取り込まない。**

## 5. お願いしたいこと

| # | 誰 | 内容 |
|---|---|---|
| 1 | **司令塔** | §2の**AかB**の裁定（これが決まらないと、ボタンと共有は動かないままです） |
| 2 | **代表** | **受付台帳の表を、ダッシュボードから読めるようにする設定**（Googleの操作が1回。手順は当方が用意します） |
| 3 | **代表** | **実データをつないでよい**という合図（①だけ先に、なども選べます） |

★**当方は、合図が出るまで実データを1件もつなぎません。**
