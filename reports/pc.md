# PC環境整備ターミナル 報告

> 本ファイルは **PC環境整備（旧 pc-kankyo）** の司令塔向け報告経路。
> **Publicリポジトリのため、個人名・書類名・金額・連絡先などの実名／機微は載せない（件数と構造のみ）。**
> 実名を含む詳細はVault（ローカル）側に置き、本ファイルにはその文書名だけを記す。

---

## 2026-08-19｜① 役割移管（旧 pc-kankyo → PC環境ターミナル）

- 旧ターミナルの作業ディレクトリ・セッション記録・受領済み指示（3便）を実測で確認。
- **「憲法」と名の付くファイルは、デスクトップ・`C:\Vault` 配下・`C:\work` 配下のいずれにも存在しなかった**（推測で補わず「無し」と報告）。
- 引き継いだ正本文書＝旧作業フォルダ配下の「ターミナル枠追加手順書_確定版」1件のみ（★印2点の実測確定を要する育成中の文書）。
- 報告経路を本ファイル（`reports/pc.md`）へ変更し、恒久ルールとしてCLAUDE.mdへ追記済み。

---

## 2026-08-19｜② デスクトップ整理実行（同日）の第三者検品

**結論＝データ消失ゼロ。ログの申告と実ファイルシステムの実測が完全一致。**

### 検品方法（4ルート）
1. 実行ログCSVの独立集計（自作スクリプト）
2. 移動計画CSVとの突き合わせ
3. **ログを根拠にせず実ファイルシステムを直接走査**した実在確認
4. Codex による独立集計との突き合わせ（二刀流・利用上限エラーなし・**全項目一致**）

### 件数（計画CSV／実行ログ／実ファイルの三者一致）

| 区分 | 件数 |
|---|---:|
| 計画CSV 総行数 | 471 |
| 実行ログ 総行数 | 471 |
| 結果=移動 | 428 |
| 結果=対象外（計画上そのまま） | 42 |
| 結果=エラー | 1 |

### (a) 移動成功428件の内訳（移動先ブロック別）

| ブロック | 計画 | 実績 | 差分 |
|---|---:|---:|---|
| Vault（新設の受け入れ区画） | 90 | 89 | **▲1**（＝エラー1件） |
| work（作業アーカイブ） | 299 | 299 | 一致 |
| Box（大容量アーカイブ） | 4 | 4 | 一致 |
| _to_delete 退避（削除ではない） | 36 | 36 | 一致 |
| **合計** | **429** | **428** | |

宛先フォルダ数＝**37**。上位の集中先は「作業ログ保管」134件、「要判断」31件、「特許関連」23件、「デザイン素材（アイコン）」23件、「報告・引き継ぎ」20件、「営業企画」19件、「退避（重複・旧）」19件、「税務決算」18件、「退避（インストーラ）」17件。
Vault側の受け入れ区画は 01〜11 の番号付きカテゴリ構成（経理／税務決算／入出金／カード／契約書／名簿／作家／案件書面／請求書領収書／取引先／行政）。

### (b) スキップ・リネームの件数と型

- **リネーム＝9件**。型はすべて同一で、**宛先で既存名と衝突しないための接尾辞付加のみ**（内訳＝Vault側5件・work側4件）。ファイル内容の変更は一切なし。
- **スキップ＝42件**。型はすべて「**計画上そのまま**」（そのまま残す41 ＋ 実行対象外1）。
  **「移動先に同名が既にあってスクリプトが無言でスキップした」ケースは0件。**
- 42件の内訳（型別）：フォルダ（プロジェクト作業用）15・ショートカット14・当面タスクのメモ8・システムファイル2・起動バッチ1・その他ファイル2。うち**稼働中ターミナルの作業フォルダは5**。
  **稼働中ターミナルの作業フォルダはすべて「残す」側に入っており、他ターミナルの作業を壊していない。**

### (c) エラーの件数と型

- **1件**。型＝**Unicode正規化（NFC/NFD）の不一致**。
  ディスク上の実ファイル名は濁点が分解形（NFD）、計画CSVは合成形（NFC）で、`Test-Path -LiteralPath` がバイト一致判定のため「元ファイルが見つかりません」となった。
- **ファイルは消失していない**（元の場所に無傷で残存を実測）。
- 計画データ側の修正が必要（＝移動ロジックの変更に当たる）ため、**未処理のまま代表判断へ回す**。

### (d) 司令塔申告 471件の内訳との差分

司令塔申告＝Vault 90・work 299・Box 4・_to_delete 36・実行対象外 1（**合計430**）。
**471との差 41行は「そのまま残す」行**で、申告の内訳に項目が立っていなかった。
正しい内訳は **471 ＝ 移動対象429 ＋ そのまま残す41 ＋ 実行対象外1**。

### 実ファイルシステムによる独立検算

| 検算 | 結果 |
|---|---|
| 移動428件が宛先に実在するか | **428 / 428（欠落0）** |
| 移動428件が元の場所に残っていないか | **残存0件**（二重化なし） |
| 非移動42件が元の場所に在るか | **42 / 42** |
| Vault受け入れ区画の再帰ファイル数 | **157 ＝ 期待157** |
| _to_delete 退避の再帰ファイル数 | **36 ＝ 36** |
| Box アーカイブの件数 | **4 ＝ 4** |

### 申し送り（代表の判断が必要／本検品では手を付けていない）

1. **削除は一切行われていない。** _to_delete は退避フォルダ。**実削除は不可逆のため承認が必要。**
2. **Vaultに新しい「正本」区画ができた。** 既存Vaultと**正本が二重になる**懸念。位置づけ（暫定／恒久）の確定を要する。AIが優劣を判断すべきでない事項。
3. **フォルダの二重入れ子が5件**（計画の「移動先」と「新ファイル名」に同じ名前が入ったため）。データは無事。平坦化するかは判断事項。
4. **Box宛4件はクラウド同期＝外部への転送が発生**（計画に明記され承認済みだが事実として記録）。
5. **エラー1件の処遇**（計画データを実名に合わせて直す／手動で移す／現状維持）。
6. **再実行するとログCSVが上書きされる**（追記オプション無し・Codex指摘）。再実行前にログを退避すること。
7. **付属の.batは現状のままでは動かない**（文字コード不整合。代表が見た「黒い画面が一瞬で消えた」の正体）。移動は完了済みのため未修正。
8. **本セッションの環境変数が旧役割（別プロジェクト）のまま**で、役割名と一致していない。取り違え防止のため整理が必要。

### 旧 pc-kankyo ターミナルの状態

**閉じてよい。** セッションの最終エントリは完了報告のテキストで、**未完了のツール実行・保留中の走行は無い**（実測）。
ただし旧作業フォルダには**PC環境整備の正本文書1件**が置かれているため、フォルダ自体は削除せず残すこと。

### 実名を含む詳細（Vault側・ローカル）

`C:\Vault\ARTS-RESPECT\_PC環境整備_整理実行検品_20260819_ATSPECT.md`
（全37宛先の件数表・リネーム9件の実名対応表・非移動42件の全一覧・エラー1件の詳細・二重入れ子5件の実パス）
