# 紙紋UI 作業報告

**ターミナル：紙紋UI ／ 使用モデル：Opus 5（claude-opus-5[1m]） ／ 作業：セキュリティ点検（P0の是正確定＋残存項目の全件棚卸し）**
最終更新：2026-08-07

## 本日やったこと

1. **最優先の項目（パストラバーサル）＝是正済みであることを実測で確定した。**
   過去の「封鎖済み」の報告を信用せず、**新しく攻撃試験を書いて自分で撃った**。
   **攻撃22件中0件通過・保存領域外のファイル2点はSHA-256が変化なし・正常系8/8。**
   さらに**同じ試験を修正前のコードに当てて5件通ることを確認**した（＝試験が穴を検出できる裏取り）。
   うち1件は保存領域外のファイルを実際に上書きしており、**是正の効果が実測で確かめられた**。

2. **証明書の発行ゲートに穴が実在した。撃って再現し、本日 是正した。**
   前回の棚卸しで「未着手」だった項目。**是正前は3件の条件で証明書が発行されHTMLも書かれた**が、
   **是正後は6件すべて期待どおり（食い違い0件）**。正常な発行は従来どおり動く。

3. **残っているセキュリティ項目を全件（14件）1件ずつコードで確認した。**
   **未是正の項目の内容・手口・該当コードは、Publicリポジトリには書かない**（紙紋の恒久ルール）。
   **全件一覧はVault（ローカル）に保存済み。** 判断を仰ぎたい点が2件ある（下記）。

4. **回帰試験＝UI 11本 ALL PASS／エンジン redteam 13-0・a3_c123 17-0（監査時の基準値と一致）。**

5. **認証情報の点検＝平文の認証情報は無い。鍵ファイル・`.env` も存在しない。**
   紙紋の作業フォルダはGitリポジトリではなく、GitHubへ上がる経路が無いことも確認した。

6. **提出書類（フリーズ済み）は本日1件も更新していない**（更新日時とSHA-256で実測）。

## 代表・司令塔に判断をお願いしたいこと

- **画面から一度も呼ばれていないのに、外から呼べる状態の機能が1つある。**
  攻撃面だけがあって使われていないが、**消すのは機能の取り下げにあたる**ため独断で行わなかった。
  （詳細はVault。選択肢は「露出をやめる／登録後は変更禁止にする／そのまま残す」の3つ）
- **`atspect-handoff` に `.gitignore` が1つも無い。**（紙紋の範囲外のため手を付けていない。
  あつぺくとシステム開発Tへの申し送りとして記載）

## 実施していないこと（正直に）

- **アプリの画面を起動しての通し確認をしていない。** 今回の変更は画面に出る文言を含むが、
  **実際の見え方は確認していない**。
- エンジンの残り3スイート（dup_semantics・collision・identify）は走らせていない
  （エンジンを変更していないため。走らせたのは安全に直結する2本）。
- 実紙・実機での精度確認はしていない（従来からの持ち越し）。
- 未是正として残った項目は**直していない**（本日の指示は「P0の確定」と「棚卸し」のため）。

## 証拠の置き場所

`reports/assets/20260807_紙紋UI_セキュリティ点検/`
（点検サマリ／実行ログ2本＝証明書ゲート是正後・パストラバーサル現行コード）
