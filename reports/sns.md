# SNS運用T 報告（2026-08-01）

## 第1便（14:30）＝軽量版パンフへの差し替え完了・旧アセット上書き完了・目視8点○

### 1. ★軽量版パンフへの差し替え＝**完了**（13.98MB → 2.89MB・79.3%減）

制作Tが14:19に軽量版を生成したため、固定名 `assets/atspect-pamphlet.pdf` の**中身だけ**を差し替えた。**固定名は変えていない。**

**正本の特定＝制作T自身の記述で確認した。** ビルド記録 `_パンフ生成記録.json` に「画面用軽量版」＝`02_パンフ_配布用.pdf`（2,894,032B・sha256 `4047076b…`）と明記されている。ファイル名から推測していない（7/31に「配布用」の旧世代を掴んだ失敗の再発防止）。
**ビルドが確定してから差し替えた**：14:19:34の実行後に14:21:29の再実行があり、2回目の記録で軽量版が `UNCHANGED（画素完全一致のため書き換えなし）`・sha256が2回とも同一であることを確認（走行中の中間生成物を掴んでいない）。

| | 差し替え前 | 差し替え後 |
|---|---|---|
| サイズ | 13,980,134 B（13.33MB） | **2,894,032 B（2.76MB）** |
| 構成 | A3二つ折り・2ページ（見開き） | **A4横・4ページ（画面で読む用）** |
| 実ブラウザ取得（幅390px） | 1,894〜2,803 ms | **457〜1,037 ms** |

**判定＝実ブラウザ（Chrome・幅390px・キャッシュ無効）でSHA-256を計算し、制作T正本と突き合わせ。**
- `/pages/terms-artist` → 2,894,032B・`4047076b…`＝**一致○**（3回連続で同一）
- 対面用エントリーページ → 同上＝**一致○**

差し替え前に現物をバックアップ済み（`_backups\_auto\theme161-atspect-pamphlet.pdf\20260801-142450_before-light.pdf`）。

### 2. 旧アセット v214／v218 の中身の上書き＝**完了**
前回、14MBのBASE64送信が `ECONNRESET` で3回失敗していた件。**同じやり方は繰り返さず、送信方式そのものを変えて**解決した。

- **採った手法＝`themeFilesUpsert` の body を `BASE64` ではなく `URL` 型にする。** 配信中のCDN URLを渡して**Shopify側にサーバ間で取りに行かせた**。こちらの回線から14MBを送出しないため `ECONNRESET` が原理的に起きない。**1回で成功。**
- 分割送信は**試していない**（1手目で通ったため不要と判断）。
- その後、軽量版が来たので v214／v218 も軽量版に統一済み（3本とも 2,894,032B・sha256一致○）。
- **削除は行っていない**（不可逆のため）。中身の上書きのみ。

### 3. ★CDNキャッシュの仕様が判明（実測・重要）
Shopify CDNは **`v=` パラメータだけをキャッシュキーに使う**ことを実測で確認した。

- `?v=<新しい値>` → 最新が返る
- **`?v=<差し替え前の値>` → 旧13.98MB版が返り続ける**（cf HIT・age 44,072秒・max-age 1年）
- `?nocache=…` のような別名パラメータでは**キャッシュを外せない**

**帰結**：フォームから開く方には影響なし（`v=` はテーマが中身から自動生成するため、差し替えと同時に `87428293…`→`85923300…` に変わったことを実測）。ただし**差し替え前のURLを直接お持ちの方には旧版が返り続ける可能性がある**。Shopify仕様のため当方からパージできない。
→ **版番号をURLに持たせず固定名にした設計は、この点でも正しく働いている。**

### 4. 目視8点の判定＝**8点すべて○**
14MBでレンダリングツールに載らなかった件は、**PDFをページ画像に変換してから見る**ことで解消。実際に画像を見て1点ずつ確認した（差し替え前の13.98MB版・差し替え後の軽量版の**両方**で実施し、両版で見分けが一致）。

| # | 見分け | 判定 |
|---|---|---|
| 1 | 基調色が藍 | ○（縦帯の実測色 `#34416E`） |
| 2 | 表紙の縦ライン有 | ○（左右端に藍の縦帯・幅3.2mm） |
| 3 | 表紙の日付なし | ○ |
| 4 | 『あつぺくと』とは | ○ |
| 5 | ステップ1「ご登録のお申し込み」 | ○ |
| 6 | ステップ2「ページ作成」 | ○ |
| 7 | 料金の注意書き3項目 | ○ |
| 8 | 裏表紙「AR-P0 第1刷」 | ○ |

QRも確認：表紙＝`https://atspect.com/`（200）／中面＝`https://atspect.com/apply`（301→申込フォーム・200）。

**制作Tへの申し送り（当方では良否を判断していません）**：印刷用ファイル（A3）では#2の藍の縦帯が**左右端から幅約3.2mm**にあり、PDFのTrimBoxは各辺3mm内側です。**帯のほぼ全部が塗り足し領域**にあたるため、**紙に断裁した後はほとんど残らない可能性**があります。軽量版（A4・塗り足しなし）では紙面内に収まって見えています。意図的な設計かもしれず、良否の判断はしていません。ご確認いただけますでしょうか。

### 5. 読み込み時間（回線速度別・差し替えの効果）
| 回線 | 旧 13.33MB | **新 2.76MB** |
|---|---|---|
| 4G良好 30Mbps | 3.6秒 | **0.7秒** |
| 4G標準 10Mbps | 10.7秒 | **2.2秒** |
| 4G混雑 5Mbps | 21.3秒 | **4.4秒** |
| 節約モード 1.5Mbps | 1.2分 | **14.7秒** |

**対面営業の場で作家さまがその場でスマートフォンから開く用途で、実用的な速さになった。**

### 6. スマホ幅の点検中に不具合を発見・是正
幅390px（iPhone 12〜15の標準幅）で、確認欄の**「必須」バッジが「必」「須」に泣き別れ**していた。
**DOM実測**（`getClientRects().length === 2`）で確認（画素の見た目だけで判断していない）。幅360px・320pxでは起きず、**390pxでのみ発生**。
→ `.atspect-aterms__req` に `white-space: nowrap` を追加して是正（`sections/atspect-terms-artist.liquid`・当Tの専任ファイル）。
→ 是正後：幅390pxで泣き別れ0件・横溢れ0件、幅320pxでも横溢れなし。バックアップ取得済み・SHA-256一致で反映確認済み。

---

## ★実施していないこと・判定していないこと（正直に記載）

- **スマートフォン実機での目視は未実施。** 実機目視は代表対応事項のため。上記は実ブラウザのモバイル幅（390/360/320px）での確認とダウンロード時間の実測にとどまる。
- **紙に印刷した現物は見ていない。** 塗り足しの申し送りはPDFのTrimBox実測に基づくもので、印刷結果の判定ではない。
- **旧 `v=` 付きURLのCDNキャッシュはパージしていない**（Shopify仕様上、当方から操作できない）。
- **旧アセットの削除は行っていない**（不可逆のため・中身の上書きのみ）。
- **分割送信は試していない**（別手法のURL型送信が1手目で通ったため）。
- **軽量版の画質・圧縮率の良否は判定していない**（制作Tの自動検査16項目がすべてpassであることを確認したにとどまる）。
- 決済・通知メール・API・作家データ・紙には**一切触れていない**。

**証拠の置き場所＝`reports/assets/20260801_pamphlet_kensho/`**（README.md＋パンフ全4面の画像＋差し替え前の各面＋モバイル是正前後のスクリーンショット）

---

## 第1便・追記（14:45）＝時間をあけての再取得・残留点検・担当外の発見1件

### 7. 時間をあけての再取得＝**合格**（1回の読み戻しで合格にしていない）
差し替え（14:25〜14:26）から**約19分後（14:44）**に取り直した結果、**4本すべて 2,894,032B・sha256が制作T正本と一致**（terms-artist／対面用エントリー／v214／v218）。**他ターミナルの上書きで戻っていないことを確認。**
Admin API（CDNとは別経路）でも3本とも `checksumMd5 da69f4430ed95f1cb80e259ef396dfa1` で一致。

### 8. 残留点検＝**旧名の参照はテーマのどこにも残っていない**
「パンフレットは常に最新・最軽量のものが配信されていなければならない」を理由に、**ライブテーマ161の全515ファイルを機械検索**した。
- `pamphlet` を含むのは**1ファイル1箇所のみ**（`sections/atspect-terms-artist.liquid` の固定名参照）。**旧名 `v214`／`v218` への参照は皆無。**
- ★**ローカルの `atspect-theme\sections\atspect-terms-artist.liquid` は古いまま**（`v214` と廃止済みのPDF開封ゲートが残存）。ローカルを見て判断すると誤る。**ライブから直接pullすること。**
- **QRも配信物から直接デコードして再確認**＝1ページ目 `https://atspect.com/`／3ページ目 `https://atspect.com/apply`（301→申込フォーム・200）。**圧縮でQRは壊れていない。**

### 9. ★担当外の発見（制作Tへ・判断待ちマスターに起票済み）
**スマートフォンの全機種で、サイト上部ナビの「作家の方へ」が画面外に切れて、見ることも押すこともできません。**
- `.atspect-sitenav__list` が `overflow-x: hidden` かつ `scrollWidth 432 > clientWidth 346`。**横スクロールもできないため、切れた項目に到達する手段がありません。**
- 幅430px（iPhone Pro Max＝一般的な最大幅）でも切れたまま。**収まるのに必要なビューポート幅は約476px＝現行のスマートフォンではすべて切れます。** 幅768px以上では切れません＝**モバイル限定**。
- 幅390pxの実画面では「作家を探す｜作品を探す｜あつぺくとについて｜お問い合わ**t**」で途切れています（`mobile390_sitenav_clipped.png`）。
- **DOM実測とスクリーンショットの両方で確認**（画素だけ・DOMだけで判断していません）。
- **当Tでは修正していません**＝`atspect-sitenav` は当ターミナルの専任ファイルではないため。**制作Tのご判断・ご対応をお願いします。**
- ※責任者ファイル既載の「フッターの『作家の方へ』が目立たない」とは**別事象（ヘッダー側・スマホでは完全に不可視）**。作家さまの入口の導線のため起票しました。

---

## 第2便（15:30）＝作家さま向け6ページのモバイル通し検査（320/360/390/430px）

### 1. ★是正した（当Tの専任ファイル）＝onboarding の文字サイズとタップ範囲

**根拠はサイト内の同種フォームとの比較。** 同じ作家さまが記入する terms-artist と並べて実測すると、ルートのfont-sizeは両方10pxで同じなのに、**onboarding だけが一貫して小さかった**。

| 項目 | terms-artist | onboarding(前) | onboarding(後) |
|---|---|---|---|
| ラベル | 14.0px | **10.0px** | 14.0px |
| ヒント文 | 16.0px | **9.2px（58%）** | 13.0px |
| 同意文 | 13.0px | **9.8px** | 13.0px |
| ラジオ | 13.5px | **10.0px** | 13.5px |
| ラジオ/チェックの実タップ高 | 47〜75px | **18〜37px** | **51〜111px** |

他の3フォーム（terms-artist 47〜75px／cancellation 47〜76px／registration-form 44〜49px）はすべて44px以上で、**onboarding だけが唯一44pxを下回っていた**。独自の基準を作らず**terms-artistと同じ方式（枠線＋padding）に揃えた**。あわせて「必須」バッジに `white-space: nowrap`（幅360pxで泣き別れていたため）。

**是正後・4幅すべてで**：44px未満のタップ範囲0件／「必須」の泣き別れ0件／横スクロールなし。変更はCSSの数値のみで、文言・HTML・送信項目には触れていない。

### 2. 実際に操作した（表示だけで通していない）
**5フォームすべてを入力→選択→チェック→送信の直前まで進めた。★送信は1件も押していない**（POSTを遮断した状態で操作）。

- **terms-artist／entry＝読了ゲートが指の操作で解除できることを確認**。解除前はチェック3つと送信が押せず、規約領域（表示488px／全長6371px）を実際のホイール操作で最後まで送ると全て押せる状態に変わった。**7/31の障害是正がスマホでも効いている。**
- **onboarding**＝メールを**わざと不一致**にして送信を押すと、送信は止まりエラーが赤字13pxで表示された。ただし**エラー位置まで画面が動くのに約2.5秒**かかり、押した直後は「何も起きていない」ように見える瞬間がある（最終的に見えるため不具合とは判定していない）。
- registration-form・cancellation＝いずれも全項目を操作でき、必須の未入力0件・HTML5検証も通る状態まで到達。

### 3. ★制作Tへ起票（当Tの専任外のため修正していません）
1. **サイト上部ナビの「作家の方へ」がスマホ全機種で切れて押せない**（幅430pxでも切れる／約476px必要／768px以上では切れない）。
   **★前便の記述を訂正します**：**フッターには「作家の方へ」が全ページ・全幅で存在します**（→`/pages/artist-guide`・90x29px）。正確には**「ヘッダーからは到達できない／フッターからは到達できる」**で、前便の「入口に辿り着けない」は言い過ぎでした。**深刻度は前便の記述より低くなります。**
2. registration-form の「← ご案内のページに戻る」が **9.5px・タップ範囲111x14px**・淡い灰色。
3. registration-form の「任意・複数選択可」が360/390/430pxで泣き別れ（430pxでは「お問い合わせ」も）。
4. cancellation の「プライバシーポリシー」が幅320pxで2行に割れる。

### 4. 機械検出のうち「不具合ではない」と判定して除外したもの
ハニーポット（`aria-hidden`・`tabindex="-1"`）／スキップリンク／閉じた言語ドロップダウン／読了ゲートの`disabled`／**フッターの「登録する」＝`span.visually-hidden`の読み上げ専用テキスト**／浮遊TOPボタンの被り／ラベルに包まれたラジオ・チェックの入力欄実寸。
**※検証中に自分の測定側の誤りも2件見つけて訂正しました**（メール不一致はblurではなくsubmitで判定する設計だった／registration-formの「入力できず」は当方スクリプトの不具合で、実際は全欄入力できた）。

### ★実施していないこと・判定していないこと
- **スマートフォン実機での目視・実機タップは未実施**（実機目視は代表対応事項）。すべてモバイル幅エミュレーション。
- **送信は1件も行っていない**＝送信後の画面・受信内容・自動返信メールは一切確認していない。**hCaptchaも通過していない。**
- **専任外の3ファイルは一切変更していない。**
- 日本語文中でリンクが改行されること自体は不具合と判定していない（挙げたのは2〜8文字の短い単位が割れるもののみ）。
- **色のコントラスト比は測定していない。**
- 決済・通知メール・API・作家データ・紙には一切触れていない。

**証拠＝`reports/assets/20260801_mobile_sweep/`**

---

## 第3便（16:10）＝繰り越しの消し込み（aria-label／非表示要素／カート残骸／PC幅）

### ★最初に：②は「実行すると壊れる」項目でした

**「DOM上の非表示要素を消す」を額面どおり実行すると、8言語サイトが壊れます。**
作品ページの非表示要素は実測**454本**で、その内訳は **`aw-l` 294本＋`footer-l` 77本＝371本が8言語の出し分けスパン**です（全336本＝42本×8言語のうち、選択中の言語42本だけが表示）。実物も確認しました＝`<span class="aw-l aw-l--ja">鈴木 千賀子</span>`。**消すと日本語以外の7言語が全滅します。**

また、繰り越しの根拠文書自体に「**当方独自に34本すべての由来を裏取りしたものではない**」と明記されており、**「34本」は元から未検証の数字**でした。本日の実測でも再現していません（カート関連は9本）。

### ②③ 3分類の結果＝**削除は0件**

- **【残すべき】**：8言語スパン／`cart-notification`一式（`visibility:hidden`は「カートに入れた瞬間に出る通知」の待機状態であって残骸ではない・**EC販売を使う前提なら必要**）／ヘッダーのカートアイコン（EC再開時に必要）／`product-form.js`／ハニーポット／読み上げ用テキスト／スキップリンク。
- **★とくに重要**：`atspect-terms-artist.liquid` 内の**生きている決済導線**（`/cart/...`「お支払いに進む（33,000円・税込）」）。2026-07-31にシステム開発Tが追加したもので、`form.posted_successfully?` の中にあり**送信に成功した方にだけ出ます**。**消すと登録料の支払い導線が切れます。** 決済は当Tの担当外のため内容にも触れていません。
- **【消してよい】＝0本。** 安全と確認でき、かつ当Tの権限で触れるものはありませんでした。**削除は1件も行っていません。**
- **【判断できない】＝1件**：`span.atspect-artwork__price-fx`（為替換算の表示枠・廃止済みかEC再開待ちか不明）→制作Tへ。
- **カート残骸が作家さま向け6ページに紛れていないかも実測＝画面に見えるカート要素0・カート文言0件。是正対象なし。**

### ① aria-labelの多言語化＝当T担当分は完了・**ただし繰り越しの範囲が狭すぎました**

繰り越しC6は「5種類・コーポレートサイト」ですが、**8言語サイトである atspect.com 側にも日本語固定のaria-labelが11〜12種類あります**（実測）。

**当T専任ファイル内の1件（規約全文領域）を8言語対応にしました。** `aria-label`は属性のためCSSで出し分けできないので、**`aria-labelledby`に変え、参照先をサイト既存の`.terms-l`（data-lang方式）の8言語スパン**にしています。非選択言語は`display:none`なので読み上げ対象にもならず、**選択中の言語だけが読まれます**（JSなし）。8言語すべてで読み上げ名が切り替わることを実測済み。日本語の文言は従来のまま。
**★読了ゲートの再検証も合格**（すぐ隣を触ったため。terms-artist・entry 両方で解除前3つ無効→実操作で解除を確認）。

残り＝atspect.com共通ヘッダー/フッター10〜11種（制作T）／atspect.co.jp 5種（コーポT・本番反映は代表承認が必要な経路）。

### ④ PC幅（1280／1440／1920）×6ページ＝**不具合0件**

18通りすべてで横スクロールなし。幅1280の「横溢れ9件」は**閉じた状態の言語ドロップダウンの待機位置**による誤検出で、**実際に開くと3幅とも8項目が画面内に収まりはみ出し0件**。PC幅ではヘッダーナビの「作家の方へ」も正常に表示されます（モバイル限定の問題であることの裏づけ）。

### ★自分の失敗と復旧（正直に記載）

**繰り越しマスターのVault側を、GitHub側の内容で上書きしてしまい、他ターミナルの記述6行を一時的に消しました。**
自動バックアップ（`_backups/_auto/..._20260801-160004`）から**復元し、そこへ自分の追記を足し直して0行欠落を確認済み**です。
原因＝2か所を「同一内容」と思い込み、**中身を比べずにコピーした**こと。**実際には両者は元から食い違っていました**（GitHub側にのみ148行、Vault側にのみ6行＝同じ行の新旧が混在）。
**この食い違いは当方が作ったものではなく、以前からのものです。どちらかに寄せると他ターミナルの新しい記述を壊すため、当方では同期させず、事実として報告します**（C3・C8・Y10・Y11・Shopify通知メール監査の節が該当。所有ターミナルでの突き合わせをお願いします）。

### ★実施していないこと・判定していないこと
- **削除は1件も行っていません**（【消してよい】が0本のため）。
- **「34本」の母集団は特定できませんでした**（元記録が未検証・本日の実測とも不一致）。
- **他ターミナルの担当ファイルは1つも変更していません**（`header.liquid`／`snippets/cart-notification.liquid`／`atspect-artwork-detail.liquid`／コーポレートサイト）。
- **決済導線には触れていません**（確認のみ）。
- **スクリーンリーダー実機（NVDA/VoiceOver等）での読み上げは未実施**。アクセシビリティツリー上の読み上げ名をDOMで実測したものです。
- **8言語の訳文は第三者レビューを受けていません**（法的文書の本文ではなくUIラベルのため）。
- **PC幅の実機目視は未実施**（エミュレーション）。
- 決済・通知メール・API・作家データ・紙には一切触れていません。

**証拠＝`reports/assets/20260801_kurikoshi/`**
