# システム開発ターミナル 報告（2026-08-10・旧URLの根本閉鎖＝★未閉鎖・明日へ持ち越し）

## ★最初にお伝えすること

**未閉鎖・原因＝ストア側が新しい権限をまだ許可していない（版は有効・許可は未反映）・持ち越し理由つき。**

- **version-3 は「有効」です**（版の一覧で実測）。**リリースは通っています。**
- **しかし合鍵を取り直しても権限は `read_orders` の1つだけ**。`read_translations`／`read_locales` は付いていません。
- ★**本日2回目の同じ型**＝「版の宣言」と「ストアの許可」は別物で、**ストア側で承認し直すまで効きません。**
  代表の「承認画面は表示されなかった」は、**その工程が走っていない**ことを示しています。
- 当方は管理画面の「インストール済み」も開きましたが、**権限の更新をうながす表示は見当たりませんでした（★未確認）**。
  **推測で手探りに押すことはしません。**

### 露出の実害評価（実測）

旧URL3件は **`noindex,nofollow`**／**sitemap.xml（親・子6本）に出現0件**／**サイト内で踏めるリンク0件**。
**→ 辿り着けるのは「旧の綴りを既に知っている人」だけ**で、**露出が広がる経路は実測で0件**です。
★ただし**「漏れたURLが開ける」状態は続いています。安全になったとは言えません。**

### 持ち越す理由

次の一手が**代表の操作に依存**し、当方から承認をやり直す入口を**特定できていない**ためです。
露出の広がりは実測で止まっており、**夜通し追う状態ではない**と判断しました。

### 明日の最初の一手

1. **代表**＝権限の承認をやり直す（うまくいかなければ**アプリを入れ直す**と承認画面が出るはず＝★当方未確認）
2. **当方**＝翻訳データを全言語で読み、**旧スラッグの残存を判定**（1回の読み取りで決着）
3. 残っていれば**旧の綴りだけ**を削除 → 旧URLを再測定（**転送が作動／404**）して閉じる

詳細＝`reports/assets/20260810_旧URL無効化/追記_version3は有効だが権限は未付与.md`

---


証拠＝`reports/assets/20260810_旧URL無効化/`（`記録.md`／`01_代表の操作手順書_翻訳の読み取り権限.md`）
★**ハンドル・URLは1文字も書いていません**（機械で混入0件を確認）。詳細の正本＝Vault。

## 1. 転送は3件作りました。★ただし、まだ効いていません

| 実測 | 結果 |
|---|---|
| 転送の作成 | **3件**（失敗0・重複0）／転送の総数 14→17件 |
| **転送先が「/」であること** | **3件とも「/」** |
| **★新しいハンドルへ向いていないこと** | **0件＝安全**（作る前・作った後・確認のたびに機械で検査） |
| 旧URL（作成直後／1分／2分／12分後） | **すべて200・フォーム表示のまま** |
| 新URL 3件 | **200・noindex維持・フォーム表示**（巻き込み事故なし） |

★**理由＝Shopifyの転送は「その道すじに中身が無いとき」にだけ働く**ためです。
いまは旧の綴りでページが生きているので、転送の出番が来ません。

**→ 前便で示した選択肢Bの前提が、実測で崩れました。**「301を作れば即座に塞がる」は誤りでした。
転送は**将来の保険**として残置します（旧の綴りが消えた瞬間から効きます）。
**転送IDを控えており、いつでも消せます。**

★**「即時の無効化」は達成できていません。旧URLはまだ開けます。**

「新URLには絶対に向けない」という最重要の規律は、**道具の中に検査を組み込みました**
（転送先が「/」以外、または新しいハンドルの文字が混じっていれば、その場で中止します）。
8/3の前例の旧URLには**一切触れていません**。

## 2. 原因調査（翻訳データ説）の手順書＝できました。**★代表の操作待ち**

**いまのどの合鍵でも翻訳データを読めない**ことを、お店からの返事の全文で確認しました。
必要な権限の**正確な名前**＝**`read_translations`**（＋言語一覧を見るなら `read_locales`）。
**どちらも読むだけ**です。

- 足す先の推し＝**本日作られた「注文読み取り（読むだけ）」**（合鍵を増やさず、読むだけの性質も変わりません）。
  ★**いつも使っている合鍵には足さないでください**（あれは書き込み権限を多数持っています）。
- **代表が新しい文字列を貼る作業はありません。** `.env`のIDとシークレットで、当方が合鍵を取り直せます。
- ★手順書には**未確認2点**（「バージョンを作成」後の用紙／ストアで承認し直す画面）を明記しました。
  **推測では書いていません。**

**権限が来たら**＝合鍵を取り直して権限3つ（書き込み0）を機械確認 → 3ページの翻訳データを読み、
**古いスラッグの残存が原因かを判定** → 残っていれば**消してよいか・どう消すか**を報告します。
★**判定するだけで、この段階では消しません。**

## 3. いまの全体像（正直に）

| 項目 | 状態 |
|---|---|
| 新URL 3件 | **使えます**（200・noindex・フォーム表示） |
| 旧URL 3件 | **まだ開けます**＝**無効化は未達** |
| 転送 3件 | **作成済み・効いていない**（保険として残置・削除可） |
| 利用者が踏むリンクの残存 | **0件** |
| 原因の特定 | **未了**（代表の操作1回で前に進みます） |

★**「旧URLは塞がった」とは、まだ書けません。**

## 4. 次にやること

1. **代表**＝翻訳の読み取り権限を足す（手順書のとおり・操作1回）
2. **当方**＝権限が来たら翻訳データを読み、原因を判定
3. **当方**＝旧URLの再測定を継続（転送が効き始めたら記録）
4. 営業用QR・リンク集・ブックマークの作り直し（3本ぶん・別便）

最終更新：2026-08-10
