# システム開発ターミナル 報告（2026-08-08 第8便）

model=Opus 5 (1M context)／担当＝あつぺくとシステム開発／作業＝**ダッシュボードを公開する準備と施錠の設計**

証拠の置き場所＝`reports/assets/20260808_ダッシュボード公開準備/`

---

## 結論を先に

**公開できていません。** 理由は1つだけです。

**このパソコンのChromeが、Cloudflareからログアウトされていました。**
2通りで確かめました（推測ではありません）。

| 確かめたこと | 結果 |
|---|---|
| このブラウザの資格で `dash.cloudflare.com/api/v4/user` を呼ぶ | HTTP 403 ／ `User session has expired. Please log in again` |
| `dash.cloudflare.com` を実際に開いて画面を見る | **「Sign in to Cloudflare」のログイン画面** |

APIの応答だけで判定せず、画面も開いています。
**パスワードは入力しない決まりのため、ここで止めました。**

★**代表にお願いしたいのは1つだけです。** デスクトップの
`いまやること_ダッシュボードを公開する_3手順.txt` の【1】（ログインし直して、タブを閉じずにお知らせ）。
そのあとの【2】【3】は当ターミナルで進められます。

---

## 作業1（公開）＝**配信物は完成。載せる操作だけが残っています**

デスクトップに `ダッシュボード一式.zip` を置きました（これをドラッグするだけの形にしてあります）。

- ファイル **19個**。それ以外は1つも入っていません。
- **実データは0件**（台帳・メールの一覧・お金の予定・接続先の4つは入れていません）
  → 画面は「見本のデータです」に切り替わります（**画像で確認済み**）。
- **12の画面 × 2つの画面幅 ＝ 24通り**をZIPから取り出した状態で開き、**ページのエラー0件**。
- 出た404は**意図した4つだけ**（`config.local.js` `ledger.json` `mail.json` `okane.json`）。
- 一式のハッシュ＝`8cb40144cc99d732ec2d56257f45ce1e550a7835715f3510dedc00109247ae02`
  **ZIPを展開し直したものも同じハッシュ**でした（＝ZIPが壊れていない）。

### 途中で自分が壊した箇所（隠しません）

配信物から「内部のコメント」を落とす仕組みを作ったところ、**JavaScriptが壊れ、
7画面すべてがエラーになりました。** 描画させて確かめたので気づけました。
**この仕組みは撤回しました**（コメントは残し、鍵で守る方針に戻しました）。
機械の検査はこのとき**全て合格していました**＝機械判定は目視の代わりにならない、の実例です。

### `.gitignore` の書き忘れを1件見つけて塞ぎました

第2便で作った**送信の記録**（差出人・宛先のドメイン・件名が入る）が、除外の対象に入っていませんでした。
**未commitのまま気づいたため、外には出ていません。** 追記して塞ぎ、機械で確認しました。

---

## 作業2（施錠）＝**設計は完了。かけていません**

★**代表が確定された条件のうち、Cloudflareの仕組みでは実現できないものがあります。**
黙って別のものに置き換えず、そのまま書きます。

| 確定条件 | できるか |
|---|---|
| メールに届く数字で入る／4人だけ／代表のみ予備鍵 | **できます** |
| **数字を4桁にする** | **できません**（桁数を変える設定がありません） |
| **連続で間違えたらロック** | **できません**（失敗回数でロックする設定がありません） |
| **無操作2時間で自動ログアウト** | **できません**（無操作を数える仕組みがありません） |
| **タブを閉じたら再ログイン** | **そのままではできません**（「閉じたら」という条件がありません） |

代わりにできるのは「**入ってから◯時間で入り直し**」（即時〜1か月）。**2時間**を推めます。
**これは「無操作2時間」ではありません。作業中でも切れます。**
本当に4桁・失敗ロックを満たすには自前のログインの仕組みが要り、サーバーが必要になります
（＝「0円・サーバーなし」の前提が変わります）。**いま決めていただく必要はありません。**

---

## 作業3（通しの実測）＝**していません**

URLが無いためです。ログイン→各画面→ログアウトの確認は、公開後にまとめて行います。

## 作業4（開き方の紙）＝**置きました**

デスクトップ `ダッシュボードの開き方.txt`。**4人にそのままお渡しできる形**です。
**URL欄は空**にしてあります（まだ無いため）。所要時間・「簡単」等は書いていません。

---

## Codexの独立レビュー（実施しました）

配信物を組み立てる仕組みに対し、**「公開してはいけないものが外に出る経路」**の1観点で依頼。
**高5件・中6件**の指摘。うち実際に穴だったものを直しました。

- **`--check-only` が古い配信物を検査して「合格」と思い込む経路があった**
  → 元の `dashboard/` と**1バイトも違わないか**まで照合するように変更
- 検査のあと差し替えられたら防げない → **一式のハッシュ**を出して照合できるように
- 近道（symlink）で外の秘密を運び込める → 拒否
- 秘密の形が7種類しか見えていなかった → 秘密鍵・JWT・各種トークン・**メールアドレス・電話番号**等を追加
- 危ないファイル名の検査が完全一致だけ（`ledger-prod.json` が素通り）→ 形でも見るように
- 見本のJSONが「名前がsampleというだけ」→ **中に「見本」の字があること**を確認するように

**守りが働くことを、5通り実際に仕込んで確かめました**（メールアドレスの混入／配信物だけの差し替え／
危ない名前のファイル／ファイルの欠落／電話番号の混入）。**5通りとも止まりました。**

★Codexの指摘のうち**「許可リストはファイル名の保証であって、中身の保証ではない」は仕組みでは消せません。**
**だから実データは配信物に入れず、鍵をかけたあとで載せます。**
**「0件」は「既知の形が無かった」という意味で、漏れなしの保証ではありません。**

---

## 実施していないこと・判定していないこと（正直に）

- **公開していません。鍵もかけていません。URLはまだありません。**
- **通しの実測（ログイン・ログアウト・ロック）はしていません**（URLが無いため）。
- Cloudflareの無料枠の実際の上限を、管理画面で確認できていません（公開資料のみ）。
- 実際のCloudflare Pages上での表示は見ていません（手元でZIPを展開して開いた状態のみ）。
- 印を4人で共有する仕組み（第7便）は、**まだ動いていません**
  （Google側の許可が代表のログインを要するため）。画面には「共有の置き場所が未設定」と出ています。

## 代表の操作をお待ちしているもの

1. **Cloudflareへログインし直す**（デスクトップの手順書【1】）★これが最優先
2. 印の共有（デスクトップ `いまやること_メールの印を4人で共有_3手順.txt`）
3. 4人のメールアドレス（鍵の許可リストに入れるため。こちらでは確定できません）
