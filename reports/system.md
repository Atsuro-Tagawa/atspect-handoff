# システム開発ターミナル

## 現状（2026-07-22）

**代表確定「作家フォームは二段階・両方残す＋呼称統一」を反映済み。審査申込フォーム（公開）→登録申込フォーム（非公開・個別URL案内）の二段構成が確定し、呼称表記の統一を本番テーマへ反映した。独立レビューは実行中（結果が出しだい反映）。**

- 代表確定＝第一段階「審査申込フォーム」（サイト内・誰でも申し込める門）／第二段階「登録申込フォーム」（審査通過者のみ個別URL・規約同意＋支払方法＋掲載作品）。両方残す。以後この二語で統一。
- 導線点検＝審査申込フォームの案内ページに「通過された場合は登録のご案内を個別にお送りする」旨が既に記述されており、第二段階への流れは自然に接続していることを確認（変更不要）。
- 呼称統一の反映（可逆・軽微の範囲）＝審査申込フォームの見出しを「審査のお申し込み」に統一・登録申込フォームの通知メール種別も統一。既存ファイルの更新は、並行編集を検知して止まる安全スクリプトを新設して実施（バックアップ・反映後の一致検証つき）。
- 登録申込フォーム本体（前回報告）＝実装・検証済み。決済の本番接続は代表確認の一歩手前で停止中（リンク設定は空欄のまま）。
- 独立レビュー（第三者チェック）は実行中。指摘が出れば反映し、この報告を更新する。

## 未着手・待ち

- 登録申込フォームの最終送信テスト1回（人の手で・キャプチャがあるため）。
- サブスク商品の公開操作・決済テスト（代表側）→完了後にフォームの決済案内を有効化。
- 台帳への項目追加（代表側・数分）／ポリシー多言語化の推奨案の可否（代表判断）。

## 今すぐ着手できるタスク

- 独立レビューの結果反映（実行中）。その後は次の指示待ち、または社内システム本体側の積み残しに着手可能。

（金額・料率・営業動線・具体的な手順の中身は機微につきチャットで扱う。）
