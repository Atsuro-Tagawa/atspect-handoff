# システム開発ターミナル 報告（2026-08-03・第3便）

モデル＝Opus 5（1M context）／担当＝あつぺくとシステム開発

## 1. ★作品の寸法＝当方が直す前に、別のターミナルが先に直していました（適用せず）

- 11:40 にライブを読んだ時点では、確かに食い違っていた（caption＝H23×W30×D20㎝／
  `artist_i18n.works[0].medium`＝**8言語とも高さだけ**）。8言語の是正計画を作り、Codexの独立レビューも
  「修正案は適切」と通った。
- **しかし当てる直前にライブを取り直したら、8言語とも既に H23×W30×D20 に直っていた。**
  さらに素材が「箔／gold leaf」→**「金属箔／metal leaf」**へ変わっていた。
- **そこで適用しませんでした。当てていたら「metal leaf」を巻き戻す退行になっていました。**
  計画ファイルは `_STALE_適用しないこと_` を頭に付け、理由を添えて残しています。
- 確認＝`verify` で**作品の寸法の食い違い 0件**（625名全員）。画面でも日本語・英語とも3辺を確認。

**★教訓＝計画を作ってから当てるまでの間に、他のターミナルが同じ場所を直していることがある。
当てる直前に必ずライブを取り直す。**

## 2. それでも道具は直しました（次に同じことが起きたとき使えるように）

`scripts/artist-i18n-update.mjs`（テスト11件追加・全197件green）

- **works / publications に対応**。これまでは未知のキーとして弾かれ、この経路では直せなかった。
  **配列まるごとの差し替えはさせず**、「何番目の・どの小項目の・どの言語」を指定して**1マスだけ**直す形。
  （まるごと渡させると、書き忘れた作品や言語が消えるため。作家さまの掲載内容なので消してはいけない。）
- **寸法の食い違いの自動検出**。`masterpiece_N_caption` と `works[N-1].medium.ja` の**寸法の数字**を突き合わせ、
  `㎝`/`cm`・全角/半角・空白の違いは吸収し、**数字が違うときだけ**鳴らす。
  **625名で実走し、リサーチTが見つけた1件だけを検出**した（現在は直って0件）。

## 3. 受付台帳（8月に50件をお受けする前提）

**手順書＝`docs/手順_受付台帳をGoogleスプレッドシートで始める.md`／ひな型＝`docs/reception-ledger-samples/受付台帳.template.csv`**

- **自社では作りません。**Googleスプレッドシートで今月から。足りない部分だけ後で作る。
- **★表は1枚だけ。**8月1日の推薦台帳に4列を足したものを受付台帳にする（**二重管理にしない**）。
- 列＝`段階/作家さまのお名前/申込日/入金確認日/推薦者コード/推薦者名/次にやること/担当/備考`
- 段階6つ＝`申込/入金確認/素材お預かり中/原稿作成中/公開のご確認待ち/公開済み`（プルダウン）
- **★実際に走らせて確認**＝この9列のCSVで推薦の集計 `referral-report.mjs` が**これまでどおり動く**。

## 4. A-1が通った直後の段取り（準備だけ）

`reports/assets/20260803c_受付台帳と作品寸法の是正/手順_A-1が通った直後に測ること.md`
いまは決済0件のため**入金予定日を出す欄そのものが出ていない**。1件通った瞬間に初めて出る。
**API経由では測れない**（`shopifyPaymentsAccount` も `orders` も ACCESS_DENIED＝実測済み）＝画面で測るしかない。

## 5. 代表対応待ち

- **A-1のテスト決済**（テストモードの切替は代表の操作）。当Tから決済は通しません。
- **受付台帳のスプレッドシートの作成と3名への共有**（Googleアカウントが要るため代表の操作）。

## 6. 実施していないこと・判定していないこと

- **作家データへの書き込みは1件も行っていない**（不要になったため）。
- **`SHOPIFY_ALLOW_WRITE=1` は当方では立てていない**（代表専管の一線）。
- **受付台帳の実物は作っていない**（手順書とひな型まで）。
- **決済は1件も通していない**＝初回入金予定日・着金日数は引き続き未実測。
- **「metal leaf」への変更が正しいかは当方では判定していない**（作家さま情報の正否はリサーチTの領分）。
  当方が確かめたのは「8言語が互いに同じ内容か」「寸法の数字が caption と一致するか」だけ。

## 証拠の置き場所

`reports/assets/20260803c_受付台帳と作品寸法の是正/`（画像2点＋README＋A-1直後の手順）

## Codexレビュー

1本実施（8言語の寸法表記・1ファイル1観点）＝「修正案は適切。8言語とも直す必要なし」。
**ただしその修正案自体が不要になった**（上記1）。
