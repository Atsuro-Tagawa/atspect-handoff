# システム開発ターミナル 報告（2026-08-11・設定を鍵の内側へ／台帳の手順書／メールの仕上げ）

★**非公開のURL・識別子の実値は書きません。社内の方のお名前も書きません。**
★**このパソコンの利用者名を含むパスも書きません**（Codexの指摘・重大／初稿から削除）。
証拠＝`reports/assets/20260811_設定を鍵の内側へ/`

---

## A．A案の実装＝**ボタンが動くようになりました**／**外からは取得できませんでした（確認した経路で）**

### やったこと

- 非公開の設定を**鍵の内側の配信物に入れる**仕組みを作りました。
  ★**入るには2つそろう必要があります**（実行時の指定＋合図の印）。
  **確認した条件では、片方だけでは入りませんでした。**
- **配信物の出発前検査は緩めていません。** 合鍵（トークン）・パスワード・秘密鍵は**いまも止まります**。
  今回外したのは**「置き場所・接続先のURL」の検査だけ**で、しかも**その1ファイルに限って**です。
  ★**この検査を外したことで見逃しうるもの**＝意図しない外部への送信先・追跡用のURL等。
  **いまは「入れる中身を人が見て決める」ことだけが歯止め**です（自動の許可先一覧は作っていません）。
- 全ページに設定の読み込みを戻しました（★空の入れ物を常に置くので、**確認した範囲では404になりませんでした**）。

### 実測（鍵の内側・2026-08-11）

| 実測 | 結果 |
|---|---|
| 設定の入れ物が読み込まれたか／中身入りの印 | **はい／はい** |
| 行き先が入った項目 | **11本**（Webメール・注文一覧・管理画面・LINE・受付台帳・Box各所） |
| 画面に「まだ設定されていません」と出る数 | **1** |
| ライブから取り直した**照合対象26点**のハッシュ | **26/26 一致** |

★**「動くようになった」の範囲**＝**ボタンに行き先が入ったこと**までを確認しました。
**押した先が開くところまでは、まだ一つずつ試していません。**

### ★外から取得できないことの実測（確認した経路）

**新旧2つのURL × 6つの道すじ（設定ファイル本体を含む）＝12通り**を、ログインなしで叩きました。

- **すべて302**で、**行き先はCloudflare Accessのログイン画面**でした。
- **応答の本文は143バイト**（転送の案内文のみ）。**その本文に非公開のURLは含まれていませんでした。**
- ★「1バイトも返らない」とは書きません（302にも短い本文があります）。**中身が返らなかった**が正確です。

### ★画面の文字に値が出ていないことも機械で確認

**全ページ×3幅＝42通り**で画面の文字を調べ、**URLは1本も表示されていませんでした**。
★ただし、**ボタンの行き先は、鍵の内側に入れた人にはHTMLやDOMから読めます**。
これは**「4人には見える／外の人には見えない」**という設計であり、**誰にも見えないという意味ではありません。**

### まだ動かないもの

**4人での共有（印・予定・伝言）は、まだ各パソコンにだけ残ります。**
共有の置き場所が**まだ公開されていない**ためです（下のB）。画面にも正直に出しています。

## B．受付台帳の読み取り設定の手順書＝**デスクトップに置きました**

**デスクトップの「いまやること_受付台帳をつなぐ_1画面ずつ.txt」**
（貼り付けるコードも同じデスクトップに「貼り付け用_ダッシュボードの入口.gs」として置いてあります）

- **1画面ずつ・押すボタンの名前まで**書いてあります（手順①〜⑧）。
- ★**この1回の設定で2つが動く想定**＝**受付台帳の読み取り**と**4人での共有**。
  ★**本番で通したわけではありません**（URLを受け取ったあとに、こちらで確かめます）。
- **受付台帳のシートには書き込みません（読むだけ）。消す機能はありません。**

### ★Codexの指摘で、公開範囲の危険に手当てを1つ足しました

指摘＝**「アクセスできるユーザー＝全員」は、URLが漏れると第三者が読める・大量に書き足せる設計に読める。**
→ **そのとおりでした。** そこで**合言葉（長い文字列）を1つ足し、合わないと読むことも足すこともできない**ようにしました。

- 合言葉は**貼り付け用のファイル**と**鍵の内側の設定**の両方に入れてあります（**報告には書きません**）。
- **URLを知っているだけでは開けません。**
- ★**まだ残っている弱さ**＝合言葉が漏れた場合／**書き足しの回数制限は設けていません**（大量投入は防げません）。
  **必要なら次便で足します。**

★**表示名について正直に**＝当方は代表のGoogleの画面を開けません。
**英語の表示名はGoogleの公式の説明書から取った確かなもの**、
**日本語の表示名は「★未確認・推測」と明記**しています。
**表示が違ったら、その画面を送っていただく**ようお願いしています（無理に進めない案内に直しました）。

## C．メール接続の道具＝**登録すればすぐ動く状態にしました（登録はしていません）**

| 実測 | 結果 |
|---|---|
| 取り込みの道具を実際に走らせた | **動きました**（書き出し件数と書き出し時刻を確認） |
| 上げる工程が動かないこと | **動きませんでした**（★合図の印が無いため＝設計どおり） |
| 定時実行の登録（見るだけの指定） | **登録される中身を確認**（30分ごと・ログインしている間だけ）。**登録はしていません** |
| 後始末 | **作業で書き出した実データを削除し、リポジトリに入っていないことを確認**しました |

★**後始末の範囲（正直に）**＝消したのは**書き出したファイル**です。
**一時ファイル・ログ・ごみ箱・過去の履歴までは調べていません。**

### ★「このパソコンが動いている間だけ」を画面に出しました

メールの画面の、最終更新時刻の**すぐ下**に、小さく次の一行を**常に**出すようにしました。

> この一覧は、会社のパソコンが動いている間だけ新しくなります（そのパソコンが止まっている間は増えません）。

従来からの**「2時間を超えると赤くなる」**表示はそのままです。3幅すべてで表示を確認しました。

## 証拠の画像（と、その限界）

- 3幅×3画面＝9枚＝**ライブと同じ配信物**を手元で描画したもの
  （**照合対象26点がライブと一致**することを確認済み。★実行時の通信や表示まで同一とは限りません）。
- ライブ2枚＝**実際のライブ**（鍵の内側）で撮ったもの。
- ★**なぜ9枚を手元で撮ったか**＝ライブの撮影に使っている道具が、**窓の大きさを変えても切り取る範囲が変わらず**、
  3幅の撮り分けができませんでした。**ライブそのものの3幅は撮れていません。**
- ★**画像の安全性について**＝**画面の文字にURLが出ないことは機械で確認**し、**画像も目で見ました**
  （中身は見本データ、ブラウザのアドレス欄・タブ・ブックマークは写っていません）。
  ただし**画像に埋め込まれた情報（撮影ソフトの記録等）までは調べていません。**

## Codexの独立レビュー＝実施。**重大4件・中7件・軽微4件。設計の手当て1件を含めて反映**

| 度合 | 指摘 | 対応 |
|---|---|---|
| **重大** | **公開範囲「全員」はURL漏洩時に読まれる・書き足される** | **合言葉を追加**（合わないと読めない・足せない）。**残る弱さも明記** |
| **重大** | 利用者名を含むパスを書いていた | **削除**（「デスクトップの〜」に統一） |
| **重大** | 配置（ファイル名・経路の組み立て方）が偵察材料になる | **表現をぼかした** |
| **重大** | 「302だから1バイトも返らない」は立証不足 | **応答本文の長さと中身を実測し直し**、表現を訂正 |
| 中 | 断定が強い（外からは読めません／絶対に入りません／404になりません 等） | **すべて「確認した経路・条件では」に限定** |
| 中 | 画面の文字の検査だけでは画像の安全は証明できない | **目視も行い、調べていない範囲を明記** |
| 中 | 「URLが表示されない」と「値が漏れない」は別 | **鍵の内側の人には読めることを明記** |
| 中 | ハッシュ一致の言い方が広すぎる | **「照合対象26点が一致」に限定** |
| 中 | 検査を外したことの代替の歯止めが不明 | **いまは人の目だけであることを明記** |
| 中 | 消した実データの残り先が不明 | **確認した範囲を明記** |
| 軽微 | 件数が構成の手掛かりになる／画像の枚数と検査通り数の関係が不明 等 | **必要な数字だけ残し、対象を書き分けた** |

## まだできていないこと

- **4人での共有は動いていません**（代表の操作1回待ち＝B）。**本番で通した確認もこれからです。**
- **実データは1件もつないでいません**（見本のまま）。
- **旧識別子3種の無効化は未達**（裁定どおり明日朝に再測定します）。
- ライブそのものの**3幅の撮影**／**ボタンを押した先が開くことの1つずつの確認**／
  **画像の埋め込み情報の確認**／**過去の履歴の秘密値の洗い直し**は、いずれもしていません。
- 合言葉の**書き足し回数の制限・記録**は設けていません。

最終更新：2026-08-11

---

# 追記（2026-08-11・ライブの3幅撮影を可能にした／全リンクの開通実測）

★非公開URL・識別子・合言葉・合鍵・社内の方のお名前は、**報告にも画像にも出していません。**
証拠＝`reports/assets/20260811_ライブ3幅とリンク実測/`（**ライブの3幅×3画面＝9枚**＋記録2件）

## 1. 3幅が撮り分けられなかった原因＝**特定しました**

**原因＝ブラウザ拡張の撮影では、窓の大きさを変えても【描画の幅が変わらない】。**

| 頼んだ窓の幅 | 拡張の返事 | 実際の描画の幅（画面自身に聞いた） |
|---|---|---|
| 1300 | 「成功しました」 | **651** |
| 900 | 「成功しました」 | **651** |
| 500 | 「成功しました」 | **651** |

3回とも**同じ651**でした。＝**窓は変わっても、中の描画は変わっていません。**
（撮れる画像が毎回同じ大きさだったのは、これが理由です。**画素の倍率は1.25**で、651×1.25≒撮影の幅。）
★**デバイスの指定・固定の枠・倍率のどれか、という当たりの付け方では決まらず、
「画面自身に幅を聞く」ことで初めて分かりました。**
★これは**この拡張・このパソコンでの実測**です。**拡張一般の性質として断定はしません。**

## 2. ★ライブで3幅を撮れるようにしました（本便の主目的）

**やったこと＝Cloudflareの「サービストークン」を1本作り、機械から鍵の内側を読めるようにしました。**
そのうえで、幅を指定して描ける道具を作り、**ライブから直接**撮りました。

| 幅 | HTTP | 画面自身が答えた描画の幅 | 狭い画面の指定 | 広い画面の指定 |
|---|---|---|---|---|
| **390** | 200 | **390** | 当たる | 当たらない |
| **768** | 200 | **768** | 当たらない | 当たらない |
| **1280** | 200 | **1280** | 当たらない | 当たる |

**＝3通りとも、頼んだ幅で描かれたことを画面自身が答えました**（見た目だけの判断ではありません）。
撮る前に**いちばん下まで送ってから戻す**動きを入れています（ご指示どおり）。
**手元描画の代替ではありません。ライブ（鍵の内側）から取得した画面です。**

### ★これは「鍵をひとつ増やした」ことになります（危険もそのまま書きます）

- **合鍵が漏れると、その1本だけで鍵の内側を読めます。人の確認（数字の入力）を通りません。**
  自動で何度でも・何時間でも取り続けられます。**気づく仕掛けは、いまありません。**
- ★**読めるのは「撮った3画面」だけではありません。** この合鍵は**そのホストの中身ぜんぶ**に効きます
  （画面も、データのファイルも）。**いまは見本データだけですが、実データを載せれば、それも読めます。**
- **範囲は狭めてあります**＝入れるのは**片方のホストだけ**。もう一方には足していません
  （実測＝合鍵を付けても、もう一方は通りませんでした）。
- **有効期間＝1年**で作りました。**責任者・見直しの時期は、まだ決めていません（要裁定）。**
- **合鍵は手元の設定ファイルにだけ置き**、報告・画像・リポジトリには出していません。
  ★ただし**平文**です。「手元だけ」でも、控え・同期・端末の不調による漏れは否定できません。
- **取り消し方**＝Zero Trustの「サービス資格情報」でこの1本を消す／
  アプリ側の「サービス認証」のポリシーを外す。**どちらも1操作**です。
  ★**消したあとに本当に入れなくなるかは、まだ実測していません**（次便で確かめます）。
- ★**実データを載せる前に、この合鍵を消すか・期限を短くするかを決めていただく必要があります。**
- **普通の利用者の入口は変えていません**（実測＝合鍵なしでは、これまでどおり認証画面へ転送）。
  ★ただし**機械用の入口を1つ増やしたので、狙われる面そのものは増えています。**
- 撮影の道具は、**合鍵をそのホスト宛の通信にだけ付け、よそへの通信は止める**作りにしました
  （はじめは全部の通信に付ける書き方で、よそへ送られうる作りでした＝Codexの指摘・重大）。

### 画像の安全確認（3点とも実施）

| 確認 | 結果 |
|---|---|
| 機械（画面の文字を数える） | **9枚とも、社内4人のお名前0件**／URLの表示0本 |
| 別途の総ざらい | **全13ページ×3幅＝39通り**でも数え直し、**0件** |
| 目視 | **9枚とも自分で見ました**（見本データのみ・ブラウザの枠や住所欄は写っていません） |
| 埋め込み情報（Exif等） | **9枚とも0件**。念のため**持たない形で保存し直し**、再確認して0件 |

★**画像には「公開用の伏せ字」を入れています（正直に）**＝担当者のお名前が出る箇所を
**「（お名前）」に置き換えてから撮っています**。**ライブの中身は変えていません**（撮る直前の画面上だけの置き換え）。

★**ここで2回、間違いを見つけて直しました。**
1. 合鍵で入ると入り口が宛先を教えないため、画面が「お名前を選んでください」の形になり、
   **4人のお名前が並んで写っていました**（1回目の撮影）。→ 名前を1つ選んでから撮るようにした。
2. それでも**広い画面（1280）の「いま誰が動いているか」の欄に、選んだお名前が残っていました**
   （Codexの指摘で気づき、39通りの数え直しで場所を特定）。→ **画面のどこに出ていても伏せる**ように広げた。
   ★**当初の「お名前0件」という報告は誤りでした。** 数えていたのが1人ぶんだけだったためです。

## 3. リンクの行き先を1つずつ実測＝**26件**（開かないもの0件）

全13ページを開き、隠れているタブも全部開いてから、**リンクの行き先を26件**拾って1件ずつ確かめました。

| 種別 | 件数 | どこまで確かめたか |
|---|---|---|
| **中（ダッシュボードの中）** | **17** | **17件とも開く**＝中身が返り、題名も想定どおり（ここは完全に確認できました） |
| **外（他のサービス）** | **9** | **9件とも応答あり。ただし「目的の画面が出た」ことまで確かめたのは3件だけ** |

- 中の行き先は、いったん308（末尾の`.html`をとる転送）を挟みますが、**追いかけると200で中身が返ります**。
- 外の**3件は、ブラウザで実際に押して、目的の画面が開くところまで**確かめました
  （管理画面のホーム／注文の一覧／受付台帳の表。いずれも正しい画面が開きました）。
- ★**残る6件（Box）は、このパソコンのブラウザでログインしていないため、ログイン画面に着きます。**
  **フォルダが実在するかは確かめられていません。**
  4人が使い始めるときは、**各自がBoxにログインしている必要があります。**

★**「全ボタン」とは書けません（正直に）。** 調べたのは**リンク（`a`）の行き先**です。
**押すと画面の中だけが変わるボタン（タブ・印・書き込み等）や、送信の動きは、この検査に入っていません。**

### ★当方の測り方の誤りを1件、自分で見つけて直しました

最初の検査で**3件を「つながらない」と判定**しました（401・403）。
実際は**「ログインしていないから断られた」だけ**で、リンク切れではありません。
→ **404と5xxだけを「開かない」と数える**ように直しました。
★**ただしこの直し方も万能ではありません**＝410や、まったく別の画面へ転送された場合も
「つながる」と数えてしまいます。**だから上の表では「どこまで確かめたか」を分けて書いています。**

## 4. Codexの独立レビュー＝実施。**重大6件・中7件をすべて反映**

★**このレビューで、公開直前に止められた誤りが3つあります。**

| 度合 | 指摘 | 対応 |
|---|---|---|
| **重大** | **画像9枚すべてに社内のお名前が出ている。「0件」は誤り** | **そのとおりでした。**画面のどこに出ていても伏せる形に直し、**39通りで数え直して0件**を確認 |
| **重大** | 氏名を数える検査が1人ぶんだけ＝安全の証明になっていない | **4人ぶんに広げた**（数える言葉は道具に書かず、一覧から読む） |
| **重大** | 道具2本に氏名・宛先が書かれている | **氏名は道具から外した**（一覧と設定から読む形に） |
| **重大** | 合鍵が**よその宛先へも送られうる**書き方だった | **その宛先だけに付け、よそへの通信は止める**形に直した |
| **重大** | 合鍵の危険の説明が足りない（範囲・期限・責任者・失効の実測） | **上の節に全部書き足した**（未決も明記） |
| **重大** | 3幅の合否判定が**同じものどうしを比べていて必ず合格**になる書き方 | **頼んだ幅と、画面が答えた幅を比べる**形に直した（9/9一致） |
| 中 | 「全ボタン・全リンク」は言い過ぎ | **「リンクの行き先26件」に限定**し、調べていない範囲を明記 |
| 中 | 外の9件を「つながる＝良し」と数えるのは過大 | **「目的の画面まで見たのは3件」**と書き分けた |
| 中 | 「拡張では幅が変わらない」は一般化しすぎ | **この拡張・この環境での実測**と限定 |
| 中 | 「利用者の守りは変わっていない」は範囲が広すぎ | **入口は変えていない／狙われる面は増えた**と両方書いた |
| 中 | `.env` は平文である | **明記した** |
| 軽微 | 埋め込み情報0件と、画像の中の文字の安全は別 | **分けて書いた** |

★**採らなかった指摘＝ありません**（すべて反映しました）。

## まだできていないこと（この追記の分）

- **旧識別子3種の再測定は本便では行っていません**（裁定どおり明朝）。
- **代表の操作待ちの工程には手を出していません。**
- **合鍵を消したあと、本当に入れなくなるか**の実測（次便）。
- **合鍵で読める範囲の棚卸し**（画面以外に何が読めるか）と、**責任者・見直し時期の決定**。
- **Boxのフォルダが実在するか**（ログインが要るため）。
- **リンク以外のボタン・送信の動き**の確認。
- **人がログインして見たときの3幅**（今回は合鍵で入った状態での撮影です）。
- 実データは1件もつないでいません。

---


---

# 追記（2026-08-12・共有設定の先頭バイトの是正／合鍵の入れ替えは未了）

★秘密値・非公開URL・識別子・社内の方のお名前は**記載していません**（公開前に機械の検査も通しています）。

## 1. 共有の設定ファイルの先頭バイト（BOM）＝**取り除きました。原因は当方の作業です**

### やったこと（BOM以外を変えていないことの証明つき）

- 先頭の**3バイトだけ**が減りました（ファイルの残りは触っていません）。
- **証明の仕方**＝「取り除く前のバイト列の**4バイト目以降**」と「取り除いたあとのファイル」の
  **SHA-256が一致**することを確認しました。**ハッシュの値は非公開の記録に残し、ここには書きません。**
- 取り除く前の控えも保存してあります（中身は出しません）。
- ★この証明が言えるのは**「先頭3バイトを取り去っただけ」**までです。

### なぜ止まっていたのか（読み方によって結果が違いました）

| 読み方 | 除去前 | 除去後 |
|---|---|---|
| 道具ごとの自前の読み取り（`=`で切って前後の空白を落とす） | **引けていた**（先頭バイトが空白として落ちるため） | 引ける |
| **Nodeの標準の読み取り**（`process.loadEnvFile`） | **★1行目の項目だけ引けない** | **引ける** |

**1行目の項目は、Shopifyを触る道具がほぼ必ず使うもの**でした。
**同じ書き方をしている道具が111か所ある**ことを確認し、**そのうちの1本で実際に停止を再現**しました
（★111本すべてを動かして確かめたわけではありません）。

### ★原因＝当方の作業だと判断しています（正直に）

**前日の夕方、資格情報を設定ファイルへ収める作業で、PowerShell の `Set-Content -Encoding utf8` を使い、
ファイル全体を書き戻しました。** Windows PowerShell 5.1 の `-Encoding utf8` は**先頭バイト付きで書きます**
（`Add-Content`・`Out-File` も同じ）。**時刻の並びと書き方から、この操作で入ったと判断しています**
（★監査の記録で突き合わせたわけではないので、「証明した」とは書きません）。

★**気づけなかった理由も書きます。** **当方の道具は自前の読み取りだったため、正常に動いていました。**
「自分の道具で動いた」ことを正しさの根拠にしてしまい、**別の読み方で壊れていることに気づけませんでした。**

### 復旧の確認（確かめた範囲）

- 除去前に停止していた読み取り専用の道具を、除去後に実行し、**その読み取り処理が成功することを確認**しました。
- 同じ書き方の別の道具でも成功を確認しました。
- ★**「全部が直った」とは書きません。**確かめたのは、実際に動かした数本ぶんです。

### 再発しない書き方に直しました

- **設定ファイルを足す・書き換える専用の道具を新設**（**Nodeで書く＝先頭バイトを付けない**）。
  書いたあとに**先頭バイトが無いことを自分で確かめて**終わります。
- **2つの読み方の両方で読めるかを確かめる道具**も新設（値は出さず、引けたかどうかだけ）。
- **教訓を正本へ記録**しました（原因の型＝「書き込みの文字の作法を確かめずに、共有ファイルを丸ごと書き戻した」）。
- ★**この作業と無関係に、別の資格情報が使えなくなっていたことも見つけて復旧**しています
  （内容は非公開の記録に残しました。**動作確認では、データを取得する操作はしていません**）。

## 2. 合鍵（サービストークン）の入れ替えと失効の実測＝**できていません（未了）**

★**裁定に対して、本便では達成できませんでした。旧の合鍵は、まだ有効なままです。**

### 分かったこと（裁定に要る材料）

1. **管理画面では「90日」を作れません。** 有効期間の選択肢は**5つだけ**で、日数を入れる欄はありません（実測）。
2. **APIを使えば任意の期間を指定できます**（Cloudflareの公式のAPIに作成の機能があります）。
   ただし**`Access: Service Tokens Write` にあたる権限を持つ認証手段が要ります。**
   ★**いまある手段で足りるかは調べていません**（＝「必ず新しい鍵が要る」とは書きません。前回の下書きでは
   断定していましたが、Codexの指摘で取り下げました）。
3. **作業の途中で、ブラウザの操作機能が不調になりました**（画面の撮影が失敗し、有効期間の選択肢が開かない）。
   **そのため新しい合鍵の作成そのものができませんでした。**

### ★あえて「先に消さなかった」理由

旧の合鍵を先に消すことはできましたが、**新しい合鍵を作れない状態で消すと、
鍵の内側を撮る手段が無くなり、前便で整えたばかりの検品の経路が使えなくなります。**
**代わりが用意でき、反映と動作確認が済むまでは消さない**、という順序を選びました。

### ★いま残っている危険（旧の合鍵が有効なままであること）

- **効く範囲**＝片方のホストの中身（画面もデータのファイルも）。**いまは見本データだけです。**
- **使われても気づく仕掛けはありません**（記録は残りますが、通知は設定していません）。
- **業務への影響**＝この合鍵を消しても、**4人の利用には影響しません**（人のログインとは別の経路です）。
  影響を受けるのは**当方の検品の道具だけ**です。
- **暫定の手当て**＝**実データを載せない**かぎり、読まれるのは見本データです。
  **実データ接続の前に必ず入れ替える**という裁定は、そのまま有効です。

### 次にやること（2026-08-12 のうちに／担当＝当ターミナル）

**入れ替えは、次の順でしか行いません**（Codexの指摘を受けて順序を明文化しました）。

1. 新しい合鍵を作る → 2. 手元の設定に収める → 3. **使っている道具すべてに反映** →
4. **新しい合鍵で入れることを確認** → 5. **旧の合鍵を消す** →
6. ★**旧の合鍵だけを送って、断られること（401/403）を確かめる**
   （★ブラウザのログイン状態やキャッシュを使わず、**古い資格情報だけを明示して送る**方法で行います） →
7. 新しい合鍵で**3幅の撮影が通ること**を確認

★**期間は、裁定をいただくまで「選べる最短」で作ります。**
　**90日を厳密にするか（APIの経路を調べる）／1年で作って90日で入れ替える運用にするか**は、
　**司令塔の裁定事項**として残します。**裁定の前に長い期間のものを作ることはしません。**

## まだできていないこと（この追記の分）

- **合鍵の入れ替えと失効の実測**（上記の1〜7すべて）。**旧の合鍵は有効なままです。**
- **90日を厳密にする経路（API）を使うかどうかの裁定**と、**実施の期限・担当の確定**。
- **旧の合鍵が使われていないかの確認**（記録の見方をまだ整えていません）。
- 旧識別子3種の再測定は、予定どおり**2026-08-12の別便**です。

## Codexの独立レビュー＝実施。**重大5件・中7件・軽微4件を反映**

| 度合 | 指摘 | 対応 |
|---|---|---|
| **重大** | 作成日時・期限・容量・本数など、秘密値でなくても手がかりになる | **削除・一般化** |
| **重大** | 「APIで作るには別の鍵が要る」は断定しすぎ | **取り下げ**（「権限を持つ手段が要る／いまある手段で足りるかは未調査」に） |
| **重大** | 裁定待ちと言いながら1年で作る方針は矛盾 | **「裁定まで最短で作る／長い期間は作らない」と明確化** |
| **重大** | 入れ替えの手順に「使っている先すべてへの反映」が無い | **7段の順序を明文化** |
| **重大** | 失効の試し方が書かれていない | **「古い資格情報だけを明示して送り、断られることを見る」と明記** |
| 中 | ハッシュを公開文に載せるのも避けるべき | **値は非公開の記録に置き、「一致を確認」とだけ記載** |
| 中 | 「この1回で入った」は証明ではない | **「判断しています」に変更** |
| 中 | 「111本がそろって落ちた」は言い過ぎ | **「111か所を確認・1本で再現」に限定** |
| 中 | 「復旧」「正常」の範囲が広い | **「その読み取り処理の成功を確認」に限定** |
| 中 | 「1件も読んでいません」は断定できない | **「取得する操作はしていません」に変更** |
| 中 | 旧の合鍵を残す判断の材料が足りない | **影響・範囲・気づく仕掛け・暫定の手当てを追記** |
| 中 | 無関係な資格情報の話は公開に不要 | **内容は非公開の記録へ移し、事実だけ1行に** |
| 軽微 | 「明朝」は後から読めない／保証が強すぎる表現 | **日付と担当を明記／「記載していません」に変更** |

★**採らなかった指摘が1つ**＝「合鍵・鍵の内側などの言い方を一般的な用語に置き換えるべき」。
**他の便・他のターミナルの報告と同じ言い方で通しているため、そのままにしました**（読み合わせのため）。

最終更新：2026-08-12

---

# システム開発ターミナル 報告（2026-08-12・合鍵の入れ替え7段／旧識別子3種の再測定）

使用モデル＝Opus 5

## 1. 合鍵（サービストークン）の入れ替え＝7段すべて実施し、結果を確認しました

裁定「管理画面で選べる最短期間で作る。APIで90日を厳密にする経路は調べない」に従いました。
**管理画面で選べるのは 1年／2年／5年／10年／無期限 の5つだけ**（本便でも再確認）なので、**1年**を選んでいます。
名前に「90日で入れ替え」と書いてありますが、**これは運用の意思であって、システム上の期限ではありません。
自動では失効しないので、90日ごとの入れ替えは人が思い出して行う必要があります。**

| 段 | やったこと | 実測の結果 |
|---|---|---|
| ① | 新しい合鍵を作る | 作成 2026-08-12 19:32／期限 2027-08-12 19:32（選べる最短＝1年） |
| ② | 手元の設定に収める | 先頭のBOMなしで書き込み。**この報告書・コミット・画像には値を書いていません**（道具は値を表示しない作りで、出た表示は鍵の名前と文字数だけでした） |
| ③ | 使っている道具に反映 | 手元の作業フォルダ全体を「合鍵の名前」で検索して見つかったのは**3本**。**その3本はいずれも手元の設定を読む作りで、直書きは見つかりませんでした**（書き換え不要）。★検索したのはこの作業フォルダの中だけです |
| ④ | 新しい合鍵で入れることを確認 | ダッシュボードのトップに新しい合鍵だけを付けて要求→**200**（鍵なしだと302でログイン画面へ送られる。つまり鍵で通れている） |
| ⑤ | 旧の合鍵を消す | 1回目は**拒否**（ポリシーがまだ参照中）。ポリシーから外して保存→削除成功。一覧は**1件**に |
| ⑥ | **旧だけを明示して送る** | そのままの形＝**302**（鍵を渡さないときと同じ＝ログイン画面へ）／XHRの形＝**401**／新＝200 |
| ⑦ | 3幅撮影が通ることの確認 | 3ページ×3幅＝**9枚**を保存。頼んだ幅と画面が答えた幅が **9/9一致**。お名前の残り **0件**（機械検査）。うち2枚は**自分の目でも見て**、幅ごとに並びが変わること・お名前が伏せられていることを確認 |

★**⑥について正直に**：裁定の文面は「401/403を実測」でしたが、**そのままの形では 401 も 403 も返りません。
302 でログイン画面へ送られます**（鍵を渡さないときと全く同じ応答）。
`X-Requested-With: XMLHttpRequest` を付けた形にすると **401** が返ります。
**確認した2つの形では、どちらも中身に到達できませんでした。**
（★確認したのはこの2つの形だけです。あらゆる経路で使えないことまでは確かめていません。）
数字が裁定の文面と違うので、そのまま書いています。

★**⑤で分かったこと**＝**今回の旧の合鍵は、ポリシーから参照されている状態では削除を拒否されました**
（「すべての参照を削除して再試行するか、ローテートしてください」と表示）。**他のすべての場合にそうなるかは確かめていません。**
**順序は「新をポリシーに足す→新で入れることを確認→旧をポリシーから外す→旧を削除」**になります。
（裁定どおり、**旧を先に消してはいません。**）

### 8/11に作れなかった理由について（観測できたこと／考えていること）

**観測できた事実**＝8/11に使っていたタブは、**表示領域が 0×0**（画面が「隠れている」状態）でした。
その状態では撮影がパラメータ異常で失敗し、選択肢も開きませんでした。
**新しいタブを作ったところ、同じ操作がすべて通りました。**
**考えていること**＝この 0×0 が原因だと考えていますが、**因果までは確かめていません**。
言えるのは「**新しいタブでは再発しなかった**」ことです。
（ブラウザ拡張そのものの故障は、今回は確認できませんでした。故障していないと断定はしません。）（ウィンドウを前面に出す操作・キー送信は、恒久禁止のとおり一切していません。）

### あわせて是正した自分の失敗を1件

作業の途中、**手元の設定の控えを、誤って作業用リポジトリの中に書き出していました**（フォルダ名が壊れた形で作られていた）。
書き出し先のフォルダ名を見たところ、区切り文字が失われた形になっていました。**自分の書いたコマンドの書き方が原因だと考えています**（同じコマンドでの再現までは試していません）。
**Vaultの正しい場所へ移し、リポジトリ内の該当フォルダは消しました。**
確認した範囲＝**gitの追跡対象に入っておらず、コミットもしていません**（`git ls-files` で確認）。**外部へは送っていません**（pushしたのは今回の報告と画像だけです）。

## 2. 旧識別子3種の再測定（8/11の差し替えから約31時間後の時点で確認）

**3種とも、旧の識別子では従来の中身が出ず、トップへ転送されることを確認しました。**
★**「消えた」わけではありません。** 旧の識別子は今も応答し、**301でトップへ送られます**。
中身が読めなくなったことが確認できたので、**「中身の無いページ」案は不要**と判断しました。

| 対象 | 旧の応答 | 遷移先 | 前回（8/11直後）との差 |
|---|---|---|---|
| 3種すべて | **301**（応答はある） | **トップ（/）** | 200（中身が出る）→ **301（トップへ）** に変化 |

- **毎回ちがう問い合わせ文字を末尾に付けて**（キャッシュに当たらないようにする書き方）**HEAD・GET を各3回**ずつ実行（計18回）、すべて 301。
- **サーバーが返したHTMLの中を調べ、申込の入力欄にあたる要素が 0個**であることを確認。
- 転送の生存＝**旧あて3本すべて生きている／行き先がトップ以外のもの 0件／新しい識別子へ向いているもの 0件**。
- 空振りの検査＝でたらめな識別子は **404**、大文字違いは **301** でした（事実のみ。大文字違いまで転送が効いている点が想定どおりかは、別途見る必要があります）。
- 新しい側は 3件とも、**確認した3項目（200・検索よけあり・入力欄4つ）は想定どおり**でした（送信・保存・通知までは見ていません）。

★**いつ切り替わったかは分かりません。** 途中を見ていないので、言えるのは
「**差し替えから約31時間後に見たら、もう切り替わっていた**」ことだけです。
8/10のときは約15時間後の確認で切り替わっていました。**「15時間で切り替わる」と決めつけないでください。**

最終更新：2026-08-12

## 3. Codexの独立レビュー（本便・14件の指摘）

**重大3件・中7件・軽微4件の指摘を受け、11か所を書き直しました。**

| 重さ | 指摘 | 直し方 |
|---|---|---|
| 重大 | **「無効化を確認」は誤解を生む**（旧の識別子は消えておらず、今も301で応答している） | **「従来の中身が出ず、トップへ転送される」に変更**。「消えたわけではない」と明記 |
| 重大 | **「約31時間かかった」は実測でない**（途中を見ていないので、いつ切り替わったかは不明） | **「31時間後に見たら切り替わっていた」に変更** |
| 重大 | **8/11の不調の原因を断定しすぎ**（0×0だったことと、それが原因であることは別） | **観測できた事実と、考えていることを分けて記載** |
| 中 | 「7段すべて実測」の「実測」の意味が段ごとに違う | 「7段すべて実施し、結果を確認」に変更 |
| 中 | 「使っている道具すべて」「直書きはゼロ」の調べた範囲が不明 | **「この作業フォルダの中を検索して見つかった3本」**と範囲を明記 |
| 中 | 「HTTP 200」だけでは何が確認できたか伝わらない | **鍵なしなら302になることと並べて記載** |
| 中 | 旧の合鍵が「使えない」ことを一般化しすぎ | **「確認した2つの形では」**に限定 |
| 中 | 削除拒否を一般仕様のように書いている | **「今回の旧の合鍵は」**に限定 |
| 中 | 「正常」「効きすぎていない」は評価であって事実でない | **事実だけ書き、評価は外した** |
| 中 | 「値は画面にもログにも出していません」の保証が強すぎる | **「この報告書・コミット・画像には書いていません」**に限定 |
| 軽微 | 「疎通」「キャッシュを避けて」「生の応答」が曖昧 | **やったことを具体的に記載** |

★**採らなかった指摘が1つ**＝「冒頭の内部レビュー表が正式報告と混在している」。
**これは前の便で既に出した部分**で、書き換えると司令塔が読んだものと食い違うため、そのままにしました。

最終更新：2026-08-12

---

# システム開発ターミナル 報告（2026-08-12・アクセス記録の見方の確立／合鍵で読める範囲の棚卸し）

使用モデル＝Opus 5

## 1. アクセス記録の見方を確立し、旧の合鍵の使われ方を確認しました

**手順書＝`docs/手順_合鍵のアクセス記録の見方.md`**（当リポジトリ側。宛先・識別子の値は書いていません）。

### 旧の合鍵（削除済み）の使われ方＝**当方の作業機以外からの利用は見つかりませんでした**

| 見たところ | 実測 |
|---|---|
| 記録の全件 | **1,384件**（2026-08-11 11:12 〜 2026-08-12 20:46・JST） |
| 出入りした身元 | **3つだけ**＝新しい合鍵／旧の合鍵／**代表ご本人のログイン2件** |
| 旧の合鍵の利用 | **1,196件**。使われた期間＝作った9分後から、削除した時まで |
| **出どころ** | **3つとも同じ1つの場所（同じ回線）から。国は日本のみ** |
| 拒否の記録 | **0件**（ただし下記のとおり、これを安心の根拠にはできません） |

### ★遡れる限界（ここは正直に）

- **記録は「鍵を掛けた時点」から。それ以前は1件もありません。**
  鍵が無かった頃は記録の仕組み自体が働いていないので、**「記録が無い＝誰も見ていない」ではなく
  「見たかどうか分からない」**です。
- **旧の合鍵が存在した期間は全部、記録が残っています。** そのため
  「旧の合鍵が当方の検品以外で使われた形跡」については、**遡りの穴なく確認できています。**
- **記録が何日で消えるか（保持期間の上限）は未確認**です。今回は鍵を掛けたのが新しく、
  上限に当たっていないため測れませんでした。

### ★この記録で「言えないこと」（実験して分かった）

**でたらめな合鍵で3回要求したところ、3回とも断られましたが、記録は1件も増えませんでした。**
つまり **「侵入の試みは無かった」とは、この記録からは言えません。**
言えるのは「**入れた出入りの中に、当方以外のものは無かった**」までです。

★なお、あらゆる拒否が残らないと決めつけてもいません（人のログイン失敗などは試していません）。
記録の1件ずつには「許可されたか」という項目があり、**拒否が入り得る作りではあります。**

### 途中で踏んだ失敗も手順書に残しました

**時間帯で区切って集めるやり方をすると、混んだ時間帯が1000件で切られて見落とします。**
細かく割ると呼び出しが爆発して途中で止まりました（実際に止まりました）。
**正しいのは「いちばん古い時刻を次の終端にする」数珠つなぎ**で、2回の呼び出しで取り切れます。

## 2. 新しい合鍵で読める範囲の棚卸し（道具を作って全経路を実測）

**道具＝`scripts/dashboard-reach-inventory-20260812.mjs`**（中身の値は一切表示しません）。

| 群 | 実測の結果 |
|---|---|
| 画面 15本 | 鍵があれば**すべて読めます** |
| 見本データ 4本 | **すべて読めます**（作り物のお名前入り） |
| **実データ 4本**（受付台帳・メール・入金・注文） | **すべて404＝まだ載っていません** |
| 基準・暦・伝言 | 読めます／送信の記録は404 |
| 設定ファイル一式 | **すべて読めます** |
| 手元だけの物（各自の設定・合図の印） | **404＝配信されていません**（正しい状態） |
| 画面やJSから参照先をたどって追加で確認 | 6件見つけましたが**すべて404**＝一覧の外に読めるものは見つかりませんでした |
| **鍵を渡さずに開いたとき** | **すべてログイン画面へ送られます＝外からは何も読めません** |

### ★実データ接続前に狭めるべき点（案・実施は裁定待ち）

**いちばん重いのは、設定ファイルが読めることです。** ここには外部の道具への入口が入っており、
**合鍵を持つ者はその一覧をそのまま読めます。**

**いまはまだ危険ではありません**＝合言葉の欄は**空**で、つなぐ先も**まだ公開されていません**（実測）。
**危ないのはこれから**です。実データにつなぐ時に合言葉を書き込むと、
**合鍵を持つ者＝合言葉も持つ者**になります。しかも**その接続先は鍵の外側**にあるため、
合言葉さえあれば**どこからでも**台帳のデータを読めてしまいます。

| 案 | 内容 | 手間 |
|---|---|---|
| **A（推し）** | **合言葉を配信物に書かない。** 鍵が出す身元証明を接続先で確かめる形にする | 中 |
| **B** | 合言葉は置くが、接続先を**読むだけ**に限定する（次善） | 小 |
| **C（推し）** | **実データを配信物に載せない**（いまの状態を維持し、画面から都度取りに行く） | 小 |
| **D** | 検品用の合鍵は**見本だけの別の配信先**に作り、実データ側には作らない | 大 |
| **E（推し）** | 期限は1年しか選べないので、**90日での入れ替えを暦に載せる**（自動失効しません） | 小 |
| **F** | 断られた試みも記録したいなら、記録の外部送信を検討（**有料の可能性あり・要裁定**） | 未調査 |

**当方の推し＝A＋C＋E。** 詳細と実測値はVaultの非公開の調査記録に置いてあります。

## 3. Codexの独立レビュー

**重大9件・中3件・軽微3件**の指摘を受け、**手順書と道具の両方を直しました**。主なもの：

| 重さ | 指摘 | 直し方 |
|---|---|---|
| 重大 | **「断られた試みは残らない」は一般化しすぎ**（試したのは1種類だけ） | **「存在しない合鍵での要求は出てこなかった」に限定**し、未検証の場合を列挙 |
| 重大 | **窓口が失敗しても「0件」と読んでしまう** | 失敗を検出して**止め、警告を出す**ようにした |
| 重大 | **身元を先頭8文字に縮めると、頭が同じ別人が1つに潰れる** | **縮めず真ん中だけ伏せる**。本当の身元の数も別に出す |
| 重大 | 同じ時刻に1000件以上あると**黙って取りこぼす** | **警告を出して止める**＋この方法の限界を明記 |
| 重大 | 遡る開始時刻とページ上限が固定で、**打ち切りが見えない** | 頭で指定する形にし、**上限に当たったら「未完了」と出す** |
| 重大 | 道具が**決め打ちの一覧しか見ておらず「何が読めるか」に答えていない** | **画面やJSから参照先をたどって追加確認**する仕組みを足した（結果は上の表） |
| 重大 | 道具の出力が**秘密の「所在と件数」を残す** | 保存前に**本文を必ず捨てる**検査を追加（出力先はgit管理外であることも確認） |
| 中 | 目印が無い行をまとめて捨てる／「どこへ」を集計して出していない | 複数項目で見分ける形にし、**経路と国も出す**ようにした |

★**Codexの指摘のうち「鍵なしの確認が3経路だけ」は、今回は直していません。**
鍵なしで開いた3経路がすべてログイン画面へ送られており、
**鍵は経路ごとではなく配信先ごとに掛かる**ためです。ただし**全経路では確かめていない**ので、
そのままにしていることを書いておきます。

## 4. 恒久記録

**`CLAUDE.md`に1行足しました**＝**予約便は、現行便の完了報告を出した時点で、合図を待たずに自分から着手する。**
（例外4つ＝法的文書の実反映／決済の本番設定／不可逆な削除／代表のログインが必要な操作、は従来どおり事前確認。）

最終更新：2026-08-12
