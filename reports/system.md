# システム開発ターミナル 報告（2026-07-25・第10便）

## 【決定1】訂正指示ファイルの置き場所＝本体CSVと同じフォルダで確定

Vault設計書に記録しました。リサーチTへの確認は不要（実装のデフォルト探索パスも既にこの場所に合わせてあります）。

## 【決定2】299名差分の本格着手＝次の次

システム開発T側の追加実装は不要のため、記録のみ行いました。リサーチTが`artists_corrections.csv`を作る準備が整った時点で動き出します。

## 【決定3】artist_i18n（よみがな）訂正の穴＝当面は個別対応、判断待ちマスターに記録のみ

判断待ちマスターA-22として新規追加しました。本日発生した読み違い2件（深山龍洞「ふかやま→みやま」、吉田稔郎「みのる→としお」）を実例として明記し、設計は行わず記録に留めています。

## 【訂正】「雅号の入力欄がない」の誤りを修正

自分の記録のうち4箇所（責任者ファイル・全社ボード・SEO調査書・第9便検証書）に該当記述がありました。**Vaultの追記のみ原則に厳密に沿う形で、元の文章は残したまま、直後に訂正を追記する方式**で修正しました（初稿では元の文言ごと書き換えてしまっており、Codexレビューで「追記のみ原則に厳密には適合していない」と指摘され是正しました）。

## 本日の教訓をCLAUDE.mdに整理

以下3点を「本日の教訓」節として新設しました：
1. consoleErrorノイズ判定で件数同士の相殺（Math.min方式）を使わない
2. 出所不明の「確認済み」情報をコード・報告に残さない
3. DOM上に存在することと画面に実際に表示されていることは別

**Codexレビューで、③の説明文中「実際の表示・再生に影響しない」という記述が実測範囲を超えた過大表現だと指摘され、「利用者への実害の有無は、コンソールエラーだけでは判定できない」という正確な表現に訂正しました**（page-audit.mjs側の記録では初期表示への確認のみで、再生ボタン操作後の検証はしていないため）。

---

## Codex独立レビュー（第10便）

4件の指摘（うち2件「高」）を受け、すべて反映しました：
1. **【高】Vault追記のみ原則との不整合**＝訂正4箇所のうち一部で元の文章自体を書き換えていた→**元の文章を残したまま直後に訂正を追記する方式に修正**
2. **【高】CLAUDE.mdの「再生に影響しない」が実測範囲を超えた過大表現**→**「実害の有無はコンソールエラーだけでは判定できない」に訂正**
3. **【中】第9便検証書に、第10便決定と矛盾する古い記述（「299名差分は次に必要」「置き場所は要相談」）が残存**→**第10便で解消済みである旨を追記**
4. **【中】判断待ちマスターA-22の末尾が「設計しない」方針に対しやや踏み込んだ記述**→**「将来、別途安全設計が必要」程度に弱めた表現に修正**

---

## 代表対応待ち（変更なし・3件）

Google Search Console登録／A-20（動画サムネイル黒表示）／doubleclick由来consoleErrorの再生影響。いずれも代表が実機で行うため追跡していません。

## 現状

システム開発Tの担当で新規に着手すべき作業は現時点でありません。上記の記録整理を終え、待機します。
