# システム開発ターミナル 報告（2026-08-11・設定を鍵の内側へ／台帳の手順書／メールの仕上げ）

★**非公開のURL・識別子の実値は書きません。社内の方のお名前も書きません。**
証拠＝`reports/assets/20260811_設定を鍵の内側へ/`（3幅×3画面＝9枚＋鍵の内側のライブ2枚）

---

## A．A案の実装＝**ボタンが動くようになりました。外からは読めません**

### やったこと

- 非公開の設定を**鍵の内側の配信物に入れる**仕組みを作りました。
  ★**入るには2つそろう必要があります**（実行時の指定＋合図の印）。片方だけでは絶対に入りません。
- **配信物の出発前検査は緩めていません。** 合鍵（トークン）・パスワード・秘密鍵は**いまも止まります**。
  今回外したのは**「置き場所・接続先のURL」の検査だけ**で、しかも**その1ファイルに限って**です。
- 全13ページに設定の読み込みを戻しました（★空の入れ物を常に置くので、**404になりません**）。

### 実測（鍵の内側）

| 実測 | 結果 |
|---|---|
| 設定の入れ物が読み込まれたか | **はい**／中身入りの印＝**はい** |
| 行き先が入った項目 | **11本**（Webメール・注文一覧・管理画面・LINE・受付台帳・Box各所） |
| 画面に「まだ設定されていません」と出る数 | **1**（＝残りは動くようになりました） |
| ライブから取り直したハッシュ照合 | **26/26 一致** |

### ★外から読めないことの実測＝**24経路すべて鍵の内側**

**新旧2つのURL × 本番/デプロイ固有 × 6つの道すじ（設定ファイル本体を含む）＝24通り**を、
ログインなしで叩きました。**すべて302（Cloudflare Accessのログインへ）で、中身は1バイトも返りません。**
　`/assets/config.private.js` を名指しで叩いても**同じく302**です。

### ★画面に値が出ていないことも機械で確認

**14枚×3幅＝42通り**で画面の文字を調べ、**URLは1本も表示されていません**（ボタンの行き先に入るだけ）。
＝**証拠のスクリーンショットを公開しても、値は写りません。**

### まだ動かないもの（正直に）

**4人での共有（印・予定・伝言）は、まだ各パソコンにだけ残ります。**
理由＝共有の置き場所（Googleのスクリプト）が**まだ公開されていない**ため。
**代表の操作1回で動きます**（下のB）。画面にも「★ほかの3人の画面には出ません」と正直に出しています。

## B．受付台帳の読み取り設定の手順書＝**デスクトップに置きました**

**`C:\Users\ataga\Desktop\いまやること_受付台帳をつなぐ_1画面ずつ.txt`**

- **1画面ずつ・押すボタンの名前まで**書いてあります（手順①〜⑧）。
- ★**この1回の設定で2つが一度に動きます**＝**受付台帳の読み取り**と**4人での共有**。
- 貼り付けるコードも用意済み（`docs/apps-script/dashboard-api.gs`）。
  **受付台帳のシートには1文字も書きません（読むだけ）。消す機能はありません。**

★**正直に書いたこと（手順書の中にも明記）**
　当方は代表のGoogleの画面を開けません。そのため
　**英語の表示名はGoogleの公式の説明書から取った確かなもの**、
　**日本語の表示名は「★未確認・推測」と明記**しています。
　違っていたら画面を送っていただければ直します。**推測のまま進めても壊れません。**

★「アクセスできるユーザー＝全員」を選ぶ理由と、その意味（URLを知っている人という意味であること、
　できるのは読む／足すだけで書き換え・削除はできないこと）も、手順書に隠さず書きました。

## C．メール接続の道具＝**登録すればすぐ動く状態にしました（登録はしていません）**

| 実測 | 結果 |
|---|---|
| 取り込みの道具を実際に走らせた | **動きました**（**248通**を書き出し・所要は書きません） |
| 上げる工程が動かないこと | **動きませんでした**（★合図の印が無いため＝設計どおり） |
| 定時実行の登録（見るだけ） | **登録される中身を確認**（30分ごと・ログインしている間だけ）。**登録はしていません** |
| 後始末 | **書き出した実データは消しました**（つなぐのは合図のあとのため） |

### ★「このパソコンが動いている間だけ」を画面に出しました

メールの画面の、最終更新時刻の**すぐ下**に、小さく次の一行を常に出すようにしました。

> この一覧は、会社のパソコンが動いている間だけ新しくなります（そのパソコンが止まっている間は増えません）。

- **古い/新しいにかかわらず、常に出ます**（新しいときだけ隠す、という作りにしていません）。
- 従来からの**「2時間を超えると赤くなる」**表示はそのままです。
- 3幅すべてで表示を確認しました（証拠の画像）。

## 証拠の画像

- `PC1440_*` / `タブレット800_*` / `モバイル390_*`（**3幅×3画面＝9枚**）
  ＝**ライブと同じ配信物**を手元で描画したもの（**ライブとのハッシュ一致26/26を確認済み**）。
- `ライブ_道具の一覧（鍵の内側）.jpg` / `ライブ_メール画面（鍵の内側）.jpg`
  ＝**実際のライブ**（鍵の内側）で撮ったもの。**ボタンが「開く」に変わっていること**が見えます。

★**なぜ9枚を手元で撮ったか（正直に）**＝ライブの撮影に使っている道具が、
**窓の大きさを変えても切り取る範囲が変わらず**、3幅の撮り分けができませんでした。
そこで**ライブと1バイトも違わない同じ配信物**を手元で描画して撮っています。
**ライブそのものの3幅は撮れていません。**

## まだできていないこと

- **4人での共有は動いていません**（代表の操作1回待ち＝B）。
- **実データは1件もつないでいません**（見本のまま）。
- **旧識別子3種の無効化は未達**（本日の裁定どおり明日朝に再測定します）。
- ライブそのものの**3幅の撮影はできていません**（上記の理由）。
- 検査で外したのは「置き場所・接続先」だけですが、
  **「鍵の内側なら安全」と言い切れるのは、鍵が正しく効いている間だけ**です。
  本日の24経路の実測は**その時点での確認**であり、今後も定期的に測り直す必要があります。

最終更新：2026-08-11
