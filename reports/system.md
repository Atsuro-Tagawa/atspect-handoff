# システム開発ターミナル 報告（2026-08-10・ダッシュボード再設計 第5便・段2＝実装）

設計図6枚のとおり実装しました。**実データは接続していません（見本のみ）。**
証拠の置き場所＝`reports/assets/20260810_ダッシュボード段2/`（詳細＝`00_段2の実装記録.md`）

## 1. カレンダーは **(a) 自前保存** に決めました（基準①で不合格）

まっさらなブラウザ（Googleにログインしていない状態）で埋め込みを開いたところ、
**予定ではなく「Sign in to continue to Google Calendar」というログイン画面が出ました**
（画像＝`根拠_埋め込みカレンダーはログイン画面が出る.png`）。
①を通すにはカレンダーを**公開**にする必要があり、営業日程には訪問先が入るため選べません。
**→ 指示どおり「1つでも駄目なら即(a)」で実装しました。**
予定は**足すだけ**・**取りやめを足す形**・誰の予定かは**名前の文字**（色分けなし）。

## 2. 実装したもの

| 決定 | 実装 |
|---|---|
| 1 設計図どおり | メール・トップ・あつぺくと中ページを作り替え。全14ページを新しい見た目に統一 |
| 3 初期データ | 読売書法展（新美8/21-30・都美8/23-29）／工芸集中（都美9/28-10/4・新美11/25-12/7）**のみ** |
| 4 メール取り込み | 候補②の仕組みを完成（定時実行の登録は**合図待ち**）。**取り込み時刻を常時表示・2時間超で赤** |
| 5 対応不要 | 私が対応します／済にする／**対応不要**／未対応に戻す。**済の中で対応した／対応不要を見分けられる** |
| 6 段階9語 | 作家台帳の9語へ。**旧15ステータスの対応表を成果物に残しました**（捨てた語は0） |
| 7 入金・A/B | 入金は中ページの「お金」だけ。A/B切替は廃止（`themes.css` を削除・残存0件） |

## 3. 実測（すべて機械＋目視）

- **6画面**：横あふれ0／タブは390pxで**1段**／44px未満の押す所0／JSエラー0／想定外の404が0。
  **トップはPC1画面に収まっています（864px／900px）。**
- **全14ページ×2画面幅＝28通り、すべて問題なし。**
- **操作の通し15項目すべてPASS**（見本データで実際に押しました）：
  「私が対応します」→「対応不要」→「未対応に戻す」で**記録は3件のまま＝1件も消えていない**／
  押した人の名前が残る／済の中で対応不要と見分けられる／迷惑・その他は未対応に出ない／
  カレンダーに書き込み→読み込み直しても残る／伝言も誰が・いつが入る。
- **配信物に実データは1つも入っていません。** 実データを入れる指定を付けても、
  切り替えの印が無ければ**止まります**（終了コード1で実測）。

## 4. ★代表の操作待ち・次の一手

1. **実データへの切り替え**＝関門2つが未達（**代表の初ログイン実測**／**許可リスト4件目の個人化**）。
   合図をいただければ切り替えます。手順書は用意済み。
2. **定時実行の登録**（`-WhatIf` で内容だけ確認済み。登録はしていません）。
3. **印・予定・伝言を4人で共有する置き場所**が未設定。いまは各パソコンの中に残ります
   （画面に必ずそう出ます）。共有にするには置き場所の設定が要ります。
4. **エピグラフの文言**（仮置き中。`data/board.json` の1行を書き替えるだけ）。
5. ★**「都美」＝東京都美術館・「新美」＝国立新美術館と読みました。** 違っていたらお知らせください。

## 5. 正直に、していないこと

- 実データ切り替え・定時実行の登録・`mail-auto-refresh` の実行（構文検査のみ・0件）はしていません。
- **実機（スマートフォンの実物）では見ていません**（幅390pxの描画まで）。
- **Codexの独立レビューは受けていません。**
- 受付台帳の実データの段階の語は書き替えていません（正本はスプレッドシート・受付担当の仕事のため）。
- ★**寄せ先が決まらなかった語が1つあります＝「物故」**。台帳側で未決（見送りで代用しない）のため、
  未確認に置いて理由を残しました。

最終更新：2026-08-10
