# システム開発ターミナル 報告（2026-08-10・9面ターミナル2便目＝既定モデルOpus5化）

## 実施内容

代表指示「9面ターミナルの既定モデルをOpus 5にする」を完遂。

- Claude Code既定モデルの指定方法3つ（①起動引数`--model`②設定ファイル`~/.claude/settings.json`の`model`キー
  ③環境変数`ANTHROPIC_MODEL`）を実測で検証（正常値でOK応答・不正値で明確なエラーの両方を確認）。
  いずれも実在・機能することを確認した上で、**①起動引数を採用**。
- 理由＝②のグローバル設定ファイルを書き換えると、8分割を含む全claude起動に影響し
  「8分割には触らない」指示に反するため。
- **9面専用の新規WTプロファイル9件**（GUID `a7599999-...`系列・`commandline`末尾を
  `claude --model opus`に変更）を追加。**8分割が使っている既存プロファイル9件（`a7591111-...`系列）は
  1文字も変更していない**（引き続きSonnet 5既定のまま・8分割の挙動は完全無変更）。
- `launch_ARTS9_command_center.ps1`のGUID参照を新プロファイル群に差し替え、変更理由をコメントに明記。
  `9面ターミナル.bat`にも運用ルール（大量作業でSonnet使用時は`/model sonnet`をその便で指示）を追記。
- 編集前バックアップ済み（`C:\Vault\ARTS-RESPECT\_backups\_auto\wt-settings.json\`・
  `...\launch_ARTS9_command_center.ps1\`）。
- **実際に1回起動し、9ペイン全部のヘッダーに「Opus 5 · Claude Max」表示をスクリーンショットで確認済み**
  （起動ログも12操作すべて成功）。**フォーカス移動・キー送信は一切使わず**、ウィンドウ生成直後の
  OS標準の状態のまま`CopyFromScreen`のみで撮影（後述の恒久禁止に準拠）。入力欄への文字混入も今回はゼロ。

## ★恒久禁止を追加（代表指示・全ターミナル共通）

前便（1便目）の検証中に発生した「別ウィンドウへの文字列誤混入」事故を受け、代表より
「他ウィンドウへのフォーカス移動・キー送信の自動操作を全ターミナル共通で禁止」の指示。
**グローバルCLAUDE.md（`~/.claude/CLAUDE.md`）に恒久ルールとして追記済み**（本ターミナルが編集担当）。
自分の引き継ぎ書（`_引き継ぎ_システム開発_20260809_ATSPECT.md`）にも追記済み。

## Codexレビューの有無

**未実施。** ローカルのランチャー設定変更のみで、本番反映・対外公開・作家原稿のいずれにも該当しないため
現行の運用方針の対象外。

## 代表対応待ち事項

**なし。** 今回（2便目）は副作用ゼロで完了。前便（1便目）で報告した「あいでぃあ」混入の件は
確認・対応をお願いした状態のまま（前便報告を参照）。

## 証拠の置き場所

`reports/assets/20260810_9面ターミナル/`（`9面グリッド_Opus確認.png`・`既定モデルOpus5化.md`・起動ログ）

引き継ぎ＝`_引き継ぎ_システム開発_20260809_ATSPECT.md`末尾の2026-08-10追記を参照
最終更新：2026-08-10
