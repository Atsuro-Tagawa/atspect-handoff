# システム開発ターミナル 報告（2026-08-02・第4便）

使用モデル＝Opus 5（1M context）／作業＝月額導線の販売プラン実測・A-1の受け皿・Codex残り3本

## 1. ★最優先だったこと＝月額の導線に、販売プランが付いていませんでした

**いま配布用に記録されているカートパーマリンクでは販売プランが付きません。
そのまま作家さまにお渡しすると、3,300円が1回きりの支払いになり、翌月以降の自動継続が始まりません。**

| 経路 | 販売プラン | 根拠（実測） |
|---|---|---|
| ①下書き注文→請求書 | **付けられない** | `DraftOrderLineItemInput` の全14項目に該当する入力が存在しない（APIスキーマを実測）＝請求書経路は情報登録料33,000円（1回きり）専用 |
| ②カートパーマリンク（現・配布用） | **付かない** | 決済画面の内部データで商品行の `sellingPlan` が **null（4箇所すべて）**。画面にも定期購入の表示なし |
| ③`/cart/add?...&return_to=/checkout` | **付く** | 同じ内部データが `sellingPlan: {"name":"1か月ごとに配達"}`。画面に「定期小計 1か月ごとに ￥3,300」「支払いを続けることにより…キャンセルポリシーに同意…」が出る |

`/cart/add.js`（AJAX）でも `selling_plan_allocation` が付くことを確認＝**プラン側の設定に問題はなく、
パーマリンク形式だけがプランを落としています。**

**画像＝`reports/assets/20260802_月額導線の販売プラン実測/`（01がNG・02と03がOK）**

### ★もう1つ確定＝販売プラン名「1か月ごとに配達」は決済画面に出ます

正しくプランが付くと、**商品名のすぐ下に「1か月ごとに配達」と表示されます**（画像02・03）。
＝**作家さまの目に「配達」の語が入ります。** 代表指示の但し書きに該当するため、プラン名の変更を**再起票**しました。

### 実施していないこと

- **リンクの差し替えはしていません**（決済の本番設定＝4例外）。正しいURLは**Vaultにのみ**追記しました。
- **決済を1件も通していません**。したがって「支払ったあとに実際にサブスク契約が作られるか」は**未確認**です。
- 検証で使ったカートは空に戻してあります（注文は1件も作っていません）。

## 2. A-1（代表のテスト決済）の受け皿を1枚作りました

`reports/assets/20260802_月額導線の販売プラン実測/手順_A-1_代表のテスト決済_1枚.md`
（URL入りの別紙＝Vault `_手順書_A-1代表のテスト決済_20260802_ATSPECT.md`）

**★冒頭に書いた最重要事項＝テストモードは「オフ」でした**（`設定→決済→Shopifyペイメント`の最下部で実測・画像05）。
**このまま決済すると本物のカードから引き落とされます。**
A案（先にテストモードをオンにしてテスト用カード `4242…`）とB案（本番で払って返金）を並記し、
**テストモードの切り替えは代表ご自身の操作**としました。

チェック欄は指示どおり①〜⑧。**⑧の確認場所＝アプリ→Subscriptions→「サブスクリプション契約」**
（実際に開いて実在と現在0件を確認・画像06）。後始末は**削除せず、タグ`テスト`とメモを付けて残す**形にしました。

### ★A-6と推薦者コードは「同じ1回では確認できない」と分かりました

- **A-6＝いまの申込フォームに備考欄がありません。** ライブの入力欄を全数取得（27項目）して確認。
  `20_ご意見・ご要望` は**0件**でした（当方の手元のテーマの写しには残っていた＝**写しが古い**）。**欄が無いので成立しません。**
- **推薦者コードは別ページ・別世代のフォームです。** 推薦ページは `03_name`・`13_きっかけ`＋隠し欄
  `90_ご推薦者コード` の**古い形**、申込ページは `04_氏名`・規約同意・本文ハッシュの**新しい形**。
  **1回の送信では両方確認できません。** どちらも作家さま向けページ＝SNS運用T範囲のため、事実の報告にとどめます。

## 3. Codexレビュー（残り3本＋前便1本＝計4本完了・全件「問題なし」）

**1ファイル1観点。終了コードだけでなく本文を読みました。**

| 本 | 観点 | 結果 | 採否 |
|---|---|---|---|
| subscription_payment_reminder（前便） | Liquidの構文・分岐対応 | 問題なし | 指摘なし＝対応不要 |
| subscription_new | 削除前後の構文・分岐が実データで意図した側に落ちるか | 問題なし | 指摘なし＝対応不要 |
| subscription_payment_failure | 3分岐のいずれかで削除ブロックが必要にならないか | 問題なし | 指摘なし＝対応不要 |
| order_confirmation | 追加した `requires_shipping` 分岐と件名Liquidの妥当性 | 問題なし | 指摘なし＝対応不要 |

★payment_failureではCodexが独自にタグ数を数え、`liquid open/end 54/54`・`table 31/31`・`tr 33/33` の均衡を確認しました。
`td 52/51` の1件不一致がありますが、**変更前も58/57で同じ**＝Shopify初期文面から存在する既存の非対称であり、
**今回の変更で生じたものではありません**（採用＝現状のままとする。理由＝当方の変更に起因せず、
Shopifyの初期文面を書き換えると別の影響が出るため）。

## 4. 記録した先

- 繰り越し＝Vault `_繰り越しマスター_ATSPECT.md` ／ 本リポジトリ `reports/assets/deferred-master.md`（**両方とも末尾追記のみ**）
  - **閉じた**＝通知メール4本の文言／payment_failureの3分岐描画（第5便で確認済みとの訂正を反映）
  - **保留**＝APIスコープ承認が前提の作業（本日も `ACCESS_DENIED` を実測）
  - **再起票**＝販売プラン名（決済画面に出るため）
  - **新規4件**＝配布用リンクの差し替え／プラン名／申込フォームに備考欄が無い／推薦と申込のフォームが別世代
  - **申し送り2件**＝「毎月」「毎月更新」は固定文字（計10箇所・頻度を変えるときは必ず見る）／
    発送を伴うサブスクを作る場合は削除した3ブロックの復旧が要る（「デフォルトに戻す」で全文復旧可・ただし今回の文言是正はやり直しになる）
- 手順＝`手順_受付処理.md` 末尾に追記（公開リポジトリのためURLは書かず、Vaultを参照する形）

## 5. ★実施していないこと・判定していないこと

- 決済は1件も通していない。**サブスク契約が実際に作られるかは未確認**（A-1で初めて分かる）。
- **テストモードで通した場合にサブスク契約が作られるかも未確認**（A案で①〜⑦は見られるが⑧は結果が変わる可能性がある）。
- テストモードのトグルは**触っていない**。配布用リンクの差し替えも**していない**。
- 申込フォーム・推薦フォーム・作家さま向けliquidには**一切触っていない**（SNS運用T専任）。紙・パンフ・作家データにも触っていない。
- 返金の操作・返金手数料・着金までの日数は確認していない。
- 通知メール4本の文言は**指示どおり閉じており、今回は1文字も触っていない**。
