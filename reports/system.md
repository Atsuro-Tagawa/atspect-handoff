# システム開発ターミナル 報告（2026-07-27）

## 【1】Codexの利用可否を実測確認＝想定復旧時刻より早く使用可能

`codex exec --sandbox read-only`で簡単な応答を求めたところ正常応答（`codex-cli 0.144.1`）。想定復旧時刻（2026-07-29 10:52）より早く、本日時点で全社共通のCodex利用枠は復旧しています。

## 【2】CSV三状態化（訂正指示オーバーレイ）の受け入れ側テストを追加

2026-07-25第9便で実装済みの`artists_corrections.csv`によるCSV三状態化は、これまで実Shopify・実データ2名＋無効8パターンでの`check`/`plan`のみ検証済みで、`apply`は一度も実行されていませんでした。実データ・実Shopifyには一切触れないダミーデータのみの自動テスト`tests/artist-csv-corrections.test.ts`（vitest・13ケース）を新設し、読み込み・3状態判定（訂正/削除/noop）・重複指示の全行無効化・`artist_i18n.*`のplan除外・未マッチ検出・plan変換までを検証・全green。実装への変更は`scripts/artist-csv-sync.mjs`への`export`追加とCLI直接実行時のみ動く`isMain`ガードのみ（既存CLI挙動は無変更）。全171件のvitestテストがgreen、`tsc --noEmit`エラーなし。実データ・実Shopifyへの`apply`は今回対象外のまま。リサーチTの`artists_corrections.csv`が届き次第、`check`（自動検出）で実データ検証を開始できる状態です。

## 【3】申込動線（パンフ中面QR→フォーム→送信→受付）の通し確認＝自動化可能な範囲を実測、実送信はhCaptchaで自動化不能と確認

対象＝`https://atspect.com/pages/entry-14ffd27c805f`（entry_mode=direct・accept_submissions=true）。

**自動化ツールで確認できたこと**（PC1440／タブレット1280x800／タブレット800x1280／モバイル390の4viewportすべて）：ページGET＝HTTP 200・リダイレクトなし・コンソールエラー0件。読了ゲート（規約全文を末尾までスクロールした結果、同意チェック欄4つ＋送信ボタンのdisabledが解除される）を確認。空欄のまま送信するとブラウザのネイティブ`required`検証でブロックされ画面遷移しないことを確認。同意チェック欄が未チェックの状態でも送信ブロック（＝同意しないと送信できない、を確認）。備考欄（`contact[20_ご意見・ご要望]`）は`required`なし・textarea内に正しくネスト。

**★実送信は自動化ツールでは到達不能でした**：PC1440でダミー試験データ（メール＝example.invalidドメイン、備考欄に`[SYSTEM-TEST-20260727-A]...実在の作家からの申込ではありません`と明記）を入力し、同意チェック欄5つにチェックして送信したところ、画像選択式hCaptchaのチャレンジが表示され、`/contact`へのPOSTは観察期間中0件（＝今回の操作による新規テスト受付は発生していません。送信件数＝0件）。全社ルールによりCAPTCHAは解いていません。2026-07-25に制作Tが同じフォームで記録した「2回連続でhCaptcha発火・自動テスト不能」と一致する再現です。

受付記録の届け先は、社内手順書（`_受付台帳_記入手順_20260725.html`）の運用に基づくとmedia@atspect.comへの通知メール方式（Shopify側の現行設定自体は今回未確認）。このメールボックスへのアクセス手段が無く、受信確認は権限外です。決済関連コードは対象フォーム・観測ネットワークの範囲では確認されず、決済には触れていません。

**Codex独立レビュー実施**（`codex exec`）＝断定しすぎの表現7点の指摘を受け報告に反映済み（IntersectionObserver発火は直接観測していない旨の明記／POST0件は「観察期間中」に限定／受信一覧の運用は手順書ベースで現行Shopify設定は未確認と明記／決済無しはスコープ限定表現に修正、等）。「hCaptchaが今回発火した＝あらゆる自動化で恒常的に不可能、までは証明していない」との指摘も受けています。

判断待ちマスターA-2（hCaptcha有効化＋テスト送信）は引き続きオープン＝残りは代表の実機操作（hCaptchaを正規に解いての実送信1回＋media@atspect.comでの受信内容確認）が必要です。

## 【4】運用ルールの更新（代表承認）

- `scripts/artist-csv-sync.mjs`のexport追加・受け入れテスト追加・`CLAUDE.md`更新をcommit（atspect-system。`git remote`無しのためpush対象なし）。
- CLAUDE.mdに恒久ルールを追加＝①テストコード・スクリプト・ドキュメント・`reports/`配下の報告commit/pushは代表承認なしで実行可（引き続き承認が必要なのは法的文書の実反映／決済の本番設定／データの不可逆削除／代表ログインが必要な操作の4つのみ）②作業が一区切りついたら指示が無くても自分からreports/system.mdを更新・push・SHA固定URL提示まで行う。

## 【5】代表対応待ち（今回発見・変更なし）

判断待ちマスターA-2の残り＝作家登録申込フォームの実送信テスト（hCaptchaを正規に解く必要があり自動化不可）。既存のテスト送信1〜2件の削除は従来どおり代表操作待ちのまま（今回は増えていません）。

---

詳細はVault `_全社ボード.md`追記187・188、`_判断待ち・未決事項マスター_ATSPECT.md`2026-07-27節2件、`_確定事項マスター_ATSPECT.md`2026-07-27節。
