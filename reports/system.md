# システム開発ターミナル 報告（2026-08-03・第5便＋割り込み）

モデル＝Opus 5（1M context）／担当＝あつぺくとシステム開発

## ★割り込み＝APIスコープの承認（最優先・完了。ボタンは1つも押していません）

**手順書＝`reports/assets/20260803e_APIスコープの承認手順/手順_APIスコープを増やす_代表向け.md`（画像7点）**

**★結論を先に：「ボタンを1つ押せば終わり」ではありませんでした。しかも最後の1手にたどり着けていません。**

- **拒否されているものを実測**（叩いて確認）＝入金／翻訳／言語一覧／注文／お客様／サブスク契約の6つ。
  Shopifyが名前を返したのは `read_shopify_payments`（または `read_shopify_payments_accounts`）／
  `read_translations`／`read_locales`（または `read_markets_home`）の3系統だけ。
  **注文・お客様・サブスク契約はShopifyがスコープ名を返さない＝名前は未確定**（推測で足していません）。
- **アプリ名＝「Claude API」**（開発者：あつぺくと・未掲載アプリ）。現在の許可は24件。
- **★ストア管理画面にはスコープを増やすボタンがありません**（3点メニューは「アプリを開く／詳細を表示する／
  アンインストール」の3つだけ、詳細ページも「アプリをアンインストール／アプリを開く」の2つだけ）。
- **増やす場所＝Dev Dashboard。** パンくず＝`設定→アプリ→「アプリを開発する」→「Dev Dashboardでアプリを開発」
  →アプリ一覧で「1 インストール」と書かれている Claude API→左メニュー「バージョン」→右上「バージョンを作成」`。
  過去の履歴もこの形（`add publications/inventory/files scopes` 等）。
- **★ただし版を上げるだけでは権限は増えません（実測）**＝有効な版が宣言している `read_customers`
  `write_draft_orders` が、ストア側の24件には入っていない。**ストア側の承認が別に要る。**
- **★その承認画面にたどり着けていません**（「アプリを開く」を押しても画面が変わらない）。
  **承認ボタンのラベルは分かりません。**
- **押した場合に何が起きるか（権限が増えるだけか／既存が外れるか／再インストールになるか／取り消せるか）は
  すべて未確認**。画面に情報がないため推測で書きません。**この状態で押していただくのはお勧めしません。**
- **押した後の確認方法は確立**＝`node scripts/_readonly-scope-audit-20260803.mjs` で ACCESS_DENIED が消えるか。

## 1. 一時停止の通知テンプレートを是正しました（5点まとめて1回・保存済み・再読込で確認）

`subscription_paused` を1回の編集で是正し、保存後にページを開き直して残っていることを確認しました。

| # | 直したもの | 変更後 |
|---|---|---|
| ① | 件名の「正常に」 | 件名を「システム利用料のお手続きを一時停止しております」に |
| ② | 「サブスクリプション」 | 見出しを「お手続きを一時停止しております」に |
| ③ | 「次回の**注文**は発生しません」 | 「停止している間、次回以降のご請求は発生しません」に |
| ④ | 「いつでも再開できます」 | **削除**し「お手続きの再開や、カードの有効期限などの更新が必要な場合は、当社より個別にご案内いたします」に |
| ⑤ | 管理ボタンのリンク先 | **お問い合わせページ**へ差し替え／ラベルを「当社にご連絡する」に（司令塔判断の中間案） |

**復旧手段＝この画面の「デフォルトに戻す」でShopify初期文面に全文戻せます**（編集前は初期文面のままでした）。

## 2〜4. 未着手（割り込み対応のため）

- 決済失敗の通知の**受信者追加の手順書**＝未作成。
- 「（税込）」＝SNS運用Tへ差し戻し済み（当Tから探し回りません）。
- A-1直後の段取り＝準備済み・代表待ち。

## 実施していないこと・判定していないこと

- **スコープ関連のボタンは1つも押していない**（アンインストール／ローテーション／バージョンを作成／アプリを削除）。
- **「バージョンを作成」の先の画面・ストア側の承認画面は見ていない。**
- 注文・お客様・サブスク契約の**スコープ名は確定していない**。
- 通知テンプレートは**プレビュー本文が描画されないため、実物の見た目は確認できていない**（コードは確認済み）。
- **テスト送信はしていない**（契約0件でいま誰にも届きません）。
- 受信者追加の手順書・税込の是正は**当便では作っていない**。

## 証拠の置き場所

`reports/assets/20260803e_APIスコープの承認手順/`（画像7点＋手順書）
※クライアントIDが写る画面はVaultのみ（このリポジトリは公開のため）。
