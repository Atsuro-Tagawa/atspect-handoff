# システム開発ターミナル 報告（2026-08-03・第4便）

モデル＝Opus 5（1M context）／担当＝あつぺくとシステム開発
**当便は設定を1つも変更していません。通知テンプレートも1文字も編集していません（読んだだけ）。**

## 0. 恒久ルールを書き込みました

**「本番データを書き換える直前に、必ずライブを取り直す」**をグローバルCLAUDE.mdと本ターミナルのCLAUDE.mdの
両方へ末尾追記（第3便で退行を1件防いだ型）。apply直前の再取得・実物の目視・`_STALE_適用しないこと_`の付与・
時間が空いた計画は作り直す、の4点。

## 1. Subscriptionsアプリの通知

### 代表の変更を画面で確認（画像01）

決済失敗＝再試行3回/7日は変更なし、**アクションは「一時停止して通知」**。
在庫＝5回/1日は変更なし、**スタッフへの通知頻度は「請求が失敗するたび」**。
※この設定画面は**表示に約18秒**かかります（前便で「空」と記録したのは待ち不足＝訂正）。

### ★一時停止のとき作家さまへ届く文面を全文取得しました（画像03・04）

件名＝**「サブスクリプションは正常に一時停止されました」**
本文＝「…今後の請求や次回の**注文**は発生しません。サブスクリプションは**いつでも再開できます**。」
＋ボタン**「サブスクリプションを管理する」**＋店舗メールへの案内。

**★配送・お届けの語は1つもありませんでした**（全110行を目で追って確認）。司令塔の懸念に対する回答です。

**直すべき箇所5点を洗い出し、文案も用意しました**（①「正常に」②用語③「注文」④「いつでも再開できます」
⑤管理ボタン）。**★当便では反映していません**＝⑤が下記2の判断待ちで、**同じテンプレートを2回編集することになる**ため。
契約0件で**いま誰にも届かない**ので急ぎません。

### スタッフ向け文面（画像05・06）

**代表の変更に自動で追従します**（`paused` の分岐に落ちる）＝**直しは不要**。
**★ただし受信者は代表お一人だけです。弦巻さまお二人は入っていません。**
50件をお受けするなら決済失敗の連絡が代表に集中します（受信者追加は代表の操作）。

## 2. ★管理URLの材料（判断は司令塔）

- **事実＝「どこにも置かれていない」のではなく、一時停止メールから既に配られます。**
  `subscription_paused` に「サブスクリプションを管理する」ボタンが**最初から入っています**。
  **「置かない」と決めるなら、このボタンを外す作業が要ります。**
- 設定画面のURLは `設定→お客様アカウント→URL` に発行済み（実URLはVault）。
  ログインリンクの表示＝**オフ**／セルフサービス返品とキャンセル＝**オフ**（※注文の返品で、サブスク解約とは別）。
  アプリ側の「お客様が管理できるようにする」は**未完了**。
- **★この画面から何ができるかは未確認**（契約0件で開けない）。**解約が何日付で効くかも未確認。**
- 規約（20日締め・到達日基準・日割なし）との食い違いで**いちばん効くのは
  「当社に何も届かないまま契約が消える」こと**＝掲載を止める・ご連絡する運用の起点が失われます。
- **中間案（当方の案・判断は司令塔）**＝ボタンは残し、**リンク先を当社への連絡導線に差し替える**。
  カード更新は当社が個別にご案内し、解約は規約どおり申し出を受け取る形を保てます。

## 3. 一時停止した契約の再開

**「サブスクリプションの再開」テンプレートが存在・オン**＝遷移の仕組みは存在します（画像02）。
**ただし再開ボタンの所在は未確認**（契約0件で契約の詳細画面を開けない）。A-1で契約ができたら確認できます。

## 4. 幅390pxの「（税込）」（画像07）

- **ライブでは再現できませんでした。** 表示中のテキストを4ページ×幅390pxで実描画し、
  **1文字ずつの描画位置をDOM実測**した結果**行またぎ0件**。他の候補の金額表示は
  **フォーム送信に成功したときだけ描画されるブロックの中**にあり測れません。
  → **SNS運用Tに「どの画面のどこか」を教えていただく必要があります。**
- **文字列側で直せることは実証しました**＝金額と（税込）の間に **U+2060（ワードジョイナー）を1文字**入れると
  同じ行に収まります（幅ゼロ・見た目が変わらない）。U+00A0でも収まりますが空白が見えます。
  **丸括弧をやめるとかえって割れます。** 設定はそのまま出力されるので**設定欄に貼るだけで効きます**。

## 5. 実施していないこと・判定していないこと

- 設定を1つも変更していない／通知テンプレートを1文字も編集していない。
- **通知メールの是正は文案までで反映していない**（管理URLの判断待ち）。
- **管理画面から何ができるかは未確認／再開ボタンの所在は未確認**（契約0件のため）。
- **「（税込）」の泣き別れはライブで再現できていない**（どの画面か未特定）。
- **販売プランは触っていない**（★年額プランは足さない＝司令塔判断を反映）。
- 決済は1件も通していない＝初回入金予定日・着金日数は引き続き未実測。

## 証拠の置き場所

`reports/assets/20260803d_サブスク通知と管理URL/`（画像7点＋README）
※管理URLが写る画面はVaultのみ（このリポジトリは公開のため）。

## Codexレビュー

当便は未実施（画面の実測とコード変更なしの調査のため）。
