# システム開発ターミナル

## 現状（2026-07-22）

**着手宣言済みの「公開反映エンジンの安全強化」を仕上げ完了。承認前に必須とされていた残項目6点をすべて実装し、独立レビューの重要指摘3件もその場で修正した。社内システム内部のみの作業で、外部への書き込みはゼロ。**

- 実装した6点＝①画面に表示した内容と実際に送る内容を同一の仕組みで作り一致を保証②承認を「承認した時点のサイト側の状態」にも結びつけ、承認後にサイト側が変わっていたら反映を自動停止③「作家の言葉」は本人承認済みの音声記録から採用する経路だけに限定（手入力での作成は不可・本文の一字一句一致を検査）④日本語原稿を直した後の古い翻訳は、承認も反映も自動停止⑤承認操作の競合防止⑥実際の反映処理を安全検査つきの一本の経路に集約（許可設定が無い限り必ず停止することをテストで実証）。
- 独立レビュー（第三者チェック・攻撃者視点）で見つかった重要3件＝反映経路がサイト側由来の問題を素通しできた穴／タイミング競合の残り窓／競合防止の仕組み自体の穴、をすべて修正。仕組み上ゼロにできない残りリスク1点は既知として明記した。
- 検証＝型チェック通過・自動テスト156件全通過（新規18件）・分離環境の実サーバで一連の操作を実測。実データは追加列のみで不変を確認。
- これは代表構想の「管制盤」（社内システムに入れるとサイトに自動反映される仕組み・リリース後の宿題）の安全基盤にあたる。
- 残り＝実際の書き込み1回の実地検証（代表の許可操作が前提・急がない）。

## 未着手・待ち

- フォーム項目の作り直し＝文面確定後の司令塔一括指示待ち（着手していない）。
- 既送メールの点検・退避（代表側・確認中）／登録申込フォームの最終送信テスト1回（人の手で）。
- サブスク公開操作・決済テスト／台帳への項目追加／ポリシー多言語化の可否（いずれも代表側）。

## 今すぐ着手できるタスク

- 次の指示待ち。または社内システム本体側の残る積み残し（画像の高度な補正機能・Shopifyへの画像アップロード連携など＝いずれも代表の許可操作が前提のものが多い）。

（金額・料率・営業動線・具体的な手順・非公開URLの中身は機微につきチャットとVaultで扱う。）
