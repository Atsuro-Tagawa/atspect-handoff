# システム開発ターミナル 報告（2026-08-07・第2便）

model=Opus 5 (1M context)／担当＝あつぺくとシステム開発

---

## 0. 先に「できなかったこと」

**メールの実測は1通もできていません。** 当ターミナルが操作できるChromeで
Webメール（`webmail.xserver.ne.jp`）を開くと**「セッションが正しくないか期限が切れています」**と出て、
ログイン画面のままでした（2経路で確認）。**パスワードは入力しない規律**のため、ここで止めました。

したがって**未測定・未確認**＝直近90日の総通数／型ごとの内訳／件名の型／
**同意記録の通知メールの実物**。よって**「取り出しを実データで1件通した」とは言えません**
（合成データでは8件通しました）。

**Shopifyの決済通知が media@ に来ているかも、確定していません。**
確かめられたのは「ストアの連絡先メールが media@ 側のアドレス」であることだけ（Admin API）。
**注文通知の宛先はそれとは別の設定**で、その画面は**3回開こうとして3回ともShopify側のエラー**でした。

## 1. やったこと

1. **同意記録の取り出しの道具を作り、合成データ8件で通した**
   （素のテキスト／base64／quoted-printable／HTML表／ハッシュ不一致／「計算中」のまま／
   パート間の食い違い／2回目の上書き拒否）。**個人情報の混入0件**。
   出力＝`<受付番号>/原本メール` と `<受付番号>/同意記録.txt`。**お名前をフォルダ名に使わない設計。**
2. **申込フォームの同意記録の項目をライブから実測**（`contact[]`全37件・hidden 7件）。
   **2026-08-06の記録の項目名の誤りを訂正**（`02b_同意文ハッシュ`→**`02b_規約本文ハッシュ`**）。
   規約本文ハッシュを再計算し**3回連続で同じ値**を確認。
3. **受付台帳に列を1つ追加＝「情報登録料の扱い」**（★列名に金額を書いていません）。
   書き方は**通常どおり／免除（決めた日・決めた人・理由）**の2通りだけ。**承認の仕組みは作っていません。**
   ダッシュボードの台帳画面にも表示。
4. **Boxに部署別フォルダ**＝**新しく作ったのは2つだけ**（`5-SNS部`／`09_規約同意記録`＋手順書1枚）。
   **あつぺくと部・経理部・SHIMON部は既存フォルダと重複するため作っていません。** 総務部は作りません。
   **既存のファイル・フォルダは1つも移動・削除・名前変更していません。**
5. **通知の振り分け設計**＝メールにするのは4つだけ。それ以外は1日1回の見回り。
   型に入らないメールの扱いと、**代表へ渡す経路**、**4人で重複しないための判定簿**を決めた。
   受付台帳に**「メールの判定簿」タブ（6列）を新設**。

## 2. Codexの独立レビュー

**2回実施し、指摘18件をすべて採用しました。**
1回目＝取り出しの道具（9件・重大3件＝**既存の証拠を無条件で上書きしていた**／
**メール全体を一括変換して日本語を壊しうる作りだった**／base64を1パートしか読めなかった）。
2回目＝振り分け設計（9件・重大4件＝**A判定が代表に届く経路が無かった**／
**処理済みの印が無く4人で重複する**／「経緯不明」の記録場所が無い／聞く担当が未定）。
**未実測の断定3か所**も指摘され是正しました。

## 3. 画像で見つけて直したもの

台帳画面に新しい欄を足したところ、**右の「同意記録」の欄が潰れる崩れ**が出ました。
**機械の判定は崩れる前もすべて通っていました。画像を見て気づきました。**
折り返しの指定を足し、**1440／800／390の3幅**で描き直して確認しています。

## 4. 代表にお願いすること

1. **★Webメールに、当ターミナルが使うChromeでログインした状態にしてください。**
   これが解ければ、メールの実測・型の確定・同意記録の実物1件が**一気に片づきます**。
2. Shopify管理画面の**通知の宛先**（スタッフへの通知）を1枚撮っていただけると、
   「決済通知が media@ に来るか」を確定できます（当方は3回とも画面エラーで開けませんでした）。
3. 見回りの**時刻**を決めてください（設計では仮に17:00と置いています）。

## 5. 証拠の置き場所

`reports/assets/20260807_同意記録の保全とBox部署分け/`
（画像4枚＝台帳画面PC・モバイル／Boxの実測／新設タブ。**お名前・差出人・金額・非公開URLは
書いていません。画像も同じ基準で選び、写り込んだ表示名は切り落としました。**）
