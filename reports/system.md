# システム開発ターミナル 報告（2026-08-11・ダッシュボードの会社名化とURL差し替え）

## ★新しいURL（代表はブックマークをこれに差し替えるだけで済みます）

**https://arts-respect-dashboard.pages.dev/**

★私用のメールアドレスは1文字も書いていません（本リポジトリはPublic・機械で混入0件を確認）。
★パスワード・カード情報の入力は一切していません。無料枠のままです。
証拠＝`reports/assets/20260811_ダッシュボード会社名化/`（記録.md／画像10枚／配信物のハッシュ）

---

## 旧URLは当面そのまま使えます（自然には死にません）

- 旧URL（`atspect-dashboard.pages.dev`）の**プロジェクトは残しました**。**消すのは不可逆の削除**にあたるため、
  当方では実施していません（代表の承認事項）。**放っておいても勝手に消えません。**
- **旧URLにも同じ最新版を上げました。**どちらを開いても中身・看板・担当者欄は同じです。
- **鍵は新旧とも同じ「社内4人」のポリシー**です（片方だけ緩い、はありません）。
- 実測＝**一度入っていれば、新URLは入り直し不要**でした（同じチームの鍵を共有するため）。

## A．許可リストの4件目＝差し替え済み（実測で4件ちょうど）

| 実測 | 結果 |
|---|---|
| ポリシー名／動作 | 社内4人／Allow |
| **件数** | **4件ちょうど** |
| 1〜3件目 | `a.tagawa` ／ `s.tsurumaki` ／ `k.tsurumaki` ＠atspect.co.jp |
| **4件目** | **結依子さま個人（差し替え済み）** |
| 旧 `media@atspect.com` の残存 | **0件** |
| Cloudflare側の更新記録 | 2026-08-11T02:28:47Z |

差し替えは管理画面で行い、**確認は管理APIの読み取り（別経路）**で行いました（同じ手で確かめない）。

## B．看板を「ARTS RESPECT ダッシュボード」に統一（3か所とも実測）

| 場所 | 前 | 後 | 確かめ方 |
|---|---|---|---|
| ヘッダーの表題 | ARTS RESPECT | **ARTS RESPECT ダッシュボード** | ライブの画面を撮影 |
| タブ名（全14ページ） | 「〜｜あつぺくと」等 | **「〜｜ARTS RESPECT ダッシュボード」** | ハッシュ照合＋ライブで `document.title` 実測 |
| Accessのログイン画面 | あつぺくと ダッシュボード | **ARTS RESPECT ダッシュボード** | **まっさらなブラウザ（鍵の外）で開いて撮影** |

- **部署名としての「あつぺくと」は残しました**（部署ボタン・部署ページの見出し・台帳の部署欄）。
- ★新旧2つのAccessアプリを**同じ名前**にしたため、一覧に同名が2つ並びます（宛先で区別できます）。
  指示が「すべて統一」だったため、そのとおりにしています。
- ★ログイン画面で「ダッシュボ／ード」と折り返すのは**Cloudflare側のレイアウト**です（文字列は正しい）。

## C．URLを会社名ベースへ（外から叩いて施錠を実測）

新プロジェクト `arts-respect-dashboard`（**希望どおりの名前が取れました**）＋Accessアプリを新規作成
（ポリシー＝社内4人／セッション6時間）＋「プレビューを制限」で `*.arts-respect-dashboard.pages.dev` も封鎖。

**ログインなしで叩いた最終実測＝9経路すべて302（鍵あり）・中身は返りません**
新URL 本番／台帳／見本データ／デプロイ固有（新・旧）、旧URL 本番／台帳／デプロイ固有（新・8/09の版）。

★Pagesの**プロジェクト名は今の管理画面では変更できます**が、**改名すると代表のブックマークがその場で切れる**ため、
改名ではなく**新規プロジェクトを作る方法**を採りました。

## D．担当者名4人＝**ログインした宛先から自動で出ます**（ライブで実測）

**ライブで、代表のログインに対し画面右上に「田川」と自動表示されることを確認しました**（撮影あり）。

- しくみ＝入り口の鍵が教える宛先を名前に変換。**宛先そのものは配信物に1文字も入れていません**
  （会社3件は宛先のsha256で照合、4人目は「どれとも一致しない」で受ける）。
- **鍵の外で宛先を読めないときだけ、4人から選ぶ形**に落ちます。
- 機械で通し試験＝4人とも正しい名前／読めないときは選ぶ形。**すべてOK。**

## E．ライブ版の確定と最新化＝**リサーチTの見立てどおり、ライブは8/09の版のままでした**

| 実測 | 結果 |
|---|---|
| 作業前のライブ | **`d6123de9`（8/09 07:55Z）＝23ファイルの版** |
| 8/10の再設計版のデプロイ記録 | **ありません**（8/09以降0件） |
| 今回上げた版 | **25ファイル**／一式のsha256 `76d6435cca11ef9b7102d13ff3ae1bc4ad5c505789b27a058f1c3a6189fcf0c1` |
| 新URLの本番デプロイ／旧URLの本番デプロイ | `0139536c`（03:11Z）／`8038e37a`（03:10Z） |
| **ライブから取り直したハッシュ照合** | **新URL 25/25一致／旧URL 25/25一致** |

★配信物の出発前検査が**今回1度、正しく止めてくれました**。担当者名の実装で最初に config へメールアドレスを
直接書いたところ「入ってはいけない文字列」で停止 → **宛先を持たない設計に変えて解決**（検査は無効化していません）。
**実データは接続していません（見本のみ）。**

## F．「Googleでログイン」＝**できます。ただしGoogle側で1回だけアプリ登録が要ります**（調査のみ・実装せず）

| 確認 | 内容 |
|---|---|
| 管理画面に Google はあるか | **あります**（Google／Google Workspace。**有料への誘導は出ませんでした**＝実測） |
| 足すのに要る入力 | **クライアントID** と **クライアントシークレット**（＋任意でPKCE） |
| それを作る場所 | **Google Cloud Console**（プロジェクト→OAuth同意画面(External)→ウェブアプリのOAuthクライアント） |
| Google側に登録するURL | 生成元＝`https://<チーム名>.cloudflareaccess.com`／リダイレクト＝同`/cdn-cgi/access/callback` |
| いまの数字方式と併存 | **できます**（公式に「OTPとIDプロバイダーを同時に設定して選ばせられる」と明記） |
| 許可リストは効き続けるか | **効きます**（判定は宛先。4件のどれかでなければGoogleでも止まります） |

**代表側の作業**＝①Google Cloud ConsoleでOAuthクライアントを作る（Googleへのログインが要るため代表の操作）
②できたIDとシークレットを当方に渡す（シークレットはGitHubに残しません）③あとは当方が入れて実測します。

**正直に**＝「1タップ」になるのは**そのブラウザで既にGoogleにログイン済みの場合**だけです。
**会社の3件（atspect.co.jp）はGoogleのアカウントではないので使えません**。楽になるのは**結依子さま**です。
**無料枠かどうかは料金表の全体を検証していないため「絶対に無料」とは書きません。**

## Codexの独立レビュー＝実施。**重大2件・中3件・軽微2件。すべて直して上げ直しました**

★**最初にデプロイした版には重大な誤りが2件入っていました。**同じ日のうちに直した版へ上書き済みです。

| 度合 | 指摘 | 対応 |
|---|---|---|
| **重大** | 暗号機能が使えない環境だと**全員が4人目の名前になる**（表示だけでなく記録者名も誤る） | 直した＝計算できないときは「決めない」＝選ぶ形に落とす |
| **重大** | 担当者が決まる前に押すと**記録者名が「お名前未設定」で残りうる** | 直した＝記録の直前に必ず確定を待つ |
| 中 | 4人目の判定は実は「照合に失敗した全員」 | 条件を狭め、**前提（許可リストが4件ちょうど）を設定ファイルに明記** |
| 中 | 例外が出ると「確かめています…」で固まる | 直した＝例外は「決まらなかった」に倒す |
| 中 | 古い控えが後日よみがえって別人の記録になる | 直した＝鍵で決まったら控えも上書き |
| 軽微 | 宛先の問い合わせが2回走りうる／呼び出し口の依存が見えにくい | 前者は修正、後者はコメントで明記 |

**★sha256の言い方を訂正しました**＝「指紋から宛先は復元できない」は強すぎました。
**候補を作って同じ指紋になるか試す照合は成立します。**これは「配信物に宛先の文字列を載せない」仕組みであって、
**宛先を秘密にする仕組みではありません**（私用の宛先は指紋すら置いていません）。

**★残る危なさ**＝許可リストに5人目を足すと、その人も「結依子」と表示されます。
**許可リストを増減するときは設定ファイルの4人の表も必ず見直してください。**

## まだできていないこと（正直に）

- **実データは未接続**（見本のまま）。切り替えは代表の合図待ちのまま変えていません。
- **旧プロジェクトは削除していません**（不可逆の削除＝代表の承認事項）。
- 旧URLのデプロイ固有URLを守る自動アプリは**許可が代表1名・セッション24時間**のまま（8/09からの状態・今回未着手）。
- **モバイル幅の“ライブ”画面は撮っていません**（手元での描画は撮影済み）。

最終更新：2026-08-11
