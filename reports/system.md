# システム開発ターミナル 報告（2026-08-08）

model=Opus 5 (1M context)／担当＝あつぺくとシステム開発

---

## 1. メールに入れました（★代表への3度目のお願いは不要でした）

**このPCのOutlookに、サービス窓口のメールボックスが設定済み**でした。
**ログイン済みのアプリを読むだけ**なのでパスワードが要らず、ここから実測できました。

切り分けの結果（実測）＝
- **ブラウザ**：当Tが操作しているのは**代表ご自身のChrome**で、プロファイルは**1つだけ**。
  それでもWebメールはログイン画面のまま。**「別プロファイル」という仮説は否定されました。**
  同じPCで**Edgeが起動している**ため、そちらでログインされた可能性が高いです。
- **IMAP**：口は開いている（Courier-IMAP・TLS・993番を実測）。**資格情報がどこにも無く未接続。**
  読み取り専用クライアントは用意済みで、値が入れば即動きます。

**読むだけ**の担保＝`Delete`/`Move`/`Save`/`UnRead` を1回も呼んでいません。
既読・移動・削除・ラベル付けは発生していません。

## 2. メールの実測（直近90日＝116通）

① 作家さまの登録関連 **22** ／ ② ECの注文 **6** ／ ③ 一般のお問い合わせ **2** ／
④ Shopifyの決済・システム通知 **30** ／ ⑤ セールス **8** ／ **⑥ 型に入らないもの 48** ／ 合計 **116**。

- **★Shopifyの決済通知は media@ に来ています（実測で確定）**＝Shopifyドメインから60通、
  うち決済・お手続き系6通。**8/7に「未確定」と書いた項目が確定しました。**
- **★①と③は件名で区別できません**（どちらも「新規お客様メッセージ」）。
  **件名で振り分ける仕組みを作ってはいけない**と実測で確定。
- **★⑥が48通で最多**（Boxの操作通知等）。「その他を必ず立てる」というご指示が正しかったことになります。

## 3. ★同意記録の実物1件を、Boxへ保全しました

`09_規約同意記録 / MAIL-20260806-1314 /` に `原本メール.msg` と `同意記録.txt`。
**Boxの画面を開いて載っていることを確かめました**（「保存したつもり」ではありません）。
判定＝**ハッシュ一致・項目も揃っている・総合一致**（8項目すべて取得）。

**★前提が根本から間違っていました。** 実物の通知メールは「項目名: 値」ではなく、
**ラベルだけの行＋次の行に値**という形でした。**昨日の道具は実物では1項目も拾えませんでした**
（合成データ8件が通っていたのは、その合成データを想定で作っていたためです）。本日作り直しました。

**★残り13通は未保全**です（同意記録を含むメールは全部で14通。うち**4通は8/1改訂前の別ハッシュ**）。
実際の申込か開発中の試験送信かの区別が要るため、こちらだけでは進められません。

## 4. .gitignore を新設しました

**この共有リポジトリには1つもありませんでした。** 秘密の値・個人情報・メールの実物が
うっかり上がる経路が開いたままでした。追跡済みに該当ファイルが無いことを先に確認（0件）。

## 5. Codexの独立レビュー（1回・指摘2件をすべて採用）

**【重大】ハッシュだけ一致すれば全体を「一致」と表示していた** → 判定を3つに分け、
必須項目が欠ければ総合は「照合できない」に。**【重大】値の複数行取り込みで氏名が混入しうる**
→ 既定を「次の1行だけ」に変更し、項目ごとに値の形を検査。
自分でも1件＝**`.msg`はバイト単位で再現しない**ことを実測し、本文のSHA-256を記録に追加。

## 6. 代表にお願いすること

1. **残り13通のうち、どれが実際の作家さまの申込かの区別**（お名前を見る必要があるため）。
2. 見回りの**時刻**を決めてください（設計では仮に17:00）。
3. **⑥の48通（Boxの通知等）を減らすか、型を1つ増やすか**のご判断。

## 7. 証拠の置き場所

`reports/assets/20260808_同意記録の実物1件をBoxへ保全/`
（Box画面の画像1枚＋実測の表。**お名前・差出人・アドレス・金額・非公開URLは書いていません。
画像に写った表示名・アカウント名は塗りつぶしました。**）
