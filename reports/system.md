# システム開発ターミナル 報告（2026-08-10・段2続便＝注文読み取りの接続）

★**直前の便（段2＝ダッシュボード実装）の報告は
`reports/assets/20260810_ダッシュボード段2/00_段2の実装記録.md` にあります。**

証拠の置き場所＝`reports/assets/20260810_注文読み取り/`
（`00_注文読み取りの記録.md` ／ `01_代表の操作手順書.md` ／ 画面2枚）
★**合鍵・実データは1つも置いていません。** 画像はすべて見本データです。

## 1. 実測で分かったこと

| 確かめたこと | 結果 |
|---|---|
| いまの合鍵に注文を読む権限があるか | **ありません**（`read_orders` は無し・許可は24個） |
| いまの合鍵に書き込み権限があるか | **あります**（`write_themes` 等）→ **注文用には使えません。別の合鍵が要ります** |
| 権限はどこにあるか | **アプリの「バージョン（版）」の中の「スコープ」欄** |
| 版の宣言とストアの許可は一致しているか | **★一致していません**（版は `read_customers` を宣言、ストアは未許可）。**宣言だけでは効きません** |

## 2. ■1＝代表の操作手順書を作りました（★代表の操作待ち）

`01_代表の操作手順書.md`。**実際に開いて確かめた画面名だけを書き、確かめられなかった3点には
「★未確認」と明記しました**（新規アプリ作成後の画面／**合鍵の文字列がどの画面に出るか**／
保護された顧客データの追加同意の有無）。

- 権限は **`read_orders` だけ**。**書き込み系は一切付けません。**
- ★**注文にはお名前・ご住所・メールが付随します**（避けられません）。そこで**取り込みの側で捨て**、
  保存するのは**注文番号・日時・支払い状況・金額・どの作家さまの登録料か**だけにします。
- 受け取った文字列は **`.env` にだけ**貼っていただきます。
  **チャット・LINE・GitHubには貼らないでください**（手順書に太字で明記）。
  **貼り終えたら「入れました」とだけ**お知らせください（文字列そのものは送らないでください）。

## 3. ■3＝画面と検証（見本データで先に作りました）

置き場所＝**あつぺくと中ページの「お金」**（トップには出しません）。

**★「承認どまり」と「入金済み」を絶対に同じ見た目にしない**＝**12項目すべてPASS**：
左の線の色（緑 `rgb(27,127,59)`／赤 `rgb(198,40,40)`）・文字の色・
「まだ入金されていません」の明記・「2件、入金がまだです」の先頭表示・
取り込み時刻の行（2時間超で赤）・横あふれ0／44px未満0／JSエラー0（390px・1440pxとも）。
**目視も行いました。** 8/3に実在した「承認どまり」の型を見本に入れてあります。

Shopifyの状態の語（`paid`/`authorized`/`pending`/`refunded`）を**そのまま持ち**、日本語を併記しています。

## 4. 設計の型（変えていません）

- **サーバー側の受け口は作っていません**（施錠をすり抜ける入口を作らない）。手元PC→鍵の内側→画面は読むだけ。
- **読み取り専用。** 確定・返金・変更の操作は画面からできません。
- `orders.json` も**切り替えの門番の対象**に入れました（印が無ければ配信物に入りません）。
  **配信物の実データは0件**であることを機械で確認済み。

## 5. していないこと

合鍵の発行・受け取り・実データの取り込み（合図待ち）／**Shopifyへの注文の問い合わせは1回もしていません**
（権限が無いため）／Codexの独立レビュー／実機のスマートフォンでの確認。

最終更新：2026-08-10
