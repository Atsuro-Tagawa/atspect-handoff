作業記録は削除しました（2026-08-20・代表の指示）。引き継ぎは引き継ぎ書が正／証拠はreports/assets/配下に保全

---

# システム開発ターミナル 報告（2026-08-20・URL照会便）

使用モデル：Opus 5／本便は**読み取り・照会のみ**（Shopify・サイト・ファイルへの変更は0件）。

証拠の置き場所＝`reports/assets/20260820_URL照会/`（URL一覧.md・画面4枚・パンフレットPDF）

## 1. 藤代 範雄さまの作家ページ（LINEでお送りする正のURL＝1本に確定）

`https://atspect.com/pages/artist/fujishiro-norio`

HTTP 200・リダイレクト0件・canonicalも同一。Shopify側のリダイレクト設定も0件（`fujishiro`で検索）。
★`/pages/artists/...`（複数形）は**404にならずHTTP 200で作家一覧が出る**ため取り違えに注意。

## 2. 販売作品3点の作品詳細ページ

`https://atspect.com/products/fujishiro-norio-glass-01` ／ `-02` ／ `-03`

3本ともHTTP 200・公開中・写真は表示される。購入導線は設計どおり「予約・お問い合わせする」フォームのみ。
★**価格は未設定**＝Shopify上の価格が0円で、画面の「価格」欄・作品名・素材・寸法がすべて `●`（仮置き）のまま。
`<title>`も`●｜あつぺくと`。**このままLINEでお送りすると、作品名も価格も伏字に見える。**

## 3. 登録申込（情報登録料 33,000円税込）

**存在する**（新設不要・本便では何も作っていない）。設定の実測＝金額33,000円／**税込**（店舗設定が内税・内訳3,000円）／
配送先を聞かない／一回払い（定期プランなし）／非公開（UNLISTED）。決済手段＝クレジットカード・PayPal・
Shop Pay・Apple Pay・Google Pay。カートパーマリンク1本で決済画面まで直行することを実測（**注文は作っていない**）。
★**実URLは本ファイルに書かない。** 知っている人は誰でも本番のカードで33,000円を決済できるため、
正本はVault `_確定_情報登録料の決済リンク_20260731_ATSPECT.md` のみに置く運用（2026-07-31確定・本便で有効を再確認）。

## 4. パンフレットPDF

- **配布用（A4・4ページ・約2.8MB）**＝本フォルダの `あつぺくとパンフレット_配布用A4_20260801.pdf`。
  正本＝Vault `_入稿データ_アクセア_修正版_20260724\02_パンフ_配布用.pdf`（2026-08-01 14:19）。
  ライブのテーマ資産3本（`atspect-pamphlet.pdf`／`-v214.pdf`／`-v218.pdf`）と**sha256完全一致**＝配信中のものと同一。
- **印刷用（トンボなし・約14MB）**＝Vault `_入稿データ_アクセア_修正版_20260724\A_トンボなし版（まず提出）\01_パンフ_印刷用_トンボなし.pdf`。
  Box `ARTS RESPECT\あつぺくと_印刷入稿_最新\11_パンフレット.pdf` とDesktop同名ファイルも**sha256完全一致**（3か所同一）。
  大きいため本フォルダには置いていない。
- ★`atspect-theme\assets\atspect-pamphlet-v214.pdf`（手元の写し）は**ライブと不一致＝古い**。
- どれを正とするかは司令塔の裁定事項（当方では決めない）。

## Codexの独立検証

別セッションから同6URLへ直接HTTP取得させ、**全件でHTTP 200・リダイレクト0件・`<title>`一致**を独立に確認。
★ただし価格表記・ボタン文言の照合は、Codex側の文字コードの不具合で数えられず**未検証**。
その部分は当方が生HTTP（curl）と実ブラウザ描画の2経路＋画像の目視で確認した。

## 代表対応待ち

1. 作品3点の**作品名・価格・素材・寸法**（`●`のまま）をどうするか。
2. （繰り越し）入金（payout）を読む許可を足すかの判断。
3. （繰り越し）Search Consoleでのトップページ再クロール依頼／QR一覧のLINE実機テスト。

---

# システム開発ターミナル 報告（2026-08-21・見本ページの仕組み便）

使用モデル：Opus 5。証拠の置き場所＝`reports/assets/20260821_見本の仕組み/`（画面3枚＋README）。

**やったこと**＝関係のできた作家さまへ「見本ページ」を先にお見せし、ご承諾・ご契約・ご入金の後に
正式掲載へ進む営業方式のために、**未承諾のページが一般公開される事故を仕組みで防ぐ土台**を作った。
本番で**ダミー作家1名**を実際に作って確かめ、**作業後は非公開（下書き）に戻してある**。
**一般公開されているページ・実在の作家さまのデータは1件も変えていない。**

## 1. 置き場の設計＝新規開発は不要だった（実測でわかったこと）

作家ページの実体はShopifyのmetaobject（`/pages/artist/<ハンドル>`）で、**公開状態を持てる**。
テーマ側にも「一覧・検索用データに出さない」フィルタが**既にある**（ただし下記の不具合あり）。
そこで新方式は作らず、**状態を3つに整理する**方式にした。

| | 状態 | 公開サイトでの見え方 |
|---|---|---|
| ① | **下書き** | **URLも404。誰からも見えない**（12回引いて12回404） |
| ② | **見本** | **URLを知る人だけ200。**一覧・索引・ストア内検索に出ない（乱数24桁のハンドル） |
| ③ | **正式掲載** | 一覧・検索・サイトマップに出る |

## 2. 公開の関所＝機械で強制した（人の判断に任せない）

道具＝`atspect-system\scripts\mihon-page.mjs`。台帳（Vault）に**承諾日・契約締結日・入金確認日**が
そろっていなければ、**コマンドが正式掲載を拒否する**。本番で両方を実演して確かめた。

- 3点が空のとき → **拒否した**（終了コード2）。
- 3点がそろったとき → 関所を全部通った（**予行演習モードで確認・実際の公開はしていない**）。
- **未来の日付・存在しない日付は拒否する**（「2026-12-01」を入れて拒否されることを確認）。
- 作家名から推測できるハンドルを見本にしようとすると**拒否する**。

## 3. ★見つかった不具合2件（どちらも本便で発見・未解決）

1. **見本ページに `noindex` が出ない。** テーマの条件式が、存在しない欄を見ているため
   **一度も実行されていなかった**（この仕組みは今まで対象が0件だったので誰も気づけなかった）。
   **直すのはテーマ＝制作ターミナルの担当。** 直し方と確認手順は
   `atspect-system\docs\手順_見本ページの作り方.md` §6(1) に書いた。
2. **見本はサイトマップに載ってしまう**（実測で627→628）。**仕組み上、外せない**
   （metaobjectにはページで使っている非表示設定を付けられないことを実測で確認）。
   守りは「noindexを効かせる」と「ハンドルを推測できない値にする」の2つだけ。

**→ このため、見本を作家さまへお見せする運用は、まだ始めていない。**
道具の側で、**noindexが出ていない見本は自動で下書きへ戻す**ようにした（本番で作動を確認）。
つまり**今は見本を作ろうとしても作れない。これは意図した動作。**

## 4. その他わかったこと

- **見本を取り下げても、すぐには消えない。** 実測で**約73秒**かかった
  （戻した直後は10回引いて8回がまだ表示された）。**「戻したからもう見えない」と即断しないこと。**

## Codexの独立レビュー

観点＝「公開事故が起きる穴はどこか」で1本レビューさせ、**指摘4件すべてを反映した**（3件は全部・1件は一部採用）。
いちばん重い指摘＝「正式掲載の関所を通らなくても、見本にする操作1回で一般公開状態になる」。
→ 見本にする操作の側にも関所を3つ足し、上記の自動差し戻しを入れた。

## 代表対応待ち

1. **テーマの `noindex` 修正**（制作ターミナルへの手配。直るまで見本の運用は開始できない）。
2. （繰り越し）作品3点の作品名・価格・素材・寸法が `●` のまま。
3. （繰り越し）入金（payout）を読む許可を足すかの判断／Search Console再クロール／QRのLINE実機テスト。

---

# システム開発ターミナル 報告（2026-08-22・見本ページ §0解除判断＋§6訂正便）

使用モデル：Opus 5。**本便でのライブへの変更＝ダミー1件の状態を見本↔下書きに動かしただけ**
（テーマ・実在の作家さまのデータ・一般公開ページは1件も変えていない）。

## ① §0（見本運用の差し止め）＝**解除した**

制作Tの2026-08-21のnoindex修正により前提が解消されたため、**手順書§0の「まだ始めてはいけない」を解除**した。
**制作Tの報告は鵜呑みにせず、手順書の持ち主として当方が独自に再実測してから解除した。**

★**ただし解除したのは「検索の索引に載る心配」だけ。** 見本はACTIVEの間、
**URLを知っていれば誰でも読めるし、サイトマップにも載る**（noindexは入室制限ではない）。
「URLを知られても読ませない」ことが要る場合は、この解除では足りない旨を§0に明記した。

## ② 当方の再実測の結果

| 実測 | 結果 |
|---|---|
| 公開中テーマ（role=MAIN）から条件式を取り直す | `metaobject.system.type == 'artist'` に是正済み。**動くコードに旧い書き方は0件**（コメント中の2件は経緯の説明）。一時プローブの残り**なし** |
| 見本ページの `meta[name="robots"]` | **ちょうど1個・`noindex, nofollow`** |
| **正式掲載ページ10件の誤爆検査** | **robots メタ 0件**（10件とも「HTTP 200 かつ本文に作家名がある」で裏取り済み。`is_listed=true` の2件を必ず含めた） |
| 関所C（見本にした直後のnoindex実測） | **通過**（3回連続確認・自動差し戻しなし） |
| サイトマップ（§6(2)再確認） | **変わらず**＝見本がACTIVEの間は載る（627→628）。下書きへ戻すと627 |

★**「0件」は対照実験で裏を取ってから採った**＝同じ検出器を見本ページに当てて**1件出る**ことを先に確認し、
それが確かめられなければ0件を採らずに異常終了する作りにした。
★`?preview_theme_id=` は**使っていない**（テーマはライブなので素のURLが検査対象＝制作Tが踏んだ302の穴を避けた）。
★検査器の誤りを自分で2回見つけて直した（コメント内の旧条件を「残っている」と誤判定／件数の引き算を誤って-1件と表示）。
**どちらも「0件・なし」を報告する前に、対照実験と行番号の表示で気づいた。**

## ③ §6(1)(2)の変更（差分の要点）

**§6(1)＝根拠が誤っていたので書き改めた。** 当方が全定義を数え直した実測：

| 定義 | `is_listed` | オンラインストア公開 | `is_listed=false` の実データ |
|---|---|---|---|
| `artist` | あり（boolean） | **有効** | ACTIVE 0件／DRAFT 2件（ダミー） |
| `donation_work` | **あり**（boolean） | **無効** | **ACTIVE 1件が実在** |

**metaobject定義は2種類しかなく、その両方が `is_listed` を持つ**＝旧記述「`is_listed` を持つのは artist だけ」は誤り。
しかも `donation_work` に `is_listed=false` かつ ACTIVE が実在する＝**当方が当初推奨した「型判定を外す」案なら、この1件が条件に一致していた。
制作Tが型判定を残した判断が正しかった。**

```diff
-`is_listed` を持つのは artist だけで、もう一方の `donation_work` は
-オンラインストア公開が無効＝ページとして描画されないため、型を見なくても取り違えは起きない
+**取り違えを防いでいる直接の根拠は①。②は、いまたまたま重なっている追加の守り。**
+1. **【直接の根拠】テーマ側で `metaobject.system.type` が `artist` であることを型判定している。**
+2. **【現状の追加の守り】`donation_work` はオンラインストア公開が無効のため、
+   Shopifyのオンラインストア上に固有のメタオブジェクトページを持たない。**
+   ※「どこにも描かれない」という意味ではない。
+**★将来 `donation_work` のオンラインストア公開を有効にする判断が出た場合は、この節を再確認すること。**
```

**§6(2)＝「2026-08-22再確認・変わらず」を追記**（結論は変わらない）。
**§5＝取り下げの所要時間を訂正**＝`約73秒` → **`2026-08-21は約73秒／2026-08-22は約193秒`・一定ではない**。
★**検証でURLを何度も引いた後ほど取り下げに時間がかかる**と思われる＝
**作家さまが実際にご覧になった見本ほど、引っ込めるのに時間がかかる**と考えておくこと（§5に明記）。

## ④ ダミーを下書きへ戻したこと

**戻した。** API上＝**②見本0件／①下書き2件／③正式掲載627件**。
公開サイト＝**10回引いて10回とも404**（193秒かけて全複製が404になるまで数えた）。
サイトマップ627件・一覧の索引627件と一致＝**漏れなし**。
★**見本ハンドルは本リポジトリに書いていない**（Vaultのみ）。

## ⑤ Codex独立レビュー（2回目・観点＝解除してよいか／新しい根拠は実測と合うか）

**穴＝無し。解除してよい**との判定。**指摘3件を採用**した。

1. **解除できるのは「索引に載る心配」に限る**。見本はACTIVE中は読めるしサイトマップにも載るため、
   秘匿が要件なら解除できない → **§0に明記した**。
2. **①が直接の根拠、②は追加の守り**という主従を整理すべき → **§6(1)を書き改めた**。
3. **「そもそもページとして描かれない」は言い過ぎ**（テーマが他ページの部品として出す可能性まで否定しない）
   → **「固有のメタオブジェクトページを持たない」に限定**し、注記を足した。

## 代表対応待ち

1. （繰り越し）作品3点の作品名・価格・素材・寸法が `●` のまま。
2. （繰り越し）入金（payout）を読む許可を足すかの判断／Search Console再クロール／QRのLINE実機テスト。
