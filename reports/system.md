# システム開発ターミナル 報告（2026-08-01・通知メール便）

## 結論：重大度A・通知メール5本の是正が完了しました。営業開始ラインのA級は終わりです。

| 本 | 件名 | 本文 | レンダリング目視 | 状態 |
|---|---|---|---|---|
| subscription_new | 直した | 直した | 済 | 完了 |
| subscription_payment_reminder | 直した | 直した | 済 | 完了 |
| subscription_payment_failure | 直した | 直した（3分岐すべて） | paused分岐のみ | 完了 |
| customer_marketing_confirmation | 変更なし | 変更なし | — | **経路が無いと確定・直す必要なし** |
| order_confirmation（前回の残り） | 前回済 | 構造ラベル8箇所を追加是正 | **両分岐とも済** | 完了 |

**証拠の置き場所＝`reports/assets/20260801_通知メール4本/`（記録5本＋画像9枚）**

---

## 1. 件名は4本すべて直しました（前回の反省点）

| 本 | 変更後の件名 |
|---|---|
| subscription_new | `システム利用料のお手続きを承りました　番号：{{ …origin_order.name }}` |
| subscription_payment_reminder | `まもなく次回のシステム利用料を承ります` |
| subscription_payment_failure | `システム利用料の決済ができませんでした` |
| order_confirmation | `お手続きの確認　番号：{{name}}`（前回のまま・変更なし） |

## 2. ★機械判定が全PASSでも、目視で見つかった不具合が2件ありました

1. **subscription_new**：禁止語0件を機械で確認したうえでプレビュー画像を見たら、
   見出しが2行に折れて**「承り／ました」と泣き別れ**していた。→ 見出しを短縮して1行に収めた。
2. **order_confirmation**：本文は前回直っていたが、**構造ラベル（注文サマリー・注文を表示する・
   注文 #1001・注文のディスカウント・shopで注文を追跡します）は分岐の外**にあり、
   **発送なしの注文にも「注文」が出ていた**。→ 発送の有無でラベル語を切り替える方式に是正。
   **発送ありのときの文言は1文字も変えていません**（同じ語を代入しているため）。

さらに **Shopify既定の「配送」の小計行が、発送の無い商品でも無条件に描画される**ことも見つけ、
サブスク系3本とも金額の条件で囲みました。

## 3. customer_marketing_confirmation は「作家さまに飛ぶ経路が無い」と確定（直していません）

司令塔の見立てとは原因が違いました。実測3つ：

1. **管理画面「マーケティングのダブルオプトイン → マーケティングの購読確認」のトグルがOFF**
   ＝**そもそも誰にも送信されない**。★このトグルはJavaScriptからは読めず（独自要素）、
   **画像で見て初めて分かりました**（7/31の「保存バーの誤判定」と同じ型）。
2. **サイト全11ページの`<form>`を`form_type`で棚卸し**した結果、`form_type=customer`（＝顧客を作る）は
   **フッターのメールマガジン欄だけ**。**作家向け6フォームはすべて`contact`型**で顧客を作らない
   （SNS運用Tの実測と一致）。
3. **決済画面にメール配信の同意欄が無い**（DOM実測でチェックボックス0個＋管理画面の
   「マーケティングオプトイン」がメール・SMSとも「表示しない」）。

## 4. Codexレビュー

`order_confirmation` の変更に対して**1ファイル1観点**で実施（Liquidの評価順序・スコープ）。
「実際に壊れる箇所は無い」。ただし「提示外の約3,900行は断定できない・`{% render %}`があると
スコープが切れる」との指摘を受け、**自分で全文を走査して解消**しました
（`render`/`include`は0件。残る「注文」はすべて発送あり分岐か非描画領域であることを1件ずつ確認）。

サブスク3本にはCodexレビューを**実施していません**（1ファイル1観点の原則により order_confirmation を優先）。

---

## 5. ★実施していないこと・判定していないこと（正直に）

- **テストメールを1通も送っていません**（外向きの操作のため）。したがって
  **実際の受信箱での見え方・件名の並び・迷惑メール判定は未確認**です。
- **実際の注文・実際の請求での描画は未確認**です（注文がまだ無いため、確認はすべて管理画面のプレビュー）。
- `subscription_payment_failure` の **canceled / skipped の2分岐は描画を見ていません**
  （プレビューのダミーが paused に固定されているため。ソース上の確認のみ）。
- `customer_marketing_confirmation` の**トグルはONにしていません**（設定変更は指示外のため）。
  将来メールマガジンを始める場合は、ONにする前に文面の書き直しが必要です（現状はShopify初期文面のまま）。

## 6. ★代表対応待ち（1件・新規）

**月額サブスクの販売プラン名が `1か月ごとに配達` になっています。**

- このプラン名は**サブスク系メールの商品行の下にそのまま描画されます**。
  **通知メールのテンプレートを直しても、ここは直りません。**
- 発送の無い商品に「配達」は実態と合いません。**推奨＝「1か月ごとに更新」**。
- **当方では直していません**＝販売プランの変更は**サブスクリプション（決済）の本番設定**に触れる操作で、
  事前確認が必要な4項目の一つに該当すると判断したためです。

## 7. 前便からの持ち越し（変化なし）

- **APIスコープの承認1クリック**が未完了（管理画面の「データアクセスの更新」→黒い「更新」ボタン）。
  押さなくても手作業の受付処理は可能です。
- **申込フォームのテスト送信**（hCaptchaのため人にしか解けません）。
- **決済のテスト**（カード番号の入力が必要なため）。
