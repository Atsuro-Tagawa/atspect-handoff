# システム開発ターミナル 報告（2026-08-20・注文読み取り用の合鍵の復旧）

## 1. 結果＝復旧した

- 保存してあった合鍵は、確かめたところ**お店に受け付けられなかった**（2026-08-20時点で無効）。
- `scripts/_orders-token-exchange-20260810.mjs` で**引き換え直したら HTTP 200 で通った**。
  新しい合鍵は `.env` にだけ収めた（**値は画面にもGitHubにも出していない**）。
- 引き換えの材料（クライアントIDとシークレット）は**生きていた**＝代表の再操作は不要だった。
  ★**なぜ無効になったのかは特定できていない**（期限切れなのか、別の理由なのかは未確認）。
  分かっているのは「引き換え直したら HTTP 200 になった」という今回1回ぶんの事実だけ。
  同じことが起きたら、まず同じ道具で引き換え直して確かめる。
- 書き換える前に `.env` の控えをVaultの自動バックアップ区画へ取ってある（置き場所はVault側に記録）。

## 2. 実測（a）注文一覧の読み取り

| 確かめたこと | 返事 | 件数 |
| --- | --- | --- |
| 注文の総数（すべての状態） | HTTP 200 | 5 件 |
| 注文一覧（欄を絞って10件まで） | HTTP 200 | 5 件 |
| 注文1件の読み取り | HTTP 200 | 1 件 |

- 5件の内訳＝**本番3件・テスト注文2件**。取消はいずれも無し。
- **お名前・ご住所・金額・注文番号は画面にもこの報告にも出していない。**

## 3. 実測（b）合鍵に付いている許可

- **`read_orders` / `read_locales` / `read_translations` の3つ。**
- **書き込み系（`write_*`）は0件。**
- ★ご依頼の文面は「`read_orders` の1つだけ」だったが、**実際は読み取りが3つ**だった。
  これは今回増えたものではなく、`_1枚_実データ接続の下準備_20260811_ATSPECT.md` に
  「許可は読み取りのみ3つ」と2026-08-11の時点で記録されている状態と**一致する**。
  **危険な差（書き込みが付いている）ではないので、こちらでは何も変えていない。**

## 4. B-10の材料＝「手数料の引かれ方」は読めた／「初回入金の予定日」は読めない

**読めたもの＝手数料の構造**（金額の実値はVault側のメモに置き、ここには書かない）

- 置き場所＝GraphQL の `order.transactions.fees`。**RESTの取引一覧には手数料の欄が無い**（ここを見ても出てこない）。
- 欄立て＝`type`（種類）／`rate`（料率）／`rateName`（料率の名前）／`flatFee`（定額分）／`amount`（手数料の合計）。
- 本番3件はすべて `processing_fee` が1件ずつ。**定額分は発生していない**＝**料率だけで決まる**。
- **端数の丸め方は四捨五入**（3件すべてを同時に説明できるのは四捨五入だけだった）。
- 料率の名前は取引ごとに違う＝国内カードの非対面と、JCBカードの非対面。
- 8月上旬に成立した**作家サブスクの実決済1件は特定できた**（品目に月ごとの販売プランが付いている注文）。

**読めなかったもの＝初回入金の予定日**

- `/shopify_payments/payouts.json`・`/shopify_payments/balance.json`・
  `/shopify_payments/balance/transactions.json` はいずれも **HTTP 403**。
  返事＝`This action requires merchant approval for read_shopify_payments_payouts scope.`
- GraphQL の `shopifyPaymentsAccount`（入金の周期と入金1件ごとの予定日が入っている場所）も **ACCESS_DENIED**。
  返事＝`Required access: the read_shopify_payments or the read_shopify_payments_accounts access scope.`
- **したがって入金の予定日はAPIからは読めない。** 権限を足す操作が要る（次項）。

## 5. 代表の操作が要ること（ここで止めた・こちらでは実行していない）

- **やること＝注文読み取り用のアプリ（2026-08-10に代表が作られたもの）へ、入金を読む許可を足して承認する。**
  足す許可の名前は、お店からの断り文言のとおり
  **`read_shopify_payments_payouts`**（GraphQLからも読むなら **`read_shopify_payments_accounts`**）。
- ★**どの画面のどこを押すか、こちらでは実物を見て確かめられなかった。**
  管理画面を開こうとしたが**アカウントを選ぶ画面で止まり**、
  さらに**この端末のブラウザ拡張は管理画面のドメインでの撮影を許可されていない**ため、
  印を付けたスクリーンショットを用意できなかった。**推測の画面名は書かない。**
- **判断をお願いしたいこと**＝許可を足すか／足さずに管理画面の支払い画面を人が見て転記する運用にするか。
  足さない場合でもB-10は埋められるが、毎回人の目が要る。

## 6. Codexの独立レビュー（実施した・1回につき1ファイル1観点／計2回）

**1回目＝この報告書そのもの（観点＝書いてはいけないものが混ざっていないか）**

| 指摘 | 採否 | 直したこと |
| --- | --- | --- |
| 「合鍵に期限がある」は、無効だった事実だけからは断定できない | **採用** | 「原因は未特定。分かっているのは引き換え直したらHTTP 200になったことだけ」に書き換えた |
| 手数料の「定額分は0」は金額の実値であり、同じ報告内の「金額は書かない」と矛盾する | **採用** | 「定額分は発生していない」に書き換えた |

**2回目＝実測に使ったスクリプト（観点＝秘密が漏れないか・読み取りしかしないか）**

| 指摘 | 採否 | 直したこと |
| --- | --- | --- |
| 通信が失敗したときの未捕捉の例外に、要求したURL（お店の識別子・注文ID）が混ざって画面へ出る | **採用** | 共通部品 `_safe-net-20260820.mjs` を新設し、例外の中身を一切出さない固定文言に置き換えた |
| お店からのエラー本文をそのまま画面へ出しており、注文番号やメールが混ざりうる | **採用** | 「ASCIIのみ・`@`を含まない・3桁以上つながった数字が無い」の3条件を機械的に満たしたものだけ通す作りにした |

- ★この指摘は**実際に起きていた**（作業中、通信の時間切れでお店の識別子が画面に出た）。
- 同じ問題が他にも残っていないか、**今回作った通信を行うスクリプト6本すべて**を直して、
  素の `fetch(` が共通部品の中以外に残っていないことを機械で確認した（残り0件）。
  **直した後、6本のうち5本は実際に動かして同じ結果が出ることを確かめた。**
  残り1本（Vaultのメモを書き出す道具）は、動かすと後から手で足した訂正の記述を消してしまうため
  **動かさず、構文の検査だけにとどめた**（未実行と正直に書く）。
- ついでに1件、こちらの自己点検で直したもの＝「未精算がゼロ＝精算済み」と読める表示。
  **この欄の意味を確かめていない**ので、断定しない表示に直した。

## 7. 秘密の扱い

- 合鍵・クライアントID・シークレットの**値は1文字も出していない**（画面・この報告・スクリプトの出力すべて）。
- **金額の実値・お名前・注文番号もこの報告には書いていない**（金額はVault側のメモにのみ記録）。
