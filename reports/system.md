# システム開発ターミナル 報告（2026-08-10・ダッシュボード再設計 第5便・段1）

## 実施したこと＝設計図のみ。実装は0。

代表の決定（憲法7項）に沿って、ダッシュボードの再設計の**設計図**を作りました。
**`dashboard/` 配下の実装ファイルは1文字も変えていません。** 実データも接続していません。

証拠の置き場所＝`reports/assets/20260810_ダッシュボード再設計/`

| ファイル | 中身 |
|---|---|
| `メール画面_390.png` / `メール画面_1440.png` | **最優先の1枚**。スマートフォンから設計しました |
| `トップ_390.png` / `トップ_1440_1画面.png` | 全社の玄関。**PCで1画面に収まることを実測**（868px／900px以内） |
| `あつぺくと中ページ_390.png` / `あつぺくと中ページ_1440.png` | 工程管理の移し先 |
| `00_設計の説明.md` | 憲法7項をどう画面に落としたか・機械で測った結果・やっていないこと |
| `01_対照表_トップと中ページ.md` | いまトップにある**全項目（28行）**の行き先。捨てたものは0 |
| `02_カレンダー保存方式の比較.md` | (a)自前保存 と (b)埋め込み の比較・推し＝(a)・ただし先に実測すべき3点 |
| `03_メールのデータの取り方の比較.md` | 候補5つ・施錠との相性・推し |

## 機械で測った結果（6枚すべて）

- **スマホでタブが2段に折り返していない**＝メール3つ・中ページ5つとも、390pxで**1段**。
- **横に溢れている画面は0**（6枚とも0px）。
- **押す所はすべて高さ44px以上**（0件が44px未満）。
- **トップはPC1画面に収まる**＝中身の高さ868px（900pxの画面）。
- JSエラー0件。**6枚とも自分で目視しました。**

作りながら実測で見つけて直したもの＝5件（タブが左に寄っていた／余計な枠線1本／390pxで予定名が読めない／小ボタン42px／PCで右列が長く左下に空白）。

## ★代表にご判断いただきたいこと（段2の着手条件）

1. **設計図全体のOK**（これが出るまで実装しません）。
2. **カレンダーの保存方式**＝(a)自前／(b)埋め込み。当Tの推しは(a)。
   ただし(b)は設定1行で試せるため、**段2の頭で「ログインしていない機械で見えるか」を実測**してから最終決定するのが安いです。
3. **メールのデータの取り方**。★**いまの作りでは、会場で新着メールを見ることはできません**
   （配信物は許可リスト方式で、実データのファイルが入らないため＝見本だけが出ます）。
4. **伝言掲示板のエピグラフの文言**（図では「敬意をもって、正確に。」を仮置き）。
5. **入金額をトップに出すか**（当Tの推し＝あつぺくと中ページの「お金」へ）。
6. **見た目の案A/Bの切り替えを廃止してよいか**（憲法1で白に確定したため）。

## ★正直に書くこと

- **実機（スマートフォンの実物）では見ていません。** 幅390pxで描画した表示までです。
- **Codexの独立レビューは受けていません**（設計段階のため）。
- **営業日程の初期データの現物を受け取っていません**（カレンダーの予定はすべて見本です）。
- **「お問い合わせ件数」を数える元が、いまはありません**（メールの型から数える形になります・段2で決めます）。

最終更新：2026-08-10
