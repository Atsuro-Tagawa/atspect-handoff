# システム開発ターミナル 報告（2026-08-03・第6便）

モデル＝Opus 5（1M context）／担当＝あつぺくとシステム開発
**このリポジトリには金額・個人名・メールアドレス・非公開URLを書きません。**
**実測値と画像はVault `_実測_A1テスト決済の結果_20260803_ATSPECT.md` ／ `_証拠_20260803_テスト決済の実測\` が正本です。**

## 0. ★依頼の前提が2つ違っていました（正直に）

1. **業務用アドレスの受信箱は、この端末からは読めません。**
   ブラウザにログインされているGoogleアカウントは**代表の私用のGmail 1つだけ**でした
   （私用のメールが並ぶ受信箱）。**前提と違ったため、中身を読まずに離れました。**
   業務メールだけに絞った検索を2回だけ行い（受信箱の一覧は撮っていません）、
   **いずれも0件＝業務用アドレスはこの私用Gmailに転送されていない**ことを確認しました。
2. **業務用アドレスは Gmail ではありません。** DNSを実測した結果、**メールはXserverで受けて**います。
   → **「Gmailのラベル・フィルタでどこまでできるか」は、そのままでは当てはまりません。**

**したがって①〜⑦のメール実物での判定は、当便では実施できていません。**
**メールを開くのは代表のログインが必要な操作＝事前確認が要る4例外の1つです。**

## 1. ★サブスク契約はできています（テストモードの決済でも作られた）

**第2便で「テストモードだと契約が作られるか未確認」としていた点の答えです。**

- 契約は**1件・状態は「有効」**。作成は本日午後2時台。元になった注文も同時刻。
- 商品＝あつぺくとシステム利用料（月額）／**周期は「毎月」**。
- **次回の請求は翌月の同じ日**。以降も毎月同じ日が並んでいます（6か月先まで表示）。
- 決済方法はテスト用カード＝**テスト決済から契約が作られた**ことが確認できます。
- **＝月額の自動継続は動いています。1回きりの支払いにはなっていません。**

**★ただし契約詳細の項目名も「配達」でした**（アプリ側の固定文言・当方では変えられません）。

### 一時停止／再開のボタン

- 契約詳細ページの**右上に「一時停止」ボタンが実在**（読み上げラベルは「契約を一時停止」）。
  隣が「サブスクリプションをキャンセルする」。
- **★「再開」ボタンの実物は未確認**。一時停止しないと現れないためです。
  **当便では押していません**（押すと通知メールが飛び、代表がこれから実物を確認される邪魔になるため）。

## 2. ★入金＝テストモードの決済は計上されません

**注文が2件・契約が1件できているのに、支払い残高は0のまま／支払い一覧は空のままでした。**
→ **テスト決済は入金の対象になりません。初回入金の予定日も出ません。**
→ **初回入金の予定日・決済日から着金までの日数は引き続き未確定です。本物の入金が1件発生するまで分かりません。**
**推測の日付は書きません。**

## 3. メール運用（前提が違ったので、その前提から）

- 分かっていること＝**メールはXserverで受けている**／Shopifyの通知の種類は把握済み／
  **スタッフ通知の受信者は代表お一人だけ**（弦巻さまお二人は未登録・第4便で実測）。
- **仕分けの案**＝「代表が動く」＝申込／情報登録料の入金／**月額の決済失敗**／作品が売れた／お問い合わせ。
  「動かなくてよい」＝月額の自動決済の成功／週次のサマリー。
- **3人で見る形の選択肢（どれも未実測・実装していません）**＝
  ①Xserverの転送で3人へ配る（手数は最少。ただし**誰が返信したか・未返信がどれかは分からない**）
  ②共有の受信箱をGmailにPOP/IMAPで取り込む（ラベル・フィルタが使える。取り込みの遅れあり）
  ③Google Workspaceへ移す（共有受信箱・担当者の割り当てができる。費用と移行作業）
- **当方の推奨（判断は司令塔）＝まず①で「代表が動くもの」だけを3人に配り、
  「誰が返信したか」の管理が本当に要ると分かってから②か③を考える。**

## 4. 後始末（代表の操作・まだであれば）

1. テストモードをオフに戻す（設定→決済→Shopifyペイメント→管理→一番下）
2. テスト注文とお客様にタグ「テスト」／注文メモに `2026-08-03 A-1テスト決済`
3. **契約は「キャンセル」で止める（削除しない）**。契約詳細の右上「サブスクリプションをキャンセルする」。
   **止めないと翌月の同じ日に次の請求が走ります**（テストモードを戻した後だと本物の課金になります）。

## 5. 実施していないこと・判定していないこと

- **メールの実物（①〜⑦）は1つも判定していない**（受信箱が開けないため）。
- **一時停止は押していない／「再開」ボタンの実物は未確認。**
- **初回入金の予定日・着金までの日数は未確定**（テスト決済は入金に計上されないため）。
- **メール運用は設計案のみ。Xserverの設定は開いていない・変更していない。**
- 注文の中身（明細・金額の内訳）は**注文画面では確認していない**（契約側の決済サマリーのみ）。

## 証拠の置き場所

**Vault `_証拠_20260803_テスト決済の実測\`（画像3点）**
※契約の画面には個人名・メールアドレス・カード下4桁・金額が写るため、**公開リポジトリには置きません。**
