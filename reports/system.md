# システム開発ターミナル 報告（2026-08-10・注文読み取りの資格情報の整頓と実測）

★**合鍵・シークレット・注文の中身は、この報告にも成果物にも一切含めていません。**

## 1. .env の整頓（★残した変数名だけを書きます）

- `SHOPIFY_ORDERS_CLIENT_ID` … **1本**（整頓前は2行→1行）
- `SHOPIFY_ORDERS_CLIENT_SECRET` … **1本**（整頓前は2行→1行）
- `SHOPIFY_ORDERS_API_VERSION` … **1本**（`2026-07`）
- `SHOPIFY_ORDERS_TOKEN` … 空の雛形行を**削除**したうえで、引き換えた合鍵を**1本**収めました

**他の変数（お店の識別子・既存の合鍵など）は1文字も触っていません。**
整頓後の `.env` は**変数15本・重複0**であることを機械で確認しました。

- **控え＝リポジトリの外のローカルにだけ**置きました（`C:\Users\ataga\_env-backups\`）。
  Vaultにも、GitHubにも、`scratch/` にも置いていません
  （★前便で `scratch/` に置いた控えも、この外側へ移しました）。
- **`.env` が GitHub の除外対象であること**を、作業の前に `git check-ignore` で確認しています
  （`.gitignore` の5行目／gitの追跡対象にも入っていない）。

## 2. 合鍵への引き換え（実測）

★**管理画面にアクセストークンの欄はありません。** 受け取るのは**クライアントIDとシークレットの2本**で、
そこから**こちら側の道具が合鍵へ引き換える**形でした（**2026-08-10に実測・成功**）。

**この事実を手順書へ反映し、古い指示（「トークンを貼ってください」）を書き替えました**
（`01_代表の操作手順書.md`。残存0件を機械で確認）。

## 3. 許可（スコープ）の実測

**許可されているのは `read_orders` の1つだけ。書き込み系は0件。**

- 前便の懸念どおり「版の宣言」と「ストアの許可」は別物ですが、**今回は一致していました。**
- 既存の合鍵（テーマ・商品用）は**別のアプリ**で、こちらには影響していません。

## 4. 注文の読み取り（実測）

**読めました。**（1件だけ要求する形で確かめました）

- **中身は画面にも報告にも出していません**（お名前・ご住所・金額・注文番号）。
- **ファイルには1バイトも保存していません。**
- 使ったのは**読み取りだけ**です。確定・返金・変更は一切行っていません。

## 5. ★実データ切り替えの関門は、残り2つになりました

| # | 関門 | 状態 |
|---|---|---|
| 1 | **代表の初ログイン実測**（まだ誰も鍵の内側に入れていません） | **未達** |
| 2 | **許可リストの4件目を個人のアドレスにする** | **未達** |
| ~~3~~ | ~~注文を読む許可~~ | **★本日クリア** |

**この2つが済めば、実データへ切り替えられます**（切り替えの手順書は用意済み）。

## 6. していないこと

- **注文データの取り込み・保存はしていません**（読めることを確かめただけです）。
- 定時実行の登録はしていません。
- Codexの独立レビューは受けていません。
- 新しいアプリを作る操作の画面は当方では見ていません（代表が操作されたため）。

最終更新：2026-08-10
