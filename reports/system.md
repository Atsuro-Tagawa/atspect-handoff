# システム開発ターミナル 報告（2026-08-08 第5便）

model=Opus 5 (1M context)／担当＝あつぺくとシステム開発／作業＝**ダッシュボード第2便（メール）**

---

## 作業0．総務部のタブを外しました
設定から1行消すだけで済み、画面の作り直しは不要でした。前後の画像を証拠に置いています。

## 作業1．共有4本の受信を1画面に

**★4本とも既にOutlookに設定済みでした（実測）＝パスワードの入力待ちは発生しませんでした。**
直近60日で **合計242通**（サービス窓口79／会社13／管理150＝上限／採用0）。
型の内訳＝① 22／② 7／③ 3／④ 31／⑤ 13／**⑥ その他 166**。

**読むだけの担保**＝`UnRead`/`Move`/`Delete`/`Save` を1回も呼びません。**サーバー側の既読は変わりません。**
**個人の3本は読みません**（対象一覧に書いていないので、書かない限り開かれません）。

★**正直に**：「見た・返した・担当」の印は**各パソコンにだけ**残ります。
**したがって「二人が同じメールに返す」ことはまだ防げません。画面にもそう書いてあります。**

## 作業2．送信＝**1通届きました。ただし要件は未達です**

代表あてに1通だけ送り、**届いたことを実測**（18:03）。署名も「あつぺくと 担当」で出ました。
**★しかし差出人が共有アドレスにならず、既定の個人アカウントから出ていました。**
確定要件「差出人は共有アドレス」を**満たしていません**。指定の仕方を直しましたが、
**「1通だけ」のご指示を超えるため追加送信はせず、未検証のままです。**

**安全の作り（実測）**＝外部あて／複数宛先／表示名つき／個人アドレスからの送信は**すべて中止**されました。

## Codexの独立レビュー（重大2件を含め、すべて採用）

- **【重大】「外部@…; 社内@…」と書くと外部にも送れた**（最後の@しか見ていなかった）→ 宛先を1件に限定。**4通りの入力で塞がったことを実測。**
- **【重大】差出人の指定に失敗しても送信を続けていた** → **送らずに中止**。
- 記録に件名を残さない／Windowsの利用者も残す／「送りました」→「Outlookに渡した（届いた保証ではない）」。

## ★事故になりかけたことを1件

スクリーンショットの見本モードが台帳しか退避しておらず、**メールの実データが写った画像**ができました。
**削除し、実データを全部どける形に直しました。公開リポジトリには上げていません。**

## 作業3．第1便の宿題と、報告の訂正

- カレンダーと営業記録のURLを**貼るだけ**の用紙をデスクトップに置きました。
- Cloudflare手順書は作業0の影響**なし**（「総務」の語は0件）。
- ★**訂正**：第1便で「手順書を用意しました」と書きましたが、**デスクトップに置いていませんでした**
  （リポジトリには実在）。置き直しました。また「**20分ほど**」は**測っていない数字**でした。**取り下げます。**

## 代表にお願いすること
1. **もう1通だけテスト送信の許可**（差出人が共有アドレスになるかを確かめます）
2. Cloudflareのアカウント作成（第1便から継続）
3. カレンダーと営業記録フォームのURL

## 実装していないこと
返信画面（AIの下書き）／差出人の検証／メール本文の表示／サーバー側の自動仕分け／
管理の受信箱の全通読み込み／実機での確認／ホスティング。

## 証拠
`reports/assets/20260808e_ダッシュボード第2便_メール/`（画像3枚。すべて**見本のデータ**です。）
