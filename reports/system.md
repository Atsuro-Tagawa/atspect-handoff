# システム開発ターミナル

## 現状（2026-07-22）

**作家の本登録用フォーム（規約同意＋支払方法の確認・非公開ページ）を本番テーマへ実装・検証済み。決済の本番接続は代表確認の一歩手前で停止中。独立レビューは実行中（結果が出しだい反映・追記する）。**

- 審査を通過した作家に個別にURLを送って使う、本登録のお手続きページを新設した（一般の来訪者からは辿れない配置・検索エンジンにも載らない設定を実測確認）。
- 内容＝規約（最新版）への同意チェック・支払方法の選択・掲載予定作品の記入欄。特別枠に関する選択肢や記述は一切存在しない。
- 継続課金（サブスク）へつなぐ導線は「器」まで実装済み。実際のリンクは空欄のままにしてあり（＝画面に出ない）、代表側の公開操作と決済テストが済んでから有効化する。
- 検証＝PC・モバイル両幅の実画面確認で崩れなし、入力チェックの動作確認済み。送信テストでは自動送信への防御（キャプチャ）が正しく働くことを実証（最終の送信1回は人の手での確認が必要・申し送り済み）。
- 独立レビュー（第三者チェック）は現在実行中。指摘が出れば反映し、この報告を更新する。
- 前回まで＝画像機能の実装・コミット完了／ポリシー多言語化の調査提案（代表判断待ち）／請求書運用の整備（代表の台帳列追加待ち）。いずれも変更なし。

## 未着手・待ち

- 本登録フォームの最終送信テスト1回（人の手で・キャプチャがあるため）。
- 新旧の同意フォームの棲み分け（既存ページ側のフォームを残すか閉じるか）＝代表判断。
- サブスク商品の公開操作・決済テスト（代表側）→完了後にフォームの決済案内を有効化。
- 台帳への項目追加（代表側・数分）／ポリシー多言語化の推奨案の可否（代表判断）。

## 今すぐ着手できるタスク

- 独立レビューの結果反映（実行中）。その後は次の指示待ち、または社内システム本体側の積み残しに着手可能。

（金額・料率・営業動線・具体的な手順の中身は機微につきチャットで扱う。）
