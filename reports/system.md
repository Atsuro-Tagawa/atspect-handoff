# システム開発ターミナル 報告（2026-08-08 第3便）

model=Opus 5 (1M context)／担当＝あつぺくとシステム開発

---

## 1. URLの実測（推測せず、全部叩いて確かめました）

作家ページの実URL＝**`https://atspect.com/pages/artist/<ハンドル>`**（46文字）。
ハンドルは**ローマ字・小文字・ハイフン区切り**（姓-名）。連番IDではありません。
**言語ごとのURLは存在しません**（`/en/…` 等はすべて404）＝**1つのURLで8言語**を切り替える作り。
→ **SNSのカードは常に日本語**になります（言語別に出すにはURL分割という大きな変更が要ります）。

## 2. 短縮URL＝**作れます**（★1本も作っていません）

根拠＝**同じ仕組みが既に動いています**。URLリダイレクト14本の中に `/apply → /pages/entry-…` があり、
**実際に301で転送される**ことを確認しました。
方式＝`atspect.com/a/<ハンドル>`（**46→33文字**）。作家さま1名につき1本の登録（ワイルドカード不可）。
★**`/a/` が予約パスでないかは未確認**。**作る前に1本だけ試して確かめてください。**

## 3. OGP＝**直しました（ライブ反映済み）**

**直す前**＝全作家が**同じ会社ロゴ**＋**同じ定型文**。カードを見ても誰か分かりませんでした。
**直したあと**＝og:image は**近影**（1200×630）、og:description は**ご本人の一行｜ジャンル・拠点**。
ライブで実測済み（月額の作家さま＝近影が出る／物故＝画像を持たないので既定画像のまま・説明文はご本人の一行）。

★**切り方は実物を見て直しました。** `crop: top` だと縦長の近影が**目のあたりで切れます**。`center` に変更。
★**許諾**＝`image_permission` が空または取得済みのときだけ画像を出す（確認できないものは出さない側）。
★**SNSのクローラはしばらく古いカードを見ます**（キャッシュがUAごとに分かれていることを実測）。

## 4. 公開/非公開スイッチ＝**設計のみ。作っていません**

`artist` は40フィールドありますが**月額かどうかを持てる欄がありません**
→ **`is_member`（boolean）を1つだけ足す**設計。非公開のときは**枠ごと出さない**（空欄を出さない）。
★既存の `is_listed` は別の軸で、**全作家で未設定＝いま誰も使っていません**。
★**SNS運用Tの寄贈枠とは重ねていません**＝`donation_work` 型が既に存在することを実測し、
**枠を出すかはこちら、枠の中身はSNS部**と切り分け、`donation_work` には触っていません。

## 5. .gitignore

**本日すでに作成・push済み**（このリポジトリには1つもありませんでした）。既存ファイルは1つも消していません。
ローカルの `.env` 点検＝**合計3件**。うち**2件はリポジトリ内で2件とも除外済み**。
**残り1件はリポジトリ外**（gitの経路には乗っていませんが平文で置かれています。場所は書きません）。

## 6. Codexの独立レビュー（1回・指摘3件をすべて採用）

【高】型の限定が無く他のmetaobjectを巻き込みうる → `system.type == 'artist'` に限定
（★正しく取れることを**ライブでの一時的なデバッグ出力で実測**。`metaobject.type` は空でした）。
【中】許諾の判定が安全側でなかった → 許可リスト方式へ。【中】説明文の長さ制限 → `truncate: 160`。

## 7. 代表にお願いすること

1. **短縮URLの形**＝`/a/<ハンドル>` でよいか（作る前に1本試作します）。
2. **`is_member` を足してよいか**（メタオブジェクトの定義変更にあたるため）。
3. **8言語のカード**＝日本語のみのままでよいか。
4. **リポジトリ外の `.env` 1件**をどうするか。

## 8. 証拠の置き場所

`reports/assets/20260808c_作家ページの名刺化_OGP是正/`
（カードの前後の画像＋切り方の比較。**月額の作家さまはお顔をぼかし、お名前を伏せています**。原本はVault。）
