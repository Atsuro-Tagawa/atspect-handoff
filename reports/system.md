# システム開発ターミナル 報告（2026-08-03・第2便）

モデル＝Opus 5（1M context）／担当＝あつぺくとシステム開発

## 1. ★入金サイクルの実測（会社の資金繰りの土台）

| # | 項目 | 実測 |
|---|---|---|
| ① | 入金の周期 | **週次・毎週金曜日**（支払い間隔＝週／曜日＝金曜日／明細書表示名＝Shopify） |
| ② | 初回入金の予定日 | **画面に無かった。** 支払い一覧は空・**支払い残高 ¥0**・注文取引 **0件**。予定日を出す欄自体が出ていない |
| ③ | 初回だけの滞留期間 | **画面に無かった**（Shopify ペイメントの全カード・「その他の操作」・ⓘを全部開いて確認） |
| ④ | 決済日から着金までの日数 | **画面に無かった** |

**★③④はShopify公式ヘルプに記載がありました（当店の画面の実測ではないので区別します）**＝
日本（JPY）の行＝**最短決済期間 5営業日／最低支払額 5円**。
**「日本のマーチャントは、週ごとまたは月ごとの支払いスケジュールを選択できますが、日ごとは選択できません。」**
**初回に固有の滞留期間の記載は、確認した2ページには無し。**

**★資金繰りとして言えること**＝周期は確か（週次・金曜）。**ただし初回入金がいつになるかは、まだ分かりません。**
決済実績が0件のため予定日が表示されないためです。**決済が1件通れば支払い一覧に出るはずで、そのとき実測できます。**
**現時点で初回入金日を当てにした資金計画は立てられません。**

## 2. 販売プラン名＝**変えられないことを確定しました**（前便の記録を訂正）

**★訂正**＝前便で「チェックアウトコンテンツ編集画面が3回とも真っ白で開かない」と書きましたが、
**壊れていませんでした。表示に60〜90秒かかるだけ**でした。待ち足りずに「開かない」と判断したのが誤りです。

開いた上で画面の検索欄で実測：
- **`定期小計` ＝0件**／**`1か月` ＝0件**
- `小計`＝**Subtotal label（小計）は編集できるが「定期小計」という項目は無い**
- `Recurring`＝Recurring total tooltip・Recurring payments（定期的な支払い）は**編集できる**
- `配達`『定期』＝配送・定期購入の**エラー文言は編集できるが、販売プラン名にあたる項目は無い**

> **確定＝決済画面の「1か月ごとに配達」と「定期小計」は、当社の手段では変えられない。**
> 司令塔判断でサブスクアプリは替えないため、これ以上追いません。

## 3. 代表対応待ち

- **A-1のテスト決済**（テストモードのオン／オフは代表の操作）。手順書v2は変更していません。
- 「配達」の先回り文言は**SNS運用T**が置きます。当Tは触りません。

## 4. 実施していないこと・判定していないこと

- 決済は1件も通していない → **初回入金予定日・実際の着金日数は未実測**。
- **入金設定は1つも変更していない**（ダイアログは開いたが「保存」は押していない）。
- **テーマのコンテンツは1文字も編集していない**（検索欄に打ち込んだだけ・保存ボタンは終始灰色）。
- **③初回の滞留期間は画面にもヘルプにも見つからず＝未確定。**
- 支払い間隔プルダウンの選択肢は列挙していない（設定を触る操作になるため）。

## 証拠の置き場所

`reports/assets/20260803b_入金サイクルと販売プラン名の確定/`（画像6点＋README）
※口座が写る画面の全体像はVaultのみに置いています（このリポジトリは公開のため）。

## 5. やらないと決めたこと

`reports/assets/deferred-master.md` の2026-08-03第2便の節に5件を末尾追記（Vaultの繰り越しマスターにも同内容）。
要点＝販売プラン名の未試行経路は追わない／アプリは替えない／先回り文言は書かない／入金設定は変更しない／
**初回入金日を推測で書かない**。

## Codexレビュー

当便は未実施（画面の実測のみでコード変更が無いため）。
