# 制作T 報告（2026-08-02）— 送信時のスクロール停止を共通側で是正／30項目の仕分け／入稿一覧の整合

使用モデル＝Opus 5（1M context）／担当＝あつぺくと制作／作業ディレクトリ＝`C:\Users\ataga\atspect-theme`

---

# 【第9便】送信時のスクロール停止を共通側で是正／30項目の仕分け／入稿一覧の整合／閉じる3件

## 結論（5行）

1. **「押しても数秒なにも起きない」を、個別ページではなく共通側（`assets/atspect.js`）で塞ぎました。**
   これで**必須欄のあるフォーム17件すべて**が覆われます。**是正前は5件だけ対応済みで、12件が無防備でした。**
2. ★**`scroll-behavior` は消していません。** 平常時は `smooth` のまま。**検証が失敗した瞬間だけ**即時にします。
3. **ライブ全347ファイルを取り直して数えました。** `html` 全体に効く `smooth` は**1箇所だけ**でした。
4. **30項目の仕分けを完了**（実装はしていません＝ご指示どおり）。**8言語の抜けは公開前チェックに含まれていない**ことを実測で確定しました。
5. **`00_入稿一覧.txt` とデスクトップ `04_入稿一覧.pdf` を同時に直しました**（文字数10,983で完全一致）。

---

## 1. 最優先＝送信時に画面が止まる件

### (a) 全数の洗い出し（★ローカルを信じず、ライブから取り直しました）

Shopify CLI が 401 で通らなかったため、**Admin API で全518アセットのうちテキスト347件を直接取得**して数えました。

| 検索対象 | ファイル数 | 箇所数 |
|---|---|---|
| `scroll-behavior` のCSS宣言 | 8 | **17** |
| JSの `behavior:'smooth'` | 8 | **13** |
| `scrollIntoView(` | 12 | 25 |
| `window.scrollTo/scrollBy(` | 4 | 4 |

★**このうち「ページ全体に効く smooth」は `assets/atspect.css` の1箇所だけ**でした。
残り16箇所は**スライダー等のスコープ付き**か、**各ページのガード用**です。

### (b) ★本題＝無防備なフォームが12件ありました

| 状態 | 件数 | ファイル |
|---|---|---|
| ガード有り | 5 | artwork-entry / terms-artist / onboarding / materials / registration |
| **ガード無し** | **12** | **artist-cancellation（必須9）** / main-article(6) / **publish-consent(5)** / artwork-detail(4) / **contact(4)** / email-signup-banner / footer / main-register / newsletter / atspect-footer / atspect-password / contact-form |

**個別ページを1枚ずつ直す方式では、この12件を取りこぼします。だから共通側で塞ぎました。**

### (c) 選んだ案と、捨てた案

| 案 | 採否 | 理由 |
|---|---|---|
| **A. 共通JSで、検証が失敗した瞬間だけ即時にする** | **採用** | 1箇所で17フォーム全部が直る。**専任ファイルに触らずに済む** |
| B. `html{scroll-behavior:smooth}` を消す | 却下 | ★ご指示で禁止。全ページの見た目が変わる |
| C. 各フォームに `novalidate` を付けて自前検証にする | 却下 | 17ファイルの書き換えになり、**専任ファイルにも手が要る** |
| D. 長いページだけ個別に直す | 却下 | 「長い」の線引きが恣意的。**今後ページが伸びたときに漏れる**（＝ナビ切れと同じ型） |

★**実装の要点**＝`invalid` はバブルしないので**捕捉フェーズ**で `document` に付けます。
戻すときは**値を覚えて書き戻すのではなく `removeProperty` で消します**。
理由＝個別ページ側のガード（600ms）と二重に走ったとき、そちらが「共通側が当てた `auto`」を覚えてしまい、
**最後に `auto` が残ってそのページだけ smooth が恒久的に消える**からです。
**「最後は必ず削除で終わる」形（共通700ms＞個別600ms）にして、順序に関係なく安全にしました。**

### (d) 実測（前後）

**登録フォーム**（高さ4,836px・必須欄12・invalid発火12回）：

| | 是正前 | 是正後 |
|---|---|---|
| 押す直前の位置 | 2,961px | 2,961px |
| **押して0.3秒後の移動量** | **0px（1pxも動かない）** | **−1,959px（既に飛んでいる）** |
| 押した瞬間の `scroll-behavior` | `smooth` | **`auto`** |
| 落ち着くまで | **2,202ms** | **9ms** |

**解約フォーム（元々ガードが無かったページ）**（高さ3,747px・必須欄9）：
平常時 **`smooth`（維持）** → 押した瞬間 **`auto`** → **0.3秒後に既に772px移動** → 1.5秒後 **`smooth` へ復帰・インライン残留なし**。

**二重ガードの安全確認（登録フォーム）**＝700ms後 `auto`、**900ms後に `smooth` へ復帰**、最終的なインライン指定は**なし**。

**SNS運用T側への影響**＝`terms-artist` は**送信ボタンが常時 `disabled`** で**そもそも `invalid` が発火しない作り**でした（悪影響なし）。

### (e) 画像

`reports/assets/20260802_scroll-smooth/`（01＝是正前・02＝是正後）
★**画像は「実測したスクロール位置を描画したもの」**で、押した瞬間の録画ではありません（正直な区別）。

### (f) ★自分の判断を1つ訂正します

`theme161-update.mjs` が **「読み戻し不一致（BYTE-MISMATCH）」でエラー終了**しました。
しかし**数秒後に取り直すと、ライブは新内容と完全一致**（`f72084b2…`・393行・F3ガード有り）。
**＝アップロード直後のAdmin APIが古い内容を返した偽陰性**でした。
★**このエラーを「反映できていない」と読むと、正しく入っているものを二重に押すことになります**＝繰-制作-15として起票。

## 2. 第3手＝30項目の仕分け（★実装はしていません）

**現物**（`_確定設計_..._20260724_ATSPECT.md` §C）を開いて全30項目を転記し、**分類を自分で付け直しました**
（他ターミナルの要約とは3点で食い違い、その理由も表に書いています）。
全文＝Vault `_仕分け_作品登録30項目の機械化可否_20260802_ATSPECT.md`。

| 分類 | 件数 |
|---|---|
| ① 機械で見られる | **24** |
| ② 人が目で見るしかない | **5**（vendor表記の決定・価格・本文・画像が実写か・表示の崩れ） |
| ③ そもそも要らない | **1**（facet系＝移行の採否が未決） |

①24項目の内訳＝**`page-audit.mjs` に既にある6**（21・22・23・24・25・27）／**足せば動く18**。

★**要点2つ**
- **「既にある」6項目も、できるのは「取得」だけです。** `page-audit.mjs` が自動で不合格を出すのは
  **HTTPの4xx/5xx とコンソールエラーだけ**で、可視テキスト・metaタグ・スクショは**取ってくるが判定しません**。
- **①のうち3項目（SKU規則・素材/技法の語彙表）は前提が未確定で、いま作っても動きません。**

### ★8言語の抜けは公開前チェックに**含まれていません**（実測で確定）

`artist-i18n-update.mjs` の `verify` は**構造上、抜けを検出できません**。根拠3つ：

1. **artist_i18n と個別フィールドの「一致」だけ**を見るため、**両方とも空なら正常として通ります。**
2. **ドイツ語は1度も検査されていません**（個別フィールドが無く `continue` される）。
3. 実データ（625名全件・読み取りのみ）：

   | フィールド | 8言語とも値がある作家 | verifyが報告した不一致 |
   |---|---|---|
   | `bio` | **625 / 625** | 各言語1〜6件 |
   | `message` | **1 / 625** | **各言語わずか1件** |

   ★`message` は**624名に1言語も無い**のに不一致は各1件＝**「無いこと」を数えていない**ことの実証です。
   ※`bio`（作家ページ本文）は**8言語とも625/625で充足**しており、**現時点で実害は出ていません。**

## 3. 繰-制作-13＝入稿一覧の不整合（両方を同時に是正）

- `00_入稿一覧.txt`＝行01を**「★欠番（2026-08-01・制作T）」＋現在の正データへの案内**に差し替え。
- `04_入稿一覧.pdf`＝**00から作り直し**。
- **機械照合＝00の全文が04に含まれる True／空白を除く文字数 10,983 対 10,983 で完全一致／古い記述の残存0。**
- **画像でも目視しました。**

★**この作業中、自分が00へ文字化けを1文字混入させました**（`\20260801` がPythonの8進エスケープで
U+0082 の1文字に化けた）。**機械照合が「全文一致 False」で止まったことで見つかりました。**
修復して残存0を確認済み。**★「機械照合を先に作ってから直す」が効いた実例です**
（数値を見ずに済ませていたら、そのまま代表の手元へ渡っていました）。

## 4. 閉じたもの（理由つきで繰り越し2か所へ記録済み）

- **繰-制作-14（TOPボタンが四角）**＝閉じました。CSSの指定は意図的と読めます。**読めるものを推測で変えない。**
  ★丸くされたくなったら `assets/atspect.css` の `#atspect-back-to-top{border-radius:0 !important}` を消せば
  JS側の `50%` が効いて丸くなります（開ける形で記録）。
- **16px化の見た目の良し悪し・スクロール量が増えることの影響**＝閉じました。読めることは実測済み。良し悪しは実機で代表のご判断。
- **是正前のページ高さの遡り計測**＝閉じました。**取れないものを取りに行かない**（本番を戻さないと測れません）。

## 5. 実施していないこと（正直な申告）

- **30項目の実装をしていません**（ご指示どおり仕分けまで）。**スクリプトは1本も作っていません。**
- **`page-audit.mjs` を1行も変更していません。**
- **`theme161-update.mjs` の偽陰性を直していません**（システム開発T所有＝繰-制作-15で起票のみ）。
- **`artist-i18n-update.mjs` を直していません**（同上＝繰-制作-16）。
- **SNS運用T専任の terms-artist / onboarding には触れていません。**
- ★**範囲の逸脱を1件申告します**＝`sections/atspect-artwork-entry.liquid`（第9便で専任と指定された `entry`）に、
  本日より前の時点で同じスクロール是正を入れてしまいました。**取り消すと不具合が戻るため残置**し、繰-制作-17として起票しました。
  **今回の共通側の是正でこのページも覆われるため、個別の是正は本来不要**です。要否はSNS運用Tのご判断に委ねます。
- **通知メール・決済・作家データに触れていません。紙にも触れていません。**
- **Codexの独立レビューは本便では未実施です。**
- **実機（スマートフォン・タブレット）での実タップは未実施**（すべて実ブラウザのモバイル幅）。

## 6. 判定していないこと（上とは別）

- **ガード無しだった12フォームのうち、実際に「何秒止まるか」を測ったのは解約フォームの1件だけ**です。
  他11件は**共通ガードが効く仕組みであることの確認まで**で、1件ずつの前後計測はしていません。
- **`main-article`（ブログのコメント欄）が実際に使われているかを確認していません。**
- **共通ガードが `invalid` 以外の場面に影響しないことは、コードの条件から言えますが、
  全ページで走らせて確かめてはいません。**
- **30項目の各実装コストを見積もっていません**（可否の話であって工数の話ではありません）。
- **`artist-i18n-update.mjs` の検証は `bio` と `message` の2フィールドのみ。** 他のミラー対象は測っていません。
- **`04_入稿一覧.pdf` の6ページのうち、目で見たのは3ページ**（該当箇所を含むp1・p2・p4）です。

## 【変更（すべてライブ反映・実体で確認済み）】
テーマ＝`assets/atspect.js`（F3ガードを追加・393行・sha256 `f72084b2…`）
Vault＝`00_入稿一覧.txt`／`_04_入稿一覧_生成用.html`（新規）／`_仕分け_作品登録30項目の機械化可否_20260802_ATSPECT.md`（新規）／
`_実装ログ_送信時のスクロール停止の共通是正と30項目仕分け_20260802_ATSPECT.md`（新規）
デスクトップ＝`04_入稿一覧.pdf`（作り直し）
繰り越し＝`reports/assets/deferred-master.md` と Vault の繰り越しマスター（**両方へ末尾追記のみ**・各+5,596バイト）

## 【バックアップ】
`_backups\_auto\theme161-atspect.js\20260801-203132.bak`／`_backups\_auto\00_入稿一覧.txt\20260802-000001_pre-mojibake-fix.txt`／
`_backups\_auto\desktop-nyuko-folder\20260801-pre-04regen\`

---

# 【第8便】登録フォーム16px化・TOPボタン・入稿フォルダ整理・宿題4件

## 結論（4行）

1. **第1段階（登録フォーム）を16pxへ揃えました。6幅×8言語＝48通りすべてPASS。**
   ★**SNS運用Tの数値は写さず、自分で現物を実測して21種類の表を作ってから**大きい側へ揃えました。
2. **TOPボタンのラベルを8.5px→12px**（タップ範囲52×52px＝44px以上を実測）。
3. **デスクトップ11_パンフレットを軽量版へ差し替え／旧体裁7ファイルを移動**（**削除0件**）。
4. ★**移動したせいで検証スクリプトが不合格になったので、参照元を付け替えて復旧**させました。

## 1. まず自分で実測しました（写していません）

現物から**21種類**の文字を拾って表にし、そこから分類しました。

| 分類 | 主なクラス | 前 | 後 |
|---|---|---|---|
| 本文 | `__lead` | 15.5px | **16px** |
| ラベル | `__label`（14箇所） | 14px | **16px** |
| 同意文 | `__check-text` | 13px | **16px** |
| 送信ボタン | `__submit` | 13px | **16px** |
| ヒント | `__hint`／`__form-note` | 13px | **15px** |
| 小ラベル | `__label--sub` | 12px | **15px** |
| 注記 | `__consent-note`／`__backlink` | 13／13px | **14px** |
| バッジ | `__req`／`__opt`／`__cond` | 12px | **12px（下げていません）** |

★`__hint` は宣言が`1.15rem`なのに**実効13px**でした（別の規則が上書き）。**宣言でなく実効値で直しています。**

## 2. 検証＝6幅（320/360/390/430/768/1280）× 8言語 ＝ 48通り

**すべてで 12px未満0・最小12.0px・タップ不足0・泣き別れ0・横溢れ0（−4）。**

### ★数値が全PASSでも見つかった不具合（1件）

**幅360pxだけで「必須」バッジが「必」「須」に泣き別れ**していました（実測＝2行・各21×22px）。
**ラベルを16pxへ上げて折返し位置が動いたため、この幅でだけ発生**。
同ファイルの他バッジ（`__opt`/`__cond`）には既に`nowrap`があり、**`__req`だけ抜けていました**。
→追加して360pxで泣き別れ0を再実測。**画像でも入力欄・同意文・送信ボタンを確認**（証拠01・02）。

### ページ高さ（是正後の実測）

ja＝1280px:3,819／768px:3,846／430px:4,652／390px:4,836／360px:4,982／320px:5,306。
★**正直な申告＝是正前の高さを測っておらず、増加量そのものは出せません。**

## 3. TOPボタン（繰-制作-12）

ラベル**8.5px→12px**（字間0.16→0.12emで幅増を抑制）。ボタン**52×52px＝44px以上**（実測）。
ラベルはボタン内に収まり、幅390pxで**他要素との重なり0件**。画像でも確認（証拠03）。

★**気づいたが直していない点**＝JSがインラインで`border-radius:50%`を指定しているのに、
`atspect.css`の`#atspect-back-to-top{border-radius:0 !important}`が勝って**四角**です。
CSS側は「Back-to-top button refinement」節にあり**意図的と読める**ため直さず、**繰-制作-14**として起票。

## 4. 入稿フォルダの整理

**(a) 11_パンフレット.pdf**＝13,980,134→**2,894,032バイト**（軽量版とMD5一致）。差し替え前にバックアップ。
`01_はじめにお読みください.txt`の「11＝A版とデータも一致」を**「12〜15は同じ。11だけは軽量版」**へ更新し、
**メモ帳で実際に開いて目視**（UTF-8 BOM／CRLF／4,382文字・崩れなし）。

**(b) 旧体裁7ファイル**＝`_パンフ制作履歴_アーカイブ_20260729_ATSPECT\20260801_旧体裁_入稿フォルダから移動\`へ**移動のみ**。
★**日付のサブフォルダを作りました**（アーカイブ直下に旧番号の01が既にあり、紛らわしくなるため。**名前は1文字も変えていません**）。
**★削除0件の検算＝移動前後のサイズ合計がともに 54,428,057 バイトで完全一致。**

### ★移動したせいで壊れたものを直しました

移動直後に`verify_before_print.py`が**3件不合格**に（01が無い）。**「動かしたら参照元を全部数え直す」**を実行し、
参照4箇所を`A_トンボなし版\01_パンフ_印刷用_トンボなし.pdf`へ付け替え、**再実行で全項目合格**。

★**残った不整合（起票）**＝`00_入稿一覧.txt`が移動した01を提出物として記載したままです。
デスクトップの`04_入稿一覧.pdf`も同内容のため**片方だけ直すと照合が崩れます**＝**繰-制作-13**。

## 5. 宿題（第6便の自己申告）

チェックリストA欄を**「サイトと突き合わせる」→「紙どうしで数字が合っているかを見る」**へ変更。
あわせて**C-1（社名）も「統一済み」へ更新**（前便の是正で記述が古くなっていました）。

## 6. やらないと決めて閉じたもの（理由つきで繰り越しへ記録）

- **build_panf_nyuko.py／ocr_win.ps1／verify_before_print.py 全体のCodexレビュー**＝
  3つとも**紙を刷る前の工程の道具**で、紙は既に刷り上がっています。次に使うのは原稿を変えるときで、
  そのときは目視チェックシートと空振り検査2本が効きます。**営業開始の合否に関わらないため閉じます。**
- **OCR検査語の拡張・日本語以外の禁止語の検出率測定**＝同じ理由。現在の検出率
  （本文100%・最小サイズ94.4%・誤検出0）で運用上十分と判断しました。

## ★第8便で実施していないこと

- **是正前のページ高さを測っておらず、増加量を出せません。**
- **`00_入稿一覧.txt`とデスクトップ`04_入稿一覧.pdf`を更新していません**（繰-制作-13）。
- **TOPボタンの`border-radius`の残骸を直していません**（意図的と読めるため・繰-制作-14）。
- SNS運用Tの専任ファイル・通知メール・決済・作家データ・**紙の現物**に**未接触**。
- **Codexレビューは本便未実施**／**実機の目視・実タップは未実施**。

## ★第8便で判定していないこと

- **16px化の見た目の良し悪しは判定していません**（読みやすさの実測と画像確認まで）。
  ★**ページが長くなるため、スクロール量が増えることの影響は測っていません。**
- **48通りの検証は「登録フォームの中身」に限っています**（ヘッダー・フッターは対象外）。
- **`__label--sub`を15pxにしたのは指示の分類に無い自分の判断です**（16pxにすると入れ子の階層が消えるため）。
- **移した7ファイルの中身は開いていません**（移動のみ・サイズとファイル名で照合）。
- **デスクトップの他ファイル（02・03・04・12〜15・A版・B版）は今回触れておらず、MD5の再照合もしていません。**

**証拠＝`reports/assets/20260801_form16px/`**／Vault実装ログ＝
`_実装ログ_登録フォーム16px化とTOPボタン・入稿フォルダ整理_20260801_ATSPECT.md`

---

# 【第7便】料金非掲載の確定・社名表記の全数統一・図版文字の全数点検

## 結論（4行）

1. **繰-制作-09（サイトに料金を載せるか）＝載せないと確定**（理由も繰り越しへ記録）。
2. **社名を「株式会社ARTS RESPECT」へ統一＝16箇所を是正。★直す前に全数を洗いました。**
3. **図・グラフ・SVG内の文字の点検＝打ち止め**（サイト全体でSVGの文字は市場図の2つだけ・是正済み）。
4. **紙には一切手を入れていません**（実測の結果、紙はもともと目標形と一致していました）。

## 1. ★ローカルを信じていたら半分見落としていました

最初にローカルをgrepしたときは**2ファイル・3箇所**しか出ませんでした。
**ライブから9ファイルを実測pullして数え直したところ13箇所**（`atspect-tokushoho.liquid`の9箇所と
`atspect-contact.liquid`の1箇所は**ローカルには無かった**）。「ローカルは古い」がまた効きました。

## 2. 社名表記の全数洗い出し（実測）

| 場所 | 空白あり | 空白なし | 対応 |
|---|---|---|---|
| `atspect-tokushoho.liquid` | **9** | 4（**同一文書内で混在**） | 是正 |
| `atspect-footer.liquid` | **2** | 0 | 是正 |
| `atspect-contact.liquid` | **1** | 0 | 是正 |
| `main-password-header.liquid` | **1** | 0 | 是正 |
| ショップポリシー `LEGAL_NOTICE`（特商法） | **3** | 3（**同一文書内で混在**） | 是正 |
| ショップポリシー他6件／ページ本文60件／locales | 0 | — | 変更なし |
| **紙（申込書4・規約4・料金1+1・パンフ3）** | **0** | **13** | ★**手を入れない**（目標形と一致） |
| 通知メール | **確認できず** | — | ★権限不足（下記） |

**合計16箇所を是正。是正後のライブ実測＝空白あり0件。**
※`atspect-terms-artist.liquid`（SNS運用T専任）は実測で**もともと空白なし3件**＝是正不要でした。

## 3. ★法的文書（特商法表記）に触れたので明記します

ショップポリシー`LEGAL_NOTICE`に3箇所ありました。**法的文書のため次を踏んでから反映**しています：
①**全ポリシー7件をバックアップ** ②置換は**社名の空白のみ**に限定 ③**長さの差が除いた空白の数(3)と一致**を確認
④**空白を除いた文字列が置換前後で完全一致**を確認（＝社名の空白以外は1文字も変えていない）⑤読み戻して0件を確認。

## 4. 通知メールは確認できませんでした（正直な申告）

`translatableResources(EMAIL_TEMPLATE)` を試しましたが **`read_translations`権限が無くACCESS_DENIED**（実測）。
**推測で「問題なし」とは書きません。** 権限を持つターミナルでの確認をお願いしたく**繰-制作-11**として起票しました。

## 5. 図・グラフ・SVG内の文字＝打ち止め

テーマ全体（sections/snippets/layout/templates）を機械検索した結果、
**SVGの`<text>`は`atspect-about.liquid`の2つ（1%・99%）だけ**でした。
他のヒットは**すべて`<textarea>`**（検索語の誤ヒット）。**その2つは前便で是正済み**（5.4px/4.1px→13.0px/16.3px）。
ページ側でも確認＝トップ0件／作家詳細0件／About 2件（是正済み）。
**＝「小さすぎて読めない図の文字」はサイト全体で市場規模図の1件だけでした。**

★**範囲外だが見つけたもの**＝サイト共通の**「TOP」ボタンのラベルが8.5px**（`assets/atspect.js:318`）。
本日の是正で他を12〜14pxへ揃えたため**いまサイト内で最も小さい文字**です。
**今回の指示範囲（図・グラフ・SVG内）の外**のため手を入れず、**繰-制作-12**として起票しました。

## ★第7便で実施していないこと

- **紙に一切手を入れていません**（刷り上がっているため・かつ実測で目標形と一致）。
- **通知メールを確認していません**（権限不足）／**「TOP」ボタン8.5pxを直していません**（範囲外・起票のみ）。
- **前便のチェックリストA欄に、料金非掲載の確定を反映していません**（次便で反映が必要です）。
- SNS運用Tの専任ファイル・入稿データA版B版・デスクトップのフォルダ・繰-制作-01/02に**未接触**。
- Codexレビューは本便未実施／実機の目視は未実施。

## ★第7便で判定していないこと

- **社名の統一形そのものの妥当性は判定していません**（指示に従いました）。
  ★**英文の`ARTS RESPECT Co., Ltd.`は対象外**として触れていません（和文の「株式会社＋社名」だけ）。
- **特商法表記の内容（条文としての妥当性）は一切判定していません**（社名の空白のみ）。
- **図の点検は「文字」に限っています**。図そのものの正しさ（円の大小・矢印の向き・色）は
  前便で市場図を目視した以外は判定していません。
- **画像（ラスタ）に焼き込まれた文字は測れません**（サイトの図はSVGのみのため今回は該当なしと判断）。
- **「TOP」ボタンの8.5pxが実際に読めないかは判定していません**（実寸を測っただけです）。

Vault実装ログ＝`_実装ログ_社名表記の全数統一と図版文字の全数点検_20260801_ATSPECT.md`

---

# 【第6便】CJK実線幅の目視確認・市場規模図の是正・紙とサイトの突き合わせ表

## 結論（3行）

1. **CJK3言語を実画像で確認＝いずれも読める**（豆腐0・線の太さ中央値は4言語とも1.0px）。
   ★**前便の「8言語ともJost」は不正確でした**＝書体スタックは言語ごとに違います（下記1）。
2. **★図版の実害を1件発見して是正**＝Aboutの市場規模図のラベルが**5.4px／4.1px**で読めませんでした。
3. **紙とサイトの突き合わせチェックリストを1枚作成**（現物を手に持ちながら使える形）。

## 1. ★前便の訂正＝書体スタックは言語ごとに違う

| 言語 | スタック（先頭2つ） |
|---|---|
| ja | Jost / **Noto Sans JP** |
| zh-cn | Jost / **Noto Sans SC** |
| zh-tw | Jost / **Noto Sans TC** |
| ko | Jost / **Noto Sans KR** |

Jostは欧文書体でCJKの字を持たないため、**CJKの字は2番目以降が描いています**。
前便は**スタックの先頭だけを見て「同一」と書いた**もので、不正確でした。

### 実測（ナビ帯の画素を直接測定）

| 言語 | インク画素 | 被覆率 | **線の太さ中央値** | 平均輝度 |
|---|---|---|---|---|
| ja | 1,074 | 1.472% | **1.0px** | 112.0 |
| zh-cn | 507 | 0.695% | **1.0px** | 110.2 |
| zh-tw | 587 | 0.805% | **1.0px** | 118.3 |
| ko | 283 | 0.388% | **1.0px** | 110.5 |

※画素数の差は**文字数の差**です。**6倍拡大で目視**＝豆腐・欠落・崩れ0件。
最も画数の多い**zh-tw「藝術家」（藝＝19画）も13.5pxで画が分離して見えます**（4言語中では最もきつい）。
**→ 是正不要と判断しました。**

## 2. ★図版の点検で実害を発見・是正（Aboutの市場規模図）

**「1%」「99%」のラベルが読めませんでした。**
原因＝`viewBox="0 0 480 190"`（480単位幅）に対し**表示は200px/152px＝0.42倍/0.32倍**。
`font-size="13"`は**画面上5.4px／4.1px**にしかならず、**サイト内で最も小さい文字**でした。
（コントラストは15.2:1で問題なし＝**色ではなく大きさの問題**。図は`aria-hidden`で読み上げにも乗りません。）

| 項目 | 前 | 後 |
|---|---|---|
| viewBox | 480×**190** | 480×**220** |
| font-size / y座標 | 13 / 145・164 | **26** / 152・205 |
| 図の幅（PC／モバイル） | 200px／152px | **240px／300px** |
| **画面上の実寸** | **5.4px／4.1px** | **13.0px／16.3px** |

重なりの検算＝小円の下端119 vs 字の上端126（余裕7）／大円157 vs 179（余裕22）／viewBox下端220 vs 211（余裕9）。
モバイルを300pxにできたのは、実測で**すでに縦積みで右に194px空いていた**ためです。横溢れは0。
★**最初の案（font-size 30・幅240）は「99%」が円の線に重なった**ため、**画像を見て却下**しviewBoxを広げました。

同ページの画像・SVG・figureは全数（10件）確認＝**読み込み失敗0・縦横比のズレ0%・altあり**。

## 3. 紙とサイトの突き合わせチェックリスト

A料金／B規約／C会社情報／Dパンフ／E返品の5欄。**✅機械照合済み**と**👀目で見る**を行ごとに明示。

**作るなかで分かったこと**：

| 項目 | 紙 | サイト | 判定 |
|---|---|---|---|
| 規約の制定日・文書番号 | 2026年8月1日／AR-K1 | 同じ | **一致** |
| 連絡先メール・電話 | media@atspect.com／070-5515-0202 | 同じ | **一致** |
| 料金（33,000円／月額3,300円／20%・30%） | 記載あり | ★**サイトには金額の記載が無い** | **判定不能** |
| 会社名 | 株式会社ARTS**RESPECT（空白なし）** | 株式会社 ARTS RESPECT（**空白あり**） | ★**表記ゆれ** |

★**ご判断をお願いしたいこと**＝①サイトに料金を載せるか（現状は紙のみ）②会社名の空白をどちらに揃えるか。
★**紙に「20%」と「30%」の2つがある**ことを冒頭に明記しました（藍AR-P1=20%／朱AR-P2=30%／パンフP3=20%）。

## ★第6便で実施していないこと

- **紙の現物とは照合していません**（受け取り前）。チェックリストの紙の値は**入稿PDFからの機械読み取り**です。
- SNS運用Tの専任ファイル・繰-制作-07/08に**未接触**（司令塔判断どおり）。
- 入稿データA版B版・デスクトップのフォルダ・繰-制作-01/02の物理移動に**未接触**。
- **CJKの確認はナビ（13.5px）のみ**（本文・フォーム・作家ページは未測定）。
- **About以外の図版は全数点検していません**（作家詳細・作品詳細は未点検）。
- Codexレビューは本便未実施／実機の目視は未実施。

## ★第6便で判定していないこと

- **市場規模図の新しい大きさが意匠として良いかは判定していません**（読めることは実測）。
  モバイルで図が約2倍になり**紙面の印象が変わっています**。
- **CJKの読みやすさを人が採点したわけではありません**（豆腐なし・画が分離・線幅同一まで）。
  **中国語・韓国語の母語話者による確認はしていません。**
- **13.5pxがCJKに十分かという基準の妥当性は判定していません**（WCAGに文字サイズの下限規定はない）。
- **チェックリストが漏れなく網羅されているかは検証していません**（作った本人の列挙です）。

**証拠＝`reports/assets/20260801_cjk-fig-checklist/`**（CJK実画面・6倍拡大・市場図の前後・チェックリスト本体）
Vault実装ログ＝`_実装ログ_CJK実線幅の目視確認と市場規模図の是正_20260801_ATSPECT.md`

---

# 【第5便】フォーム文字サイズの不揃い是正・未測定3件の消化

## 結論（4行）

1. **第1段階のラベルを13px→14pxへ**（大きいほうに揃えた）。必須バッジ12pxは下げていません。
2. **未測定と申告した3件をすべて片づけました**（3ページのコントラスト／hover・focus／8言語の太さ差）。
3. **★測定器のバグを2件、自分で見つけて直しました**（「0件」という結果を疑ったことで発見）。
4. 追加で**2ページの不足（2.57:1）を発見して是正**、**専任外の不揃い2件を起票**しました。

## 1. 文字サイズの不揃い（SNS運用T起票）

| 画面 | 必須バッジ | ラベル |
|---|---|---|
| **第1段階（登録フォーム）** | 12px | **13px → 14px（是正）** |
| 第2段階・terms・entry | **10px（専任外・未是正）** | 14px |

**「第1段階だけ逆向き」は解消**し、**ラベルは全画面14pxで統一**。
必須バッジ12pxは**下げていません**（指示「小さいほうに揃えない」）。他画面の10px→12pxは**専任外のため起票**。
★あわせて気づいた点＝**termsはヒント16pxがラベル14pxより大きい逆転**（専任外・起票）。

## 2. 未測定だった3ページのコントラスト

※URLはランダム接尾辞付きの**非公開URLのため本報告には書きません**（ページ名のみ）。

| ページ | 結果 |
|---|---|
| 作家ご登録のお手続き（onboarding・SNS運用T専任） | **不足0件**（起票なし） |
| 作家情報のご提出 | 任意バッジ **2.57:1** → **是正済み** |
| 作品情報のご提出 | 任意バッジ **2.57:1** → **是正済み** |

※後者2ファイルは担当表記が無く、SNS運用Tが専任を「2本」と明記しているため**当方担当と判断**して是正しました
（違っていればお知らせください）。

## 3. hover時・focus時

**focus**＝`element.focus()`で実状態を作り**全数実測＝不足0件**。唯一の検出（♡アイコン3.35:1）は
**文字を持たない**ため基準3:1で**合格**。輪郭＝`solid 1.6px rgb(26,26,26)`。
**hover**＝**実際にホバーして**実測＝戻るリンク**15.58:1**／送信ボタン**8.62:1**。

## 4. 8言語のフォントの太さ

**8言語とも 書体Jost・太さ400・13.5px・コントラスト4.97:1で完全に同一**（CSS上）。
★CJKは書体スタック後方へフォールバックするため、**実際に描かれる線の太さの差は未測定**です。

## 5. ★測定器のバグを2件、自分で見つけて直しました

1. **CSS走査が規則を1件も拾えていなかった**（`:hover`が0件）。原因＝**モダンChromeでは
   CSSStyleRuleも空の`cssRules`を持つ**ため、`if(r.cssRules)再帰`を先に書くと**全規則が再帰側へ吸われる**。
   → `selectorText`を先に見る形へ直し1777件を収集。
2. **静的読み取りでは判定できないものがある**（ボタンはhoverで背景も変わるため「1:1」と誤判定／
   CSS変数は静的解決できない）→ **実際にホバーして計算後の値を読む方式**へ切り替え。

## ★第5便で実施していないこと

- **SNS運用Tの専任ファイル（terms-artist・onboarding）に触れていません。**
- **必須バッジ10px→12pxの是正／termsのヒント逆転の是正は未実施**（専任外・起票のみ）。
- **hoverは2要素しか実測していません**（ナビ・フッター等のhoverは未実施）。
- 入稿データA版B版・デスクトップのフォルダ・繰-制作-01/02の物理移動に**未接触**。
- 実機での目視・実タップは未実施／**Codexレビューは本便未実施**（測定器のバグは自分で発見・是正）。

## ★第5便で判定していないこと

- ラベル14px化の**見た目の良し悪しは判定していません**（不揃いの解消は実測）。
- **CJKフォールバック時の実際の線の太さは測っていません。**
- **hover実測は2要素のみ＝他要素のhoverは未判定。**
- 色覚特性への配慮は未検証。
- **materials／artwork-entryの担当が当方かは断定できません**（担当表記が無いため判断）。

**証拠＝`reports/assets/20260801_form-size-contrast/計測結果.md`**／Vault実装ログ＝
`_実装ログ_フォーム文字サイズの不揃い是正と未測定3件の消化_20260801_ATSPECT.md`

---

# 【第4便】裁定3件の実施・8言語通し検証・コントラスト全数是正・Codexレビュー

## 結論（4行）

1. 裁定どおり**日本語以外では「作家の方へ」を出さない形にした**（訳は付けない）。**8言語×3幅＝24通り全合格。**
2. **★残留点検で、同じ理由の導線が他に2本残っていた**（フッター／About末尾CTA）。同じ扱いに揃えた。
3. **淡色文字を全数測って9種を是正**（基準＝WCAG 2.1 AA）。**最悪値は1.75:1**だった。
4. Codexレビューで**自分の測り方の死角を1件発見**（`::placeholder`はDOM走査で拾えない）。

## 1. 裁定の実装（繰-制作-03）＋★フェイルセーフの作り

印1つ（`data-nav-ja-only`）と判定1行：
`html[data-lang]:not([data-lang="ja"]) [data-nav-ja-only] { display: none !important; }`

★条件に **`html[data-lang]` を含めた**のが要点です。`data-lang`は言語切替JSが付ける属性で、
**サーバ描画のHTMLには付いていません**（実測＝`<html class="js" lang="ja">`）。
`html:not([data-lang="ja"])` と書くと、**JSが走る前の一瞬とJSが動かない環境で、日本語でも消えます。**
属性があるときだけ判定し、**迷ったら「出す」側へ倒しました。** 区切り線も対にして隠しています。

## 2. ★残留点検＝同じ理由の導線を全数洗った（6本）

裁定の理由は「**artist-guideは日本語のみのページ**だから他言語から誘導しない」。この**理由**で全数調査：

| 場所 | 判断 |
|---|---|
| 上部ナビ | **非表示**（裁定どおり） |
| **フッター「作家の方へ」** | **非表示にした**。★ナビだけ直しても英語表示に日本語リンクが残っていた（実測）。2026-07-25 C-1「作家向け導線は日本語のみ」方針の完成形 |
| **About末尾のCTA** | **非表示にした**。★**8言語の訳付き**で英語では「For Artists — about registration」と誘っておきながら**着地先は日本語のみ**＝裁定が防ごうとした事象そのもの |
| 登録／解約／規約 の各「← …に戻る」3本 | **そのまま**。作家向け日本語ページ内の**戻り**リンクで、隠すと戻れなくなる |

※訳spanは**消していません**（将来ページを多言語化したとき戻せるように）。

## 3. 8言語 × 幅320/390/430px（前便の「未確認」を片づけ）

★言語切替は**サイト自身の切替リンクをクリック**して行いました（テーブルの再現ではなく本番経路）。

**24通りすべて＝切れ0・横溢れ0・最小タップ高44px。**
ja＝5項目/2行/89px、他7言語＝**4項目/1行/45px**。
**他言語は4項目に戻るため、元の計算式（4項目前提）の前提とも一致します**＝ご指摘のとおりでした。
PC1280px＝ja5項目+線4本／他言語4項目+**線3本**（末尾に線だけ残らない）・**PCは無変更**。

## 4. コントラスト（★使った基準＝WCAG 2.1 AA）

**通常文字4.5:1／大きい文字（24px以上・太字18.66px以上）3:1／非テキストUI 3:1（SC 1.4.11）。**
測り方＝実効文字色（半透明は合成）と**祖先を遡った実効背景**から比を出し、
不足は**二分探索で必要値を解いてから**是正（推定で直していません）。

| 対象 | 前 | 後 |
|---|---|---|
| フッター 古物営業法の表示 | **2.53** | 4.97 |
| フッター Newsletter見出し／コピーライト／言語ラベル | 3.27／3.74／3.81 | 4.97／4.97／5.09 |
| フッター メール入力欄のプレースホルダ | **1.75** | 4.5以上 |
| artist-guide 結びの副文 | 3.20 | 4.70 |
| 登録フォーム「Registration」（金茶） | **2.99** | 4.88 |
| 登録フォーム 任意バッジ／解約フォーム 任意バッジ | 走査漏れ／2.57 | 5.70／12.99 |

**是正後の残り不足＝artist-guide 0件／登録フォーム 0件／解約フォーム 0件。**

★**過去の是正が不十分だったと判明**＝フッターに「A-2 低コントラスト是正 0.45→0.6」等のコメントが
残っていましたが、**0.6でも実測3.74〜3.81でAAに届いていません**でした（基準値を決めずに濃くしただけ）。
★**私の1回目も外しました**＝金茶を`#8a715a`にしたが**別ページの背景で解いた値**で実測3.99でなお不足。
**同じページの実背景で解き直して`#786549`**（4.88）。

## 5. Codex独立レビュー（1ファイル1観点・2観点）

**観点1＝言語別非表示のCSS：7指摘→実コード検証で該当2・到達不能5。**
到達不能の根拠＝`applyLang`が`if(!T[lang]) lang='ja'`で正規化してから設定／`removeAttribute('data-lang')`は**0件**（実測）。
該当2件（JS前のFOUC／JS不動作時に出たまま）は**フェイルセーフ設計の代償として受容**しました。
サーバ側は言語を知り得ない（Liquidに任意クエリを読む仕組みが無い）ため初期HTMLで出し分けられません。
**「日本語の人に見えない」より「他言語の人に一瞬見える」方が害が小さい**という判断です。

**観点2＝フッターの低コントラスト：7指摘→実測で該当1・偽陽性6。**
★該当1件＝**`::placeholder`（1.75:1）。私のライブ走査は擬似要素を拾えず見落としていました**＝測り方の死角。
偽陽性の内訳＝①0.45の2件は**後続ルールで上書きされ実効6.12**（カスケード未解決）②地球儀アイコンは
**文字を持たない**ので基準は3:1＝**3.81で合格**（基準の取り違え）③3件は**現在の画面に存在しない**。

## 6. 消し込み（両マスターへ追記・同一性を機械照合＝1398文字・完全一致）

**閉じた**＝繰-制作-03（実装）／繰-制作-04（現状維持）／繰-制作-05（やらないと決めて閉じる）／
前便の申告3件（8言語未確認・コントラスト1箇所のみ・Codex未実施）。

## 7. ★新規起票1件（他ターミナルの専任ファイル）

**繰-制作-06**＝`sections/atspect-terms-artist.liquid` の `.atspect-aterms__form-eyebrow`（金茶・10.5px）が
**実測3.42:1でAA未満**。同じ金茶なので **`#786549`** に揃えれば4.5:1以上になります。
**SNS運用Tの専任ファイルのため当方では変更していません**（担当ターミナルでの是正をお願いします）。

## ★第4便で実施していないこと

- **`atspect-terms-artist.liquid`を触っていない**（専任ファイル・上記7で申し送り）。
- **onboarding・materials・artwork-entry の3ページは測っていない**（onboardingはSNS運用T専任）。
- **繰-制作-01/02の物理移動なし**／**入稿データA版B版・デスクトップのフォルダに触れていない**。
- **実機での目視・実タップは未実施**（実機確認は代表対応事項）。
- **ナビの訳は付けていません**（裁定どおり）。訳spanも消していません。
- **色覚特性（色弱）への配慮の検証はしていない**（コントラスト比のみ）。

## ★第4便で判定していないこと（上とは別）

- **色を濃くしたことの意匠の良し悪しは判定していません**。とくに金茶`#a08763→#786549`は
  **ブランド色の明度変更**にあたるため、意匠として許容できるかは代表のご判断です（**色相は保持**）。
- **AAA（7:1）には合わせていません**（AAの4.5:1まで）。
- **ホバー時・フォーカス時の色は測っていません**（通常状態のみ）。
- **8言語それぞれでのコントラストは測っていません**（文字色は言語で変わらないが、フォントの太さの差は未検証）。
- **FOUCの実時間は測っていません**（起こりうることは確認）。

**証拠＝`reports/assets/20260801_nav-lang-contrast/計測結果.md`**／Vault実装ログ＝
`_実装ログ_ナビ言語別非表示と8言語通し検証・コントラスト是正_20260801_ATSPECT.md`

---

# 【第3便】スマホ上部ナビ切れの是正・SNS運用T起票4件・図版点検・番号衝突の整理

## 結論（5行）

1. **スマホで切れていた「作家の方へ」が、幅320/390/430pxすべてで見えるようになった**（ライブ反映済み）。
2. SNS運用T起票の残り3件も是正し、**同じ理由で割れうる箇所を全数まとめて保護**した。
3. 図版点検＝**実画面を撮って確認**。判断待ちの「建築のアイコン」「stroke統一」は**どちらも解決済み**だった。
4. 番号の二重衝突＝**実態は衝突0件**。記載が古い。残る6ファイルは繰-制作-02へ統合して閉じた。
5. 作業中に**新たな要判断3件**を起票（うち1件は7言語の訳が無い＝対外文言のため裁定待ち）。

## 1. ナビが切れていた原因（推測ではなくコード上の根拠）

`layout/theme.liquid` のモバイル用ブロックに、フォントサイズの計算式の導出がそのまま書かれている：

> 固定オーバーヘッド＝… リンク左右パディング2px×**4項目**×2 ＋ gap6px×6箇所(**4項目**+3区切り線)…＝85px
> → `clamp(8.5px, calc((100vw - 85px) / 25), 12px)`

**＝この式は4項目・25字前提。** 2026-07-25に5項目目「作家の方へ」を足したとき、**式が更新されなかった**。
文字数25→30字・オーバーヘッドも+17px増え、必要幅が約476pxとなって現行スマホ全機種で溢れた。

## 2. 比べた案と、選ばなかった理由（すべてライブで実測）

| 案 | 実測 | 採否 |
|---|---|---|
| A 文字を小さくして1行に詰める | 390pxで**なお46px不足**。収めるには320pxで**約7.3px**必要 | **不採用**＝「文字を小さくして収めない」原則に反し、読めなくなる |
| **B 折り返しを許す（flex-wrap）** | 320/390/430pxすべて**切れ0**・12px・タップ44px | **採用** |
| C 横スクロール許可 | **切れは解消しない**（スワイプしないと見つからない） | **不採用**＝作家さまの入口として弱い |
| D 項目を減らす | — | **不採用**＝導線の取捨は代表・司令塔の判断（5項目は7/25の司令塔指示で確定） |
| E モバイルだけ短いラベル | — | **不採用**＝対外文言を独断で変えない |

**Bを選んだ核＝「入らないときだけ働く」。** 固定の計算式は言語やラベルが変わるたび破綻する（今回の不具合そのもの）。
実際、**独語では1行のまま・ナビ高45pxに自動収束**した（実測）。

## 3. 実測（是正前→是正後）

| 幅 | 是正前 | 是正後 |
|---|---|---|
| 320px | 切れ2項目／文字**9.4px** | **切れ0**／文字**12px**／タップ44px |
| 390px | 切れ2項目（scrollW432>clientW346） | **切れ0**／12px／44px |
| 430px | 切れ1項目 | **切れ0**／12px／44px |
| **PC1280px** | list820px・13.5px・1行・区切り線4本・高さ57px | **すべて同一＝無変更（回帰なし）** |

横溢れは全幅0。区切り線は折り返しの行頭に線だけ残る形を避けるためこの帯のみ非表示（600px以上は無変更）。

## 4. ★作業中に見つけた別件＝モバイルで固定ナビがヘッダーの下に潜る

2行化したら**2行目だけが半端に覗く**見え方に。画素とDOMが食い違ったので**同一時点・静定後**に測り直した：

| 状態 | 重なり | ナビの見えている高さ |
|---|---|---|
| **是正前（1行48px）** | 45px | **0px＝完全に隠れていた** |
| 2行化後（89px） | 63px | 25px（半端に覗く） |
| **モバイルで固定解除（採用）** | **0px** | — |

**＝この重なりは私の変更前から存在し、モバイルの固定ナビは元々まったく見えていなかった。**
固定をやめても失う機能は無いため `position: static` にした。
★**「ヘッダーの下に正しく固定する」形にするかは未着手＝代表の判断待ち**（63+89=152px＝画面の18%を常時占有するため）。

★**訂正の記録**＝途中で一度「PCでも覆われている」「重なりは無い」と誤った中間結論を出した。原因は
①判定式の丸め誤差（roundした値と生値を比較して偽陽性）②`window.scrollTo`が効かずscrollY=0のまま測っていた。
**測り方を直してから結論を出し直した。**

## 5. SNS運用T起票の残り3件

| 件 | 実測（前→後） |
|---|---|
| 「← ご案内のページに戻る」が小さく淡い | 9.5px→**13px**／111×14px→**151×44px**／色#6f6a63→**#57534d**＝コントラスト比**7.64:1**（AAA基準7:1を満たす。★起票時は未測定だった値を実測） |
| 「任意・複数選択可」の泣き別れ | 幅320/430pxで**泣き別れ0件** |
| 「お問い合わせ」の泣き別れ | 同上 |
| cancellation「プライバシーポリシー」320pxで2行 | **1行・123px**／横溢れ0 |

**★残留点検＝「直した理由」で全数保護。** 理由＝「短いバッジ・文中リンクが語の途中で割れる」。
同じ理由で割れうるクラスを両ファイルで数え直し、`__opt`/`__cond`/`__link`(登録)・`__link`(解約)を
**まとめて**保護。nowrapで横溢れしないことも実測（対象は全数列挙して最長14字）。

## 6. 図版・ジャンルアイコンの点検（★実画面を撮って確認）

**過去に古いスクショを根拠に実物と食い違う指示が出た経緯があるため、必ず実画面を撮った。**

| 判断待ちだった項目 | 実測・実画面 | 結論 |
|---|---|---|
| 建築のアイコン1点が必要 | トップに**8点すべて描画**（PC/モバイル両方をスクショで確認） | **すでに存在＝閉じる** |
| E-3 アイコンstroke統一 | 8点すべて**1.7px／viewBox 0 0 48 48／42×42px／rgb(58,50,42)**で一致 | **すでに統一済み＝閉じる** |

★線幅は完全統一だが**光学的な大きさは不揃い**（インク高さ＝現代美術18.3／書27.0・線の総延長は2.8倍差）。
題材が違えば線数は変わるため**不具合とは判定しない**。揃えるかは意匠の判断＝裁定待ち（繰-制作-05）。

## 7. 番号の二重衝突（繰り越し⑨／E1）＝Vault側の整理のみ

**実測＝同じ番号が2つの意味で使われている箇所は0件**（00〜06・09〜15・26〜31が各1件）。
E1の「パンフ01-23と書類02-08が衝突」は**古い記載**（01〜23は7/29にアーカイブ済み）。
残る**版番号系26〜31の6ファイル**を動かす作業は**繰-制作-02と物理的に同一**のため、
**E1を繰-制作-02へ統合して閉じた**。本日は司令塔判断により**物理移動そのものを未実施**。
`00_入稿一覧.txt`も未更新（デスクトップ`04_入稿一覧.pdf`と内容一致で照合しているため）。

## 8. 消し込み（両マスターへ追記・内容同一を機械照合済み＝2583文字・完全一致）

**閉じた**＝ナビ切れ／戻るリンク／泣き別れ2件／プライバシーポリシー／建築アイコン／stroke統一／E1（統合）。
**やらないと決めて閉じた**＝E1を独立項目として追うこと。
**本日は動かさない（司令塔判断・継続）**＝繰-制作-01・02。

## 9. ★新たに起票した3件

| # | 内容 | 実測 |
|---|---|---|
| 繰-制作-03 | **ナビ「作家の方へ」に7言語の訳が無い**。`T`テーブルに`artist-guide`キーが無く、en/zh-cn/zh-tw/ko/fr/es/de**すべてで日本語のまま** | 独語で実測＝`Künstler / Werke / Über uns / Kontakt / 作家の方へ`。**対外文言のため訳を独断で作らない＝裁定待ち** |
| 繰-制作-04 | モバイルの固定ナビをヘッダー下に正しく固定するか | 上記4 |
| 繰-制作-05 | ジャンルアイコンの光学的な大きさを揃えるか | 上記6 |

## ★第3便で実施していないこと

- **入稿用データ（A版・B版）に一切触れていない**（MD5照合で無変更を確認）。
- **デスクトップ`あつぺくと_印刷入稿_最新`に一切触れていない**（司令塔判断どおり）。
- **繰-制作-01/02の物理移動を実施していない**（移動も含めて未実施）。
- **ナビの訳（7言語）を追加していない**（対外文言のため裁定待ち）。
- **ジャンルアイコンの意匠には手を入れていない**（点検のみ）。
- **実機での目視・実タップは未実施**（実機確認は代表対応事項。すべて実ブラウザのモバイル幅）。
- **Codexの独立レビューは本便では未実施**（CSSのレイアウト是正が中心で対外文言・不可逆操作を含まないため）。
- **8言語の通し確認はしていない**（ja・deのみ実測。他6言語は未確認）。

## ★第3便で判定していないこと（上とは別）

- **コントラスト比は直した1箇所（7.64:1）しか測っていない。** 他の淡色文字は未測定。
- **ナビ2行化の「見え方の良し悪し」は判定していない**（切れないこと・読めることは実測した）。
- **ジャンルアイコンの意匠の良否は判定していない**（寸法・線幅の実測のみ）。
- **固定解除による回遊性への影響は測っていない**（元から見えていなかったため機能減は無いと判断）。

**証拠＝`reports/assets/20260801_sitenav-mobile/`**／Vault実装ログ＝
`_実装ログ_スマホ上部ナビ切れの是正とSNS運用T起票4件_20260801_ATSPECT.md`

★**ローカルは古かった**（SNS運用Tの警告どおり）。編集前にライブ161から実測pullし、
`theme.liquid`は465行・`atspect-artist-registration.liquid`は936行の差分があった。
**ローカルを見て判断していたら誤っていた。**

---

# 【第2便】Codex独立レビューと、パンフ文言検査の死角解消

## ★訂正：第1便の「空振り検査で確認した」という報告は、実際には成立していなかった

Codexの独立レビューで、**自分が作った空振り検査（`test_panf_drift.py`）自体が、
ほぼ無条件で合格する作りだった**ことが判明した。理由は2つ：

1. **毎回まっさらな出力先へ新規生成していた**ため、**既存ファイルがある状態で上書きされるか**
   という肝心の経路（`install()`）を**一度も通っていなかった**。
2. **合否をPDF全体のMD5不一致で見ていた**が、reportlab/PyMuPDFは生成日時・文書IDを毎回変える。
   **＝原稿の変更が完全に無視されていてもMD5は必ず変わり、いつでも合格していた。**

→ **全面的に作り直した（判定20項目・全PASS）。** 今度は
①無改変で2回走らせて**画素が一致し `[UNCHANGED]` で書き換えない**ことを先に確かめる（対照実験）
②**既存ファイルのある同じ出力先**へ改変版を生成し、3つとも`[UPDATED]`で差し替わることを見る
③**変更が意図した場所に出ている**（塗った場所の平均輝度12.3）／無関係な対照領域は不変
④**原稿を戻すと画素も戻る**、まで見る。**これで初めて「仕組みが効く」と言える。**

## Codexレビュー6観点の結果（1ファイル1観点で分割・指摘は全件を自分で実測検証）

| 観点 | 指摘 | 実測で該当 | 主な是正 |
|---|---|---|---|
| 1 `install()`/`same_pixels()` | 8件 | **3件** | 比較を200→**300dpi**へ（丸めで1画素差が消えていた）／0ページ同士でTrueを返す穴を封鎖 |
| 2 V3（文字の消失検査） | 8件 | **1件（実害）** | **淡いグレー文字がV3の対象外だった**（P4版表記は最小輝度140＝一度も検査されていない）→ **V3bを新設**（16→20項目） |
| 3 空振り検査そのもの | 8件 | **2件（重大）** | 上記のとおり全面作り直し |
| 4 OCR方式 | 8件 | **5件** | 画像ブロックを文字扱いしOCRを飛ばす穴／OCR失敗を空文字で通す穴／**前のPDFの結果を読む**穴／空をキャッシュする穴／キャッシュキーに読み取り条件が入らない穴 |
| 5 退役ガード | 6件 | **6件** | **`main()`だけ止めても import して書き込み関数を直接呼べば迂回できた**→ 書き込む関数側で止める方式へ |
| 6 目視シート | 8件 | 6件 | シートへ追加（B-7〜B-11・C-9） |

**★Codexの指摘のうち2件は自分で実測して否定した**＝①「写真中心のページで比率が下がりOCRを飛ばす」
＝写真のインクは分子にも入るため比率は1へ近づく（実測0.988〜1.000）＝**逆向き**
②「定数がコメントに飲み込まれ未定義」＝独立行にあり正常動作を実測確認＝**事実誤認**。

**退役ガードの空振り検査**＝Codexが挙げた迂回経路6本（main／`_retired_main`／`gen_dist_pdf`／
`gen_print_pdf`／runpy／コマンドライン）を**実際に試して6本とも止まること**、
試行後も2ファイルのSHA-256が不変であることを実測確認。

## パンフの文言検査を、実際に効く形にした

**何が空振りしていたか**＝`verify_before_print.py` の§5（社内向け文字列）・§6（対外禁止事項）は
`page.get_text()` を検査していたが、**パンフは初版（7/24）から一貫して画像だけのPDFで抽出文字数0**
（アーカイブの最初期版まで遡って実測）。**＝空文字列を検査＝必ず合格していた。**

**採った手＝Windows標準OCR（`Windows.Media.Ocr`・日本語）。追加インストール不要。**
1ページを3通り（原寸／2倍／2倍＋コントラスト強調）で読んで和集合を取る。

**捨てた手と理由**
- **元原稿のテキストソースを検査する**→**存在しない**。パンフは初版から画像で、HTML等の文字ソースが
  工程のどこにも無い（実測確認）。仮にあっても、原稿は`text_edit_*.py`でラスタへ直接手を入れて
  育ってきたため、**ソースと現物がずれる可能性そのものが、検査したいバグの種類**にあたる。
- **Tesseract / EasyOCR / PaddleOCR**→未導入。**導入はシステム変更＝要承認**であり、OS同梱で
  同等の結果が出る以上、ツールを増やす理由がない。
- **文字数のしきい値でOCRの要否を決める**→**決められない**。実測＝本物の全文を持つ料金案内が
  **161字**、ラベルしか持たない印刷用パンフが**379字**＝**多い方が中身を持っていない**。
  → **「テキスト枠の外にあるインクの割合」で判定**（実測＝画像PDF 0.988〜1.000／文字PDF
  0.033〜0.400・しきい値0.7は**崖ではなく広い谷の中**）。★この変更前は`01_パンフ_印刷用.pdf`が
  OCR経路に入っていなかった。

**★実力（禁止語18語を実際に原稿へ焼き込み、本番と同じ経路で測った）**

| 条件 | 検出率 |
|---|---|
| 本文サイズ（40px） | **18/18＝100%** |
| このパンフの最小サイズ（25px＝注記） | **17/18＝94.4%** |
| 素の原稿での誤検出 | **0件** |

唯一の見落とし＝**「1年間」**（最小サイズで「間」が落ち「1年」と読まれた）。
参考＝**長文の完全一致での再現率は76.6%**＝**短い禁止語の検出には使えるが、長文の逐語照合には
使えない。この検査は「網」であって「証明」ではない。**

→ **漏れる分は目視チェックシート1枚にまとめた**＝`_パンフ目視チェックシート_20260801.md`
（A欄＝**OCRが実際に読み落とした5箇所**＝P2の8ジャンルのラベル／P4の淡い版表記／表紙の英文リード／
「あつぺくと」の小さい「ぺ」／「1年間」。B欄＝文字コードで表せないもの11件。C欄＝数字と固有名詞9件。
**機械が見ている項目は「目視不要」と明記**して人の手間を増やさない）。

## 後回し2件の記録（司令塔判断＝本日はやらない）

`reports/assets/deferred-master.md` と Vault `_繰り越しマスター_ATSPECT.md` の**両方へ記録**
（件名／なぜ後回しか／営業開始の合否に関わるか／再開に必要な前提／根拠の場所）。
**追記部分の内容が両ファイルで同一であることを機械照合済み（差分0）。**
繰-制作-01＝デスクトップ`11_パンフレット.pdf`の差し替え／繰-制作-02＝旧体裁PDFの退避。
**いずれも営業開始の合否には関わらない**（11は代表の控え・旧体裁は誰にも渡らない）。

## 恒久ルール化

`atspect-theme\CLAUDE.md` に「**直した内容ではなく、直した理由を検索語にする**」節を新設。
グローバル版（文章・資料が対象）に対し、**同じ考え方をコード・生成経路へ広げる**部分を追加した
＝「成果物を直したら、その成果物を**書き換えられる経路**を全部数え直す」「古い経路は注意書きでは
なく**実行を止める**」「止めたら、**止まったことを実測する**」。実例として本日の件を記載。

## 第2便で実施していないこと

- **テーマ161への反映なし**＝**作家さまに届いているのは依然13.98MBのまま**（SNS運用T担当・要承認）。
- **デスクトップの入稿フォルダ・旧体裁PDFに触れていない**（司令塔判断どおり・移動も未実施）。
- **OCRを他の書類（03〜06）へは効かせていない**（テキストレイヤーを持つため不要と実測）。
- **目視チェックシートを実際に上から通してはいない**（作っただけ。次に原稿を変える人が使う）。
- **Codexレビューは6観点で止めた**（`build_panf_nyuko.py`の版面ロジック・`ocr_win.ps1`の実装・
  `verify_before_print.py`全体は未レビュー）。

## 第2便で判定していないこと（上とは別）

- OCRの検出率は**「1語ずつ焼き込む」条件**での測定。**紙面に馴染んで置かれた禁止語**
  （他の文字と重なる・写真の上に乗る等）での検出率は測っていない。
- **日本語以外の禁止語は測っていない**（現在の検査語はすべて日本語）。
- 誤検出率は素の原稿の18語で0件を見ただけで、**検査語を増やした場合の誤検出率は未測定**。
- **`verify_before_print.py` の断裁安全マージン検査は、パンフに対しては依然として機能しない**
  （同スクリプト自身が警告を出す）。**本便でも直していない**＝目視シートB-8で人が見る。

---

# 【第1便】画面用パンフレットの軽量版と、旧版配信事故の再発防止

## 結論（4行）

1. **画面用の軽量版を作った**＝**2,894,032バイト（2.76MiB）**。配信中の13,980,134バイト（13.33MiB）から**79.3%減**。
2. **解像度は1画素も落としていない**（300dpi据え置き）。**文字の消失0px・発生0px**（4面すべて実測）。
3. **印刷用だけを更新できる経路を無くした**＝1コマンドで印刷用A版・B版・軽量版の3つが同時に作られる。
4. **入稿用データ（A版・B版）は1バイトも変えていない**（MD5照合OK・タイムスタンプ 7/31 21:19 のまま）。

## SNS運用T向け：完成品の置き場所

| 項目 | 値 |
|---|---|
| **パス** | `C:\Vault\ARTS-RESPECT\_入稿データ_アクセア_修正版_20260724\02_パンフ_配布用.pdf` |
| サイズ | **2,894,032 バイト** |
| SHA-256 | `4047076bda8f65c967ab001d59263a242b33eb324975e92ff9e0e75222fd402f` |
| 構成 | A4横4ページ・読む順・300dpi・JPEG 4:4:4 品質89 |
| 用途 | テーマ `assets/atspect-pamphlet.pdf` の中身の差し替え（印刷所には渡さない） |

`00_入稿一覧.txt` で 02 は元から「画面確認用・印刷所へは渡さない」スロット。番号体系v2の
「固定スロットへ内容だけ差し替える」原則どおり、**名前を変えずに中身だけ差し替えた**
（参照する側の書き換えが要らない）。

## 事故の構造と、仕組みでの対処

7/31に印刷用の生成が新スクリプト（`build_panf_nyuko.py`）へ移ったとき、配布用がその経路から
抜け落ちた。**手順書に「両方実行する」と書く方式では、片方を忘れる事故を構造的に防げない。**

→ **`build_panf_all.py` を唯一の入口にした。** 1回の実行で、同じソースPNGから3つが同時に出る。
**印刷用だけを更新するモードは用意していない。**

- 出力が既存とレンダリング画素で完全一致なら**書き換えない**（入稿済みデータのMD5・
  タイムスタンプを無意味に壊さないため）。1画素でも違えばバックアップして差し替える。
- **空振り検査（`test_panf_drift.py`）＝独立したコピー環境で原稿1行を黒く塗りつぶし、
  同じ1コマンドを実行 → A版・B版・軽量版の3つともMD5が変化＝作り直されることを実測確認した。**

## ★残留点検で、もう1つの再発経路を発見して塞いだ

「直した内容」ではなく**「直した理由＝印刷用だけが更新され画面用が取り残される」**を検索語に
してフォルダ全体を機械検索した結果、**`gen_pamphlet_numbered.py` が `02_パンフ_配布用.pdf` を
旧世代の `31_`（7/31 14:15で更新停止）から上書きする**ことが判明した。走らせると今日の軽量版が
**旧版に戻り、まったく同じ事故が再発する。**

→ **同スクリプトを退役させ実行を止めた**（理由を表示して `exit(2)`・処理本体は `_retired_main()`
として残置・削除していない）。**空振り検査＝実行前後で02のSHA-256が変化しないことを実測確認。**

その他＝`build_v218.py` は `30_`/`31_` しか書かない（実害なし・残置）／`gen_numbered_files.py`
はパンフを扱わない／`layout_check.py`・`verify_before_print.py` は読むだけ。

### 既存の機械判定の回帰確認（差し替え後に再実行）

- `verify_before_print.py` ＝**すべての項目に合格**
- `layout_check.py --all` ＝**総合判定PASS**（E-1〜E-5含む）

## 機械照合（全16項目PASS・実行のたびに自動で走り、1つ落ちれば異常終了）

| ID | 内容 | 実測値 |
|---|---|---|
| V2-p1〜p4 | 軽量版とソースPNGの画素差 | 平均 0.685〜1.227／**PSNR 42.28〜48.77dB** |
| V3-p1〜p4 | 文字の消失・発生 | **4面すべて 消失0px・発生0px** |
| V4 | QRデコード | 表紙 `https://atspect.com/`／中面 `https://atspect.com/apply` ＝一致 |
| V5-p1〜p4 | 印刷用A版の4面すべてとの照合 | PSNR 29.56〜41.41dB／相関 0.968〜0.999 |
| V6 | ページ数・寸法 | **4ページ・297.00 x 210.00mm** |

### ★「文字列の全一致」を文字列比較でできなかった理由（正直な申告）

**印刷用・軽量版のどちらもテキストレイヤーを持たない画像PDF**（実測＝抽出文字数が両方とも**0**）。
原稿はPNG画像でPDFに文字コードが無く、**文字列を抽出して突き合わせる比較は原理的にできない**。
代わりに画素レベルで照合した＝**V3（ソースで確実にインクの画素が軽量版で地の色に飛んでいないか、
その逆が起きていないか）が4面とも0px**＝1文字も消えておらず、無かった墨も湧いていない。

## 最小文字の確認（★前提の訂正を含む）

**このパンフの最小文字は8.8ptではなく、それより小さい。** 実測＝P3注記のインク高さ**約2.1mm**
（24〜26px@300dpi）・P4版表記「AR-P0　第1刷」**約1.9mm**（22px）。8.8pt相当は約32pxなので、
**指示より厳しい条件で検証したことになる**。

拡大画像（3倍・最近傍）で上下に並べて目視＝上記2箇所＋色つきの「出典：文化庁委託調査」＋
表紙の「右のQRコードから、…」の計4箇所。**いずれも元原稿と見分けがつかない。**

軽量化にあたり、解像度・品質を **6解像度 × 2サブサンプリング × 5品質 = 60通り**実測してから
決めた。**解像度を落とさずに上限内へ収まる**ため落としていない。クロマは**4:4:4**（4:2:0だと
藍色・朱色の小さい文字がにじむ）。品質は**上限バイト数に収まる最高値を毎回自動探索**する
（固定値だと将来ページを差し替えたとき黙って上限を超える）。

## ★実施していないこと

- **テーマ161への反映をしていない**（`assets/atspect-pamphlet.pdf` の差し替えはSNS運用Tの担当、
  かつ本番反映は事前承認が必要）。**作家さまに届いているのは依然13.98MBのまま。**
- **デスクトップ `あつぺくと_印刷入稿_最新` に触れていない**。同フォルダの `11_パンフレット.pdf`
  は「画面で読む用」だが中身はA版と同一（13.98MB）のまま。差し替えれば画面用として適切に
  なるが、`01_はじめにお読みください.txt` の「11＝A版とデータも一致」という記述とMD5照合の
  根拠が崩れる。**本日18時に代表が窓口へ持っていくフォルダを直前に変えるのは混乱のもとと
  判断し、代表の判断待ちとした。**
- **`00_入稿一覧.txt` を更新していない**（デスクトップの `04_入稿一覧.pdf` と内容一致で照合して
  いるため、片方だけ変えると照合が崩れる）。
- **`01_パンフ_印刷用.pdf`（フォルダ直下・11.5MB・7/31 14:15）は古いまま残っている**。A版・B版に
  置き換わった旧体裁のファイル。取り違えの種だが、**退避（移動）は不可逆操作のため未実施**。
- **Codexの独立レビュー未実施**（本便は判定ロジックを新設した＝本来「死角がないか」を見せる対象）。
- **実機スマートフォンでの読み込み速度を計測していない**（容量は実測、体感は未計測）。

## ★判定していないこと（上とは別）

- **色（CMYK総インク量・色差）を判定していない**（軽量版は画面用でRGBのまま）。
- **Acrobat / iOS Safari / Android Chrome の実アプリでの表示を確認していない**
  （4ページ・297x210mm・非暗号化で開けることは実測）。
- **V5の閾値の感度（どの程度の改変でFAILになるか）を測っていない**。V3が0pxで効いているため
  実害はないと考えるが、V5単独の検出力は未検証。
- **★パンフの文言検査は、既存ツールでも機械では効いていない**。`verify_before_print.py` の
  「社内向け文字列の混入検査」「対外文言の禁止事項の検査」は、パンフが画像PDFでテキストを
  持たないため**実質的に空振り**（同スクリプト自身も「本スクリプトのPASSはこの項目を保証
  しない」と注意を出す）。本便で作った問題ではなく既存の死角だが、明記しておく。

## ★代表の判断をお願いしたいこと

1. デスクトップ `あつぺくと_印刷入稿_最新` の `11_パンフレット.pdf` を軽量版へ差し替えるか
   （画面で読む用としては軽量版が適切。ただし01の記述とMD5照合の更新が要る）。
   **本日18時の入稿が終わってからの方が安全と考えます。**
2. 旧体裁の `01_パンフ_印刷用.pdf` と `26_`〜`31_` の版付きファイルをアーカイブへ退避してよいか
   （移動のみ・削除しない）。

## 証拠

`reports/assets/20260801_panf-keiryoban/`
（01=軽量版の全4ページ／02〜05=最小文字の3倍拡大比較（上=印刷用原稿・下=軽量版）／06=検証ログ全16項目）

## 記録

Vault実装ログ＝`_実装ログ_パンフ画面用軽量版と生成の一本化_20260801_ATSPECT.md`
恒久ルール＝`atspect-theme\CLAUDE.md`「パンフレットの更新は build_panf_all.py が唯一の入口」節
