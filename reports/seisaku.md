# あつぺくと制作T 報告（2026-08-11・第3便）

**ターミナル：あつぺくと制作 ／ 使用モデル：★セッション起動時コンテキストは Fable 5（司令塔指示は Opus 5。`/model` の表示は「新規セッションの既定として保存」で、本セッション内で切り替わったかは当方から確認できません＝前便と同じ状態です。Opus 5 で回す必要があればセッションを開き直してください） ／ 作業：生成経路の退役＋見出しの語中割れ是正**

証拠＝`reports/assets/20260811_生成経路の退役と見出しの是正/`（スクショ20枚・checks.json・README退役の写し・記録.md）

## A：build-gifts.mjs の退役（完了）

**退役先＝`Desktop\SNS運用\_scratch_0810\_退役_20260811_再実行禁止\`**（削除ではなく移動・README同梱）。

★**移動だけでは止まりませんでした**（相対importが保たれ、そのフォルダの中でなら動く）。実行停止を挿入し、**実測**で確認しています＝退役先から実行→**終了コード1・出力0バイト**／元の場所→ファイル不在で実行不能。

**可逆であることをハッシュで証明**＝停止部分を取り除くと移動前のMD5と**完全一致**（build-gifts `cc708b0c…`／gifts-content `55235f98…`）＝**追加したのは停止だけで、処理本体も文言データも1バイトも変えていません。**

Codexの指摘2件も妥当として対応＝①データ側 `gifts-content.mjs` も **import された時点で止まる**ようにした（データは1文字も消していません）②可逆性の裏取り（上記のMD5）。

★**自分が入れた誤字を自分の検索で発見**＝停止文の「巻き戻る」を「巧き戻る」と書いていました。全文検索で2箇所是正し、**是正後にMD5の可逆性を再証明**しました。

**SNS運用T向けの申し送り1行**（そちらの持ち場のファイルのため、当方は`reports/sns.md`を書き換えず、ここに置きます）：
> 2026-08-11／制作T：`_scratch_0810/build-gifts.mjs` と `gifts-content.mjs` は代表確定により退役し、`_退役_20260811_再実行禁止/` へ移動＋実行停止しました（削除していません・戻し方はREADME）。今後 `/pages/gifts` はライブの `sections/atspect-gifts.liquid` を直接編集してください。生成をやめたぶん「8言語スパン数の検算」が無くなるので、編集のたびに数えてください。

## B：見出しの語中割れ是正

全27ページを走査＝**WebKitで11件**。★前便で「1件」としたのは*両エンジンの食い違い*を見ていたためで、*語中割れそのもの*を基準にすると11件です＝**精度不足として訂正**します。

### ご指示のCSS方式は、当てて測ってひとつ撤回しました
- `text-wrap: balance`＝**採用**（Chromiumで改善。外すと1文字孤立になる）。
- `word-break: keep-all`＝★**撤回**。当てると**WebKitで悪化**しました（寄贈h1に**1文字孤立が新発生**）。
- ★**さらに、`auto-phrase` は明示しても文節で折れていないと実測**＝**2026-08-10の是正はほとんど効いていなかった可能性**があります（ヘッドレスChromiumでの結果・**実機Chromeは未確認**）。
- **結論＝CSSだけでは日本語の見出しの折り返し位置は決められない。**

### 採った方式＝文節を `.nb`（nowrap）で包む（マークアップ側で示す）
**各塊が390pxに収まることを実ページの実フォントで実測してから**適用。**6箇所に当てて5箇所を是正**。

★**1箇所は撤回しました**＝「作家さまの活動を、世界へ。」で**当方の`.nb`がWebKitに横溢れ6pxを生みました**（塊の境目で折り返されず1行のまま372px＞見出し幅346px。Chromiumは正常）。**横溢れは語中割れより重い**ので戻し、**戻した後は両エンジンとも横溢れ0**を実測。
★**反省＝収まりの実測をChromiumだけで行っていました。WebKitでは同じ文字列が約1.8倍の幅**でした。

### 検証
ページごとにブラウザを立て直して再測定（同一ブラウザでは配信キャッシュで古いHTMLを掴む実例があったため）＝**是正5箇所は両エンジンとも語中割れ0**。スクショ**20枚すべて横溢れ0**。`.nb`を入れた見出しで**8言語とも出し分け正常**。

★**当方の検出器の偽陽性も1件発見**＝privacyの「孤立」11件は**画面外のDawn要素**を拾っていたものでした。**実数としては扱いません**（確実な語中割れ5件のみを実数とします）。

### 残る語中割れ＝5件（すべて理由あり）
撤回した1件（2ページ）／**規約書・プライバシー＝法的文書のため不可触**3件。

## 実施していないこと
iPhone実機での確認（WebKit代替のみ）／法的文書2ファイルは1文字も触っていません／実機Chromeでの`auto-phrase`確認／所要時間は測っていません。

## 要判断
1. 「作家さまの活動を、世界へ。」＝**文言を変えずに直す手がありません**（塊にすると溢れる）。①このまま ②文言を短く（確定コピーのため要裁定）③この見出しだけ字を少し小さく。
2. 法的文書2ファイルの見出しの割れ＝触ってよいか。
3. 2026-08-10の`auto-phrase`是正が実は効いていなかった可能性＝実機Chromeでの確認。

---

## ★★最優先の申し送り（本便の作業外・実測で発見）＝検索の索引が古いままで、同じ作家が2通りに見えています

翻訳品質Tが本日**作家名ローマ字A案（符号つき）を266名へ一括変換**されました。
**metaobject 側には入っています**（先頭60名を実測＝29名が符号つき）。
**しかし検索の索引 `assets/artists.json` は符号つき0名のまま**で、**再生成されていません**。

**利用者に見える食い違い（ライブで実測）**：

| 見る場所 | 表示 |
|---|---|
| 「作家と作品を探す」で `kano` を検索した一覧 | **Kano Hogai**（古い） |
| その作家ページ | **Kanō Hōgai**（新しい） |

＝**同じ作家が、同じサイトの中で2通りに表示されています。**

- 原因＝一覧の検索はJSが `assets/artists.json` を読む作りで、この索引が変換前のまま。
- 直し方＝`gen-artists-json.mjs` で索引を作り直してテーマへ反映する。
- ★**当方は実行していません。** 理由＝①本便の範囲（退役・見出し）外 ②作家データは翻訳品質Tの持ち場
  ③生成スクリプトの出力（`name_kana`・検索エイリアス・`is_listed` の引き継ぎ）を本日検証しておらず、
  当てると検索が壊れる恐れがあるため。**どなたが実行するかの指示をお願いします。**
- なお**検索の照合そのものは符号つきに対応済み**です（本日第2便で是正済み。`kato`／`katou`／`katō` のいずれでも当たる）。
  索引を作り直せば、そのまま効きます。

最終更新：2026-08-11
