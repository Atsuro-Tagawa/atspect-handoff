# あつぺくと制作T 報告（2026-08-11・第2便）

**ターミナル：あつぺくと制作 ／ 使用モデル：★セッション起動時コンテキストは Fable 5（司令塔指示は Opus 5。`/model` の表示は「新規セッションの既定として保存」であり、本セッション内で切り替わったかは当方から確認できないため推測せず記録します） ／ 作業：符号つきローマ字の検索前提の実測・是正＋寄贈の検索窓のタイトル照合降格**

証拠＝`reports/assets/20260811_ローマ字検索の前提と寄贈検索降格/`（スクショ11枚・検査スクリプト5本・記録.md）

## 着手前の必須確認
`reports/sns.md`＝8/10のプレースホルダのまま・**本日のSNS運用Tのコミット無し**。giftsは**ライブが12:20の取得とバイト単位で同一**＝編集なしを確認してから着手しました。作業Aはテーマ不触で先行。

## A＝実測（結論：前提条件は既に満たされていた）

現行610名に**符号つき表記は0件**のため、本番を書き換えない判定方法を2つ設計しました＝①ライブの正規化モジュールを実ファイルのままNodeへ読み込む単体検証 ②実ページを開き**当方のブラウザ内だけで**JSON応答を架空データへ差し替える実ブラウザ検証（サーバーは無変更）。

| 入力 | 結果 |
|---|---|
| ① kato / Kato / KATO / kAtO | **当たる**（大小文字の揺れも可） |
| ② katou / KATOU / kouichi | **当たる** |
| ③ katō / KATŌ / 分解形 | **当たる** |
| ★穴1 | データが分解形(NFD)だと「katou」が**落ちる** |
| ★穴2 | 「おお」由来（Ōno を oono / ohno）が**落ちる** |

## B＝是正（穴2件・入力側の吸収は不採用）

**指示にあった「ou→o の入力側吸収」は、実データ610名で数えて不採用**としました＝「inoe」で**井上さま2名が誤ヒット**（ご懸念のとおり）、「ishii」が**2件→31件**、姓総当たりで**+55件**。入力を潰すと情報が減るため誤ヒットは避けられません。**データ側にキーの形を足す**方向で是正しました（NFC正規化の追加＋ō→oo・ō→oh のキー）。

- **退行ゼロを証明**＝現行610名で旧実装と新実装の**キー配列が完全一致（変化0名）**。
- **是正の必要性を証明**＝符号つき化を模擬すると、**旧実装では実在作家2名（下岡 Shimooka／諸岡 Morooka）が自分の綴りで引けなくなる**。新実装では0名。
- 単体31/31 PASS（旧版で同検査＝3 FAIL＝空振りでない対照実験）／実ブラウザ**8言語112/112 PASS**／既存検索は前便の実測値とすべて一致（ito 18・ota 18・kato 9・inoue 2・sato 5・鈴木 千賀子の作品3 等）。

## C＝寄贈の検索窓のタイトル照合を降格（実施済み）

作品名を照合キーから外し（**旧キーはコメント残置＝可逆**・作品名の表示は無変更）、文言を8言語で「作家名」に統一。★**変更前は ja だけ「作家名・作品名」・en だけ「Titles and artist names」で、他6言語は元から作家名のみ＝言語間の食い違いがあった**のも解消しました。
実データ0点で検索窓が出ないため、**テンプレート名からURLを推測せず Admin API で handle を実測**した見本ページで測定＝作品名「夏の記憶」「山の朝」→**0件**／作家名→2・3・1件・空欄3件＝**6/6 PASS**。

## Codex二刀流（2分割・ハング0）

- gifts 8言語通読＝**GO**（8言語全PASS）。
- 正規化の穴突き＝**NO-GO 2件 → 実測で1件反証・1件は仕様と確認**。(a)「接合部に `toh` が生まれる」＝**旧実装のキーに元から存在**し旧でも同じ2名が当たる＝反証。(b)「`ohta` で Ōta と Ohta が両方当たる」＝**同じ姓「太田」の別綴りで、当たるのが目的**。
- 害の量を実データで測定＝人が打ちうる1,461通りの総当たりで、増えた当たり90件・減った当たり0件・**姓にも名にも無い「接合部由来」の誤ヒット0件**。
- ★**空振り検査で自分の誤りを1件発見**＝最初の対照実験は模擬データに存在しない文字を壊しており**何も踏まずに合格**していました。実在する文字で組み直して初めて検査が生きていると言えました。

## ★申し送り（本便では直していません）

1. **`Desktop/SNS運用/_scratch_0810/build-gifts.mjs` を再実行すると、本便のgifts変更が5箇所すべて旧に戻ります**（97行 placeholder／102・163行 注記／116・137行 キー／354行 PH。データ元は `gifts-content.mjs` 70行・77行）。**SNS運用Tの持ち場のため当方は触っていません**＝退役させるか、データ元を直すかの指示をお願いします。
2. **寄贈のh1が390pxで語中割れ**（WebKit＝「承ってい／る作品」）。見出しは `auto-phrase` 頼みで**WebKit非対応**。全7ページ走査で食い違いはこの1件のみ。全見出しに効くCSS変更のため本便範囲外としました。
   ★**前便の「WebKit可視残存0件」は当方の検出器の判定条件での0件**であり、見出しの語中割れはその条件に掛かりません。範囲を明確にして訂正します。

## 実施していないこと

- iPhone実機での確認（WebKitエンジンでの代替実測のみ）。
- **本番データ（artists.json・metaobject）の書き換えは0件**＝符号つき表記への実際の変換は行っていません（是正は検索側のみ）。
- 寄贈の実データ（0点のまま）での検索確認。所要時間は測っていません。

## 要判断

1. **符号つき表記（A案）採用の可否**＝検索側の前提条件は満たしました（実測つき）。**URLスラッグは変えない**ため、表示とURLの綴りが違う状態になる点のご了承。
2. build-gifts.mjs の扱い（SNS運用Tへの指示）。
3. 寄贈h1の語中割れを次便で直すか。

最終更新：2026-08-11
