# あつぺくと制作T 報告（2026-08-11・第4便）

**ターミナル：あつぺくと制作 ／ 使用モデル：★セッション起動時コンテキストは Fable 5（司令塔指示は Opus 5。`/model` の表示は「新規セッションの既定として保存」で、本セッション内で切り替わったかは当方から確認できません＝前3便と同じ状態です。Opus 5 で回す必要があればセッションを開き直してください） ／ 作業：検索索引 assets/artists.json の再生成と反映**

証拠＝`reports/assets/20260811_検索索引の再生成/`（スクショ8枚・checks.json・検証スクリプト2本・安全装置つき生成スクリプトの写し・記録.md）

**結論＝直りました。** 検索一覧と作家ページで割れていた綴りが揃い、**「狩野 芳崖／Kanō Hōgai」**が両方で同じ表示になりました。

## 反映前の検証（約束した3点）＝すべて合格

当てる前に、生成物と現行索引を1件ずつ突き合わせました。

| # | 検証 | 結果 |
|---|---|---|
| ① | `name_kana` | 610件すべて同じ・**失われた0件**・新索引でかなが空の作家**0名** |
| ② | 検索エイリアス | 42件 → 42件・**失われた/変わった0件** |
| ③ | `is_listed` | 除外0名＝★**この検査は空振り**（実データは全625名が未設定のため。条件式が正しいことは別途、対照実験で確認） |

項目別の欠落検査＝`e`（ローマ字）が266件変更のみ。**他の項目は変更0・欠落0**。

## 反映後の実測（約束した4点）＝すべて確認

| # | 実測 | 結果 |
|---|---|---|
| ① | `kano` 検索の綴り | **Kanō Hōgai**＝作家ページと**揃った** |
| ② | `kato`/`katou`/`katō` | 9／**7**／9件＝**加藤（Katō）7名は3通りとも当たる**。差の2名は「香取（Katori）」で`kato`の部分一致＝正しい差 |
| ③ | 既存の退行 | 鈴木 千賀子1・inoue 2・ito 18・作品タブ3など**すべて一致**。ota 18→19・sato 5→6は**増えた作家（大高正人）**による正しい増加 |
| ④ | 符号つきの名数 | **266名**＝翻訳品質Tの変換数と一致 |

索引は **610→625名**（+15は建築の作家・SSRには既に載っていたことを実測）、かな625/625、エイリアス42件。

## Codex二刀流＝2本ともNO-GO。妥当な指摘は全部埋めました

**1本目（検証の死角）**＝5項目の指摘。**すべて追加実測で解消**しました。
とくに「基準にした現行版が正しい保証がない」という指摘に対し、**独立正本（metaobject 625名）と索引を直接照合して不一致0件**を確認したのが、いちばん強い証拠です。あわせて重複0・文字化け0・266名の変換が「符号を足しただけ」であること0件逸脱・**266名×3通り＋エイリアス42件の全数検索テストで引けない作家0名**を実測しました。

**2本目（生成スクリプトの穴突き）**＝「今日の生成物は壊れていないが、仕組みとして危険」。指摘どおり**安全装置**を入れました。
- ★最重大＝**APIが一部しか返さなくても、エラー無しで少ない索引に丸ごと上書きしてしまう**（作家が静かに検索から消える）→ **前回より20件以上/10%以上減ったら書かずに止める**。
- ほかに、重複・必須項目・エイリアスのhandle消失・かな取得失敗を検査。
- **対照実験で効果を実測**＝「200件しか返らない」模擬で**止まり、索引は1バイトも変わらず**／「かな5名欠落」模擬でも**止まる**／正常時は**通る**。
- ★**1回目の対照実験は当方の作りが誤っていて素通りしました。** 作り直して初めて「**かなが空でも警告すら出ずに通る穴**」が実在すると分かり、その穴も塞ぎました。

## 作業中に起きたこと（正直に）

- ★**翻訳品質Tが同時に是正を進めており**、押した直後に**7件の差**が出ました（`Nakamura Ken'ichi` のアポストロフィ、荻須さまのかな訂正など）。**正本と不一致0件を確認して押し直しています。**
- ★**当方の測り間違いを1件、測り直して見つけました**＝「符号つきが266→258に減った」と読みましたが、**検索式に大文字の符号（Ō等）を入れ忘れていた**だけで、3ファイルとも266で一致＝データは無傷でした。驚いた数字をそのまま報告せずに済みました。

## 実施していないこと
iPhone実機での確認（WebKit代替のみ）／**266名の綴りが正しいかの判定はしていません**（翻訳品質Tの持ち場。当方は「正本と索引が一致しているか」だけを見ています）／増えた15名の掲載可否の判断／検索画面の撮影はjaのみ／所要時間は測っていません。

## 申し送り
1. ★**索引は作家データを直しても自動では追随しません。** データを変えたら `gen-artists-json.mjs` の実行が要ります。**今回の割れもこれが抜けたことが原因**でした。**翻訳品質Tの是正が一段落した時点で、もう一度の再生成が必要です。**
2. `is_listed` は現在625名すべて未設定で、ソフト公開の仕組みは働いていません。条件式が文字列比較のため `False`（大文字始まり）では除外されない点も要注意。
3. 生成スクリプトは止まったとき `ARTISTS_JSON_FORCE=1` で意図的に上書きできます（止めっぱなしにはしていません）。

最終更新：2026-08-11
